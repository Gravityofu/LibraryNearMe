# 15단계 (6) — 남은 작업 한 번에 정리 (GitHub 기준 확인 완료)

> GitHub에서 최신 코드를 직접 확인해봤어요. 15-3·15-4(관리자 역할/비밀번호/빈 줄)까지는
> 정확히 잘 반영되어 있었어요. 다만 아래 5가지는 아직 실제 코드에 안 들어가 있어서,
> 이번에 한 번에 정리해서 드립니다.
>
> 1. 회원 등록/수정 모달 — 모서리 둥글게(스크롤 있어도)
> 2. 자료 등록 "태그 설명" 모달 — 모서리 둥글게(스크롤 있어도)
> 3. 회원 등록/수정 모달 — 휴대폰 번호 자동 하이픈+검사, 이메일 형식 검사
> 4. 홈페이지 회원가입 화면 — 같은 휴대폰/이메일 검사 적용
> 5. 수정 모달에서 생년월일·주소가 안 채워지는 문제 — 백엔드가 그 값을 아예 안 보내주고 있었음(원인 확인됨)
>
> 소요 시간: 약 30분

---

## 1. 백엔드 — 목록 조회에 생년월일·주소 포함 (약 3분)

원인: `users.service.ts`의 `list()` 함수가 회원 목록을 돌려줄 때 `select`에 `birthDate`·`address`가 빠져 있어서, 애초에 프론트로 그 값이 전달되지 않고 있었어요. 그래서 수정 모달을 열어도 항상 빈 칸이었던 거예요.

`backend/src/users/users.service.ts` 를 엽니다. `list()` 함수 안, 아래 부분을 찾으세요.

```typescript
        select: {
          id: true,
          loginId: true,
          name: true,
          phone: true,
          email: true,
          memberNo: true,
          role: true,
          status: true,
          createdAt: true,
          // passwordHash는 절대 내보내지 않습니다.
        },
```

이렇게 바꿔주세요. (`birthDate`, `address` 두 줄 추가)

```typescript
        select: {
          id: true,
          loginId: true,
          name: true,
          phone: true,
          email: true,
          memberNo: true,
          birthDate: true,
          address: true,
          role: true,
          status: true,
          createdAt: true,
          // passwordHash는 절대 내보내지 않습니다.
        },
```

---

## 2. 프론트 — `admin/members/page.tsx` 수정 (약 15분)

`frontend/src/app/admin/members/page.tsx` 를 엽니다.

### 2-1. 타입에 birthDate·address 추가

```tsx
type MemberRow = {
  id: number;
  loginId: string | null;
  name: string;
  phone: string | null;
  email: string | null;
  memberNo: string | null;
  role: string;
  status: string;
  createdAt: string;
};
```

이렇게 바꿔주세요.

```tsx
type MemberRow = {
  id: number;
  loginId: string | null;
  name: string;
  phone: string | null;
  email: string | null;
  memberNo: string | null;
  birthDate: string | null;
  address: string | null;
  role: string;
  status: string;
  createdAt: string;
};
```

### 2-2. 전화번호 자동 서식·검사 함수 추가

`EMPTY_FORM` 바로 아래에 추가하세요.

```tsx
// 숫자만 남기고, 3자리-4자리-4자리 모양으로 하이픈을 자동으로 붙여줍니다.
function formatPhone(raw: string) {
  const digits = raw.replace(/\D/g, "").slice(0, 11);
  if (digits.length < 4) return digits;
  if (digits.length < 8) return `${digits.slice(0, 3)}-${digits.slice(3)}`;
  return `${digits.slice(0, 3)}-${digits.slice(3, 7)}-${digits.slice(7)}`;
}

// 하이픈을 뺀 숫자가 정확히 11자리인지 확인합니다.
function isValidPhone(phone: string) {
  return phone.replace(/\D/g, "").length === 11;
}

// 이메일은 입력했을 때만(선택 항목이라) 형식을 확인합니다.
function isValidEmail(email: string) {
  if (!email.trim()) return true;
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim());
}
```

### 2-3. 수정 모달 열 때 생년월일·주소도 채우기

```tsx
  function openEditModal(row: MemberRow) {
    setEditingId(row.id);
    setForm({
      loginId: row.loginId || "",
      password: "",
      name: row.name,
      phone: row.phone || "",
      email: row.email || "",
      memberNo: row.memberNo || "",
      birthDate: "",
      address: "",
      role: row.role,
      status: row.status,
    });
    setShowForm(true);
  }
```

이렇게 바꿔주세요. (`birthDate`는 서버가 `2000-01-01T00:00:00.000Z` 같은 모양으로 주기 때문에, 앞 10글자만 잘라서 `<input type="date">`가 이해하는 `2000-01-01` 모양으로 만들어줘요.)

```tsx
  function openEditModal(row: MemberRow) {
    setEditingId(row.id);
    setForm({
      loginId: row.loginId || "",
      password: "",
      name: row.name,
      phone: row.phone || "",
      email: row.email || "",
      memberNo: row.memberNo || "",
      birthDate: row.birthDate ? row.birthDate.slice(0, 10) : "",
      address: row.address || "",
      role: row.role,
      status: row.status,
    });
    setShowForm(true);
  }
```

### 2-4. 저장 전 검사 추가

```tsx
    if (!editingId && (!form.loginId.trim() || !form.password.trim() || !form.name.trim())) {
      notify("❌ " + t("members.form.requiredFields"), "error");
      return;
    }
```

이렇게 바꿔주세요.

```tsx
    if (!editingId && (!form.loginId.trim() || !form.password.trim() || !form.name.trim())) {
      notify("❌ " + t("members.form.requiredFields"), "error");
      return;
    }
    if (!isValidPhone(form.phone)) {
      notify("❌ " + t("members.form.invalidPhone"), "error");
      return;
    }
    if (!isValidEmail(form.email)) {
      notify("❌ " + t("members.form.invalidEmail"), "error");
      return;
    }
```

### 2-5. 전화번호 입력칸에 자동 서식 적용

```tsx
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.phone")}</span>
                <input
                  value={form.phone}
                  onChange={(e) => setForm({ ...form, phone: e.target.value })}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
```

이렇게 바꿔주세요.

```tsx
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.phone")} *</span>
                <input
                  value={form.phone}
                  onChange={(e) => setForm({ ...form, phone: formatPhone(e.target.value) })}
                  placeholder="000-0000-0000"
                  inputMode="numeric"
                  maxLength={13}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
```

### 2-6. 등록/수정 모달 — 모서리 둥글게(스크롤 있어도)

```tsx
          <div
            className="max-h-[85vh] w-full max-w-md overflow-auto rounded-xl bg-white p-6 shadow-xl"
            onClick={(e) => e.stopPropagation()}
          >
            <p className="mb-4 text-sm font-semibold">
```

이렇게 바꿔주세요.

```tsx
          <div
            className="max-h-[85vh] w-full max-w-md overflow-hidden rounded-xl bg-white shadow-xl"
            onClick={(e) => e.stopPropagation()}
          >
            <div className="max-h-[85vh] overflow-auto p-6">
            <p className="mb-4 text-sm font-semibold">
```

그리고 이 모달 맨 끝부분(저장 버튼 다음)을 찾으세요.

```tsx
            <button
              onClick={handleSave}
              className="mt-5 w-full cursor-pointer rounded-lg bg-[#383838] py-2.5 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("members.form.save")}
            </button>
          </div>
        </div>
      )}
```

이렇게 바꿔주세요.

```tsx
            <button
              onClick={handleSave}
              className="mt-5 w-full cursor-pointer rounded-lg bg-[#383838] py-2.5 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("members.form.save")}
            </button>
            </div>
          </div>
        </div>
      )}
```

---

## 3. 프론트 — `admin/materials/new/page.tsx` 태그 설명 모달 (약 3분)

아래 부분을 찾으세요.

```tsx
          <div
            className="max-h-[80vh] w-full max-w-6xl overflow-auto rounded-none bg-white p-6 shadow-xl"
            onClick={(e) => e.stopPropagation()}
          >
            <div className="mb-4 flex items-center justify-between">
```

이렇게 바꿔주세요. (`rounded-none` → `rounded-xl`로 되돌리고, `overflow-hidden`으로 바꿨어요.)

```tsx
          <div
            className="max-h-[80vh] w-full max-w-6xl overflow-hidden rounded-xl bg-white shadow-xl"
            onClick={(e) => e.stopPropagation()}
          >
            <div className="max-h-[80vh] overflow-auto p-6">
            <div className="mb-4 flex items-center justify-between">
```

그리고 이 모달 맨 끝부분(표 다음)을 찾으세요.

```tsx
              </tbody>
            </table>
          </div>
        </div>
      )}
```

이렇게 바꿔주세요.

```tsx
              </tbody>
            </table>
            </div>
          </div>
        </div>
      )}
```

---

## 4. dictionary.ts 수정 (약 5분)

`frontend/src/lib/dictionary.ts` 를 엽니다.

### 4-1. "전화번호" → "휴대폰 번호" (회원 관리 3곳)

**한글 쪽 3줄 값 변경:**

```ts
    "members.list.col.phone": "전화번호",
    "members.list.field.phone": "전화번호",
    "members.form.field.phone": "전화번호",
```
→
```ts
    "members.list.col.phone": "휴대폰 번호",
    "members.list.field.phone": "휴대폰 번호",
    "members.form.field.phone": "휴대폰 번호",
```

**영어 쪽 3줄 값 변경:**

```ts
    "members.list.col.phone": "Phone",
    "members.list.field.phone": "Phone",
    "members.form.field.phone": "Phone",
```
→
```ts
    "members.list.col.phone": "Mobile Phone",
    "members.list.field.phone": "Mobile Phone",
    "members.form.field.phone": "Mobile Phone",
```

### 4-2. 회원 관리 오류 문구 추가

`"members.form.saveFail"` 줄 바로 아래(한글·영어 각각)에 추가하세요.

**한글:**
```ts
    "members.form.invalidPhone": "휴대폰 번호 11자리를 모두 입력해주세요.",
    "members.form.invalidEmail": "이메일 형식이 올바르지 않습니다.",
```

**영어:**
```ts
    "members.form.invalidPhone": "Please enter all 11 digits of the mobile phone number.",
    "members.form.invalidEmail": "Please enter a valid email address.",
```

### 4-3. 회원가입 화면 오류 문구 추가

`"signup.fail"` 줄 바로 아래(한글·영어 각각)에 추가하세요.

**한글:**
```ts
    "signup.invalidPhone": "휴대폰 번호 11자리를 모두 입력해주세요.",
    "signup.invalidEmail": "이메일 형식이 올바르지 않습니다.",
```

**영어:**
```ts
    "signup.invalidPhone": "Please enter all 11 digits of the mobile phone number.",
    "signup.invalidEmail": "Please enter a valid email address.",
```

---

## 5. 프론트 — 홈페이지 회원가입 화면 (약 5분)

`frontend/src/app/(site)/signup/page.tsx` 를 통째로 아래 내용으로 바꿔주세요.

```tsx
"use client";

import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = "http://localhost:3001";

// 숫자만 남기고, 3자리-4자리-4자리 모양으로 하이픈을 자동으로 붙여줍니다.
function formatPhone(raw: string) {
  const digits = raw.replace(/\D/g, "").slice(0, 11);
  if (digits.length < 4) return digits;
  if (digits.length < 8) return `${digits.slice(0, 3)}-${digits.slice(3)}`;
  return `${digits.slice(0, 3)}-${digits.slice(3, 7)}-${digits.slice(7)}`;
}

function isValidPhone(phone: string) {
  return phone.replace(/\D/g, "").length === 11;
}

function isValidEmail(email: string) {
  if (!email.trim()) return true;
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim());
}

export default function SignupPage() {
  const { t } = useI18n();
  const [loginId, setLoginId] = useState("");
  const [password, setPassword] = useState("");
  const [name, setName] = useState("");
  const [phone, setPhone] = useState("");
  const [email, setEmail] = useState("");
  const { notify } = useNotify();

  async function handleSignup() {
    if (!isValidPhone(phone)) {
      notify("❌ " + t("signup.invalidPhone"), "error");
      return;
    }
    if (!isValidEmail(email)) {
      notify("❌ " + t("signup.invalidEmail"), "error");
      return;
    }

    const res = await fetch(`${API_URL}/users`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ loginId, password, name, phone, email }),
    });
    if (res.ok) {
      notify(t("signup.success"), "success");
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("signup.fail")), "error");
    }
  }

  return (
    <main className="mx-auto max-w-md p-8">
      <Card>
        <CardHeader>
          <CardTitle>{t("signup.title")}</CardTitle>
        </CardHeader>
        <CardContent className="flex flex-col gap-4">
          <div className="flex flex-col gap-2">
            <Label htmlFor="loginId">{t("signup.id")}</Label>
            <Input id="loginId" value={loginId} onChange={(e) => setLoginId(e.target.value)} />
          </div>
          <div className="flex flex-col gap-2">
            <Label htmlFor="password">{t("signup.password")}</Label>
            <Input id="password" type="password" value={password} onChange={(e) => setPassword(e.target.value)} />
          </div>
          <div className="flex flex-col gap-2">
            <Label htmlFor="name">{t("signup.name")}</Label>
            <Input id="name" value={name} onChange={(e) => setName(e.target.value)} />
          </div>
          <div className="flex flex-col gap-2">
            <Label htmlFor="phone">{t("signup.phone")}</Label>
            <Input
              id="phone"
              value={phone}
              onChange={(e) => setPhone(formatPhone(e.target.value))}
              placeholder="000-0000-0000"
              inputMode="numeric"
              maxLength={13}
            />
          </div>
          <div className="flex flex-col gap-2">
            <Label htmlFor="email">{t("signup.email")}</Label>
            <Input id="email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
          </div>
          <Button className="cursor-pointer" onClick={handleSignup}>
            {t("signup.submit")}
          </Button>
        </CardContent>
      </Card>
    </main>
  );
}
```

- 원래 있던 `placeholder="010-1234-5678"`는 `"000-0000-0000"`(0으로 통일)으로 바꿨어요 — 회원 관리 화면과 같은 형식이에요.
- 회원가입은 `phone`이 원래도 필수였으니(백엔드 `signup()`이 이미 필수로 확인), 새로 검사만 추가하면 됩니다.

---

## 6. 확인하기 — 약 10분

1. 서버(backend·frontend) 재시작 후 `/admin/members`에서 회원 하나를 등록할 때 생년월일을 입력해서 저장합니다.
2. 그 회원의 "수정" 버튼을 눌러서, 생년월일·주소가 이제 채워져서 나오는지 확인합니다.
3. 등록/수정 모달, "태그 설명" 모달 모두 내용이 길어 스크롤이 생겨도 네 모서리가 다 둥글게 보이는지 확인합니다.
4. 휴대폰 번호 칸에 숫자만 입력하면 자동으로 하이픈이 붙는지, 11자리를 안 채우면 저장이 막히는지 확인합니다.
5. 이메일을 이상한 형식으로 넣으면 저장이 막히는지 확인합니다.
6. 홈페이지(`/signup`)에서도 같은 휴대폰 번호 자동 하이픈·검사, 이메일 검사가 동작하는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## 7. GitHub에 저장하기

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "15단계(6): 생년월일/주소 수정모달 반영, 모달 모서리 둥글게, 휴대폰/이메일 검사(회원관리+회원가입)"
git push
```

---

## 최종 점검표

- [ ] `users.service.ts` — `list()` select에 `birthDate`/`address` 추가
- [ ] `members/page.tsx` — 타입/함수/openEditModal/검사/입력서식/모달구조 모두 반영
- [ ] `materials/new/page.tsx` — 태그 설명 모달 모서리 둥글게
- [ ] `dictionary.ts` — 휴대폰번호 라벨, 회원관리·회원가입 오류문구 추가
- [ ] `(site)/signup/page.tsx` 전체 교체
- [ ] 생년월일·주소 수정모달 반영 확인
- [ ] 두 모달 모두 모서리 둥글게 확인
- [ ] 휴대폰/이메일 검사 회원관리+회원가입 둘 다 확인
- [ ] GitHub에 올림

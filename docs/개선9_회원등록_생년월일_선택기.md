# 개선 (9) — 회원 등록 화면의 생년월일 입력을 편하게 만들기

> 목표: 지금은 브라우저가 기본으로 제공하는 달력형 날짜 입력(YYYY/MM/DD 칸이 붙어있는
> 모양)을 쓰고 있는데, 이걸 우리가 직접 만든 "년/월/일 3칸 + 버튼 선택기"로 바꿉니다.
> 칸을 클릭하면 숫자패드나 월/일 버튼이 나와서 고를 수도 있고, 직접 타이핑할 수도
> 있게 만들어요. 저장할 때는 1800년이나 13월처럼 말이 안 되는 날짜는 막습니다.
> 소요 시간: 약 25분

---

## 오늘 만드는 것 (그림으로)

```
[YYYY] 년  [MM] 월  [DD] 일

년도 칸 클릭 →
┌───────────┐
│ 1  2  3   │   숫자를 누르면 년도 칸에 순서대로 채워집니다.
│ 4  5  6   │   4자리를 다 채우면 자동으로 닫혀요.
│ 7  8  9   │
│    0      │
└───────────┘

월 칸 클릭 →
┌─────────────────┐
│ 1  2  3  4       │
│ 5  6  7  8       │   원하는 달을 누르면 바로 채워지고 닫혀요.
│ 9 10 11 12       │
└─────────────────┘

일 칸 클릭 → 1일~31일 버튼이 똑같은 방식으로 나옵니다.

다른 곳을 클릭하면 열려있던 패널은 그냥 닫힙니다.
```

세 칸 모두 버튼으로 고를 수도 있고, 원하면 직접 숫자를 타이핑해도 됩니다.

---

## 1. 생년월일 선택기 부품(컴포넌트) 새로 만들기 — 약 12분

`frontend/src/components` 폴더 안에 **`birth-date-field.tsx`** 파일을 새로 만들고
아래 내용을 통째로 붙여넣습니다.

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

// 년/월/일을 각각 문자열로 담습니다. (예: {year:"1990", month:"03", day:"05"})
export type BirthDateValue = {
  year: string;
  month: string;
  day: string;
};

type Props = {
  value: BirthDateValue;
  onChange: (value: BirthDateValue) => void;
};

// 전화기 숫자패드처럼 1~9, 그리고 가운데에 0 하나만 있는 배열입니다.
const KEYPAD_ROWS = [
  ["1", "2", "3"],
  ["4", "5", "6"],
  ["7", "8", "9"],
  ["", "0", ""],
];

const MONTHS = Array.from({ length: 12 }, (_, i) => i + 1); // 1~12
const DAYS = Array.from({ length: 31 }, (_, i) => i + 1); // 1~31

export function BirthDateField({ value, onChange }: Props) {
  // 지금 열려있는 패널이 무엇인지 기억합니다. (년/월/일 중 하나, 또는 아무것도 안 열림)
  const [open, setOpen] = useState<"year" | "month" | "day" | null>(null);
  const wrapRef = useRef<HTMLDivElement>(null);

  // 이 컴포넌트 바깥을 클릭하면 열려있던 패널을 닫습니다.
  useEffect(() => {
    function handleOutside(e: MouseEvent) {
      if (wrapRef.current && !wrapRef.current.contains(e.target as Node)) {
        setOpen(null);
      }
    }
    document.addEventListener("mousedown", handleOutside);
    return () => document.removeEventListener("mousedown", handleOutside);
  }, []);

  // 숫자패드에서 숫자를 누르면 년도 칸 뒤에 이어붙입니다. 4자리가 다 차면 패널을 닫습니다.
  function pressYearDigit(digit: string) {
    const next = (value.year + digit).slice(0, 4);
    onChange({ ...value, year: next });
    if (next.length === 4) setOpen(null);
  }

  function pickMonth(m: number) {
    onChange({ ...value, month: String(m).padStart(2, "0") });
    setOpen(null);
  }

  function pickDay(d: number) {
    onChange({ ...value, day: String(d).padStart(2, "0") });
    setOpen(null);
  }

  return (
    <div ref={wrapRef} className="flex items-center gap-1.5">
      {/* 년도 */}
      <div className="relative">
        <input
          value={value.year}
          onChange={(e) => onChange({ ...value, year: e.target.value.replace(/\D/g, "").slice(0, 4) })}
          onFocus={() => setOpen("year")}
          placeholder="YYYY"
          inputMode="numeric"
          maxLength={4}
          className="w-20 rounded-lg border px-3 py-2 text-center text-sm"
        />
        {open === "year" && (
          <div className="absolute left-0 top-full z-20 mt-1 w-32 rounded-lg border bg-white p-2 shadow-lg">
            {KEYPAD_ROWS.map((row, i) => (
              <div key={i} className="grid grid-cols-3 gap-1">
                {row.map((digit, j) =>
                  digit === "" ? (
                    <span key={j} />
                  ) : (
                    <button
                      key={j}
                      type="button"
                      onClick={() => pressYearDigit(digit)}
                      className="cursor-pointer rounded-md border py-1.5 text-sm hover:bg-neutral-100"
                    >
                      {digit}
                    </button>
                  ),
                )}
              </div>
            ))}
          </div>
        )}
      </div>
      <span className="text-sm text-neutral-400">년</span>

      {/* 월 */}
      <div className="relative">
        <input
          value={value.month}
          onChange={(e) => onChange({ ...value, month: e.target.value.replace(/\D/g, "").slice(0, 2) })}
          onFocus={() => setOpen("month")}
          placeholder="MM"
          inputMode="numeric"
          maxLength={2}
          className="w-14 rounded-lg border px-3 py-2 text-center text-sm"
        />
        {open === "month" && (
          <div className="absolute left-0 top-full z-20 mt-1 grid w-48 grid-cols-4 gap-1 rounded-lg border bg-white p-2 shadow-lg">
            {MONTHS.map((m) => (
              <button
                key={m}
                type="button"
                onClick={() => pickMonth(m)}
                className="cursor-pointer rounded-md border py-1.5 text-sm hover:bg-neutral-100"
              >
                {m}
              </button>
            ))}
          </div>
        )}
      </div>
      <span className="text-sm text-neutral-400">월</span>

      {/* 일 */}
      <div className="relative">
        <input
          value={value.day}
          onChange={(e) => onChange({ ...value, day: e.target.value.replace(/\D/g, "").slice(0, 2) })}
          onFocus={() => setOpen("day")}
          placeholder="DD"
          inputMode="numeric"
          maxLength={2}
          className="w-14 rounded-lg border px-3 py-2 text-center text-sm"
        />
        {open === "day" && (
          <div className="absolute left-0 top-full z-20 mt-1 grid max-h-48 w-56 grid-cols-6 gap-1 overflow-auto rounded-lg border bg-white p-2 shadow-lg">
            {DAYS.map((d) => (
              <button
                key={d}
                type="button"
                onClick={() => pickDay(d)}
                className="cursor-pointer rounded-md border py-1.5 text-sm hover:bg-neutral-100"
              >
                {d}
              </button>
            ))}
          </div>
        )}
      </div>
      <span className="text-sm text-neutral-400">일</span>
    </div>
  );
}

// 진짜 존재하는 날짜인지 확인합니다. (13월, 32일, 2월 30일 같은 건 걸러냅니다.)
export function isValidBirthDate(year: string, month: string, day: string) {
  const y = Number(year);
  const m = Number(month);
  const d = Number(day);
  const currentYear = new Date().getFullYear();

  if (!year || !month || !day) return false;
  if (year.length !== 4) return false;
  if (y < 1900 || y > currentYear) return false;
  if (m < 1 || m > 12) return false;
  if (d < 1 || d > 31) return false;

  // 예를 들어 2월 30일처럼 달력에 없는 날짜를 만들면, JS가 자동으로 3월로
  // 넘겨버리는데(3/2), 이렇게 "만든 날짜"와 "우리가 입력한 값"이 다르면 가짜 날짜인 거예요.
  const date = new Date(y, m - 1, d);
  return date.getFullYear() === y && date.getMonth() === m - 1 && date.getDate() === d;
}
```

- 이 파일은 **부품(컴포넌트)** 이라서, 나중에 홈페이지 회원가입 화면 등 다른 곳에서도
  똑같이 갖다 쓸 수 있어요. 오늘은 관리자 회원 등록 화면에만 연결합니다.
- `isValidBirthDate()` 함수도 여기 같이 넣어뒀어요. 회원 등록 화면에서 저장할 때
  이 함수로 진짜 날짜인지 확인할 거예요.

---

## 2. dictionary.ts에 오류 문구 추가 — 약 2분

`frontend/src/lib/dictionary.ts` 를 엽니다. `"members.form.invalidEmail"` 줄 바로
아래(한글·영어 각각)에 추가하세요. (없다면 `"members.form.saveFail"` 근처 아무 곳이나
괜찮아요.)

**한글 쪽:**

```ts
    "members.form.invalidBirthDate": "생년월일을 올바르게 입력해주세요.",
```

**영어 쪽:**

```ts
    "members.form.invalidBirthDate": "Please enter a valid date of birth.",
```

---

## 3. `admin/members/page.tsx` 수정 — 약 10분

`frontend/src/app/admin/members/page.tsx` 를 엽니다.

### 3-1. import 추가

맨 위, 아래 부분을 찾으세요.

```tsx
import { useNotify } from "@/components/notify-provider";
import { useI18n } from "@/components/language-provider";
```

이렇게 바꿔주세요.

```tsx
import { useNotify } from "@/components/notify-provider";
import { useI18n } from "@/components/language-provider";
import { BirthDateField, isValidBirthDate } from "@/components/birth-date-field";
```

### 3-2. EMPTY_FORM 수정

아래 부분을 찾으세요.

```tsx
const EMPTY_FORM = {
  loginId: "",
  password: "",
  name: "",
  phone: "",
  email: "",
  memberNo: "",
  birthDate: "",
  address: "",
  role: "MEMBER",
  status: "ACTIVE",
};
```

`birthDate: "",` 한 줄을 아래처럼 세 줄로 바꿔주세요. (년/월/일을 따로따로 담을
거예요.)

```tsx
const EMPTY_FORM = {
  loginId: "",
  password: "",
  name: "",
  phone: "",
  email: "",
  memberNo: "",
  birthYear: "",
  birthMonth: "",
  birthDay: "",
  address: "",
  role: "MEMBER",
  status: "ACTIVE",
};
```

### 3-3. 수정 모달을 열 때 년/월/일로 나눠 담기

아래 부분을 찾으세요.

```tsx
  function openEditModal(row: MemberRow) {
    setEditingId(row.id);
    setForm({
      loginId: row.loginId || "",
      password: "",
      name: row.name,
      phone: row.phone || "",
      email: row.email || "",
      memberNo: row.memberNo || "",
      birthDate: row.birthDate ? row.birthDate.slice(0, 10) : "",
      address: row.address || "",      
      role: row.role,
      status: row.status,
    });
    setShowForm(true);
  }
```

이렇게 바꿔주세요.

```tsx
  function openEditModal(row: MemberRow) {
    setEditingId(row.id);
    const [by, bm, bd] = row.birthDate ? row.birthDate.slice(0, 10).split("-") : ["", "", ""];
    setForm({
      loginId: row.loginId || "",
      password: "",
      name: row.name,
      phone: row.phone || "",
      email: row.email || "",
      memberNo: row.memberNo || "",
      birthYear: by,
      birthMonth: bm,
      birthDay: bd,
      address: row.address || "",
      role: row.role,
      status: row.status,
    });
    setShowForm(true);
  }
```

- 서버에서는 생년월일이 `"1990-03-05"` 처럼 하이픈으로 이어진 하나의 글자로 와요.
  `split("-")`로 년/월/일 세 조각으로 쪼개서 각각의 칸에 나눠 담습니다.

### 3-4. 저장할 때 생년월일 검사 + 합치기

아래 부분을 찾으세요.

```tsx
    if (!isValidPhone(form.phone)) {
      notify("❌ " + t("members.form.invalidPhone"), "error");
      return;
    }
    if (!isValidEmail(form.email)) {
      notify("❌ " + t("members.form.invalidEmail"), "error");
      return;
    }

    const url = editingId ? `${API_URL}/users/${editingId}` : `${API_URL}/users/admin`;
```

이렇게 바꿔주세요. (생년월일 검사 부분과, 년/월/일을 하나로 합치는 부분을
추가했어요.)

```tsx
    if (!isValidPhone(form.phone)) {
      notify("❌ " + t("members.form.invalidPhone"), "error");
      return;
    }
    if (!isValidEmail(form.email)) {
      notify("❌ " + t("members.form.invalidEmail"), "error");
      return;
    }

    // 생년월일: 세 칸 중 하나라도 입력했으면 셋 다 채워져 있어야 하고, 진짜 존재하는 날짜여야 합니다.
    const anyBirthFilled = form.birthYear || form.birthMonth || form.birthDay;
    const allBirthFilled = form.birthYear && form.birthMonth && form.birthDay;
    if (anyBirthFilled && !allBirthFilled) {
      notify("❌ " + t("members.form.invalidBirthDate"), "error");
      return;
    }
    if (allBirthFilled && !isValidBirthDate(form.birthYear, form.birthMonth, form.birthDay)) {
      notify("❌ " + t("members.form.invalidBirthDate"), "error");
      return;
    }
    const birthDateValue = allBirthFilled
      ? `${form.birthYear}-${form.birthMonth.padStart(2, "0")}-${form.birthDay.padStart(2, "0")}`
      : undefined;

    const url = editingId ? `${API_URL}/users/${editingId}` : `${API_URL}/users/admin`;
```

### 3-5. 서버로 보내는 값 바꾸기

아래 부분을 찾으세요. (`birthDate: form.birthDate || undefined,` 가 두 군데
나와요 — **둘 다** 바꿔주세요.)

```tsx
          birthDate: form.birthDate || undefined,
```

두 곳 모두 이렇게 바꿔주세요.

```tsx
          birthDate: birthDateValue,
```

### 3-6. 생년월일 입력칸을 새 선택기로 바꾸기

아래 부분을 찾으세요.

```tsx
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.birthDate")}</span>
                <input
                  type="date"
                  value={form.birthDate}
                  onChange={(e) => setForm({ ...form, birthDate: e.target.value })}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
```

이렇게 바꿔주세요.

```tsx
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.birthDate")}</span>
                <BirthDateField
                  value={{ year: form.birthYear, month: form.birthMonth, day: form.birthDay }}
                  onChange={(next) =>
                    setForm({ ...form, birthYear: next.year, birthMonth: next.month, birthDay: next.day })
                  }
                />
              </label>
```

---

## 4. 확인하기 — 약 6분

1. "회원" → "등록" 화면에서 생년월일의 **년도 칸**을 클릭하면 숫자패드(1~9, 0)가
   나오는지 확인합니다.
2. 숫자 4개를 누르면(예: 1→9→9→0) 자동으로 패드가 닫히고 칸에 `1990`이 채워지는지
   확인합니다.
3. **월 칸**을 클릭하면 1~12 버튼이 나오고, 하나를 누르면 채워지며 닫히는지
   확인합니다.
4. **일 칸**을 클릭하면 1~31 버튼이 나오고, 하나를 누르면 채워지며 닫히는지
   확인합니다.
5. 패널이 열린 상태에서 패널 바깥(예: 이름 칸)을 클릭하면 패널이 닫히는지
   확인합니다.
6. 각 칸에 직접 숫자를 타이핑해도 잘 입력되는지 확인합니다.
7. 년/월/일 중 일부만 입력하고 저장하면 "생년월일을 올바르게 입력해주세요." 오류가
   뜨는지 확인합니다.
8. `1800`년, `13`월, `32`일처럼 말이 안 되는 값을 넣고 저장하면 같은 오류로
   막히는지 확인합니다.
9. 올바른 생년월일(예: `1990`/`03`/`05`)로 저장하면 정상 저장되는지 확인합니다.
10. 이미 생년월일이 있는 회원을 "수정"으로 열었을 때, 년/월/일 칸에 기존 값이 잘
    채워져 있는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## 5. GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선(9): 회원 등록 생년월일 입력을 3칸+선택 버튼 방식으로 변경, 유효성 검사 추가"
git push
```

---

## 최종 점검표

- [ ] `components/birth-date-field.tsx` 새로 생성
- [ ] `dictionary.ts` — `members.form.invalidBirthDate` 한글/영어 추가
- [ ] `members/page.tsx` — import 추가
- [ ] `EMPTY_FORM` — `birthDate` → `birthYear`/`birthMonth`/`birthDay`
- [ ] `openEditModal` — 기존 생년월일을 년/월/일로 분리해서 채우기
- [ ] `handleSave` — 생년월일 검사 + 합치기 로직 추가
- [ ] 서버로 보내는 두 곳 모두 `birthDate: birthDateValue`로 변경
- [ ] 입력칸을 `BirthDateField`로 교체
- [ ] 숫자패드/월버튼/일버튼 동작 확인
- [ ] 잘못된 날짜 저장 차단 확인
- [ ] 수정 모달에서 기존 값 잘 채워지는지 확인
- [ ] GitHub에 올림

---

## 다음 단계

오늘 만든 `BirthDateField` 부품은 나중에 홈페이지 회원가입 화면에도 똑같이 붙일 수
있어요. 원하시면 다음에 진행해 드릴게요. 완료되면 알려주세요!

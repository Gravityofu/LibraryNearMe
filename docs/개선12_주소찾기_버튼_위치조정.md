# 개선 (12) — "주소 찾기" 버튼 위치 조정

> 목표: "주소 찾기" 버튼이 오른쪽 끝에 떨어져 있던 것을, "주소" 글자 바로 다음에
> 붙어 나오도록 옮깁니다.
> 소요 시간: 약 2분

---

## `admin/members/page.tsx` 수정

`frontend/src/app/admin/members/page.tsx` 를 엽니다.

아래 부분을 찾으세요.

```tsx
                <div className="mb-1 flex items-center justify-between">
                  <span className="text-sm text-neutral-500">{t("members.form.field.address")}</span>
                  <button
                    type="button"
                    onClick={() =>
                      openAddressSearch((address) => setForm((prev) => ({ ...prev, addressMain: address, addressDetail: "" })))
                    }
                    className="cursor-pointer rounded-full border border-neutral-200 bg-white px-3 py-1 text-xs font-medium text-neutral-700 shadow-sm hover:bg-neutral-50"
                  >
                    {t("members.form.findAddressBtn")}
                  </button>
                </div>
```

`justify-between` 을 지우고 `gap-2` 를 추가해주세요. (`justify-between`은 양쪽
끝으로 밀어내는 배치라 버튼이 오른쪽 끝으로 갔던 거예요. `gap-2`로 바꾸면 글자
바로 다음에 간격만 살짝 두고 붙어서 나옵니다.)

```tsx
                <div className="mb-1 flex items-center gap-2">
                  <span className="text-sm text-neutral-500">{t("members.form.field.address")}</span>
                  <button
                    type="button"
                    onClick={() =>
                      openAddressSearch((address) => setForm((prev) => ({ ...prev, addressMain: address, addressDetail: "" })))
                    }
                    className="cursor-pointer rounded-full border border-neutral-200 bg-white px-3 py-1 text-xs font-medium text-neutral-700 shadow-sm hover:bg-neutral-50"
                  >
                    {t("members.form.findAddressBtn")}
                  </button>
                </div>
```

---

## 확인하기

"회원" → "등록" 화면에서 "주소" 글자 바로 옆에 "주소 찾기" 버튼이 붙어 있는지
확인합니다.

✅ 확인되면 성공입니다! 🎉

---

## GitHub에 저장하기

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선(12): 주소 찾기 버튼 위치를 '주소' 글자 옆으로 조정"
git push
```

완료되면 알려주세요!

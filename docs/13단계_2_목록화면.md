# 13단계 (2) — 목록(List) 화면

> 목표: 등록된 실물 자료를 표로 한눈에 볼 수 있는 "목록" 화면을 만듭니다.
> 상세 검색·등록번호 검색(모달) · 보기 개수 · 페이지네이션이 포함돼요.
> 소요 시간: 약 45분

---

## 오늘 만드는 것 (그림으로)

```
[상세 검색]  [등록번호 검색]              보기 개수 [10 ▾]
┌─────────────────────────────────────────────────────┐
│ 번호 종류 서지번호 등록번호 제목 저자 ... 실물등록      │  ← 테이블(많으면 스크롤)
│  1   도서   12    2024-0001 노랜드 ...   [등록]        │
│  2   도서   12    2024-0002 노랜드 ...   [등록]        │
└─────────────────────────────────────────────────────┘
        1 / 3 페이지 (총 25건)  ← 페이지네이션
                                          [자료 등록] ← 하단 버튼
```

- 이 화면의 표는 **실물(Copy) 한 개 = 한 줄**이에요. 서지만 등록되고 아직 실물이 없는 자료는 여기 안 보이고, 지금까지 쓰던 "실물 등록" 화면에서 검색해서 등록하시면 돼요.
- "실물 등록" 칸의 **등록** 버튼은 그 줄의 서지(자료)로 "실물 등록" 화면을 열어줘요 — 다만 그 화면 자체를 다듬는 건 다음 단계(13-3)에서 해요. 오늘은 버튼 연결까지만 만들어 둡니다.
- 아직 왼쪽 메뉴에 "목록" 링크는 없어요(13-4에서 추가). 오늘은 주소창에 직접 `http://localhost:3000/admin/materials/list`를 입력해서 확인하시면 됩니다.

준비: backend·frontend 두 서버를 모두 켜 두세요. (13-1의 `/copies` API가 이미 만들어져 있어야 해요.)

---

## 1. dictionary.ts에 문구 추가 (약 10분)

`dictionary.ts`에서 지난번(12-6) 추가했던 `materials.copies.*` 문구들 아래에, **한글 쪽·영어 쪽** 각각 추가하세요.

**한글 쪽:**

```typescript
    "materials.list.detailSearch": "상세 검색",
    "materials.list.regNoSearch": "등록번호 검색",
    "materials.list.pageSizeLabel": "보기 개수",
    "materials.list.anyType": "전체",
    "materials.list.regNoHint": "등록번호를 한 줄에 하나씩 입력하세요 (Enter로 구분).",
    "materials.list.registerBtn": "등록",
    "materials.list.addMaterialBtn": "자료 등록",
    "materials.list.noResults": "검색 결과가 없습니다.",
    "materials.list.searchFail": "검색에 실패했습니다.",
    "materials.list.col.no": "번호",
    "materials.list.col.type": "종류",
    "materials.list.col.materialId": "서지정보 번호",
    "materials.list.col.registrationNo": "등록번호",
    "materials.list.col.title": "제목",
    "materials.list.col.creator": "저자",
    "materials.list.col.publisher": "출판사",
    "materials.list.col.pubYear": "출판년도",
    "materials.list.col.specialCode": "별치기호",
    "materials.list.col.classNumber": "분류기호",
    "materials.list.col.authorCode": "저자기호",
    "materials.list.col.volume": "권/호",
    "materials.list.col.copyNumber": "복본",
    "materials.list.col.shelfNo": "서가번호",
    "materials.list.col.location": "소장처",
    "materials.list.col.register": "실물 등록",
```

**영어 쪽:**

```typescript
    "materials.list.detailSearch": "Advanced Search",
    "materials.list.regNoSearch": "Search by Reg. No.",
    "materials.list.pageSizeLabel": "Rows per page",
    "materials.list.anyType": "All",
    "materials.list.regNoHint": "Enter one registration number per line.",
    "materials.list.registerBtn": "Register",
    "materials.list.addMaterialBtn": "Add Material",
    "materials.list.noResults": "No results.",
    "materials.list.searchFail": "Search failed.",
    "materials.list.col.no": "No.",
    "materials.list.col.type": "Type",
    "materials.list.col.materialId": "Material ID",
    "materials.list.col.registrationNo": "Reg. No.",
    "materials.list.col.title": "Title",
    "materials.list.col.creator": "Author",
    "materials.list.col.publisher": "Publisher",
    "materials.list.col.pubYear": "Pub. Year",
    "materials.list.col.specialCode": "Special Code",
    "materials.list.col.classNumber": "Class No.",
    "materials.list.col.authorCode": "Author Code",
    "materials.list.col.volume": "Volume/Issue",
    "materials.list.col.copyNumber": "Duplicate No.",
    "materials.list.col.shelfNo": "Shelf No.",
    "materials.list.col.location": "Location",
    "materials.list.col.register": "Register Copy",
```

---

## 2. 목록 화면 만들기 (약 25분)

`frontend/src/app/admin/materials` 폴더 안에 **`list`** 폴더를 새로 만들고, 그 안에 **`page.tsx`** 파일을 만들어 아래 내용을 붙여넣습니다.

```tsx
"use client";

import { useState, useEffect } from "react";
import Link from "next/link";
import { MATERIAL_TYPES } from "@/lib/material-types";
import { useNotify } from "@/components/notify-provider";
import { useI18n } from "@/components/language-provider";

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";

type CopyRow = {
  id: number;
  materialId: number;
  registrationNo: string;
  authorCode?: string;
  specialCode?: string;
  shelfNo?: string;
  location?: string;
  volume?: string;
  copyNumber?: string;
  status: string;
  material: {
    id: number;
    type: string;
    title: string;
    creator?: string;
    publisher?: string;
    pubYear?: string;
    classNumber?: string;
  };
};

type Filters = {
  type?: string;
  title?: string;
  creator?: string;
  subject?: string;
  registrationNos?: string[];
};

const PAGE_SIZE_OPTIONS = [10, 20, 30, 40, 50];

export default function MaterialsListPage() {
  const { notify } = useNotify();
  const { t, lang } = useI18n();

  const [rows, setRows] = useState<CopyRow[]>([]);
  const [total, setTotal] = useState(0);
  const [page, setPage] = useState(1);
  const [pageSize, setPageSize] = useState(10);
  const [filters, setFilters] = useState<Filters>({});

  const [showDetail, setShowDetail] = useState(false);
  const [showRegNo, setShowRegNo] = useState(false);

  const [detailForm, setDetailForm] = useState({ type: "", title: "", creator: "", subject: "" });
  const [regNoText, setRegNoText] = useState("");

  async function fetchList(p: number, size: number, f: Filters) {
    const token = localStorage.getItem("token");
    if (!token) return;
    const params = new URLSearchParams();
    params.set("page", String(p));
    params.set("pageSize", String(size));
    if (f.type) params.set("type", f.type);
    if (f.title) params.set("title", f.title);
    if (f.creator) params.set("creator", f.creator);
    if (f.subject) params.set("subject", f.subject);
    if (f.registrationNos && f.registrationNos.length > 0) {
      params.set("registrationNos", f.registrationNos.join(","));
    }
    const res = await fetch(`${API_URL}/copies?${params.toString()}`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      const data = await res.json();
      setRows(data.items);
      setTotal(data.total);
      setPage(data.page);
      setPageSize(data.pageSize);
    } else {
      notify("❌ " + t("materials.list.searchFail"), "error");
    }
  }

  useEffect(() => {
    fetchList(1, pageSize, {});
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  function applyDetailSearch() {
    const f: Filters = {
      type: detailForm.type || undefined,
      title: detailForm.title || undefined,
      creator: detailForm.creator || undefined,
      subject: detailForm.subject || undefined,
    };
    setFilters(f);
    setShowDetail(false);
    fetchList(1, pageSize, f);
  }

  function applyRegNoSearch() {
    const nos = regNoText
      .split("\n")
      .map((s) => s.trim())
      .filter(Boolean);
    const f: Filters = { registrationNos: nos };
    setFilters(f);
    setShowRegNo(false);
    fetchList(1, pageSize, f);
  }

  function changePageSize(size: number) {
    setPageSize(size);
    fetchList(1, size, filters);
  }

  const totalPages = Math.max(1, Math.ceil(total / pageSize));

  return (
    <div className="flex flex-col gap-3">
      {/* 상단 버튼 영역 */}
      <div className="flex items-center gap-2">
        <button
          type="button"
          onClick={() => setShowDetail(true)}
          className="cursor-pointer rounded-lg border px-3 py-2 text-sm"
        >
          {t("materials.list.detailSearch")}
        </button>
        <button
          type="button"
          onClick={() => setShowRegNo(true)}
          className="cursor-pointer rounded-lg border px-3 py-2 text-sm"
        >
          {t("materials.list.regNoSearch")}
        </button>
        <div className="ml-auto flex items-center gap-2">
          <span className="text-sm text-neutral-500">{t("materials.list.pageSizeLabel")}</span>
          <select
            value={pageSize}
            onChange={(e) => changePageSize(Number(e.target.value))}
            className="cursor-pointer rounded-lg border px-2 py-1.5 text-sm"
          >
            {PAGE_SIZE_OPTIONS.map((n) => (
              <option key={n} value={n}>
                {n}
              </option>
            ))}
          </select>
        </div>
      </div>

      {/* 중간 테이블 영역 (많으면 스크롤) */}
      <div className="max-h-[65vh] overflow-auto rounded-lg border border-neutral-200 bg-white">
        <table className="w-full min-w-[1400px] text-left text-sm">
          <thead className="sticky top-0 bg-neutral-100 text-neutral-500">
            <tr>
              <th className="px-3 py-2">{t("materials.list.col.no")}</th>
              <th className="px-3 py-2">{t("materials.list.col.type")}</th>
              <th className="px-3 py-2">{t("materials.list.col.materialId")}</th>
              <th className="px-3 py-2">{t("materials.list.col.registrationNo")}</th>
              <th className="px-3 py-2">{t("materials.list.col.title")}</th>
              <th className="px-3 py-2">{t("materials.list.col.creator")}</th>
              <th className="px-3 py-2">{t("materials.list.col.publisher")}</th>
              <th className="px-3 py-2">{t("materials.list.col.pubYear")}</th>
              <th className="px-3 py-2">{t("materials.list.col.specialCode")}</th>
              <th className="px-3 py-2">{t("materials.list.col.classNumber")}</th>
              <th className="px-3 py-2">{t("materials.list.col.authorCode")}</th>
              <th className="px-3 py-2">{t("materials.list.col.volume")}</th>
              <th className="px-3 py-2">{t("materials.list.col.copyNumber")}</th>
              <th className="px-3 py-2">{t("materials.list.col.shelfNo")}</th>
              <th className="px-3 py-2">{t("materials.list.col.location")}</th>
              <th className="px-3 py-2">{t("materials.list.col.register")}</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-neutral-100">
            {rows.map((row, i) => {
              const typeInfo = MATERIAL_TYPES.find((m) => m.code === row.material.type);
              return (
                <tr key={row.id}>
                  <td className="whitespace-nowrap px-3 py-2">{(page - 1) * pageSize + i + 1}</td>
                  <td className="whitespace-nowrap px-3 py-2">
                    {lang === "ko" ? typeInfo?.nameKo ?? row.material.type : typeInfo?.nameEn ?? row.material.type}
                  </td>
                  <td className="whitespace-nowrap px-3 py-2">{row.materialId}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.registrationNo}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.material.title}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.material.creator || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.material.publisher || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.material.pubYear || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.specialCode || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.material.classNumber || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.authorCode || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.volume || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.copyNumber || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.shelfNo || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">{row.location || "-"}</td>
                  <td className="whitespace-nowrap px-3 py-2">
                    <Link
                      href={`/admin/materials/copies?materialId=${row.materialId}`}
                      className="rounded border px-2 py-1 text-xs"
                    >
                      {t("materials.list.registerBtn")}
                    </Link>
                  </td>
                </tr>
              );
            })}
            {rows.length === 0 && (
              <tr>
                <td colSpan={16} className="px-3 py-6 text-center text-neutral-400">
                  {t("materials.list.noResults")}
                </td>
              </tr>
            )}
          </tbody>
        </table>
      </div>

      {/* 페이지네이션 */}
      {total > 0 && (
        <div className="flex items-center justify-center gap-3 text-sm">
          <button
            type="button"
            disabled={page <= 1}
            onClick={() => fetchList(page - 1, pageSize, filters)}
            className="cursor-pointer rounded border px-3 py-1 disabled:cursor-not-allowed disabled:opacity-40"
          >
            {t("materials.new.pagePrev")}
          </button>
          <span className="text-neutral-500">
            {page} / {totalPages} {t("materials.pageWord")} ({t("materials.totalWord")} {total}
            {t("materials.countUnit")})
          </span>
          <button
            type="button"
            disabled={page >= totalPages}
            onClick={() => fetchList(page + 1, pageSize, filters)}
            className="cursor-pointer rounded border px-3 py-1 disabled:cursor-not-allowed disabled:opacity-40"
          >
            {t("materials.new.pageNext")}
          </button>
        </div>
      )}

      {/* 하단 버튼 영역 */}
      <div className="flex justify-end">
        <Link
          href="/admin/materials/new"
          className="cursor-pointer rounded-lg bg-[#383838] px-5 py-2.5 text-sm font-semibold text-[#F9F6F0]"
        >
          {t("materials.list.addMaterialBtn")}
        </Link>
      </div>

      {/* 상세 검색 모달 */}
      {showDetail && (
        <div
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"
          onClick={() => setShowDetail(false)}
        >
          <div className="w-full max-w-sm rounded-xl bg-white p-6 shadow-xl" onClick={(e) => e.stopPropagation()}>
            <p className="mb-4 text-sm font-semibold">{t("materials.list.detailSearch")}</p>
            <div className="space-y-3">
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("materials.new.typeLabel")}</span>
                <select
                  value={detailForm.type}
                  onChange={(e) => setDetailForm({ ...detailForm, type: e.target.value })}
                  className="w-full cursor-pointer rounded-lg border px-3 py-2 text-sm"
                >
                  <option value="">{t("materials.list.anyType")}</option>
                  {MATERIAL_TYPES.map((m) => (
                    <option key={m.code} value={m.code}>
                      {lang === "ko" ? m.nameKo ?? m.code : m.nameEn ?? m.code}
                    </option>
                  ))}
                </select>
              </label>
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("materials.new.field.title")}</span>
                <input
                  value={detailForm.title}
                  onChange={(e) => setDetailForm({ ...detailForm, title: e.target.value })}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("materials.new.field.creator")}</span>
                <input
                  value={detailForm.creator}
                  onChange={(e) => setDetailForm({ ...detailForm, creator: e.target.value })}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("materials.new.field.subject")}</span>
                <input
                  value={detailForm.subject}
                  onChange={(e) => setDetailForm({ ...detailForm, subject: e.target.value })}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
            </div>
            <button
              onClick={applyDetailSearch}
              className="mt-5 w-full cursor-pointer rounded-lg bg-[#383838] py-2.5 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("materials.search")}
            </button>
          </div>
        </div>
      )}

      {/* 등록번호 검색 모달 */}
      {showRegNo && (
        <div
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"
          onClick={() => setShowRegNo(false)}
        >
          <div className="w-full max-w-sm rounded-xl bg-white p-6 shadow-xl" onClick={(e) => e.stopPropagation()}>
            <p className="mb-2 text-sm font-semibold">{t("materials.list.regNoSearch")}</p>
            <p className="mb-3 text-xs text-neutral-400">{t("materials.list.regNoHint")}</p>
            <textarea
              value={regNoText}
              onChange={(e) => setRegNoText(e.target.value)}
              rows={8}
              placeholder={"2024-0001\n2024-0002\n2024-0003"}
              className="w-full rounded-lg border px-3 py-2 text-sm"
            />
            <button
              onClick={applyRegNoSearch}
              className="mt-4 w-full cursor-pointer rounded-lg bg-[#383838] py-2.5 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("materials.search")}
            </button>
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## 3. 확인하기 (약 8분)

브라우저에서 로그인한 상태로 주소창에 직접 입력해서 열어보세요.

```
http://localhost:3000/admin/materials/list
```

1. 지금까지 등록한 실물이 표에 쭉 나오는지 확인합니다. (실물이 없는 서지는 안 보이는 게 정상이에요.)
2. **상세 검색** 클릭 → 모달이 뜨는지, 제목에 아는 단어를 넣고 검색하면 모달이 닫히면서 그 자료만 나오는지 확인합니다.
3. **등록번호 검색** 클릭 → 여러 등록번호를 줄바꿈으로 넣고 검색하면 그것들만 나오는지 확인합니다.
4. **보기 개수**를 10 → 20으로 바꿔서 한 페이지에 더 많이 나오는지 확인합니다.
5. 결과가 많으면 아래 **이전/다음** 페이지 버튼이 잘 동작하는지 확인합니다.
6. 아무 줄의 **등록** 버튼을 눌러 `/admin/materials/copies?materialId=...` 로 이동은 하는지 확인합니다. (그 화면 자체가 이 번호를 활용해서 자동으로 자료를 선택해주는 건 13-3에서 만들 거라, 지금은 그냥 이동만 되면 정상이에요.)
7. 맨 아래 **자료 등록** 버튼이 자료 등록 화면으로 이동하는지 확인합니다.
8. ENG 토글도 눌러서 표 제목·버튼들이 번역되는지 확인합니다.

✅ 표가 나오고 두 모달·페이지네이션·보기개수가 잘 동작하면 13-2 완성입니다! 🎉

---

## 4. GitHub에 저장 (약 2분)

```
cd C:\projects\LibraryNearMe
git status
```

🔒 `.env`가 목록에 없는지 확인한 뒤:

```
git add .
git commit -m "13단계(2): 자료 목록(List) 화면 + 상세검색/등록번호검색/페이지네이션"
git push
```

---

## 최종 점검표

- [ ] `dictionary.ts` — `materials.list.*` 문구 한글/영어 추가
- [ ] `admin/materials/list/page.tsx` 생성
- [ ] 표에 실물 데이터가 실물 1개=1행으로 나옴
- [ ] 상세 검색 모달 동작
- [ ] 등록번호 검색 모달 동작
- [ ] 보기 개수 변경 동작
- [ ] 페이지네이션 동작
- [ ] "등록" 버튼 → 실물 등록 화면으로 이동
- [ ] "자료 등록" 버튼 → 자료 등록 화면으로 이동
- [ ] ENG 토글 확인
- [ ] GitHub에 올림

---

## 다음 단계 예고 — 13단계 (3): 실물 등록 화면 개편

지금의 "실물 등록" 화면(검색 → 선택 → 폼)을, 목록에서 자료를 딱 골라서 들어오는 방식에 맞게 다시 만듭니다. 왼쪽 위에는 그 자료의 MARC 편집기(수정도 가능), 왼쪽 아래에는 이미 등록된 실물 목록(스크롤), 오른쪽에는 실물 등록/수정 폼이 배치돼요. 완료되면 알려주세요!

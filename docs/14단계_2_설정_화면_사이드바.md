# 14단계 (2) — "설정" 화면 만들기 (Tabs + KORMARC 태그 관리)

> 목표: 관리자 사이드바 맨 아래에 "설정" 메뉴를 추가하고, 그 화면 안에
> Tabs(탭) UI로 구분된 설정 화면을 만듭니다. 첫 번째 탭은 "KORMARC 태그"로,
> 어제 만든 API를 이용해 표로 보여주고 수정·추가할 수 있게 만듭니다.
> 소요 시간: 약 30분

준비: backend·frontend 두 서버를 모두 켜 두세요. (14-1을 먼저 완료해야 해요.)

---

## 1. Tabs 부품 설치 — 약 3분

지금까지 쓴 Button·Input·Label·Card처럼, Tabs도 shadcn이라는 도구로 설치할 수 있는 기본 부품이에요. frontend 폴더에서 실행하세요.

```
cd C:\projects\LibraryNearMe\frontend
npx shadcn@latest add tabs
```

중간에 물어보는 게 있으면 기본값(Enter)으로 진행하시면 됩니다.

✅ **확인하기**: `frontend/src/components/ui/tabs.tsx` 파일이 새로 생겼는지 확인하세요.

---

## 2. dictionary.ts에 문구 추가 — 약 8분

`frontend/src/lib/dictionary.ts` 를 엽니다. `"materials.list.col.register"` 줄 바로 아래(한글·영어 각각)에 추가하세요.

**한글 쪽:**

```ts
    "admin.menu.systemSettings": "설정",
    "settings.title": "설정",
    "settings.tabs.kormarcTags": "KORMARC 태그",
    "settings.tags.addBtn": "새 태그 추가",
    "settings.tags.editBtn": "수정",
    "settings.tags.col.tag": "태그",
    "settings.tags.col.fieldName": "필드명",
    "settings.tags.col.indicators": "지시기호",
    "settings.tags.col.subfieldCodes": "식별기호",
    "settings.tags.col.example": "예시",
    "settings.tags.col.action": "관리",
    "settings.tags.modal.addTitle": "새 태그 추가",
    "settings.tags.modal.editTitle": "태그 수정",
    "settings.tags.field.tag": "태그",
    "settings.tags.field.fieldName": "필드명",
    "settings.tags.field.indicators": "지시기호 설명",
    "settings.tags.field.subfieldCodes": "주요 식별기호 및 의미",
    "settings.tags.field.example": "예시",
    "settings.tags.save": "저장",
    "settings.tags.tagRequired": "태그는 필수입니다.",
    "settings.tags.saveSuccess": "저장되었습니다.",
    "settings.tags.saveFail": "저장에 실패했습니다.",
    "settings.tags.loadFail": "태그 목록을 불러오지 못했습니다.",
```

**영어 쪽:**

```ts
    "admin.menu.systemSettings": "Settings",
    "settings.title": "Settings",
    "settings.tabs.kormarcTags": "KORMARC Tags",
    "settings.tags.addBtn": "Add Tag",
    "settings.tags.editBtn": "Edit",
    "settings.tags.col.tag": "Tag",
    "settings.tags.col.fieldName": "Field Name",
    "settings.tags.col.indicators": "Indicators",
    "settings.tags.col.subfieldCodes": "Subfield Codes",
    "settings.tags.col.example": "Example",
    "settings.tags.col.action": "Actions",
    "settings.tags.modal.addTitle": "Add New Tag",
    "settings.tags.modal.editTitle": "Edit Tag",
    "settings.tags.field.tag": "Tag",
    "settings.tags.field.fieldName": "Field Name",
    "settings.tags.field.indicators": "Indicator Description",
    "settings.tags.field.subfieldCodes": "Subfield Codes & Meaning",
    "settings.tags.field.example": "Example",
    "settings.tags.save": "Save",
    "settings.tags.tagRequired": "Tag is required.",
    "settings.tags.saveSuccess": "Saved.",
    "settings.tags.saveFail": "Failed to save.",
    "settings.tags.loadFail": "Failed to load tag list.",
```

---

## 3. 사이드바에 "설정" 메뉴 추가 — 약 3분

`frontend/src/app/admin/layout.tsx` 를 엽니다. 아래 부분을 찾으세요.

```tsx
          <span className="px-3 py-2.5 text-[rgba(249,246,240,0.6)]">{t("admin.menu.members")}</span>
          <span className="px-3 py-2.5 text-[rgba(249,246,240,0.6)]">{t("admin.menu.loans")}</span>
        </nav>
```

이렇게 바꿔주세요. (맨 아래에 "설정" 링크 한 줄이 추가됐어요.)

```tsx
          <span className="px-3 py-2.5 text-[rgba(249,246,240,0.6)]">{t("admin.menu.members")}</span>
          <span className="px-3 py-2.5 text-[rgba(249,246,240,0.6)]">{t("admin.menu.loans")}</span>
          <Link href="/admin/settings" className={navClass("/admin/settings")}>
            {t("admin.menu.systemSettings")}
          </Link>
        </nav>
```

> 참고: `/admin`(도서관 설정) 메뉴와 이름이 비슷해 보일 수 있는데, 서로 다른 메뉴예요. `admin.menu.settings`는 기존 "도서관 설정"(이름·색상 등), `admin.menu.systemSettings`는 오늘 새로 만드는 "설정"(태그 등 시스템 값 관리)입니다.

---

## 4. 설정 화면 만들기 — 약 16분

`frontend/src/app/admin` 폴더 안에 **`settings`** 폴더를 새로 만들고, 그 안에 **`page.tsx`** 파일을 만들어 아래를 붙여넣습니다.

```tsx
"use client";

import { useEffect, useState } from "react";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { useNotify } from "@/components/notify-provider";
import { useI18n } from "@/components/language-provider";

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";

type KormarcTag = {
  id: number;
  tag: string;
  fieldName: string;
  indicators?: string;
  subfieldCodes?: string;
  example?: string;
};

const EMPTY_TAG = { tag: "", fieldName: "", indicators: "", subfieldCodes: "", example: "" };

export default function AdminSettingsPage() {
  const { notify } = useNotify();
  const { t } = useI18n();

  const [tags, setTags] = useState<KormarcTag[]>([]);
  const [showModal, setShowModal] = useState(false);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [form, setForm] = useState(EMPTY_TAG);

  async function loadTags() {
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/settings/kormarc-tags`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      setTags(await res.json());
    } else {
      notify("❌ " + t("settings.tags.loadFail"), "error");
    }
  }

  useEffect(() => {
    loadTags();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  function openAddModal() {
    setEditingId(null);
    setForm(EMPTY_TAG);
    setShowModal(true);
  }

  function openEditModal(tag: KormarcTag) {
    setEditingId(tag.id);
    setForm({
      tag: tag.tag,
      fieldName: tag.fieldName,
      indicators: tag.indicators || "",
      subfieldCodes: tag.subfieldCodes || "",
      example: tag.example || "",
    });
    setShowModal(true);
  }

  async function handleSave() {
    const token = localStorage.getItem("token");
    if (!token) return;
    if (!form.tag.trim()) {
      notify("❌ " + t("settings.tags.tagRequired"), "error");
      return;
    }
    const url = editingId
      ? `${API_URL}/settings/kormarc-tags/${editingId}`
      : `${API_URL}/settings/kormarc-tags`;
    const res = await fetch(url, {
      method: editingId ? "PATCH" : "POST",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify(form),
    });
    if (res.ok) {
      notify("✅ " + t("settings.tags.saveSuccess"), "success");
      setShowModal(false);
      await loadTags();
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("settings.tags.saveFail")), "error");
    }
  }

  return (
    <div className="p-6">
      <h1 className="mb-4 text-lg font-bold">{t("settings.title")}</h1>

      <Tabs defaultValue="kormarcTags">
        <TabsList>
          <TabsTrigger value="kormarcTags">{t("settings.tabs.kormarcTags")}</TabsTrigger>
        </TabsList>

        <TabsContent value="kormarcTags" className="mt-4">
          <div className="mb-3 flex justify-end">
            <button
              type="button"
              onClick={openAddModal}
              className="cursor-pointer rounded-lg bg-[#383838] px-4 py-2 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("settings.tags.addBtn")}
            </button>
          </div>

          <div className="max-h-[65vh] overflow-auto rounded-lg border border-neutral-200 bg-white">
            <table className="w-full min-w-[900px] text-left text-sm">
              <thead className="sticky top-0 bg-neutral-100 text-neutral-500">
                <tr>
                  <th className="px-3 py-2">{t("settings.tags.col.tag")}</th>
                  <th className="px-3 py-2">{t("settings.tags.col.fieldName")}</th>
                  <th className="px-3 py-2">{t("settings.tags.col.indicators")}</th>
                  <th className="px-3 py-2">{t("settings.tags.col.subfieldCodes")}</th>
                  <th className="px-3 py-2">{t("settings.tags.col.example")}</th>
                  <th className="px-3 py-2">{t("settings.tags.col.action")}</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-neutral-100">
                {tags.map((tag) => (
                  <tr key={tag.id}>
                    <td className="whitespace-nowrap px-3 py-2 font-medium">{tag.tag}</td>
                    <td className="whitespace-nowrap px-3 py-2">{tag.fieldName}</td>
                    <td className="px-3 py-2 text-neutral-500">{tag.indicators || "-"}</td>
                    <td className="px-3 py-2 text-neutral-500">{tag.subfieldCodes || "-"}</td>
                    <td className="whitespace-nowrap px-3 py-2 text-neutral-500">{tag.example || "-"}</td>
                    <td className="whitespace-nowrap px-3 py-2">
                      <button
                        type="button"
                        onClick={() => openEditModal(tag)}
                        className="cursor-pointer rounded border px-2 py-1 text-xs"
                      >
                        {t("settings.tags.editBtn")}
                      </button>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </TabsContent>
      </Tabs>

      {showModal && (
        <div
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"
          onClick={() => setShowModal(false)}
        >
          <div className="w-full max-w-md rounded-xl bg-white p-6 shadow-xl" onClick={(e) => e.stopPropagation()}>
            <p className="mb-4 text-sm font-semibold">
              {editingId ? t("settings.tags.modal.editTitle") : t("settings.tags.modal.addTitle")}
            </p>
            <div className="space-y-3">
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("settings.tags.field.tag")} *</span>
                <input
                  value={form.tag}
                  onChange={(e) => setForm({ ...form, tag: e.target.value })}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("settings.tags.field.fieldName")}</span>
                <input
                  value={form.fieldName}
                  onChange={(e) => setForm({ ...form, fieldName: e.target.value })}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("settings.tags.field.indicators")}</span>
                <textarea
                  value={form.indicators}
                  onChange={(e) => setForm({ ...form, indicators: e.target.value })}
                  rows={2}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">
                  {t("settings.tags.field.subfieldCodes")}
                </span>
                <textarea
                  value={form.subfieldCodes}
                  onChange={(e) => setForm({ ...form, subfieldCodes: e.target.value })}
                  rows={2}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("settings.tags.field.example")}</span>
                <input
                  value={form.example}
                  onChange={(e) => setForm({ ...form, example: e.target.value })}
                  className="w-full rounded-lg border px-3 py-2 text-sm"
                />
              </label>
            </div>
            <button
              onClick={handleSave}
              className="mt-5 w-full cursor-pointer rounded-lg bg-[#383838] py-2.5 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("settings.tags.save")}
            </button>
          </div>
        </div>
      )}
    </div>
  );
}
```

이 화면은 우리가 지금까지 여러 번 써본 패턴이에요 — 표로 목록을 보여주고, "새 태그 추가"나 각 줄의 "수정" 버튼을 누르면 모달이 뜨고, 저장하면 목록이 새로고침됩니다. `Tabs`는 지금은 "KORMARC 태그" 탭 하나뿐이지만, 나중에 다른 설정 항목이 생기면 `TabsTrigger`·`TabsContent`만 하나씩 더 추가하면 됩니다.

---

## 5. 확인하기 — 약 5분

1. 관리자 화면(`/admin`) 왼쪽 사이드바 맨 아래에 **"설정"** 메뉴가 보이는지 확인합니다.
2. 클릭하면 `/admin/settings`로 이동하고, **"KORMARC 태그"** 탭 아래 표에 58개 태그가 나오는지 확인합니다.
3. 아무 줄의 **수정** 버튼을 눌러 모달이 뜨는지, 값을 바꾸고 저장하면 표에 반영되는지 확인합니다.
4. **새 태그 추가** 버튼을 눌러 새 태그(예: 태그 "999", 필드명 "테스트")를 추가해 보고 표에 나오는지 확인합니다.
5. 이미 있는 태그 번호로 또 추가해 보면 "이미 등록된 태그입니다." 오류가 뜨는지 확인합니다.
6. ENG 토글을 눌러 탭 이름·버튼·표 머리글이 번역되는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## 6. GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "14단계(2): 설정 화면 신설 (Tabs UI) + KORMARC 태그 관리 화면 + 사이드바 메뉴"
git push
```

---

## 최종 점검표

- [ ] `npx shadcn@latest add tabs` 실행, `components/ui/tabs.tsx` 생성 확인
- [ ] `dictionary.ts` — `admin.menu.systemSettings`·`settings.*` 한글/영어 추가
- [ ] `admin/layout.tsx` — 사이드바 맨 아래 "설정" 메뉴 추가
- [ ] `admin/settings/page.tsx` 생성 (Tabs + KORMARC 태그 표 + 추가/수정 모달)
- [ ] 태그 58개 표시, 수정·추가 동작 확인
- [ ] 중복 태그 등록 시 오류 확인
- [ ] ENG 토글 확인
- [ ] GitHub에 올림

---

## 다음 단계 예고 — 14단계 (3): 자료 등록 화면에 "태그 설명" 버튼

마지막으로, "자료 등록" 화면 상단에 "태그 설명" 버튼을 만들어서 방금 만든 태그 목록을 모달로 바로 볼 수 있게 하고, 모달 안에 "더 보기" 버튼으로 국립중앙도서관 KORMARC 안내 페이지를 새 창으로 열도록 만듭니다. 완료되면 알려주세요!

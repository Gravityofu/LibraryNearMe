# 개선137: public-boards.controller.ts / public-posts.controller.ts 내용이 서로 바뀐 문제 수정

## 목표

개선136 가이드를 적용하신 후 아래와 같은 오류가 발생했다고 알려주셨습니다.

```
src/app.module.ts:44:10 - error TS2724: '"./public-boards/public-boards.controller"' has no exported member named 'PublicBoardsController'. Did you mean 'PublicPostsController'?
src/app.module.ts:45:10 - error TS2724: '"./public-boards/public-posts.controller"' has no exported member named 'PublicPostsController'. Did you mean 'PublicBoardsController'?
```

이 오류는 `public-boards.controller.ts` 파일 안에 `PublicPostsController`가, `public-posts.controller.ts` 파일 안에 `PublicBoardsController`가 들어있어서 생깁니다. 즉 두 파일의 내용이 서로 바뀌어 저장된 상태입니다. (아마 개선136 가이드의 2단계와 3단계 코드를 반대 파일에 붙여넣으신 것 같습니다.) 이번 가이드는 두 파일에 각각 올바른 내용을 다시 넣어 바로잡습니다.

---

## 1단계: `public-posts.controller.ts`에 올바른 내용 넣기

`backend/src/public-boards/public-posts.controller.ts` 파일을 여세요. 파일 안의 내용을 전부 지우고, 아래 내용으로 완전히 바꿔주세요. (파일 맨 위에 `export class PublicPostsController`가 있는지 저장 후 꼭 확인해 주세요.)

```typescript
import { Controller, ForbiddenException, Get, NotFoundException, Param, Post, Body, Req } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { PrismaService } from '../prisma.service';
import { PostsService } from '../posts/posts.service';

// 로그인 없이 누구나 볼 수 있는 글 상세 API + 회원/비회원 글쓰기 API입니다.
@Controller('public/posts')
export class PublicPostsController {
  constructor(
    private prisma: PrismaService,
    private postsService: PostsService,
    private jwt: JwtService,
  ) {}

  private async getLibraryId() {
    const library = await this.prisma.library.findFirst();
    if (!library) {
      throw new NotFoundException('도서관 정보를 찾을 수 없습니다.');
    }
    return library.id;
  }

  // 로그인한 회원이면 그 회원의 id를, 로그인하지 않았거나 토큰이 잘못됐으면 null을 돌려줍니다.
  // (관리자 전용 AdminGuard와 달리, 이 API는 로그인 안 해도 일단 통과시키고 회원인지만 구분합니다.)
  private async getOptionalUserId(req: any): Promise<number | null> {
    const authHeader = req.headers['authorization'];
    const token = authHeader && authHeader.startsWith('Bearer ') ? authHeader.slice(7) : null;
    if (!token) return null;
    try {
      const payload = await this.jwt.verifyAsync(token, { secret: process.env.JWT_SECRET });
      return payload.sub;
    } catch {
      return null;
    }
  }

  @Get(':id')
  async getPost(@Param('id') id: string) {
    const libraryId = await this.getLibraryId();
    return this.postsService.findOnePublic(libraryId, parseInt(id, 10));
  }

  // 홈페이지에서 글쓰기. 게시판이 회원 글쓰기를 허용해야 하고, 비회원이라면 그 게시판이
  // 비회원 글쓰기까지 허용해야만 저장됩니다.
  @Post()
  async createPost(@Req() req: any, @Body() body: any) {
    const libraryId = await this.getLibraryId();
    const board = await this.prisma.board.findFirst({ where: { id: Number(body.boardId), libraryId } });
    if (!board) {
      throw new NotFoundException('게시판을 찾을 수 없습니다.');
    }
    if (!board.allowMemberWrite) {
      throw new ForbiddenException('이 게시판은 글쓰기를 지원하지 않습니다.');
    }

    const authorUserId = await this.getOptionalUserId(req);
    if (!authorUserId && !board.allowGuestWrite) {
      throw new ForbiddenException('로그인이 필요합니다.');
    }

    return this.postsService.create(libraryId, authorUserId, body);
  }
}
```

---

## 2단계: `public-boards.controller.ts`에 올바른 내용 넣기

`backend/src/public-boards/public-boards.controller.ts` 파일을 여세요. 파일 안의 내용을 전부 지우고, 아래 내용으로 완전히 바꿔주세요. (파일 맨 위에 `export class PublicBoardsController`가 있는지 저장 후 꼭 확인해 주세요.)

```typescript
import { Controller, Get, NotFoundException, Param, Query } from '@nestjs/common';
import { PrismaService } from '../prisma.service';
import { BoardsService } from '../settings/boards.service';
import { PostsService } from '../posts/posts.service';

// 로그인 없이 누구나 볼 수 있는 게시판 목록/글목록 API입니다. (홈페이지에서 씁니다)
@Controller('public/boards')
export class PublicBoardsController {
  constructor(
    private prisma: PrismaService,
    private boardsService: BoardsService,
    private postsService: PostsService,
  ) {}

  // 지금은 도서관이 하나뿐인 시스템이라, 로그인 정보 없이도 첫 번째 도서관을 그대로 씁니다.
  private async getLibraryId() {
    const library = await this.prisma.library.findFirst();
    if (!library) {
      throw new NotFoundException('도서관 정보를 찾을 수 없습니다.');
    }
    return library.id;
  }

  // 게시판 10개 목록
  @Get()
  async list() {
    const libraryId = await this.getLibraryId();
    return this.boardsService.list(libraryId);
  }

  // 특정 게시판의 글 목록 (예: /public/boards/notice/posts?page=1)
  @Get(':code/posts')
  async listPosts(@Param('code') code: string, @Query('page') page = '1') {
    const libraryId = await this.getLibraryId();
    return this.postsService.listPublic(libraryId, code, parseInt(page, 10) || 1);
  }

  // '자료를 신청합니다' 게시판 글쓰기 화면에서 자료 종류 드롭다운에 쓸 목록을 내려줍니다.
  // (로그인하지 않은 사람도 봐야 하므로 공개 API로 둡니다.)
  @Get(':code/material-request-options')
  async materialRequestOptions(@Param('code') code: string) {
    const libraryId = await this.getLibraryId();
    const board = await this.prisma.board.findFirst({ where: { code, libraryId } });
    if (!board) {
      throw new NotFoundException('게시판을 찾을 수 없습니다.');
    }
    return this.postsService.getMaterialRequestOptions(libraryId);
  }
}
```

---

## 확인하기

1. 두 파일을 저장한 뒤, 파일 이름과 그 안의 `export class` 이름이 서로 맞는지 다시 한번 확인해 주세요.
   - `public-posts.controller.ts` → `export class PublicPostsController`
   - `public-boards.controller.ts` → `export class PublicBoardsController`
2. `backend` 서버를 다시 켜서, 지난번과 같은 오류(`TS2724`)가 더 이상 나오지 않는지 확인해 주세요.
3. 서버가 정상적으로 켜지면, 개선136에서 안내드린 확인 절차(홈페이지 글쓰기·댓글, 자료신청 드롭다운 등)를 다시 한번 테스트해 주세요.

---

## GitHub 커밋

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선137 가이드: public-boards/public-posts 컨트롤러 파일 내용이 서로 바뀐 문제 수정"
git push
```

---

## 최종 점검표

- [ ] `public-posts.controller.ts`에 `PublicPostsController`가 들어있다.
- [ ] `public-boards.controller.ts`에 `PublicBoardsController`가 들어있다.
- [ ] 백엔드 서버가 오류 없이 정상적으로 켜진다.
- [ ] GitHub에 커밋 및 푸시를 완료했다.

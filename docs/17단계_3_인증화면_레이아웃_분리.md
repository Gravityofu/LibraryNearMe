# 17단계 (3) — 탭 여백 조정 + 로그인 계열 화면 레이아웃 분리

> 목표: ① "설정" 화면의 탭 버튼 사이 여백을 넓힙니다. ② 로그인/회원가입/아이디
> 찾기/비밀번호 찾기, 이 네 화면에서는 왼쪽 메뉴와 오른쪽 최신글 창을 숨기고
> 가운데 박스만 보이게 합니다. 이 박스 맨 위에는 도서관 로고·이름을, 그
> 아래 줄에는 화면 제목과 "뒤로 가기" 버튼을 넣어서 다른 페이지로 나갈 수
> 있게 만듭니다.
> 소요 시간: 약 40분

---

## 오늘 만드는 것 (그림으로)

### ① 탭 여백

```
수정 전: [KORMARC 태그][도서관]     ← 버튼이 붙어있음
수정 후: [KORMARC 태그]  [도서관]    ← 사이에 여백
```

### ② 로그인 계열 화면

```
수정 전 (다른 페이지들과 똑같은 3단 구성)
┌────────┬──────────────┬────────┐
│ 왼쪽메뉴  │   로그인 박스     │ 최신글  │
└────────┴──────────────┴────────┘

수정 후 (로그인/회원가입/아이디찾기/비밀번호찾기 네 화면만)
              ┌──────────────────┐
              │   [로고]  도서관이름   │  ← 맨 위
              │  ────────────────  │
              │  로그인      [뒤로가기] │  ← 제목 + 뒤로가기
              │  ────────────────  │
              │   (아이디/비밀번호 등    │
              │    기존 입력 폼)        │
              └──────────────────┘
```

---

## 1. "설정" 탭 여백 조정 — 약 3분

`frontend/src/app/admin/settings/page.tsx` 를 엽니다. 아래 부분을 찾으세요.

```tsx
      <Tabs defaultValue="kormarcTags">
        <TabsList>
          <TabsTrigger value="kormarcTags">{t("settings.tabs.kormarcTags")}</TabsTrigger>
          <TabsTrigger value="library">{t("settings.tabs.library")}</TabsTrigger>
        </TabsList>
```

이렇게 바꿔주세요. (`TabsList`에 여백 클래스 `gap-2`를 추가했어요.)

```tsx
      <Tabs defaultValue="kormarcTags">
        <TabsList className="gap-2">
          <TabsTrigger value="kormarcTags">{t("settings.tabs.kormarcTags")}</TabsTrigger>
          <TabsTrigger value="library">{t("settings.tabs.library")}</TabsTrigger>
        </TabsList>
```

✅ 확인: "설정" 화면에서 두 탭 버튼 사이에 간격이 생겼는지 확인하세요.

---

## 2. dictionary.ts에 "뒤로 가기" 문구 추가 — 약 2분

`frontend/src/lib/dictionary.ts` 를 엽니다. `"login.findPasswordLink"` 줄
바로 아래(한글·영어 각각)에 추가하세요.

**한글 쪽:**

```ts
    "auth.backBtn": "뒤로 가기",
```

**영어 쪽:**

```ts
    "auth.backBtn": "Back",
```

---

## 3. 공용 부품 `AuthPageBox` 만들기 — 약 8분

로그인/회원가입/아이디 찾기/비밀번호 찾기 네 화면 모두 "로고+이름 → 제목+뒤로가기
→ 내용" 구조가 똑같기 때문에, 이 틀을 부품 하나로 만들어서 네 화면이 같이
씁니다.

`frontend/src/components` 폴더 안에 **`auth-page-box.tsx`** 파일을 새로
만들고 아래 내용을 붙여넣습니다.

```tsx
"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Card, CardContent } from "@/components/ui/card";
import { useI18n } from "@/components/language-provider";

const API_URL = "http://localhost:3001";

type Props = {
  title: string;
  children: React.ReactNode;
};

// 로그인/회원가입/아이디 찾기/비밀번호 찾기 화면이 공통으로 쓰는 박스입니다.
// 맨 위에 도서관 로고+이름, 그 아래 줄에 화면 제목과 "뒤로 가기" 버튼을 보여줍니다.
export default function AuthPageBox({ title, children }: Props) {
  const { t } = useI18n();
  const router = useRouter();
  const [name, setName] = useState("도서관");
  const [logoUrl, setLogoUrl] = useState<string | null>(null);

  useEffect(() => {
    fetch(`${API_URL}/library`)
      .then((res) => res.json())
      .then((data) => {
        if (data) {
          setName(data.name);
          setLogoUrl(data.logoUrl || null);
        }
      })
      .catch(() => {});
  }, []);

  return (
    <main className="mx-auto max-w-md p-8">
      <Card>
        <CardContent className="pt-6">
          <div className="mb-5 flex flex-col items-center gap-1.5">
            {logoUrl && <img src={logoUrl} alt={name} className="h-9 w-auto" />}
            <span className="text-base font-extrabold">{name}</span>
          </div>
          <div className="mb-4 flex items-center justify-between">
            <h1 className="text-lg font-bold">{title}</h1>
            <button
              type="button"
              onClick={() => router.back()}
              className="cursor-pointer rounded-full border border-neutral-200 bg-white px-3 py-1.5 text-xs font-medium text-neutral-700 shadow-sm hover:bg-neutral-50"
            >
              {t("auth.backBtn")}
            </button>
          </div>
          {children}
        </CardContent>
      </Card>
    </main>
  );
}
```

- 이 화면들에는 사이드바가 없어서 뒤로 나갈 방법이 없어요. `router.back()`은
  브라우저의 "뒤로 가기"와 같은 동작이라, 이 화면에 들어오기 전 페이지로
  돌아갑니다.

---

## 4. `(auth)` 폴더 새로 만들기 — 약 3분

`frontend/src/app` 폴더 안에 **`(auth)`** 라는 이름의 폴더를 새로 만드세요.
(괄호도 이름의 일부예요. 이렇게 괄호로 감싼 폴더는 주소에는 포함되지 않고,
화면들을 그룹으로 묶어서 레이아웃만 따로 적용할 때 씁니다. `(site)` 폴더와
같은 방식이에요.)

그 안에 **`layout.tsx`** 파일을 만들어 아래 내용을 붙여넣습니다.

```tsx
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div className="min-h-screen">{children}</div>;
}
```

- 이 레이아웃은 `(site)/layout.tsx`처럼 사이드바나 최신글 창을 넣지 않아요.
  그래서 이 그룹 안에 있는 화면들은 가운데 박스만 보이게 됩니다.

---

## 5. 로그인 화면 옮기기 — 약 8분

`frontend/src/app/(auth)` 폴더 안에 **`login`** 폴더를 만들고, 그 안에
**`page.tsx`** 를 만들어 아래 내용을 붙여넣습니다.

```tsx
"use client";

import { useState, useEffect } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";
import { useAuth } from "@/components/auth-provider";
import AuthPageBox from "@/components/auth-page-box";

const API_URL = "http://localhost:3001";

export default function LoginPage() {
  const { t } = useI18n();
  const { notify } = useNotify();
  const { login } = useAuth();
  const router = useRouter();
  const [loginId, setLoginId] = useState("");
  const [password, setPassword] = useState("");
  const [redirectParam, setRedirectParam] = useState("");

  useEffect(() => {
    const r = new URLSearchParams(window.location.search).get("redirect");
    if (r) setRedirectParam(r);
  }, []);

  async function handleLogin() {
    const res = await fetch(`${API_URL}/auth/login`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ loginId, password }),
    });
    if (res.ok) {
      const data = await res.json();
      login({ token: data.token, userName: data.user.name, role: data.user.role });
      notify(t("login.welcome").replace("{name}", data.user.name), "success");
      router.push(redirectParam || "/");
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("login.fail")), "error");
    }
  }

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    handleLogin();
  }

  return (
    <AuthPageBox title={t("login.title")}>
      <form onSubmit={handleSubmit} className="flex flex-col gap-4">
        <div className="flex flex-col gap-2">
          <Label htmlFor="loginId">{t("login.id")}</Label>
          <Input id="loginId" value={loginId} onChange={(e) => setLoginId(e.target.value)} />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="password">{t("login.password")}</Label>
          <Input id="password" type="password" value={password} onChange={(e) => setPassword(e.target.value)} />
        </div>
        <Button type="submit" className="cursor-pointer">
          {t("login.submit")}
        </Button>
      </form>
      <div className="mt-4 flex items-center justify-center gap-3 text-sm text-neutral-500">
        <Link
          href={redirectParam ? `/signup?redirect=${encodeURIComponent(redirectParam)}` : "/signup"}
          className="hover:text-neutral-800"
        >
          {t("login.signupLink")}
        </Link>
        <span className="text-neutral-300">|</span>
        <Link href="/find-id" className="hover:text-neutral-800">
          {t("login.findIdLink")}
        </Link>
        <span className="text-neutral-300">|</span>
        <Link href="/find-password" className="hover:text-neutral-800">
          {t("login.findPasswordLink")}
        </Link>
      </div>
    </AuthPageBox>
  );
}
```

이제 예전 위치의 로그인 화면을 지웁니다. `frontend/src/app/(site)/login`
**폴더 전체**를 삭제하세요.

---

## 6. 회원가입 화면 옮기기 — 약 8분

`frontend/src/app/(auth)` 폴더 안에 **`signup`** 폴더를 만들고, 그 안에
**`page.tsx`** 를 만들어 아래 내용을 붙여넣습니다.

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";
import { useAuth } from "@/components/auth-provider";
import AuthPageBox from "@/components/auth-page-box";

const API_URL = "http://localhost:3001";

function formatPhone(raw: string) {
  const digits = raw.replace(/\D/g, "").slice(0, 11);
  if (digits.length < 4) return digits;
  if (digits.length < 8) return `${digits.slice(0, 3)}-${digits.slice(3)}`;
  return `${digits.slice(0, 3)}-${digits.slice(3, 7)}-${digits.slice(7)}`;
}

function isValidPhone(phone: string) {
  return phone.replace(/\D/g, "").length === 11;
}

function isValidEmail(email: string) {
  if (!email.trim()) return true;
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim());
}

export default function SignupPage() {
  const { t } = useI18n();
  const { notify } = useNotify();
  const { login } = useAuth();
  const router = useRouter();
  const [loginId, setLoginId] = useState("");
  const [password, setPassword] = useState("");
  const [name, setName] = useState("");
  const [phone, setPhone] = useState("");
  const [email, setEmail] = useState("");

  async function handleSignup() {
    if (!isValidPhone(phone)) {
      notify("❌ " + t("signup.invalidPhone"), "error");
      return;
    }
    if (!isValidEmail(email)) {
      notify("❌ " + t("signup.invalidEmail"), "error");
      return;
    }

    const res = await fetch(`${API_URL}/users`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ loginId, password, name, phone, email }),
    });

    if (res.ok) {
      const loginRes = await fetch(`${API_URL}/auth/login`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ loginId, password }),
      });

      if (loginRes.ok) {
        const loginData = await loginRes.json();
        login({ token: loginData.token, userName: loginData.user.name, role: loginData.user.role });
        notify(t("signup.success"), "success");
        const redirect = new URLSearchParams(window.location.search).get("redirect") || "/";
        router.push(redirect);
      } else {
        notify(t("signup.success"), "success");
        router.push("/login");
      }
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("signup.fail")), "error");
    }
  }

  return (
    <AuthPageBox title={t("signup.title")}>
      <div className="flex flex-col gap-4">
        <div className="flex flex-col gap-2">
          <Label htmlFor="loginId">{t("signup.id")}</Label>
          <Input id="loginId" value={loginId} onChange={(e) => setLoginId(e.target.value)} />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="password">{t("signup.password")}</Label>
          <Input id="password" type="password" value={password} onChange={(e) => setPassword(e.target.value)} />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="name">{t("signup.name")}</Label>
          <Input id="name" value={name} onChange={(e) => setName(e.target.value)} />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="phone">{t("signup.phone")}</Label>
          <Input
            id="phone"
            value={phone}
            onChange={(e) => setPhone(formatPhone(e.target.value))}
            placeholder="000-0000-0000"
            inputMode="numeric"
            maxLength={13}
          />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="email">{t("signup.email")}</Label>
          <Input id="email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
        </div>
        <Button className="cursor-pointer" onClick={handleSignup}>
          {t("signup.submit")}
        </Button>
      </div>
    </AuthPageBox>
  );
}
```

이제 예전 위치의 회원가입 화면을 지웁니다. `frontend/src/app/(site)/signup`
**폴더 전체**를 삭제하세요.

> 💡 혹시 이전에 안내드린 "회원가입 완료 후 자동 로그인 및 이동" 가이드
> (16단계 2)를 아직 반영하지 않으셨다면, 위 코드에 이미 그 내용까지
> 포함되어 있으니 따로 다시 작업하지 않으셔도 됩니다.

---

## 7. "아이디 찾기" 화면 옮기기 — 약 5분

`frontend/src/app/(auth)` 폴더 안에 **`find-id`** 폴더를 만들고, 그 안에
**`page.tsx`** 를 만들어 아래 내용을 붙여넣습니다.

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";
import AuthPageBox from "@/components/auth-page-box";

const API_URL = "http://localhost:3001";

function formatPhone(raw: string) {
  const digits = raw.replace(/\D/g, "").slice(0, 11);
  if (digits.length < 4) return digits;
  if (digits.length < 8) return `${digits.slice(0, 3)}-${digits.slice(3)}`;
  return `${digits.slice(0, 3)}-${digits.slice(3, 7)}-${digits.slice(7)}`;
}

function isValidPhone(phone: string) {
  return phone.replace(/\D/g, "").length === 11;
}

export default function FindIdPage() {
  const { t } = useI18n();
  const { notify } = useNotify();
  const [name, setName] = useState("");
  const [phone, setPhone] = useState("");
  const [result, setResult] = useState<string[] | null>(null);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();

    if (!name.trim() || !phone.trim()) {
      notify("❌ " + t("findId.requiredFields"), "error");
      return;
    }
    if (!isValidPhone(phone)) {
      notify("❌ " + t("findId.invalidPhone"), "error");
      return;
    }

    const res = await fetch(`${API_URL}/users/find-id`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ name, phone }),
    });

    if (res.ok) {
      const data = await res.json();
      setResult(data.loginIds || []);
    } else {
      notify("❌ " + t("findId.fail"), "error");
    }
  }

  return (
    <AuthPageBox title={t("findId.title")}>
      {result === null ? (
        <form onSubmit={handleSubmit} className="flex flex-col gap-4">
          <div className="flex flex-col gap-2">
            <Label htmlFor="name">{t("findId.name")}</Label>
            <Input id="name" value={name} onChange={(e) => setName(e.target.value)} />
          </div>
          <div className="flex flex-col gap-2">
            <Label htmlFor="phone">{t("findId.phone")}</Label>
            <Input
              id="phone"
              value={phone}
              onChange={(e) => setPhone(formatPhone(e.target.value))}
              placeholder="000-0000-0000"
              inputMode="numeric"
              maxLength={13}
            />
          </div>
          <Button type="submit" className="cursor-pointer">
            {t("findId.submit")}
          </Button>
        </form>
      ) : (
        <div className="flex flex-col gap-4">
          {result.length > 0 ? (
            <div className="flex flex-col gap-1">
              <p className="text-sm text-neutral-500">{t("findId.resultFound")}</p>
              {result.map((id, i) => (
                <p key={i} className="text-lg font-semibold">
                  {id}
                </p>
              ))}
            </div>
          ) : (
            <p className="text-sm text-neutral-500">{t("findId.resultNotFound")}</p>
          )}
          <Link href="/login" className="text-center text-sm text-neutral-500 hover:text-neutral-800">
            {t("findId.backToLogin")}
          </Link>
        </div>
      )}
    </AuthPageBox>
  );
}
```

이제 예전 위치를 지웁니다. `frontend/src/app/(site)/find-id` **폴더 전체**를
삭제하세요.

---

## 8. "비밀번호 찾기" 화면 옮기기 — 약 5분

`frontend/src/app/(auth)` 폴더 안에 **`find-password`** 폴더를 만들고, 그
안에 **`page.tsx`** 를 만들어 아래 내용을 붙여넣습니다.

```tsx
"use client";

import Link from "next/link";
import { useI18n } from "@/components/language-provider";
import AuthPageBox from "@/components/auth-page-box";

export default function FindPasswordPage() {
  const { t } = useI18n();

  return (
    <AuthPageBox title={t("findPassword.title")}>
      <div className="flex flex-col gap-4">
        <p className="text-sm text-neutral-500">{t("findPassword.message")}</p>
        <Link href="/login" className="text-center text-sm text-neutral-500 hover:text-neutral-800">
          {t("findPassword.backToLogin")}
        </Link>
      </div>
    </AuthPageBox>
  );
}
```

- 지금까지는 이 화면만 언어 전환(다국어)이 안 되어 있었는데, 오늘 `AuthPageBox`로
  옮기면서 `dictionary.ts`에 이미 있던 `findPassword.*` 문구를 사용하도록
  같이 정리했어요.

이제 예전 위치를 지웁니다. `frontend/src/app/(site)/find-password`
**폴더 전체**를 삭제하세요.

---

## 9. 확인하기 — 약 8분

1. `/login`, `/signup`, `/find-id`, `/find-password` 네 화면 모두 왼쪽 메뉴와
   오른쪽 최신글 창이 없이, 가운데 박스만 보이는지 확인합니다.
2. 각 박스 맨 위에 도서관 로고(있다면)와 이름이 보이는지 확인합니다.
3. 그 아래 줄에 화면 제목(예: "로그인")과 오른쪽에 "뒤로 가기" 버튼이 있는지
   확인합니다.
4. "뒤로 가기"를 누르면 이 화면에 들어오기 전 페이지로 돌아가는지 확인합니다.
5. 로그인 화면 아래 "회원가입 | 아이디 찾기 | 비밀번호 찾기" 링크가 잘
   작동하는지 확인합니다.
6. 실제로 로그인, 회원가입, 아이디 찾기가 예전처럼 정상 동작하는지 확인합니다.
7. 그 외 다른 화면들(홈, 회원, 목록 등)은 여전히 왼쪽 메뉴·오른쪽 최신글
   창이 잘 보이는지 확인합니다. (`(site)` 그룹은 그대로 남아있어서 영향이
   없어야 해요.)

✅ 모두 확인되면 성공입니다! 🎉

---

## 10. GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "17단계(3): 설정 탭 여백 조정, 로그인 계열 화면을 별도 레이아웃으로 분리"
git push
```

---

## 최종 점검표

- [ ] `admin/settings/page.tsx` — `TabsList`에 `gap-2` 추가
- [ ] `dictionary.ts` — `auth.backBtn` 한글/영어 추가
- [ ] `components/auth-page-box.tsx` 새로 생성
- [ ] `app/(auth)/layout.tsx` 새로 생성
- [ ] `app/(auth)/login/page.tsx` 새로 생성, `(site)/login` 삭제
- [ ] `app/(auth)/signup/page.tsx` 새로 생성, `(site)/signup` 삭제
- [ ] `app/(auth)/find-id/page.tsx` 새로 생성, `(site)/find-id` 삭제
- [ ] `app/(auth)/find-password/page.tsx` 새로 생성, `(site)/find-password` 삭제
- [ ] 네 화면 모두 사이드바/최신글 없이 가운데 박스만 보임
- [ ] 로고+이름, 제목+뒤로가기 줄 확인
- [ ] 뒤로 가기 동작 확인
- [ ] 다른 화면(홈 등)은 그대로인지 확인
- [ ] GitHub에 올림

완료되면 알려주세요!

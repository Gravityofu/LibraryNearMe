# 개선75 - '회원 관리' 목록 상태 색상 + 정지(대출제한) 이력 등록 기능

## 목표

1. '회원 관리' 메뉴의 회원 목록 화면에도, '대출/반납' 화면과 같은 방식으로 상태 글자에
   색을 입힙니다. (활성-파란색, 대기-노란색, 정지-주황색, 탈퇴-빨간색)
2. '회원 정보 수정' 모달 안에, 이 회원에게 새로운 정지(대출제한) 기록을 등록할 수 있는
   입력 폼을 추가합니다. 종료일과 사유를 입력하고 '등록' 버튼을 누르면, 정지 기록이 새로
   만들어지고 이 회원의 상태도 자동으로 '정지'로 바뀝니다.

> 참고: 회원님의 '회원 관리' 목록 화면은 '대출/반납' 화면보다 간단한 형태로 되어 있어서
> (생년월일 3칸 입력, 주소 검색, 회원등급/역할 같은 항목이 아직 없는 버전입니다), 이번
> 가이드는 지금 실제로 있는 화면 그대로에 맞춰서 만들었습니다.

---

## 1. 백엔드 - 정지 기록을 새로 만드는 API 추가하기

### 1-1. `backend/src/loan-restrictions/loan-restrictions.service.ts` 수정하기

`createRestriction` 함수에, 입력값이 올바른지 확인하는 부분을 추가합니다.

**찾을 코드:**

```ts
import { Injectable, Logger } from '@nestjs/common';
import { Cron, CronExpression } from '@nestjs/schedule';
import { PrismaService } from '../prisma.service';

@Injectable()
export class LoanRestrictionsService {
  private readonly logger = new Logger(LoanRestrictionsService.name);

  constructor(private prisma: PrismaService) {}

  // 대출제한 기록을 새로 만들고, 회원 상태를 '정지'로 바꿉니다.
  // (실제로 이 함수를 호출하는 "반납 처리(연체 계산)" 기능은 다음 단계에서 만듭니다.)
  async createRestriction(libraryId: number, userId: number, endDate: Date, reason?: string) {
    const [restriction] = await this.prisma.$transaction([
      this.prisma.loanRestriction.create({
        data: { libraryId, userId, endDate, reason },
      }),
      this.prisma.user.update({
        where: { id: userId },
        data: { status: 'SUSPENDED' },
      }),
    ]);
    return restriction;
  }
```

**바꿀 코드:**

```ts
import { Injectable, Logger, BadRequestException, NotFoundException } from '@nestjs/common';
import { Cron, CronExpression } from '@nestjs/schedule';
import { PrismaService } from '../prisma.service';

@Injectable()
export class LoanRestrictionsService {
  private readonly logger = new Logger(LoanRestrictionsService.name);

  constructor(private prisma: PrismaService) {}

  // 대출제한 기록을 새로 만들고, 회원 상태를 '정지'로 바꿉니다.
  // (관리자가 '회원 정보 수정' 모달에서 직접 등록하거나, 나중에 "반납 처리(연체 계산)"
  // 기능에서 자동으로 호출할 수 있습니다.)
  async createRestriction(libraryId: number, userId: number, endDate: Date, reason?: string) {
    if (Number.isNaN(endDate.getTime())) {
      throw new BadRequestException('제한 종료일이 올바르지 않습니다.');
    }

    const member = await this.prisma.user.findFirst({ where: { id: userId, libraryId } });
    if (!member) {
      throw new NotFoundException('해당 회원을 찾을 수 없습니다.');
    }

    const [restriction] = await this.prisma.$transaction([
      this.prisma.loanRestriction.create({
        data: { libraryId, userId, endDate, reason },
      }),
      this.prisma.user.update({
        where: { id: userId },
        data: { status: 'SUSPENDED' },
      }),
    ]);
    return restriction;
  }
```

### 1-2. `backend/src/loan-restrictions/loan-restrictions.controller.ts` 수정하기

새 정지 기록을 등록하는 라우트(`POST /loan-restrictions`)를 추가합니다.

**찾을 코드:**

```ts
import { Controller, Get, Param, Req, UseGuards } from '@nestjs/common';
import { LoanRestrictionsService } from './loan-restrictions.service';
import { AdminGuard } from '../auth/admin.guard';

@Controller('loan-restrictions')
export class LoanRestrictionsController {
  constructor(private loanRestrictionsService: LoanRestrictionsService) {}

  // 특정 회원의 대출제한 이력 전체를 돌려줍니다. (최신순)
  @Get(':userId')
  @UseGuards(AdminGuard)
  findHistory(@Req() req: any, @Param('userId') userId: string) {
    return this.loanRestrictionsService.findHistory(req.user.libraryId, parseInt(userId, 10));
  }
}
```

**바꿀 코드:**

```ts
import { Body, Controller, Get, Param, Post, Req, UseGuards } from '@nestjs/common';
import { LoanRestrictionsService } from './loan-restrictions.service';
import { AdminGuard } from '../auth/admin.guard';

@Controller('loan-restrictions')
export class LoanRestrictionsController {
  constructor(private loanRestrictionsService: LoanRestrictionsService) {}

  // 특정 회원의 새 정지(대출제한) 기록을 등록합니다.
  @Post()
  @UseGuards(AdminGuard)
  create(@Req() req: any, @Body() body: { userId: number; endDate: string; reason?: string }) {
    return this.loanRestrictionsService.createRestriction(
      req.user.libraryId,
      body.userId,
      new Date(body.endDate),
      body.reason,
    );
  }

  // 특정 회원의 대출제한 이력 전체를 돌려줍니다. (최신순)
  @Get(':userId')
  @UseGuards(AdminGuard)
  findHistory(@Req() req: any, @Param('userId') userId: string) {
    return this.loanRestrictionsService.findHistory(req.user.libraryId, parseInt(userId, 10));
  }
}
```

---

## 2. `frontend/src/lib/dictionary.ts` 수정하기

### 2-1. 한국어(ko) 문구 추가하기

**찾을 코드:**

```ts
    "members.form.requiredFields": "아이디, 비밀번호, 이름은 필수입니다.",
    "members.form.saveSuccess": "저장되었습니다.",
    "members.form.saveFail": "저장에 실패했습니다.",
```

**바꿀 코드:**

```ts
    "members.form.requiredFields": "아이디, 비밀번호, 이름은 필수입니다.",
    "members.form.saveSuccess": "저장되었습니다.",
    "members.form.saveFail": "저장에 실패했습니다.",
    "members.restriction.formTitle": "대출 제한 이력 등록",
    "members.restriction.field.endDate": "제한 종료일",
    "members.restriction.field.reason": "사유",
    "members.restriction.field.reasonPlaceholder": "예: 연체 5일",
    "members.restriction.addBtn": "등록",
    "members.restriction.endDateRequired": "제한 종료일을 입력해 주세요.",
    "members.restriction.saveSuccess": "대출 제한 이력이 등록되었습니다.",
    "members.restriction.saveFail": "대출 제한 이력 등록에 실패했습니다.",
```

### 2-2. 영어(en) 문구 추가하기

**찾을 코드:**

```ts
    "members.form.saveSuccess": "Saved.",
    "members.form.saveFail": "Failed to save.",
```

**바꿀 코드:**

```ts
    "members.form.saveSuccess": "Saved.",
    "members.form.saveFail": "Failed to save.",
    "members.restriction.formTitle": "Add Restriction Record",
    "members.restriction.field.endDate": "Restriction End Date",
    "members.restriction.field.reason": "Reason",
    "members.restriction.field.reasonPlaceholder": "e.g. 5 days overdue",
    "members.restriction.addBtn": "Register",
    "members.restriction.endDateRequired": "Please enter the restriction end date.",
    "members.restriction.saveSuccess": "Restriction record added.",
    "members.restriction.saveFail": "Failed to add restriction record.",
```

---

## 3. `frontend/src/app/admin/members/page.tsx` 수정하기

### 3-1. 상태 색상 도우미 함수 추가하기

**찾을 코드:**

```ts
const EMPTY_FORM = {
  loginId: "",
  password: "",
  name: "",
  phone: "",
  email: "",
  memberNo: "",
  birthDate: "",
  address: "",
  status: "ACTIVE",
};

export default function MembersPage() {
```

**바꿀 코드:**

```ts
const EMPTY_FORM = {
  loginId: "",
  password: "",
  name: "",
  phone: "",
  email: "",
  memberNo: "",
  birthDate: "",
  address: "",
  status: "ACTIVE",
};

// 회원 상태에 따라 글자 색을 다르게 보여주기 위한 도우미 함수입니다.
function statusColorClass(status: string | undefined) {
  switch (status) {
    case "ACTIVE":
      return "text-blue-600";
    case "PENDING":
      return "text-yellow-600";
    case "SUSPENDED":
      return "text-orange-600";
    case "WITHDRAWN":
      return "text-red-600";
    default:
      return "";
  }
}

export default function MembersPage() {
```

### 3-2. 정지 이력 등록 폼의 입력값을 담을 상태 추가하기

**찾을 코드:**

```ts
  const [showForm, setShowForm] = useState(false);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [form, setForm] = useState(EMPTY_FORM);
```

**바꿀 코드:**

```ts
  const [showForm, setShowForm] = useState(false);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [form, setForm] = useState(EMPTY_FORM);
  const [restrictionForm, setRestrictionForm] = useState({ endDate: "", reason: "" });
```

### 3-3. 모달을 열 때마다 정지 이력 등록 폼도 함께 초기화하기

**찾을 코드:**

```ts
  function openAddModal() {
    setEditingId(null);
    setForm(EMPTY_FORM);
    setShowForm(true);
  }

  function openEditModal(row: MemberRow) {
    setEditingId(row.id);
    setForm({
      loginId: row.loginId || "",
      password: "",
      name: row.name,
      phone: row.phone || "",
      email: row.email || "",
      memberNo: row.memberNo || "",
      birthDate: "",
      address: "",
      status: row.status,
    });
    setShowForm(true);
  }
```

**바꿀 코드:**

```ts
  function openAddModal() {
    setEditingId(null);
    setForm(EMPTY_FORM);
    setRestrictionForm({ endDate: "", reason: "" });
    setShowForm(true);
  }

  function openEditModal(row: MemberRow) {
    setEditingId(row.id);
    setForm({
      loginId: row.loginId || "",
      password: "",
      name: row.name,
      phone: row.phone || "",
      email: row.email || "",
      memberNo: row.memberNo || "",
      birthDate: "",
      address: "",
      status: row.status,
    });
    setRestrictionForm({ endDate: "", reason: "" });
    setShowForm(true);
  }
```

### 3-4. 정지 이력을 등록하는 함수 추가하기

**찾을 코드:**

```ts
    if (res.ok) {
      notify("✅ " + t("members.form.saveSuccess"), "success");
      setShowForm(false);
      await fetchList(page, pageSize, filters);
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("members.form.saveFail")), "error");
    }
  }

  const totalPages = Math.max(1, Math.ceil(total / pageSize));
```

**바꿀 코드:**

```ts
    if (res.ok) {
      notify("✅ " + t("members.form.saveSuccess"), "success");
      setShowForm(false);
      await fetchList(page, pageSize, filters);
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("members.form.saveFail")), "error");
    }
  }

  // '등록' 버튼을 눌렀을 때 호출됩니다. 이 회원에게 새 정지 기록을 남기고, 상태를 '정지'로 바꿉니다.
  async function handleAddRestriction() {
    if (!editingId) return;
    if (!restrictionForm.endDate) {
      notify("❌ " + t("members.restriction.endDateRequired"), "error");
      return;
    }
    const token = localStorage.getItem("token");
    if (!token) return;

    const res = await fetch(`${API_URL}/loan-restrictions`, {
      method: "POST",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify({
        userId: editingId,
        endDate: restrictionForm.endDate,
        reason: restrictionForm.reason.trim() || undefined,
      }),
    });

    if (res.ok) {
      notify("✅ " + t("members.restriction.saveSuccess"), "success");
      setRestrictionForm({ endDate: "", reason: "" });
      setForm((prev) => ({ ...prev, status: "SUSPENDED" }));
      await fetchList(page, pageSize, filters);
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("members.restriction.saveFail")), "error");
    }
  }

  const totalPages = Math.max(1, Math.ceil(total / pageSize));
```

### 3-5. 목록 표의 '상태' 칸에 색상 적용하기

**찾을 코드:**

```tsx
                <td className="whitespace-nowrap px-4 py-2.5">
                  {t(`members.status.${row.status}`)}
                </td>
```

**바꿀 코드:**

```tsx
                <td className={`whitespace-nowrap px-4 py-2.5 font-semibold ${statusColorClass(row.status)}`}>
                  {t(`members.status.${row.status}`)}
                </td>
```

### 3-6. '회원 정보 수정' 모달에 정지 이력 등록 폼 추가하기

**찾을 코드:**

```tsx
              {editingId && (
                <label className="block">
                  <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.status")}</span>
                  <select
                    value={form.status}
                    onChange={(e) => setForm({ ...form, status: e.target.value })}
                    className="w-full cursor-pointer rounded-lg border px-3 py-2 text-sm"
                  >
                    {STATUS_VALUES.map((s) => (
                      <option key={s} value={s}>
                        {t(`members.status.${s}`)}
                      </option>
                    ))}
                  </select>
                </label>
              )}
            </div>
```

**바꿀 코드:**

```tsx
              {editingId && (
                <>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.status")}</span>
                    <select
                      value={form.status}
                      onChange={(e) => setForm({ ...form, status: e.target.value })}
                      className="w-full cursor-pointer rounded-lg border px-3 py-2 text-sm"
                    >
                      {STATUS_VALUES.map((s) => (
                        <option key={s} value={s}>
                          {t(`members.status.${s}`)}
                        </option>
                      ))}
                    </select>
                  </label>

                  <div className="rounded-lg border border-orange-200 bg-orange-50 p-3">
                    <p className="mb-2 text-sm font-semibold text-orange-700">
                      {t("members.restriction.formTitle")}
                    </p>
                    <label className="block">
                      <span className="mb-1 block text-sm text-neutral-500">
                        {t("members.restriction.field.endDate")} *
                      </span>
                      <input
                        type="date"
                        value={restrictionForm.endDate}
                        onChange={(e) => setRestrictionForm({ ...restrictionForm, endDate: e.target.value })}
                        className="w-full rounded-lg border px-3 py-2 text-sm"
                      />
                    </label>
                    <label className="mt-2 block">
                      <span className="mb-1 block text-sm text-neutral-500">
                        {t("members.restriction.field.reason")}
                      </span>
                      <input
                        value={restrictionForm.reason}
                        onChange={(e) => setRestrictionForm({ ...restrictionForm, reason: e.target.value })}
                        placeholder={t("members.restriction.field.reasonPlaceholder")}
                        className="w-full rounded-lg border px-3 py-2 text-sm"
                      />
                    </label>
                    <button
                      type="button"
                      onClick={handleAddRestriction}
                      className="mt-3 w-full cursor-pointer rounded-lg bg-orange-600 py-2 text-sm font-semibold text-white hover:bg-orange-700"
                    >
                      {t("members.restriction.addBtn")}
                    </button>
                  </div>
                </>
              )}
            </div>
```

---

## 확인하기

1. 파일을 모두 저장합니다.
2. 백엔드/프론트엔드 서버를 재시작합니다 (각 터미널에서 `Ctrl+C`로 종료 후 다시 실행).
3. '회원 관리' 메뉴의 회원 목록에서, 상태가 '활성'인 회원은 파란색, '대기'는 노란색,
   '정지'는 주황색으로 보이는지 확인합니다.
4. 회원 한 명을 눌러 '회원 정보 수정' 모달을 엽니다. 아래쪽에 주황색 박스로 '대출 제한
   이력 등록' 폼이 나오는지 확인합니다.
5. 종료일을 입력하지 않고 '등록' 버튼을 눌러서, "제한 종료일을 입력해 주세요." 안내가
   뜨는지 확인합니다.
6. 종료일(예: 내일 날짜)과 사유(예: "연체 5일")를 입력하고 '등록'을 눌러서, 등록 성공
   안내가 뜨고 이 회원의 상태가 자동으로 '정지'로 바뀌는지 확인합니다. 모달을 닫고
   목록에서도 이 회원의 상태 칸이 주황색 '정지'로 바뀌어 있는지 확인합니다.
7. '대출/반납' 메뉴에서 이 회원을 검색해서, 개선74에서 만든 '정지 이력 보기' 버튼으로
   방금 등록한 이력이 잘 보이는지도 함께 확인해 주세요.

---

## GitHub 커밋

작업이 잘 확인되면 아래 명령을 순서대로 실행해서 GitHub에 저장해 주세요.

```
cd C:\projects\LibraryNearMe
git add backend/src/loan-restrictions/loan-restrictions.service.ts
git add backend/src/loan-restrictions/loan-restrictions.controller.ts
git add frontend/src/lib/dictionary.ts
git add frontend/src/app/admin/members/page.tsx
git commit -m "개선75: 회원 목록 상태 색상 적용 및 정지(대출제한) 이력 등록 기능 추가"
git push
```

---

## 최종 점검표

- [ ] `loan-restrictions.service.ts`의 `createRestriction`에 유효성 검사를 추가했다
- [ ] `loan-restrictions.controller.ts`에 `POST /loan-restrictions` 라우트를 추가했다
- [ ] `dictionary.ts`에 한국어/영어 문구를 모두 추가했다
- [ ] `members/page.tsx`에 `statusColorClass` 함수를 추가했다
- [ ] `restrictionForm` 상태와 `handleAddRestriction` 함수를 추가했다
- [ ] `openAddModal`/`openEditModal`에서 `restrictionForm`도 함께 초기화되도록 했다
- [ ] 목록 표의 '상태' 칸에 색상이 적용되었다
- [ ] '회원 정보 수정' 모달에 정지 이력 등록 폼을 추가했다
- [ ] 백엔드/프론트엔드 서버를 재시작했다
- [ ] 확인하기의 모든 항목을 확인했다
- [ ] GitHub에 커밋 및 푸시했다

# 개선 69: 자료 저장 후 '목록'으로 이동 + 'MARC 수정'/'삭제' 버튼 크기 통일

## 목표

두 가지를 고칩니다.

1. '자료 등록' 3단계에서 정보를 입력하고 저장하면, 지금은 계속 같은 입력 화면에 머물러 있습니다. 저장이 끝나면 방금 저장한 자료의 제목으로 검색된 '목록' 화면으로 자동으로 이동하도록 바꿉니다.
2. 실물 자료 등록 화면(도서 등 MARC를 쓰는 자료)의 왼쪽 박스 아래에 있는 'MARC 수정' 버튼과 '삭제' 버튼의 크기가 서로 다릅니다. 두 버튼을 같은 크기로 맞추고, 위아래로 나란히 정렬합니다.

---

## 1. 저장 후 '목록' 화면으로 이동하기: `page.tsx`

`C:\projects\LibraryNearMe\frontend\src\app\admin\materials\new\page.tsx` 파일을 여세요.

### 1-1. 화면 이동 기능 가져오기

아래 부분을 찾으세요.

```tsx
import { useEffect, useRef, useState, KeyboardEvent } from "react";
import { useNotify } from "@/components/notify-provider";
```

이렇게 바꿔주세요.

```tsx
import { useEffect, useRef, useState, KeyboardEvent } from "react";
import { useRouter } from "next/navigation";
import { useNotify } from "@/components/notify-provider";
```

### 1-2. `router` 준비하기

아래 부분을 찾으세요.

```tsx
export default function NewMaterialPage() {
  const { notify } = useNotify();
  const { t, lang } = useI18n();
```

이렇게 바꿔주세요.

```tsx
export default function NewMaterialPage() {
  const router = useRouter();
  const { notify } = useNotify();
  const { t, lang } = useI18n();
```

### 1-3. 저장 성공 시, 방금 저장한 제목으로 '목록' 화면으로 이동하기

아래 부분을 찾으세요.

```tsx
    if (res.ok) {
      notify("✅ " + t("materials.new.saveSuccess"), "success");
      setForm({});
      setMarc(DEFAULT_FIELDS);
      setMarcRaw(undefined);
      setKolisResults([]);
      setSubjectWords([""]);
    } else {
```

이렇게 바꿔주세요.

```tsx
    if (res.ok) {
      const data = await res.json();
      notify("✅ " + t("materials.new.saveSuccess"), "success");

      // '목록' 화면이 열릴 때 이 조건으로 자동 검색하도록, 방금 저장한 자료의 제목을 미리 저장해둡니다.
      // (이 키 이름은 '목록' 화면의 검색 조건 기억 기능과 반드시 똑같아야 합니다.)
      sessionStorage.setItem(
        "lnm-materials-list-search",
        JSON.stringify({ filters: { title: data?.title || "" }, page: 1, pageSize: 10 }),
      );
      router.push("/admin/materials/list");
    } else {
```

파일을 저장하세요.

(참고: '목록' 화면은 개선63에서 만들어드린 기능 덕분에, `sessionStorage`에 저장된 검색 조건이 있으면 화면이 열릴 때 자동으로 그 조건으로 검색합니다. 이번에는 그 기능을 그대로 활용해서, 저장 직후에 방금 등록한 자료의 제목을 검색 조건으로 미리 넣어두고 '목록' 화면으로 이동하는 방식입니다.)

---

## 2. 'MARC 수정'/'삭제' 버튼 크기 맞추기: `page.tsx`

`C:\projects\LibraryNearMe\frontend\src\app\admin\materials\copies\page.tsx` 파일을 여세요.

아래 부분을 찾으세요.

```tsx
                <MarcEditor fields={marc} onChange={setMarc} />
                <button
                  type="button"
                  onClick={handleSaveMarc}
                  className="mt-3 cursor-pointer rounded-lg border px-4 py-2 text-sm font-semibold"
                >
                  {t("materials.copies.marcEditSave")}
                </button>
                <button
                  type="button"
                  onClick={() => setShowDeleteMaterialConfirm(true)}
                  className="mt-2 w-full cursor-pointer rounded-lg bg-red-600 py-2.5 text-sm font-semibold text-white hover:bg-red-700"
                >
                  {t("materials.copies.deleteMaterialBtn")}
                </button>
```

이렇게 바꿔주세요.

```tsx
                <MarcEditor fields={marc} onChange={setMarc} />
                <div className="mt-3 flex flex-col gap-2">
                  <button
                    type="button"
                    onClick={handleSaveMarc}
                    className="w-full cursor-pointer rounded-lg border px-4 py-2.5 text-sm font-semibold"
                  >
                    {t("materials.copies.marcEditSave")}
                  </button>
                  <button
                    type="button"
                    onClick={() => setShowDeleteMaterialConfirm(true)}
                    className="w-full cursor-pointer rounded-lg bg-red-600 py-2.5 text-sm font-semibold text-white hover:bg-red-700"
                  >
                    {t("materials.copies.deleteMaterialBtn")}
                  </button>
                </div>
```

무엇이 달라졌는지 설명드립니다.

- 두 버튼을 `flex flex-col gap-2`로 감싼 하나의 세로 묶음 안에 넣었습니다. 이렇게 하면 두 버튼이 항상 위아래로 나란히 정렬됩니다.
- 'MARC 수정' 버튼에도 `w-full`(가로 폭 꽉 채우기)과 `py-2.5`(위아래 여백)를 넣어서, '삭제' 버튼과 정확히 같은 크기가 되도록 맞췄습니다.

파일을 저장하세요.

---

## 확인하기

1. 프런트엔드 서버를 재시작하세요. (`npm run dev`)
2. '자료 등록' 화면에서 새 자료를 하나 등록하고 저장해보세요. 저장이 끝나면 자동으로 '목록' 화면으로 이동하면서, 방금 등록한 자료가 검색되어 나오는지 확인하세요.
3. MARC를 쓰는 자료(예: 도서)의 실물 자료 등록 화면을 열어보세요. 왼쪽 박스 아래의 'MARC 수정' 버튼과 '삭제' 버튼이 같은 크기로, 위아래로 나란히 보이는지 확인하세요.
4. 'MARC 수정' 버튼을 눌러 정상적으로 저장되는지, '삭제' 버튼을 눌러 삭제 확인 모달이 정상적으로 뜨는지 확인하세요.

---

## GitHub 커밋

모든 확인이 끝났다면 아래 명령어로 변경 내용을 저장하세요.

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선69: 자료 저장 후 목록 화면으로 이동, MARC 수정/삭제 버튼 크기 통일"
git push
```

---

## 최종 점검표

- [ ] 자료 저장 후 '목록' 화면으로 자동 이동한다.
- [ ] '목록' 화면에 방금 저장한 자료가 검색되어 나온다.
- [ ] 'MARC 수정' 버튼과 '삭제' 버튼의 크기가 같다.
- [ ] 두 버튼이 위아래로 나란히 정렬되어 있다.
- [ ] 두 버튼 모두 원래 기능(저장/삭제 확인)이 정상 작동한다.
- [ ] 변경 내용을 GitHub에 커밋 및 푸시했다.

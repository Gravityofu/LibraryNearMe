# 개선 (14) — 중복 페이지 타이틀 삭제 + 로그인 계열 화면 로고 클릭 시 홈으로 이동

> 목표: ① 브레드크럼이 이미 있는 "대시보드"/"설정" 화면에서, 그 아래 한 번 더
> 나오던 페이지 제목을 지웁니다. ② 로그인/회원가입/아이디 찾기/비밀번호
> 찾기 화면 상단의 도서관 로고+이름을 누르면 홈페이지로 이동하게 만듭니다.
> 소요 시간: 약 8분

---

## 1. "대시보드" 화면 타이틀 삭제 — 약 2분

`frontend/src/app/admin/page.tsx` 를 엽니다. 아래 부분을 찾으세요.

```tsx
  return (
    <div className="p-6">
      <h1 className="text-lg font-bold">{t("admin.menu.dashboard")}</h1>
      <p className="mt-4 text-sm text-neutral-400">{t("admin.dashboard.wip")}</p>
    </div>
  );
```

이렇게 바꿔주세요. (제목 줄만 지웠어요. 브레드크럼이 이미 "대시보드"를
보여주고 있어서 중복이었어요.)

```tsx
  return (
    <div className="p-6">
      <p className="text-sm text-neutral-400">{t("admin.dashboard.wip")}</p>
    </div>
  );
```

---

## 2. "설정" 화면 타이틀 삭제 — 약 2분

`frontend/src/app/admin/settings/page.tsx` 를 엽니다. 아래 부분을
찾으세요.

```tsx
  return (
    <div className="p-6">
      <h1 className="mb-4 text-lg font-bold">{t("settings.title")}</h1>

      <Tabs defaultValue="kormarcTags">
```

이렇게 바꿔주세요.

```tsx
  return (
    <div className="p-6">
      <Tabs defaultValue="kormarcTags">
```

---

## 3. 로그인 계열 화면 — 로고·이름 클릭 시 홈으로 이동 — 약 4분

`frontend/src/components/auth-page-box.tsx` 를 엽니다.

**① import 추가** — 맨 위, 아래 부분을 찾으세요.

```tsx
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
```

이렇게 바꿔주세요.

```tsx
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
```

**② 로고·이름을 링크로 감싸기** — 아래 부분을 찾으세요.

```tsx
          <div className="mb-5 flex flex-col items-center gap-1.5">
            {logoUrl && <img src={logoUrl} alt={name} className="h-9 w-auto" />}
            <span className="text-base font-extrabold">{name}</span>
          </div>
```

이렇게 바꿔주세요.

```tsx
          <Link href="/" className="mb-5 flex flex-col items-center gap-1.5">
            {logoUrl && <img src={logoUrl} alt={name} className="h-9 w-auto" />}
            <span className="text-base font-extrabold">{name}</span>
          </Link>
```

---

## 확인하기 — 약 4분

1. "대시보드" 화면에서 브레드크럼 아래에 더 이상 "대시보드"라는 큰 글씨
   제목이 없는지 확인합니다.
2. "설정" 화면에서도 마찬가지로 중복 제목이 사라졌는지 확인합니다.
3. `/login`, `/signup`, `/find-id`, `/find-password` 화면 상단의 로고·
   도서관 이름을 클릭하면 홈페이지(`/`)로 이동하는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선(14): 대시보드/설정 화면 중복 타이틀 삭제, 로그인 계열 화면 로고 클릭 시 홈 이동"
git push
```

---

## 최종 점검표

- [ ] `admin/page.tsx` — 타이틀 삭제
- [ ] `admin/settings/page.tsx` — 타이틀 삭제
- [ ] `components/auth-page-box.tsx` — 로고·이름을 `Link`로 감싸기
- [ ] 대시보드/설정 화면 중복 제목 사라짐 확인
- [ ] 로그인 계열 화면 로고 클릭 시 홈 이동 확인
- [ ] GitHub에 올림

완료되면 알려주세요!

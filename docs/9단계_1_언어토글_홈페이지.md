# 9단계 (1) — 언어 토글 만들기 + 홈페이지 번역

> 목표: 한국어/영어를 오가는 토글 버튼을 만들고, 홈페이지 화면의 고정 문구를
> 두 언어로 전환되게 합니다.
> 소요 시간: 약 50분

---

## 오늘의 원리: "번역 사전"

UI 문구를 코드에 직접 한국어로 쓰지 않고, **번역 사전**에 모아둡니다. 예를 들어 `nav.search`라는 이름표(키) 하나에 한국어 "자료검색"과 영어 "Search"를 짝지어 저장해요. 그러면 화면은 "지금 언어가 한국어면 한국어 값을, 영어면 영어 값을 꺼내 보여줘"라고 사전을 참고합니다. 토글 버튼을 누르면 언어 설정이 바뀌고 전체가 그 언어로 바뀌는 거죠.

이 방식의 장점 — 앞으로 새 메뉴를 만들 때 사전에 두 줄(한국어/영어)만 추가하면 자동으로 두 언어가 됩니다.

이번(1)에서는 홈페이지 화면(왼쪽 메뉴·검색창·본문·오른쪽 글)까지 번역하고, 로그인·회원가입·관리자 화면은 다음(2)에서 이어갑니다.

준비: 두 서버를 켜두세요.

---

## 1. 번역 사전 만들기 — 약 10분

`frontend` → `src` → **`lib`** 폴더 안에 **`dictionary.ts`** 파일을 만들어 붙여넣습니다. (`lib` 폴더는 shadcn 설치 때 생겼을 거예요. 없으면 새로 만드세요.)

```ts
export type Lang = "ko" | "en";

// 왼쪽이 이름표(키), 오른쪽이 실제 문구입니다.
export const dictionary: Record<Lang, Record<string, string>> = {
  ko: {
    "auth.button": "로그인 / 회원가입",

    "nav.search": "자료검색",
    "nav.search.all": "통합 검색",
    "nav.search.new": "신착 자료",
    "nav.search.popular": "인기 자료",
    "nav.use": "도서관 이용",
    "nav.use.guide": "이용 안내",
    "nav.use.location": "오시는 길",
    "nav.use.hours": "운영 시간",
    "nav.community": "커뮤니티",
    "nav.community.notice": "공지사항",
    "nav.community.free": "자유게시판",
    "nav.community.club": "독서모임",
    "nav.community.request": "자료 신청",
    "nav.myshelf": "내 서재",
    "nav.myshelf.loans": "대출 현황",
    "nav.myshelf.reservations": "예약 현황",
    "nav.myshelf.card": "모바일 회원증",
    "nav.about": "도서관 소개",
    "nav.about.greeting": "인사말",
    "nav.about.facilities": "시설 안내",

    "home.banner": "대표 이미지 · 관리자 페이지에서 변경 가능",
    "home.searchPlaceholder": "검색어를 입력하고 Enter를 누르세요",
    "home.searchHint": "Enter를 누르면 바로 검색됩니다 (검색 버튼 없음)",
    "home.searchEmpty": "검색어를 입력해 주세요",
    "home.searchTodo": '"{q}" 검색은 도서 관리 단계에서 연결됩니다',
    "home.welcome": "책과 사람을 잇는 공간, {name}입니다.",
    "home.welcomeSub": "왼쪽 메뉴에서 원하는 서비스를 골라보세요.",

    "posts.more": "더보기 ›",
    "common.library": "도서관",
  },
  en: {
    "auth.button": "Login / Sign up",

    "nav.search": "Search",
    "nav.search.all": "Integrated Search",
    "nav.search.new": "New Arrivals",
    "nav.search.popular": "Popular",
    "nav.use": "Using the Library",
    "nav.use.guide": "Guide",
    "nav.use.location": "Directions",
    "nav.use.hours": "Hours",
    "nav.community": "Community",
    "nav.community.notice": "Notices",
    "nav.community.free": "Open Board",
    "nav.community.club": "Book Clubs",
    "nav.community.request": "Request Materials",
    "nav.myshelf": "My Shelf",
    "nav.myshelf.loans": "My Loans",
    "nav.myshelf.reservations": "My Reservations",
    "nav.myshelf.card": "Mobile Card",
    "nav.about": "About",
    "nav.about.greeting": "Greeting",
    "nav.about.facilities": "Facilities",

    "home.banner": "Main image · editable in admin",
    "home.searchPlaceholder": "Type a keyword and press Enter",
    "home.searchHint": "Press Enter to search (no button)",
    "home.searchEmpty": "Please enter a keyword",
    "home.searchTodo": 'Search for "{q}" will be connected in the book step',
    "home.welcome": "{name} is a space that connects books and people.",
    "home.welcomeSub": "Choose a service from the left menu.",

    "posts.more": "More ›",
    "common.library": "Library",
  },
};
```

> 영어 번역은 지금 제가 넣어드린 걸로 시작하고, 나중에 어색한 표현이 있으면 이 파일에서 고치면 됩니다. 사전 한 곳만 고치면 전체에 반영돼요.

---

## 2. 언어 상태를 관리하는 Provider 만들기 — 약 10분

`components` 폴더에 **`language-provider.tsx`** 파일을 만들어 붙여넣습니다. 이게 "현재 언어가 무엇인지"를 앱 전체가 공유하게 해주는 장치예요.

```tsx
"use client";

import { createContext, useContext, useEffect, useState } from "react";
import { dictionary, type Lang } from "@/lib/dictionary";

type I18n = {
  lang: Lang;
  setLang: (l: Lang) => void;
  t: (key: string) => string;
};

const I18nContext = createContext<I18n | null>(null);

export function LanguageProvider({ children }: { children: React.ReactNode }) {
  const [lang, setLangState] = useState<Lang>("ko");

  // 저장돼 있던 언어 설정을 불러옵니다.
  useEffect(() => {
    const saved = localStorage.getItem("lang");
    if (saved === "ko" || saved === "en") setLangState(saved);
  }, []);

  function setLang(l: Lang) {
    setLangState(l);
    localStorage.setItem("lang", l); // 다음에 와도 유지되게 저장
  }

  // 이름표(key)를 넣으면 현재 언어의 문구를 돌려줍니다.
  function t(key: string) {
    return dictionary[lang][key] ?? key;
  }

  return (
    <I18nContext.Provider value={{ lang, setLang, t }}>
      {children}
    </I18nContext.Provider>
  );
}

// 화면 어디서든 언어와 번역 함수를 꺼내 쓰는 도구
export function useI18n() {
  const ctx = useContext(I18nContext);
  if (!ctx) throw new Error("LanguageProvider 안에서만 사용하세요.");
  return ctx;
}
```

---

## 3. 토글 버튼 만들기 — 약 5분

`components` 폴더에 **`language-toggle.tsx`** 파일을 만들어 붙여넣습니다.

```tsx
"use client";

import { useI18n } from "@/components/language-provider";

export default function LanguageToggle() {
  const { lang, setLang } = useI18n();
  return (
    <button
      onClick={() => setLang(lang === "ko" ? "en" : "ko")}
      className="rounded-md border border-neutral-200 px-2 py-1 text-xs font-semibold text-neutral-600 hover:bg-neutral-50"
    >
      {lang === "ko" ? "ENG" : "한국어"}
    </button>
  );
}
```

한국어일 때는 "ENG"(영어로 바꾸기), 영어일 때는 "한국어"가 표시돼요.

---

## 4. 앱 전체를 Provider로 감싸기 — 약 4분

`frontend` → `src` → `app` → **`layout.tsx`** (루트)를 엽니다. `LanguageProvider`를 불러와서 `{children}`을 감쌉니다.

맨 위 import에 한 줄 추가:

```tsx
import { LanguageProvider } from "@/components/language-provider";
```

그리고 `<body ...>` 안의 `{children}`을 아래처럼 Provider로 감싸주세요.

```tsx
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased bg-[#F9F6F0] text-neutral-900`}>
        <LanguageProvider>{children}</LanguageProvider>
      </body>
```

이제 앱 어디서든 언어 정보를 꺼내 쓸 수 있게 됐습니다.

---

## 5. 왼쪽 메뉴에 번역·토글 적용 — 약 10분

`components` → **`site-sidebar.tsx`** 를 열고 내용을 전부 지운 뒤 아래로 바꿔주세요.

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import { useI18n } from "@/components/language-provider";
import LanguageToggle from "@/components/language-toggle";

// 이제 글자 대신 '이름표(키)'를 씁니다.
const MENU = [
  { key: "nav.search", items: [
    { key: "nav.search.all", href: "/" },
    { key: "nav.search.new", href: "#" },
    { key: "nav.search.popular", href: "#" },
  ]},
  { key: "nav.use", items: [
    { key: "nav.use.guide", href: "#" },
    { key: "nav.use.location", href: "#" },
    { key: "nav.use.hours", href: "#" },
  ]},
  { key: "nav.community", items: [
    { key: "nav.community.notice", href: "#" },
    { key: "nav.community.free", href: "#" },
    { key: "nav.community.club", href: "#" },
    { key: "nav.community.request", href: "#" },
  ]},
  { key: "nav.myshelf", items: [
    { key: "nav.myshelf.loans", href: "#" },
    { key: "nav.myshelf.reservations", href: "#" },
    { key: "nav.myshelf.card", href: "#" },
  ]},
  { key: "nav.about", items: [
    { key: "nav.about.greeting", href: "#" },
    { key: "nav.about.facilities", href: "#" },
  ]},
];

export default function SiteSidebar({
  name,
  primaryColor,
}: {
  name: string;
  primaryColor?: string;
}) {
  const { t } = useI18n();
  const [open, setOpen] = useState<number[]>([0]);

  function toggle(i: number) {
    setOpen((prev) =>
      prev.includes(i) ? prev.filter((n) => n !== i) : [...prev, i]
    );
  }

  return (
    <aside className="rounded-xl border border-neutral-200 bg-white p-3.5">
      {/* 로고 + 언어 토글 */}
      <div className="flex items-center justify-between px-2 pb-3.5 pt-2">
        <Link href="/" className="text-lg font-extrabold" style={{ color: primaryColor }}>
          {name}
        </Link>
        <LanguageToggle />
      </div>

      <Link
        href="/login"
        className="block rounded-lg bg-[#383838] py-2.5 text-center text-sm font-semibold text-[#F9F6F0]"
      >
        {t("auth.button")}
      </Link>

      <nav className="mt-4">
        {MENU.map((group, i) => (
          <div key={group.key} className="border-b border-neutral-100 last:border-b-0">
            <button
              onClick={() => toggle(i)}
              className="flex w-full items-center justify-between px-2 py-2.5 text-sm font-semibold"
            >
              {t(group.key)}
              <span
                className={`text-[10px] text-neutral-400 transition-transform ${
                  open.includes(i) ? "rotate-90" : ""
                }`}
              >
                ▸
              </span>
            </button>
            {open.includes(i) && (
              <div className="pb-2">
                {group.items.map((item) => (
                  <Link
                    key={item.key}
                    href={item.href}
                    className="block rounded-md px-4 py-1.5 text-[13px] text-neutral-600 hover:bg-neutral-50"
                  >
                    {t(item.key)}
                  </Link>
                ))}
              </div>
            )}
          </div>
        ))}
      </nav>
    </aside>
  );
}
```

`{t("nav.search")}` 처럼, 글자 대신 이름표를 넣으면 번역 함수가 현재 언어에 맞는 문구로 바꿔줍니다.

---

## 6. 검색창에 번역 적용 — 약 6분

`components` → **`search-box.tsx`** 를 아래로 바꿔주세요.

```tsx
"use client";

import { useState } from "react";
import { useI18n } from "@/components/language-provider";

export default function SearchBox() {
  const { t } = useI18n();
  const [q, setQ] = useState("");
  const [searched, setSearched] = useState(false);

  function onKeyDown(e: React.KeyboardEvent<HTMLInputElement>) {
    if (e.key === "Enter") setSearched(true);
  }

  const hint = !searched
    ? t("home.searchHint")
    : q
    ? t("home.searchTodo").replace("{q}", q)
    : t("home.searchEmpty");

  return (
    <div className="mt-5">
      <div className="relative">
        <span className="pointer-events-none absolute left-5 top-1/2 -translate-y-1/2 text-neutral-400">
          🔍
        </span>
        <input
          value={q}
          onChange={(e) => setQ(e.target.value)}
          onKeyDown={onKeyDown}
          placeholder={t("home.searchPlaceholder")}
          className="h-[54px] w-full rounded-full border border-[#ddd6c9] bg-white pl-12 pr-6 text-[15px] outline-none focus:border-neutral-400"
        />
      </div>
      <p className="mt-2 text-center text-xs text-neutral-400">{hint}</p>
    </div>
  );
}
```

---

## 7. 오른쪽 최근 글에 번역 적용 — 약 4분

`components` → **`recent-posts.tsx`** 를 엽니다. **맨 첫 줄에 `"use client";` 를 추가**하고, `useI18n`을 불러온 뒤 "더보기"만 번역으로 바꿉니다. (게시판 이름·글 제목은 예시 데이터라 그대로 둡니다 — 나중에 진짜 게시판 글로 바뀔 부분이에요.)

파일 맨 위를 이렇게 바꿔주세요.

```tsx
"use client";

import { useI18n } from "@/components/language-provider";

const BOARDS = [
  // ... (기존 BOARDS 배열 그대로) ...
];

export default function RecentPosts() {
  const { t } = useI18n();
  return (
    <div className="flex flex-col gap-3.5">
      {BOARDS.map((board) => (
        <div key={board.name} className="rounded-xl border border-neutral-200 bg-white p-3.5">
          <h4 className="mb-2.5 flex items-center px-1 text-[13px] font-extrabold">
            {board.name}
            <span className="ml-auto text-[11px] font-normal text-neutral-400">
              {t("posts.more")}
            </span>
          </h4>
          {board.posts.map((p, idx) => (
            <div
              key={idx}
              className="flex items-baseline gap-2 border-t border-neutral-100 px-1 py-1.5 text-[12.5px] first:border-t-0"
            >
              <span className="truncate text-neutral-600">{p.title}</span>
              <span className="ml-auto shrink-0 text-[11px] text-neutral-400">{p.date}</span>
            </div>
          ))}
        </div>
      ))}
    </div>
  );
}
```

> `BOARDS` 배열은 기존 것을 그대로 두고, 위·아래 코드만 이렇게 맞춰주면 됩니다.

---

## 8. 홈 본문 번역 — 약 8분

홈의 사진 캡션과 환영 문구도 번역해야 하는데, 이 부분은 지금 홈 페이지(서버에서 데이터를 가져오는 곳)에 있어요. 번역은 브라우저(클라이언트)에서 언어를 아는 부품이 처리하므로, 본문을 담당할 작은 클라이언트 부품을 하나 만들어 분리합니다.

**① `components` 폴더에 `home-body.tsx` 만들기:**

```tsx
"use client";

import { useI18n } from "@/components/language-provider";
import SearchBox from "@/components/search-box";

export default function HomeBody({ libraryName }: { libraryName?: string }) {
  const { t } = useI18n();
  const name = libraryName ?? t("common.library");

  return (
    <main>
      <div className="flex h-[250px] items-center justify-center rounded-xl border border-[#e6ded0] bg-gradient-to-br from-[#c9d6ef] via-[#e8dfd0] to-[#d8c9b6]">
        <span className="rounded-full bg-black/25 px-3 py-1.5 text-xs text-white">
          {t("home.banner")}
        </span>
      </div>

      <SearchBox />

      <p className="mt-6 text-center text-sm leading-8 text-neutral-500">
        {t("home.welcome").replace("{name}", name)}
        <br />
        {t("home.welcomeSub")}
      </p>
    </main>
  );
}
```

**② `(site)` → `page.tsx` 를 아래로 바꾸기** (서버에서 도서관 이름을 가져와 위 부품에 전달):

```tsx
import HomeBody from "@/components/home-body";

const API_URL = "http://localhost:3001";

async function getLibrary() {
  try {
    const res = await fetch(`${API_URL}/library`, { cache: "no-store" });
    if (!res.ok) return null;
    return res.json();
  } catch {
    return null;
  }
}

export default async function Home() {
  const library = await getLibrary();
  return <HomeBody libraryName={library?.name} />;
}
```

---

## 9. 확인하기 — 약 4분

`localhost:3000` 에 접속합니다.

✅ 왼쪽 메뉴 로고 옆에 **ENG 버튼**이 보입니다.
✅ **ENG**를 누르면 메뉴(자료검색→Search 등), 로그인 버튼, 검색창 안내, 환영 문구가 **영어로 바뀝니다.**
✅ 다시 **한국어**를 누르면 한국어로 돌아옵니다.
✅ 영어로 바꾼 뒤 **새로고침(F5)** 해도 영어로 유지됩니다. (설정이 저장되니까요.)

오른쪽 게시판 글과 도서관 이름은 그대로예요 — 이건 나중에 관리할 실제 데이터라 번역 대상이 아닙니다.

---

## 10. GitHub에 저장 — 약 3분

```
cd C:\projects\LibraryNearMe
git status
```
🔒 `.env` 없는지 확인 후:
```
git add .
git commit -m "9단계(1): 언어 토글(한/영) + 홈페이지 번역"
git push
```

---

## 최종 점검표

- [ ] `lib/dictionary.ts` 번역 사전
- [ ] `language-provider.tsx`, `language-toggle.tsx` 생성
- [ ] 루트 layout에 `LanguageProvider`로 감쌈
- [ ] site-sidebar / search-box / recent-posts / home-body 번역 적용
- [ ] `(site)/page.tsx`가 HomeBody에 이름 전달
- [ ] 토글로 홈 화면 한/영 전환 + 새로고침 후 유지
- [ ] GitHub에 올림

---

## 다음 단계 예고 — 9단계 (2)

로그인·회원가입·관리자 화면의 고정 문구(아이디, 비밀번호, 저장하기 등)도 사전에 추가해서 두 언어로 만듭니다. 그러면 앱 전체가 한/영 전환돼요. 그다음엔 프리텐다드 폰트를 적용합니다. 완료되면 알려주세요!

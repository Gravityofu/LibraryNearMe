# 15단계 (1) — 회원 관리 백엔드 (목록·검색·등록·수정)

> 목표: 관리자가 회원(이용자)을 목록으로 조회·검색하고, 새 회원을 직접 등록하고,
> 정보를 수정하거나 "정지" 상태로 바꿀 수 있는 API를 만듭니다.
> (관리자 계정 자체를 관리하는 기능은 아니고, "이용자(MEMBER)"만 다룹니다.)
> 소요 시간: 약 25분

---

## 오늘 결정한 설계 (다음 단계 화면 만들 때도 이 원칙을 따라갑니다)

- **등록**: 회원가입 화면과 별도로, 관리자가 화면에서 바로 새 회원을 등록할 수 있습니다(아이디·초기 비밀번호를 관리자가 직접 입력).
- **삭제**: 표에서 완전히 지우지 않고, 상태를 **"정지"(SUSPENDED)**로 바꿉니다. 대출 기록 등 다른 표와 연결된 데이터가 있을 수 있어서, 완전 삭제 대신 로그인만 막는 방식이 안전해요(도서관 실무에서도 보통 이렇게 합니다).
- **범위**: 이 화면은 "이용자(MEMBER)"만 다룹니다. 관리자·슈퍼 계정 관리는 아직 안 만듭니다.
- **검색**: 이름·전화번호·아이디·회원번호·상태로 검색하는 "상세 검색"을 자료 목록처럼 만듭니다(다음 단계).

준비: backend 서버를 켜두세요.

---

## 1. 서비스에 목록·등록·수정 함수 추가 — 약 12분

`backend/src/users/users.service.ts` 를 엽니다. 맨 위 import 줄들을 아래로 바꿔주세요(`Role` import가 추가됐어요).

```typescript
import {
  Injectable,
  ConflictException,
  BadRequestException,
  NotFoundException,
} from '@nestjs/common';
import { PrismaService } from '../prisma.service';
import * as bcrypt from 'bcryptjs';
import { randomUUID } from 'crypto';
import { Role, UserStatus } from '@prisma/client';
```

기존 `signup` 함수는 그대로 두고, 그 아래(파일 맨 끝, 마지막 닫는 중괄호 `}` 바로 앞)에 아래 세 함수를 추가하세요.

```typescript
  // 회원 목록 조회 (검색 + 페이지네이션). role=MEMBER인 이용자만 대상.
  async list(
    libraryId: number,
    params: {
      page?: number;
      pageSize?: number;
      name?: string;
      phone?: string;
      loginId?: string;
      memberNo?: string;
      status?: string;
    },
  ) {
    const page = params.page && params.page > 0 ? params.page : 1;
    const pageSize = params.pageSize && params.pageSize > 0 ? params.pageSize : 10;

    const where: any = { libraryId, role: Role.MEMBER };
    if (params.name) where.name = { contains: params.name, mode: 'insensitive' };
    if (params.phone) where.phone = { contains: params.phone };
    if (params.loginId) where.loginId = { contains: params.loginId, mode: 'insensitive' };
    if (params.memberNo) where.memberNo = { contains: params.memberNo, mode: 'insensitive' };
    if (params.status) where.status = params.status;

    const [items, total] = await Promise.all([
      this.prisma.user.findMany({
        where,
        orderBy: { createdAt: 'desc' },
        skip: (page - 1) * pageSize,
        take: pageSize,
        select: {
          id: true,
          loginId: true,
          name: true,
          phone: true,
          email: true,
          memberNo: true,
          birthDate: true,
          address: true,
          status: true,
          createdAt: true,
          // passwordHash는 절대 내보내지 않습니다.
        },
      }),
      this.prisma.user.count({ where }),
    ]);

    return { items, total, page, pageSize };
  }

  // 관리자가 새 회원을 직접 등록
  async adminCreate(
    libraryId: number,
    data: {
      loginId: string;
      password: string;
      name: string;
      phone?: string;
      email?: string;
      memberNo?: string;
      birthDate?: string;
      address?: string;
    },
  ) {
    if (!data.loginId || !data.password || !data.name) {
      throw new BadRequestException('아이디, 비밀번호, 이름은 필수입니다.');
    }

    const existing = await this.prisma.user.findFirst({
      where: { libraryId, loginId: data.loginId },
    });
    if (existing) {
      throw new ConflictException('이미 사용 중인 아이디입니다.');
    }

    const passwordHash = await bcrypt.hash(data.password, 10);
    const cardToken = randomUUID();

    try {
      await this.prisma.user.create({
        data: {
          libraryId,
          loginId: data.loginId,
          passwordHash,
          name: data.name,
          phone: data.phone || undefined,
          email: data.email || undefined,
          memberNo: data.memberNo || undefined,
          birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
          address: data.address || undefined,
          cardToken,
        },
      });
    } catch (e: any) {
      if (e.code === 'P2002') {
        throw new ConflictException('이미 사용 중인 회원번호입니다.');
      }
      throw e;
    }

    return { message: '회원이 등록되었습니다.' };
  }

  // 회원 정보 수정 (이름/연락처/상태 등). 아이디·비밀번호·역할은 여기서 안 바꿉니다.
  async update(
    libraryId: number,
    id: number,
    data: {
      name?: string;
      phone?: string;
      email?: string;
      memberNo?: string;
      birthDate?: string;
      address?: string;
      status?: string;
    },
  ) {
    const existing = await this.prisma.user.findFirst({
      where: { id, libraryId, role: Role.MEMBER },
    });
    if (!existing) {
      throw new NotFoundException('회원을 찾을 수 없습니다.');
    }

    try {
      return await this.prisma.user.update({
        where: { id },
        data: {
          name: data.name,
          phone: data.phone,
          email: data.email || undefined,
          memberNo: data.memberNo || undefined,
          birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
          address: data.address || undefined,
          status: data.status ? (data.status as UserStatus) : undefined,
        },
        select: { id: true, name: true, status: true },
      });
    } catch (e: any) {
      if (e.code === 'P2002') {
        throw new ConflictException('이미 사용 중인 회원번호입니다.');
      }
      throw e;
    }
  }
```

- `list()`: 자료 목록 API와 같은 패턴이에요 — 검색 조건을 `where`에 하나씩 쌓고, `findMany`+`count`를 동시에 실행해서 목록과 전체 개수를 같이 돌려줍니다. `role: Role.MEMBER`를 항상 넣어서, 관리자·슈퍼 계정은 이 목록에 절대 안 나오게 막아뒀어요.
- `adminCreate()`: 회원가입(`signup`)과 거의 같은데, 도서관을 `library.findFirst()`가 아니라 로그인한 관리자의 `libraryId`로 받는다는 점이 달라요(관리자는 이미 로그인했으니까요).
- `update()`: **다른 도서관 회원 수정 방지** 확인(`findFirst({id, libraryId, role: MEMBER})`)을 먼저 하고 나서만 수정합니다. 아이디·비밀번호·역할은 이 함수로 못 바꾸게 일부러 뺐어요(더 민감한 값이라 나중에 필요해지면 별도 기능으로 만드는 게 안전해요).

---

## 2. 컨트롤러에 경로(route) 추가 — 약 8분

`backend/src/users/users.controller.ts` 를 엽니다. 전체를 아래로 바꿔주세요.

```typescript
import { Controller, Get, Post, Patch, Body, Param, Query, Req, UseGuards } from '@nestjs/common';
import { UsersService } from './users.service';
import { AdminGuard } from '../auth/admin.guard';

@Controller('users')
export class UsersController {
  constructor(private usersService: UsersService) {}

  // POST 요청: 새 회원 만들기(가입) — 누구나 접근 가능(홈페이지 회원가입)
  @Post()
  signup(
    @Body()
    body: {
      loginId: string;
      password: string;
      name: string;
      phone: string;
      email?: string;
    },
  ) {
    return this.usersService.signup(body);
  }

  // GET 요청: 회원 목록 조회(검색 포함) — 관리자만
  @Get()
  @UseGuards(AdminGuard)
  list(@Req() req: any, @Query() query: any) {
    return this.usersService.list(req.user.libraryId, {
      page: query.page ? parseInt(query.page, 10) : undefined,
      pageSize: query.pageSize ? parseInt(query.pageSize, 10) : undefined,
      name: query.name,
      phone: query.phone,
      loginId: query.loginId,
      memberNo: query.memberNo,
      status: query.status,
    });
  }

  // POST 요청: 관리자가 회원 직접 등록 — 관리자만
  @Post('admin')
  @UseGuards(AdminGuard)
  adminCreate(@Req() req: any, @Body() body: any) {
    return this.usersService.adminCreate(req.user.libraryId, body);
  }

  // PATCH 요청: 회원 정보 수정(상태 변경 포함) — 관리자만
  @Patch(':id')
  @UseGuards(AdminGuard)
  update(@Req() req: any, @Param('id') id: string, @Body() body: any) {
    return this.usersService.update(req.user.libraryId, parseInt(id, 10), body);
  }
}
```

정리하면 API 주소는 이렇게 됩니다.

| 메서드 | 주소 | 용도 | 접근 |
|---|---|---|---|
| POST | `/users` | 홈페이지 회원가입 | 누구나 |
| GET | `/users` | 회원 목록/검색 | 관리자만 |
| POST | `/users/admin` | 관리자 직접 등록 | 관리자만 |
| PATCH | `/users/:id` | 회원 정보 수정/상태변경 | 관리자만 |

---

## 3. 확인하기 — 약 5분

화면은 다음 단계(15-2)에서 만드니, 지금은 F12 콘솔로 간단히 확인해봐요. (관리자 계정으로 로그인되어 있어야 해요.)

```js
fetch("http://localhost:3001/users", {
  headers: { Authorization: "Bearer " + localStorage.getItem("token") },
}).then((r) => r.json()).then(console.log);
```

가입해둔 회원들의 목록(`items`, `total`, `page`, `pageSize`)이 콘솔에 나오면 성공입니다.

✅ 목록이 잘 나오면 완성입니다! 🎉

---

## 4. GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "15단계(1): 회원 관리 백엔드 (목록/검색 + 관리자 직접등록 + 수정/상태변경 API)"
git push
```

---

## 최종 점검표

- [ ] `users.service.ts` — `list()`/`adminCreate()`/`update()` 추가
- [ ] `users.controller.ts` — `GET /users`, `POST /users/admin`, `PATCH /users/:id` 추가 (모두 AdminGuard)
- [ ] 콘솔에서 회원 목록 조회 확인
- [ ] GitHub에 올림

---

## 다음 단계 예고 — 15단계 (2): 회원 목록 화면

이제 이 API를 쓰는 화면을 만들어요. 관리자 사이드바의 "회원 관리" 메뉴를 활성화하고, 자료 목록처럼 표 + "상세 검색"(이름·전화번호·아이디·회원번호·상태) + 페이지네이션 화면을 만듭니다. 완료되면 알려주세요!

# 7단계 (4) — 관리자 페이지에 자물쇠 걸기

> 목표: 관리자 역할을 가진 사람이 로그인했을 때만 도서관 설정을 바꿀 수 있게 만듭니다.
> 이걸로 계정 시스템(7단계)이 완성됩니다!
> 소요 시간: 약 35분

---

## 오늘의 위치

7단계(계정 시스템)의 **마지막 네 번째**입니다.

1. 사용자 표 만들기 ✅
2. 회원가입 기능 ✅
3. 로그인 기능 ✅
4. **관리자 페이지 자물쇠 ← 오늘**

## 오늘의 원리: 문지기(Guard)

지금 관리자 페이지의 저장 창구(`PATCH /library`)는 아무나 부를 수 있어요. 여기에 **문지기(Guard)** 를 세웁니다. 문지기는 이렇게 검사해요.

1. 손목밴드(토큰)를 가지고 왔는가? → 없으면 "로그인이 필요합니다" 하고 돌려보냄
2. 그 손목밴드가 진짜인가(위조 아닌가)? → 가짜면 거부
3. 관리자(ADMIN) 또는 슈퍼(SUPER) 역할인가? → 일반 회원이면 "관리자만 가능" 하고 거부

세 관문을 다 통과해야만 저장 창구로 들어갈 수 있습니다.

---

## 준비

주방·홀 두 서버를 켜두세요.

---

# A. 주방(NestJS) 쪽 — 문지기 세우기

## A-1. 문지기 만들기 — 약 10분

`backend` → `src` → `auth` 폴더 안에 **`admin.guard.ts`** 파일을 새로 만들어 아래를 붙여넣습니다.

```typescript
import {
  CanActivate,
  ExecutionContext,
  Injectable,
  UnauthorizedException,
  ForbiddenException,
} from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';

@Injectable()
export class AdminGuard implements CanActivate {
  constructor(private jwt: JwtService) {}

  async canActivate(context: ExecutionContext): Promise<boolean> {
    const request = context.switchToHttp().getRequest();

    // 1. 요청 헤더에서 손목밴드(토큰) 꺼내기 ("Bearer xxxxx" 모양)
    const authHeader = request.headers['authorization'];
    const token =
      authHeader && authHeader.startsWith('Bearer ')
        ? authHeader.slice(7)
        : null;

    if (!token) {
      throw new UnauthorizedException('로그인이 필요합니다.');
    }

    // 2. 손목밴드가 진짜인지(위조 안 됐는지) 확인
    let payload: any;
    try {
      payload = await this.jwt.verifyAsync(token, {
        secret: process.env.JWT_SECRET,
      });
    } catch {
      throw new UnauthorizedException('로그인 정보가 유효하지 않습니다.');
    }

    // 3. 관리자(ADMIN) 또는 슈퍼(SUPER)만 통과
    if (payload.role !== 'ADMIN' && payload.role !== 'SUPER') {
      throw new ForbiddenException('관리자만 접근할 수 있습니다.');
    }

    // 세 관문 통과!
    request.user = payload;
    return true;
  }
}
```

이 문지기가 앞서 말한 세 관문 검사를 그대로 코드로 옮긴 거예요. `verifyAsync`가 "이 토큰이 우리 비밀 열쇠로 서명된 진짜인지"를 확인합니다.

## A-2. 저장 창구에 문지기 배치하기 — 약 5분

`backend` → `src` → `library` → **`library.controller.ts`** 를 열고 아래로 통째로 바꿔주세요. (저장(`PATCH`) 창구에만 문지기 `@UseGuards(AdminGuard)`를 붙였어요. 가져오기(`GET`)는 홈페이지가 로그인 없이도 봐야 하니 그대로 열어둡니다.)

```typescript
import { Controller, Get, Patch, Body, UseGuards } from '@nestjs/common';
import { LibraryService } from './library.service';
import { AdminGuard } from '../auth/admin.guard';

@Controller('library')
export class LibraryController {
  constructor(private libraryService: LibraryService) {}

  // 가져오기: 누구나 가능 (홈페이지 표시용)
  @Get()
  getLibrary() {
    return this.libraryService.getLibrary();
  }

  // 바꾸기: 문지기 통과(관리자 로그인)해야만 가능
  @UseGuards(AdminGuard)
  @Patch()
  updateLibrary(@Body() body: { name: string; primaryColor: string }) {
    return this.libraryService.updateLibrary(body);
  }
}
```

저장하면 주방 서버가 다시 시작됩니다. `successfully started` 확인하세요. (app.module은 이번엔 바꿀 게 없어요.)

---

# B. 홀(Next.js) 쪽 — 관리자 화면에 손목밴드 채우기

## B-1. /admin 페이지 수정 — 약 10분

이제 관리자 화면이 두 가지를 해야 해요. **로그인 안 했으면 로그인하러 보내고**, 저장할 때는 **손목밴드(토큰)를 함께 보내는** 것입니다.

`frontend` → `src` → `app` → `admin` → **`page.tsx`** 를 열고 아래로 통째로 바꿔주세요.

```tsx
"use client";

import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const API_URL = "http://localhost:3001";

export default function AdminPage() {
  const [token, setToken] = useState<string | null>(null);
  const [checked, setChecked] = useState(false);
  const [name, setName] = useState("");
  const [primaryColor, setPrimaryColor] = useState("#2563eb");
  const [status, setStatus] = useState("");

  useEffect(() => {
    // 저장된 손목밴드(토큰)가 있는지 확인
    setToken(localStorage.getItem("token"));
    setChecked(true);

    // 현재 도서관 설정 불러오기 (가져오기는 로그인 없이 가능)
    fetch(`${API_URL}/library`)
      .then((res) => res.json())
      .then((data) => {
        if (data) {
          setName(data.name);
          setPrimaryColor(data.primaryColor);
        }
      });
  }, []);

  async function handleSave() {
    setStatus("저장 중...");
    const res = await fetch(`${API_URL}/library`, {
      method: "PATCH",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${token}`, // 손목밴드를 함께 보냄
      },
      body: JSON.stringify({ name, primaryColor }),
    });
    if (res.ok) {
      setStatus("✅ 저장되었습니다!");
    } else {
      const data = await res.json().catch(() => null);
      setStatus("❌ " + (data?.message || "저장에 실패했습니다."));
    }
  }

  if (!checked) return null; // 확인 전 잠깐 빈 화면

  // 로그인 안 했으면 로그인 안내 화면
  if (!token) {
    return (
      <main className="mx-auto max-w-md p-8">
        <Card>
          <CardHeader>
            <CardTitle>관리자 페이지</CardTitle>
          </CardHeader>
          <CardContent className="flex flex-col gap-4">
            <p>관리자 로그인이 필요합니다.</p>
            <a href="/login">
              <Button>로그인하러 가기</Button>
            </a>
          </CardContent>
        </Card>
      </main>
    );
  }

  // 로그인 했으면 설정 화면
  return (
    <main className="mx-auto max-w-md p-8">
      <Card>
        <CardHeader>
          <CardTitle>도서관 설정 (관리자)</CardTitle>
        </CardHeader>
        <CardContent className="flex flex-col gap-4">
          <div className="flex flex-col gap-2">
            <Label htmlFor="name">도서관 이름</Label>
            <Input id="name" value={name} onChange={(e) => setName(e.target.value)} />
          </div>
          <div className="flex flex-col gap-2">
            <Label htmlFor="color">대표 색상</Label>
            <Input id="color" type="color" value={primaryColor} onChange={(e) => setPrimaryColor(e.target.value)} className="h-10 w-20 p-1" />
          </div>
          <Button onClick={handleSave}>저장하기</Button>
          <p className="text-sm text-muted-foreground">{status}</p>
        </CardContent>
      </Card>
    </main>
  );
}
```

---

## C. 확인하기 — 자물쇠가 잘 걸렸는지 3단계 테스트 — 약 8분

### 테스트 1 — 로그인 안 하면 못 들어감

만약 지금 로그인 상태라면 먼저 손목밴드를 지워봅시다. `F12` → **Application** → **Local Storage** → `localhost:3000` → `token` 항목을 오른쪽 클릭해 삭제(또는 값 지우기)한 뒤, `http://localhost:3000/admin` 접속.

✅ "관리자 로그인이 필요합니다" 화면과 로그인 버튼이 보이면 성공.

### 테스트 2 — 일반 회원은 저장 못 함

`http://localhost:3000/login` 에서 7단계(2)에서 가입한 **일반 회원** 아이디로 로그인 → `/admin` 이동 → 설정을 바꾸고 **저장하기**.

✅ "❌ 관리자만 접근할 수 있습니다." 가 뜨면, 역할 검사가 잘 작동하는 거예요. (일반 회원은 로그인해도 관리자 기능은 못 씀!)

### 테스트 3 — 관리자로 승격하면 저장 성공

1. 새 터미널에서 `cd C:\projects\LibraryNearMe\backend` → `npx prisma studio`
2. `User` 표에서 그 회원의 `role` 칸을 **`MEMBER` → `ADMIN`** 으로 바꾸고 저장.
3. **중요:** 다시 `http://localhost:3000/login` 에서 **로그인을 한 번 더** 하세요.
   - 왜냐하면 손목밴드(토큰)는 **로그인하는 순간의 역할을 기억**하거든요. 역할을 바꿨으면 새 손목밴드를 받아야 "나는 이제 관리자다"가 반영됩니다.
4. `/admin`에서 이름·색상을 바꾸고 **저장하기**.

✅ "✅ 저장되었습니다!" 가 뜨면 **대성공입니다!** 🎉🎉 홈페이지(`localhost:3000`)를 새로고침하면 바뀐 값도 보여요.

Prisma Studio는 `Ctrl+C`로 꺼주세요.

---

## D. GitHub에 저장하기 — 약 4분

서버가 도는 터미널 말고 다른 터미널에서:

```
cd C:\projects\LibraryNearMe
git status
```

🔒 `.env`가 목록에 없는지 확인 후:

```
git add .
git commit -m "7단계(4): 관리자 페이지 보호 (문지기 Guard, 역할 확인)"
git push
```

✅ GitHub에 `backend/src/auth/admin.guard.ts`가 보이면 완료입니다! 🎉

---

## 최종 점검표

- [ ] `auth/admin.guard.ts` 문지기 생성
- [ ] `library.controller.ts`의 PATCH에 `@UseGuards(AdminGuard)` 적용
- [ ] `admin/page.tsx`에 토큰 확인 + 저장 시 Authorization 헤더 추가
- [ ] 테스트1: 미로그인 → 로그인 안내 뜸
- [ ] 테스트2: 일반 회원 → 저장 거부(관리자만)
- [ ] 테스트3: ADMIN으로 바꾸고 재로그인 → 저장 성공
- [ ] GitHub에 올림 (`.env`는 안 올라감)

---

## 🎉 7단계 완성 — 계정 시스템 완료!

축하합니다! 이제 우리 시스템은 이런 걸 할 수 있어요.
- 회원가입(비밀번호 암호화, 회원증 코드 발급)
- 로그인(토큰 발급)
- 역할(슈퍼/관리자/이용자) 구분
- 관리자만 관리 기능 사용 가능

웹 서비스의 가장 어려운 관문 중 하나인 "인증과 권한"을 직접 만들어 넘으신 거예요. 정말 큰 산을 넘으셨습니다.

## 다음 단계 예고 — 8단계: 책(도서) 관리 시작

이제 진짜 도서관의 핵심 기능으로 들어갑니다. 책 정보를 담을 표(Book)를 만들고, 관리자가 책을 등록·수정·삭제하고, 이용자가 홈페이지에서 검색하는 기능을 단계별로 만들어요. 지금까지 익힌 흐름(모델 → 창구 → 화면 → 보호)을 그대로 반복하는 거라 한결 익숙하실 거예요. 완료되면 알려주세요!

# 15단계 (3) — 회원 검색에 관리자 포함 + 관리자 계정 생성 + 사이드바 라벨 수정

> 확인해보니 15-1 백엔드 코드(`list`/`adminCreate`/`update`, `GET`·`PATCH /users`)가
> 실제 파일에는 반영되어 있지 않았어요(저장이 안 됐거나 다른 곳에 붙여넣어진 것 같아요).
> 그래서 이번 가이드는 **백엔드 파일 2개를 통째로 새 내용으로 교체**하는 방식으로 드립니다.
> 기존 내용을 다 지우고 아래 내용으로 덮어써 주세요. 여기에 이번에 요청하신
> "관리자 아이디도 검색/생성 가능"까지 함께 반영했습니다.
> 소요 시간: 약 20분

---

## 오늘 바뀌는 것

- 회원 목록 화면에서 **관리자(ADMIN) 계정도** 검색·조회됩니다. (슈퍼 계정(SUPER)은 이 화면 범위 밖으로 계속 제외합니다 — 슈퍼 계정은 나중에 별도 화면에서, 더 신중하게 다루는 게 안전해요.)
- 회원 등록 모달에 **"역할"** 선택칸이 생겨서, "회원"으로 등록할지 "관리자"로 등록할지 고를 수 있습니다.
- 표에 **"역할"** 칸이 추가됩니다.
- 왼쪽 사이드바 메뉴 이름이 "회원 관리 (준비 중)" → **"회원"**으로 바뀝니다.

---

## 1. `users.service.ts` 전체 교체 — 약 8분

`backend/src/users/users.service.ts` 파일 내용을 전부 지우고, 아래 내용으로 통째로 바꿔주세요.

```typescript
import {
  Injectable,
  ConflictException,
  BadRequestException,
  NotFoundException,
} from '@nestjs/common';
import { PrismaService } from '../prisma.service';
import * as bcrypt from 'bcryptjs';
import { randomUUID } from 'crypto';
import { Role, UserStatus } from '@prisma/client';

// 가입 화면에서 넘어오는 정보의 모양
type SignupData = {
  loginId: string;
  password: string;
  name: string;
  phone: string;
  email?: string;
};

@Injectable()
export class UsersService {
  constructor(private prisma: PrismaService) {}

  async signup(data: SignupData) {
    if (!data.loginId || !data.password || !data.name || !data.phone) {
      throw new BadRequestException(
        '아이디, 비밀번호, 이름, 휴대폰 번호는 필수입니다.',
      );
    }

    const library = await this.prisma.library.findFirst();
    if (!library) {
      throw new BadRequestException('도서관 설정이 없습니다.');
    }

    const existing = await this.prisma.user.findFirst({
      where: { libraryId: library.id, loginId: data.loginId },
    });
    if (existing) {
      throw new ConflictException('이미 사용 중인 아이디입니다.');
    }

    const passwordHash = await bcrypt.hash(data.password, 10);
    const cardToken = randomUUID();

    await this.prisma.user.create({
      data: {
        libraryId: library.id,
        loginId: data.loginId,
        passwordHash,
        name: data.name,
        phone: data.phone,
        email: data.email || undefined,
        cardToken,
      },
    });

    return { message: '회원가입이 완료되었습니다.' };
  }

  // 회원(+관리자) 목록 조회 (검색 + 페이지네이션). 슈퍼 계정(SUPER)은 항상 제외.
  async list(
    libraryId: number,
    params: {
      page?: number;
      pageSize?: number;
      name?: string;
      phone?: string;
      loginId?: string;
      memberNo?: string;
      status?: string;
    },
  ) {
    const page = params.page && params.page > 0 ? params.page : 1;
    const pageSize = params.pageSize && params.pageSize > 0 ? params.pageSize : 10;

    const where: any = { libraryId, role: { in: [Role.MEMBER, Role.ADMIN] } };
    if (params.name) where.name = { contains: params.name, mode: 'insensitive' };
    if (params.phone) where.phone = { contains: params.phone };
    if (params.loginId) where.loginId = { contains: params.loginId, mode: 'insensitive' };
    if (params.memberNo) where.memberNo = { contains: params.memberNo, mode: 'insensitive' };
    if (params.status) where.status = params.status;

    const [items, total] = await Promise.all([
      this.prisma.user.findMany({
        where,
        orderBy: { createdAt: 'desc' },
        skip: (page - 1) * pageSize,
        take: pageSize,
        select: {
          id: true,
          loginId: true,
          name: true,
          phone: true,
          email: true,
          memberNo: true,
          role: true,
          status: true,
          createdAt: true,
          // passwordHash는 절대 내보내지 않습니다.
        },
      }),
      this.prisma.user.count({ where }),
    ]);

    return { items, total, page, pageSize };
  }

  // 관리자가 회원 또는 관리자 계정을 직접 등록
  async adminCreate(
    libraryId: number,
    data: {
      loginId: string;
      password: string;
      name: string;
      phone?: string;
      email?: string;
      memberNo?: string;
      birthDate?: string;
      address?: string;
      role?: string;
    },
  ) {
    if (!data.loginId || !data.password || !data.name) {
      throw new BadRequestException('아이디, 비밀번호, 이름은 필수입니다.');
    }

    // role은 MEMBER 또는 ADMIN만 허용. 그 외 값(SUPER 포함)은 막습니다.
    const role =
      data.role === 'ADMIN' ? Role.ADMIN : Role.MEMBER;

    const existing = await this.prisma.user.findFirst({
      where: { libraryId, loginId: data.loginId },
    });
    if (existing) {
      throw new ConflictException('이미 사용 중인 아이디입니다.');
    }

    const passwordHash = await bcrypt.hash(data.password, 10);
    const cardToken = randomUUID();

    try {
      await this.prisma.user.create({
        data: {
          libraryId,
          loginId: data.loginId,
          passwordHash,
          name: data.name,
          phone: data.phone || undefined,
          email: data.email || undefined,
          memberNo: data.memberNo || undefined,
          birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
          address: data.address || undefined,
          role,
          cardToken,
        },
      });
    } catch (e: any) {
      if (e.code === 'P2002') {
        throw new ConflictException('이미 사용 중인 회원번호입니다.');
      }
      throw e;
    }

    return { message: '등록되었습니다.' };
  }

  // 회원/관리자 정보 수정 (이름/연락처/상태 등). 아이디·비밀번호·역할은 여기서 안 바꿉니다.
  async update(
    libraryId: number,
    id: number,
    data: {
      name?: string;
      phone?: string;
      email?: string;
      memberNo?: string;
      birthDate?: string;
      address?: string;
      status?: string;
    },
  ) {
    const existing = await this.prisma.user.findFirst({
      where: { id, libraryId, role: { in: [Role.MEMBER, Role.ADMIN] } },
    });
    if (!existing) {
      throw new NotFoundException('회원을 찾을 수 없습니다.');
    }

    try {
      return await this.prisma.user.update({
        where: { id },
        data: {
          name: data.name,
          phone: data.phone,
          email: data.email || undefined,
          memberNo: data.memberNo || undefined,
          birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
          address: data.address || undefined,
          status: data.status ? (data.status as UserStatus) : undefined,
        },
        select: { id: true, name: true, status: true },
      });
    } catch (e: any) {
      if (e.code === 'P2002') {
        throw new ConflictException('이미 사용 중인 회원번호입니다.');
      }
      throw e;
    }
  }
}
```

- `list()`·`update()`의 `role: { in: [Role.MEMBER, Role.ADMIN] } }`가 이번에 바뀐 핵심이에요 — 전에는 `role: Role.MEMBER`만 있었는데, 이제 `ADMIN`도 함께 조회되게 했어요. `SUPER`는 목록에 넣지 않았어요(슈퍼 계정까지 이 화면에서 만들고 정지시킬 수 있으면 실수로 전체 시스템 접근 권한을 건드릴 위험이 있어서, 일부러 범위 밖에 뒀습니다).
- `adminCreate()`의 `role` 값은 프론트에서 `"ADMIN"`을 보내야만 관리자로 만들어지고, 그 외(빈 값 등)는 전부 안전하게 `MEMBER`가 됩니다. 프론트가 실수로든 고의로든 `"SUPER"`를 보내도 무시되고 `MEMBER`가 돼요.

---

## 2. `users.controller.ts` 전체 교체 — 약 3분

`backend/src/users/users.controller.ts` 파일 내용을 전부 지우고, 아래로 바꿔주세요.

```typescript
import { Controller, Get, Post, Patch, Body, Param, Query, Req, UseGuards } from '@nestjs/common';
import { UsersService } from './users.service';
import { AdminGuard } from '../auth/admin.guard';

@Controller('users')
export class UsersController {
  constructor(private usersService: UsersService) {}

  // POST 요청: 새 회원 만들기(가입) — 누구나 접근 가능(홈페이지 회원가입)
  @Post()
  signup(
    @Body()
    body: {
      loginId: string;
      password: string;
      name: string;
      phone: string;
      email?: string;
    },
  ) {
    return this.usersService.signup(body);
  }

  // GET 요청: 회원/관리자 목록 조회(검색 포함) — 관리자만
  @Get()
  @UseGuards(AdminGuard)
  list(@Req() req: any, @Query() query: any) {
    return this.usersService.list(req.user.libraryId, {
      page: query.page ? parseInt(query.page, 10) : undefined,
      pageSize: query.pageSize ? parseInt(query.pageSize, 10) : undefined,
      name: query.name,
      phone: query.phone,
      loginId: query.loginId,
      memberNo: query.memberNo,
      status: query.status,
    });
  }

  // POST 요청: 관리자가 회원/관리자 계정 직접 등록 — 관리자만
  @Post('admin')
  @UseGuards(AdminGuard)
  adminCreate(@Req() req: any, @Body() body: any) {
    return this.usersService.adminCreate(req.user.libraryId, body);
  }

  // PATCH 요청: 회원/관리자 정보 수정(상태 변경 포함) — 관리자만
  @Patch(':id')
  @UseGuards(AdminGuard)
  update(@Req() req: any, @Param('id') id: string, @Body() body: any) {
    return this.usersService.update(req.user.libraryId, parseInt(id, 10), body);
  }
}
```

---

## 3. dictionary.ts 수정 — 약 4분

`frontend/src/lib/dictionary.ts` 를 엽니다.

### 3-1. 사이드바 라벨 수정

아래 두 줄을 찾아서 값만 바꿔주세요.

**한글 쪽:**

```ts
    "admin.menu.members": "회원 관리 (준비 중)",
```

이렇게 바꿔주세요.

```ts
    "admin.menu.members": "회원",
```

**영어 쪽:**

```ts
    "admin.menu.members": "Member Management (soon)",
```

이렇게 바꿔주세요.

```ts
    "admin.menu.members": "Members",
```

### 3-2. 역할 관련 문구 추가

`"members.status.SUSPENDED"` 줄 바로 아래(한글·영어 각각)에 추가하세요.

**한글 쪽:**

```ts
    "members.list.col.role": "역할",
    "members.form.field.role": "역할",
    "members.role.MEMBER": "회원",
    "members.role.ADMIN": "관리자",
```

**영어 쪽:**

```ts
    "members.list.col.role": "Role",
    "members.form.field.role": "Role",
    "members.role.MEMBER": "Member",
    "members.role.ADMIN": "Admin",
```

---

## 4. `admin/members/page.tsx` 수정 — 약 5분

`frontend/src/app/admin/members/page.tsx` 를 엽니다.

### 4-1. 타입·기본값에 role 추가

아래 부분을 찾으세요.

```tsx
type MemberRow = {
  id: number;
  loginId: string | null;
  name: string;
  phone: string | null;
  email: string | null;
  memberNo: string | null;
  status: string;
  createdAt: string;
};
```

이렇게 바꿔주세요. (`role` 한 줄 추가)

```tsx
type MemberRow = {
  id: number;
  loginId: string | null;
  name: string;
  phone: string | null;
  email: string | null;
  memberNo: string | null;
  role: string;
  status: string;
  createdAt: string;
};
```

이어서 아래 부분을 찾으세요.

```tsx
const EMPTY_FORM = {
  loginId: "",
  password: "",
  name: "",
  phone: "",
  email: "",
  memberNo: "",
  birthDate: "",
  address: "",
  status: "ACTIVE",
};
```

이렇게 바꿔주세요. (`role: "MEMBER"` 추가)

```tsx
const EMPTY_FORM = {
  loginId: "",
  password: "",
  name: "",
  phone: "",
  email: "",
  memberNo: "",
  birthDate: "",
  address: "",
  role: "MEMBER",
  status: "ACTIVE",
};
```

### 4-2. 등록 시 role도 함께 전송

아래 부분을 찾으세요.

```tsx
      : {
          loginId: form.loginId,
          password: form.password,
          name: form.name,
          phone: form.phone,
          email: form.email,
          memberNo: form.memberNo,
          birthDate: form.birthDate || undefined,
          address: form.address,
        };
```

이렇게 바꿔주세요. (`role: form.role` 추가)

```tsx
      : {
          loginId: form.loginId,
          password: form.password,
          name: form.name,
          phone: form.phone,
          email: form.email,
          memberNo: form.memberNo,
          birthDate: form.birthDate || undefined,
          address: form.address,
          role: form.role,
        };
```

### 4-3. 표에 "역할" 칸 추가

아래 부분을 찾으세요.

```tsx
              <th className="px-4 py-2.5">{t("members.list.col.memberNo")}</th>
              <th className="px-4 py-2.5">{t("members.list.col.status")}</th>
```

이렇게 바꿔주세요.

```tsx
              <th className="px-4 py-2.5">{t("members.list.col.memberNo")}</th>
              <th className="px-4 py-2.5">{t("members.list.col.role")}</th>
              <th className="px-4 py-2.5">{t("members.list.col.status")}</th>
```

바로 아래, 표 몸통(`<tbody>`) 안에서 아래 부분을 찾으세요.

```tsx
                <td className="whitespace-nowrap px-4 py-2.5">{row.memberNo || "-"}</td>
                <td className="whitespace-nowrap px-4 py-2.5">
                  {t(`members.status.${row.status}`)}
                </td>
```

이렇게 바꿔주세요.

```tsx
                <td className="whitespace-nowrap px-4 py-2.5">{row.memberNo || "-"}</td>
                <td className="whitespace-nowrap px-4 py-2.5">{t(`members.role.${row.role}`)}</td>
                <td className="whitespace-nowrap px-4 py-2.5">
                  {t(`members.status.${row.status}`)}
                </td>
```

그리고 데이터 없을 때 나오는 빈 줄의 칸 개수(`colSpan`)도 하나 늘어난 칸 수에 맞춰야 해요. 아래를 찾으세요.

```tsx
                <td colSpan={9} className="px-4 py-6 text-center text-neutral-400">
```

이렇게 바꿔주세요. (칸이 하나 늘었으니 9 → 10)

```tsx
                <td colSpan={10} className="px-4 py-6 text-center text-neutral-400">
```

### 4-4. 등록 모달에 "역할" 선택칸 추가

아래 부분을 찾으세요. (등록 모달에서 아이디·초기 비밀번호 입력칸이 있는 자리예요.)

```tsx
              {!editingId && (
                <>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.loginId")} *</span>
                    <input
                      value={form.loginId}
                      onChange={(e) => setForm({ ...form, loginId: e.target.value })}
                      className="w-full rounded-lg border px-3 py-2 text-sm"
                    />
                  </label>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.password")} *</span>
                    <input
                      type="password"
                      value={form.password}
                      onChange={(e) => setForm({ ...form, password: e.target.value })}
                      className="w-full rounded-lg border px-3 py-2 text-sm"
                    />
                  </label>
                </>
              )}
```

이렇게 바꿔주세요. (맨 끝에 "역할" 선택칸이 추가됐어요.)

```tsx
              {!editingId && (
                <>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.loginId")} *</span>
                    <input
                      value={form.loginId}
                      onChange={(e) => setForm({ ...form, loginId: e.target.value })}
                      className="w-full rounded-lg border px-3 py-2 text-sm"
                    />
                  </label>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.password")} *</span>
                    <input
                      type="password"
                      value={form.password}
                      onChange={(e) => setForm({ ...form, password: e.target.value })}
                      className="w-full rounded-lg border px-3 py-2 text-sm"
                    />
                  </label>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.role")}</span>
                    <select
                      value={form.role}
                      onChange={(e) => setForm({ ...form, role: e.target.value })}
                      className="w-full cursor-pointer rounded-lg border px-3 py-2 text-sm"
                    >
                      <option value="MEMBER">{t("members.role.MEMBER")}</option>
                      <option value="ADMIN">{t("members.role.ADMIN")}</option>
                    </select>
                  </label>
                </>
              )}
```

- "역할" 선택칸은 **등록할 때만** 보여요(`!editingId`). 이미 있는 계정의 역할을 바꾸는 기능은 이번엔 안 만들었어요 — 역할 변경은 더 민감한 조작이라, 필요해지면 별도로 신중하게 만드는 게 안전합니다.

---

## 5. 확인하기 — 약 5분

1. backend·frontend 서버를 재시작하고(코드를 많이 바꿨으니 새로고침만으로 부족할 수 있어요), `/admin/members`로 이동합니다.
2. 사이드바 메뉴 이름이 **"회원"**으로 바뀌었는지 확인합니다.
3. "상세 검색"을 조건 없이 눌러서, 이제 관리자 계정도 표에 함께 나오는지 확인합니다(표에 "역할" 칸이 추가되어 "회원"/"관리자"로 표시돼요).
4. "회원 등록" 버튼 → 역할을 "관리자"로 선택해서 새 계정을 만들고, 그 계정으로 로그인해서 관리자 페이지에 들어가지는지 확인합니다.
5. 아이디로 검색했을 때 관리자 계정도 걸리는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## 6. GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "15단계(3): 회원 목록에 관리자 계정 포함, 관리자 계정 직접생성 지원, 사이드바 라벨 수정"
git push
```

---

## 최종 점검표

- [ ] `users.service.ts` 전체 교체 (list/update가 ADMIN도 포함하도록)
- [ ] `users.controller.ts` 전체 교체
- [ ] `dictionary.ts` — 사이드바 라벨 "회원"으로 수정, `members.role.*`/`members.list.col.role`/`members.form.field.role` 추가
- [ ] `admin/members/page.tsx` — role 타입/기본값/등록전송/표 칸/등록모달 선택칸 추가
- [ ] 사이드바 "회원" 라벨 확인
- [ ] 관리자 계정도 검색됨 확인
- [ ] 관리자 계정 등록 후 로그인 확인
- [ ] GitHub에 올림

# 22단계_2. 자료 종류를 관리자가 직접 추가·수정할 수 있게 만들기 (2) - 관리 화면

## 🎯 목표

지난 22단계_1에서 자료 종류(도서, DVD, 논문...)를 데이터베이스에서 관리하도록 서버를 바꿨어요. 이번엔 그걸 실제로 다루는 **화면**을 만들 차례예요.

'설정' 메뉴에 '자료 종류'라는 새 탭을 만들고, 그 안에서

- 실물 자료 / 디지털 자료 목록을 각각 표로 볼 수 있고
- '+ 새 자료 종류 추가' 버튼으로 새 종류를 만들 수 있고 (실물이면 대출 가능 권수·대출 기간을 그 자리에서 함께 입력)
- 각 항목을 눌러 이름을 수정하거나(실물이면 대출 설정도 함께) 삭제할 수 있고
- '도서' 항목에만 따로 있는 '**KDC 하위 규칙**' 버튼을 누르면, 도서 안에서 KDC 번호별로 별도 대출 규칙(예: 만화는 5권)을 추가·수정·삭제할 수 있게

만들 거예요.

> 💡 시작하기 전에, 이 가이드는 실제 저장소 파일(`dictionary.ts`, `settings/page.tsx`, `copy-options-settings-form.tsx`)을 직접 확인하고 그 내용을 기준으로 작성했어요.

---

## 1단계. 다국어 사전에 문구 추가하기

**파일**: `frontend/src/lib/dictionary.ts`

이 파일에는 한국어(ko)와 영어(en) 두 블록이 있어요. 아래 두 군데를 각각 찾아서 수정해주세요.

### 1) 한국어(ko) 블록 - 탭 이름 추가

**찾기:**

```typescript
    "settings.tabs.design": "디자인",
    "settings.tabs.copyOptions": "목록",
```

**이렇게 바꿔주세요:**

```typescript
    "settings.tabs.design": "디자인",
    "settings.tabs.copyOptions": "목록",
    "settings.tabs.materialTypes": "자료 종류",
```

### 2) 한국어(ko) 블록 - 자료 종류 관련 문구 추가

**찾기:**

```typescript
    "settings.copyOptions.deleteSuccess": "삭제되었습니다.",
    "settings.copyOptions.deleteFail": "삭제에 실패했습니다.",
    "settings.copyOptions.loadFail": "목록을 불러오지 못했습니다.",

    "design.footerSection": "Footer 색상",
```

**이렇게 바꿔주세요:**

```typescript
    "settings.copyOptions.deleteSuccess": "삭제되었습니다.",
    "settings.copyOptions.deleteFail": "삭제에 실패했습니다.",
    "settings.copyOptions.loadFail": "목록을 불러오지 못했습니다.",

    "settings.materialTypes.sectionPhysical": "실물 자료",
    "settings.materialTypes.sectionDigital": "디지털 자료",
    "settings.materialTypes.col.nameKo": "이름",
    "settings.materialTypes.col.code": "코드",
    "settings.materialTypes.col.usesMarc": "MARC 편집기",
    "settings.materialTypes.col.maxLoanCount": "대출 가능 권수",
    "settings.materialTypes.col.loanPeriodDays": "대출 기간(일)",
    "settings.materialTypes.col.action": "관리",
    "settings.materialTypes.editBtn": "수정",
    "settings.materialTypes.kdcBtn": "KDC 하위 규칙",
    "settings.materialTypes.addBtn": "+ 새 자료 종류 추가",
    "settings.materialTypes.yes": "사용",
    "settings.materialTypes.no": "미사용",
    "settings.materialTypes.modal.addTitle": "자료 종류 추가",
    "settings.materialTypes.modal.editTitle": "자료 종류 수정",
    "settings.materialTypes.field.category": "구분",
    "settings.materialTypes.field.categoryPhysical": "실물 자료",
    "settings.materialTypes.field.categoryDigital": "디지털 자료",
    "settings.materialTypes.field.code": "코드 (영문, 예: board_game)",
    "settings.materialTypes.field.nameKo": "이름(국문)",
    "settings.materialTypes.field.nameEn": "이름(영문)",
    "settings.materialTypes.field.usesMarc": "MARC 편집기 사용",
    "settings.materialTypes.field.maxLoanCount": "대출 가능 권수",
    "settings.materialTypes.field.loanPeriodDays": "대출 기간(일)",
    "settings.materialTypes.save": "저장",
    "settings.materialTypes.deleteBtn": "삭제",
    "settings.materialTypes.deleteConfirm": "정말 삭제하시겠어요?",
    "settings.materialTypes.codeRequired": "코드와 이름을 입력하세요.",
    "settings.materialTypes.loanFieldsRequired": "실물 자료는 대출 가능 권수와 대출 기간을 입력해야 합니다.",
    "settings.materialTypes.saveSuccess": "저장되었습니다.",
    "settings.materialTypes.saveFail": "저장에 실패했습니다.",
    "settings.materialTypes.deleteSuccess": "삭제되었습니다.",
    "settings.materialTypes.deleteFail": "삭제에 실패했습니다.",
    "settings.materialTypes.loadFail": "자료 종류를 불러오지 못했습니다.",
    "settings.materialTypes.kdc.modalTitle": "도서 KDC 하위 규칙",
    "settings.materialTypes.kdc.addBtn": "+ 규칙 추가",
    "settings.materialTypes.kdc.modal.addTitle": "KDC 규칙 추가",
    "settings.materialTypes.kdc.modal.editTitle": "KDC 규칙 수정",
    "settings.materialTypes.kdc.field.kdcPrefix": "KDC 번호 (예: 657)",
    "settings.materialTypes.kdc.field.label": "이름 (예: 만화)",
    "settings.materialTypes.kdc.field.maxLoanCount": "대출 가능 권수",
    "settings.materialTypes.kdc.save": "저장",
    "settings.materialTypes.kdc.deleteBtn": "삭제",
    "settings.materialTypes.kdc.deleteConfirm": "정말 삭제하시겠어요?",
    "settings.materialTypes.kdc.fieldsRequired": "KDC 번호, 이름, 대출 가능 권수를 모두 입력하세요.",
    "settings.materialTypes.kdc.saveSuccess": "저장되었습니다.",
    "settings.materialTypes.kdc.saveFail": "저장에 실패했습니다.",
    "settings.materialTypes.kdc.deleteSuccess": "삭제되었습니다.",
    "settings.materialTypes.kdc.deleteFail": "삭제에 실패했습니다.",
    "settings.materialTypes.kdc.empty": "아직 등록된 KDC 하위 규칙이 없습니다.",
    "settings.materialTypes.kdc.close": "닫기",

    "design.footerSection": "Footer 색상",
```

### 3) 영어(en) 블록 - 탭 이름 추가

**찾기:**

```typescript
    "settings.tabs.design": "Design",
    "settings.tabs.copyOptions": "Lists",
```

**이렇게 바꿔주세요:**

```typescript
    "settings.tabs.design": "Design",
    "settings.tabs.copyOptions": "Lists",
    "settings.tabs.materialTypes": "Material Types",
```

### 4) 영어(en) 블록 - 자료 종류 관련 문구 추가

`settings.copyOptions.deleteSuccess` 등 영어 블록에서도 똑같은 문구들이 있어요. 이번엔 **찾기**할 곳이 조금 다릅니다. 영어 블록에서 `"settings.copyOptions.loadFail": "Could not load the list."` 같은 줄(정확한 문구는 파일에서 확인해주세요) 바로 다음, `"design.footerSection"`으로 시작하는 영어 줄 앞에 아래 내용을 통째로 붙여넣어주세요.

```typescript
    "settings.materialTypes.sectionPhysical": "Physical Materials",
    "settings.materialTypes.sectionDigital": "Digital Materials",
    "settings.materialTypes.col.nameKo": "Name",
    "settings.materialTypes.col.code": "Code",
    "settings.materialTypes.col.usesMarc": "Uses MARC",
    "settings.materialTypes.col.maxLoanCount": "Max Loan Count",
    "settings.materialTypes.col.loanPeriodDays": "Loan Period (days)",
    "settings.materialTypes.col.action": "Manage",
    "settings.materialTypes.editBtn": "Edit",
    "settings.materialTypes.kdcBtn": "KDC Sub-rules",
    "settings.materialTypes.addBtn": "+ Add Material Type",
    "settings.materialTypes.yes": "Yes",
    "settings.materialTypes.no": "No",
    "settings.materialTypes.modal.addTitle": "Add Material Type",
    "settings.materialTypes.modal.editTitle": "Edit Material Type",
    "settings.materialTypes.field.category": "Category",
    "settings.materialTypes.field.categoryPhysical": "Physical",
    "settings.materialTypes.field.categoryDigital": "Digital",
    "settings.materialTypes.field.code": "Code (English, e.g. board_game)",
    "settings.materialTypes.field.nameKo": "Name (Korean)",
    "settings.materialTypes.field.nameEn": "Name (English)",
    "settings.materialTypes.field.usesMarc": "Uses MARC editor",
    "settings.materialTypes.field.maxLoanCount": "Max Loan Count",
    "settings.materialTypes.field.loanPeriodDays": "Loan Period (days)",
    "settings.materialTypes.save": "Save",
    "settings.materialTypes.deleteBtn": "Delete",
    "settings.materialTypes.deleteConfirm": "Delete this material type?",
    "settings.materialTypes.codeRequired": "Please enter a code and name.",
    "settings.materialTypes.loanFieldsRequired": "Physical materials require a max loan count and loan period.",
    "settings.materialTypes.saveSuccess": "Saved.",
    "settings.materialTypes.saveFail": "Save failed.",
    "settings.materialTypes.deleteSuccess": "Deleted.",
    "settings.materialTypes.deleteFail": "Delete failed.",
    "settings.materialTypes.loadFail": "Could not load material types.",
    "settings.materialTypes.kdc.modalTitle": "Book KDC Sub-rules",
    "settings.materialTypes.kdc.addBtn": "+ Add Rule",
    "settings.materialTypes.kdc.modal.addTitle": "Add KDC Rule",
    "settings.materialTypes.kdc.modal.editTitle": "Edit KDC Rule",
    "settings.materialTypes.kdc.field.kdcPrefix": "KDC number (e.g. 657)",
    "settings.materialTypes.kdc.field.label": "Name (e.g. Comics)",
    "settings.materialTypes.kdc.field.maxLoanCount": "Max Loan Count",
    "settings.materialTypes.kdc.save": "Save",
    "settings.materialTypes.kdc.deleteBtn": "Delete",
    "settings.materialTypes.kdc.deleteConfirm": "Delete this rule?",
    "settings.materialTypes.kdc.fieldsRequired": "Please enter the KDC number, name, and max loan count.",
    "settings.materialTypes.kdc.saveSuccess": "Saved.",
    "settings.materialTypes.kdc.saveFail": "Save failed.",
    "settings.materialTypes.kdc.deleteSuccess": "Deleted.",
    "settings.materialTypes.kdc.deleteFail": "Delete failed.",
    "settings.materialTypes.kdc.empty": "No KDC sub-rules yet.",
    "settings.materialTypes.kdc.close": "Close",
```

> 💡 영어 블록은 한국어 블록과 거의 같은 구조가 반복돼요. Ctrl+F로 `"settings.copyOptions.loadFail"`을 검색하면 한국어 블록과 영어 블록 두 곳이 나올 텐데, **두 번째로 나오는 곳**(영어 문장으로 되어있는 곳)이 영어 블록이에요. 거기서 찾은 줄 바로 다음 줄에 붙여넣어주세요.

---

## 2단계. 새 컴포넌트 파일 만들기

**새 파일**: `frontend/src/components/material-types-settings-form.tsx`

기존 `copy-options-settings-form.tsx`와 같은 스타일로 만들었어요. 아래 내용을 그대로 붙여넣어주세요.

```tsx
"use client";

import { useEffect, useState } from "react";
import ThemedButton from "@/components/themed-button";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";

type KdcRule = { id: number; kdcPrefix: string; label: string; maxLoanCount: number };
type MaterialType = {
  id: number;
  code: string;
  nameKo: string;
  nameEn: string;
  category: "PHYSICAL" | "DIGITAL";
  usesMarc: boolean;
  maxLoanCount: number | null;
  loanPeriodDays: number | null;
  kdcRules: KdcRule[];
};

const EMPTY_FORM = {
  category: "PHYSICAL" as "PHYSICAL" | "DIGITAL",
  code: "",
  nameKo: "",
  nameEn: "",
  usesMarc: false,
  maxLoanCount: "",
  loanPeriodDays: "",
};

const EMPTY_KDC_FORM = { kdcPrefix: "", label: "", maxLoanCount: "" };

export default function MaterialTypesSettingsForm() {
  const { t } = useI18n();
  const { notify } = useNotify();

  const [types, setTypes] = useState<MaterialType[]>([]);
  const [showModal, setShowModal] = useState(false);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [form, setForm] = useState(EMPTY_FORM);

  const [showKdcModal, setShowKdcModal] = useState(false);
  const [showKdcForm, setShowKdcForm] = useState(false);
  const [kdcEditingId, setKdcEditingId] = useState<number | null>(null);
  const [kdcForm, setKdcForm] = useState(EMPTY_KDC_FORM);

  async function loadTypes() {
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/material-types`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      setTypes(await res.json());
    } else {
      notify("❌ " + t("settings.materialTypes.loadFail"), "error");
    }
  }

  useEffect(() => {
    loadTypes();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  const bookType = types.find((mt) => mt.code === "book");

  function openAddModal() {
    setEditingId(null);
    setForm(EMPTY_FORM);
    setShowModal(true);
  }

  function openEditModal(item: MaterialType) {
    setEditingId(item.id);
    setForm({
      category: item.category,
      code: item.code,
      nameKo: item.nameKo,
      nameEn: item.nameEn,
      usesMarc: item.usesMarc,
      maxLoanCount: item.maxLoanCount !== null ? String(item.maxLoanCount) : "",
      loanPeriodDays: item.loanPeriodDays !== null ? String(item.loanPeriodDays) : "",
    });
    setShowModal(true);
  }

  async function handleSave() {
    const token = localStorage.getItem("token");
    if (!token) return;
    if (!form.nameKo.trim() || (!editingId && !form.code.trim())) {
      notify("❌ " + t("settings.materialTypes.codeRequired"), "error");
      return;
    }
    if (form.category === "PHYSICAL" && (!form.maxLoanCount.trim() || !form.loanPeriodDays.trim())) {
      notify("❌ " + t("settings.materialTypes.loanFieldsRequired"), "error");
      return;
    }

    // 새로 추가할 때만 코드/구분/MARC 사용 여부를 함께 보냅니다. (수정할 땐 서버가 이 값들을 바꾸지 않아요.)
    const body: any = {
      nameKo: form.nameKo.trim(),
      nameEn: form.nameEn.trim(),
    };
    if (!editingId) {
      body.code = form.code.trim();
      body.category = form.category;
      body.usesMarc = form.usesMarc;
    }
    if (form.category === "PHYSICAL") {
      body.maxLoanCount = Number(form.maxLoanCount);
      body.loanPeriodDays = Number(form.loanPeriodDays);
    }

    const url = editingId ? `${API_URL}/material-types/${editingId}` : `${API_URL}/material-types`;
    const res = await fetch(url, {
      method: editingId ? "PATCH" : "POST",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify(body),
    });
    if (res.ok) {
      notify("✅ " + t("settings.materialTypes.saveSuccess"), "success");
      setShowModal(false);
      await loadTypes();
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("settings.materialTypes.saveFail")), "error");
    }
  }

  async function handleDelete() {
    if (!editingId) return;
    if (!window.confirm(t("settings.materialTypes.deleteConfirm"))) return;
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/material-types/${editingId}`, {
      method: "DELETE",
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      notify("✅ " + t("settings.materialTypes.deleteSuccess"), "success");
      setShowModal(false);
      await loadTypes();
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("settings.materialTypes.deleteFail")), "error");
    }
  }

  function openKdcModal() {
    setShowKdcForm(false);
    setKdcEditingId(null);
    setKdcForm(EMPTY_KDC_FORM);
    setShowKdcModal(true);
  }

  function closeKdcModal() {
    setShowKdcModal(false);
    setShowKdcForm(false);
    setKdcEditingId(null);
    setKdcForm(EMPTY_KDC_FORM);
  }

  function openAddKdcForm() {
    setKdcEditingId(null);
    setKdcForm(EMPTY_KDC_FORM);
    setShowKdcForm(true);
  }

  function openEditKdcForm(rule: KdcRule) {
    setKdcEditingId(rule.id);
    setKdcForm({ kdcPrefix: rule.kdcPrefix, label: rule.label, maxLoanCount: String(rule.maxLoanCount) });
    setShowKdcForm(true);
  }

  async function handleSaveKdc() {
    if (!bookType) return;
    const token = localStorage.getItem("token");
    if (!token) return;
    if (!kdcForm.kdcPrefix.trim() || !kdcForm.label.trim() || !kdcForm.maxLoanCount.trim()) {
      notify("❌ " + t("settings.materialTypes.kdc.fieldsRequired"), "error");
      return;
    }
    const body = {
      kdcPrefix: kdcForm.kdcPrefix.trim(),
      label: kdcForm.label.trim(),
      maxLoanCount: Number(kdcForm.maxLoanCount),
    };
    const url = kdcEditingId
      ? `${API_URL}/material-types/kdc-rules/${kdcEditingId}`
      : `${API_URL}/material-types/${bookType.id}/kdc-rules`;
    const res = await fetch(url, {
      method: kdcEditingId ? "PATCH" : "POST",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify(body),
    });
    if (res.ok) {
      notify("✅ " + t("settings.materialTypes.kdc.saveSuccess"), "success");
      setShowKdcForm(false);
      setKdcEditingId(null);
      setKdcForm(EMPTY_KDC_FORM);
      await loadTypes();
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("settings.materialTypes.kdc.saveFail")), "error");
    }
  }

  async function handleDeleteKdc() {
    if (!kdcEditingId) return;
    if (!window.confirm(t("settings.materialTypes.kdc.deleteConfirm"))) return;
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/material-types/kdc-rules/${kdcEditingId}`, {
      method: "DELETE",
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      notify("✅ " + t("settings.materialTypes.kdc.deleteSuccess"), "success");
      setShowKdcForm(false);
      setKdcEditingId(null);
      setKdcForm(EMPTY_KDC_FORM);
      await loadTypes();
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("settings.materialTypes.kdc.deleteFail")), "error");
    }
  }

  function renderTable(category: "PHYSICAL" | "DIGITAL") {
    const rows = types.filter((mt) => mt.category === category);
    return (
      <div className="overflow-hidden rounded-lg border border-neutral-200 bg-white">
        <table className="w-full min-w-[720px] text-left text-sm">
          <thead className="bg-neutral-100 text-neutral-500">
            <tr>
              <th className="px-3 py-2">{t("settings.materialTypes.col.nameKo")}</th>
              <th className="px-3 py-2">{t("settings.materialTypes.col.code")}</th>
              <th className="px-3 py-2">{t("settings.materialTypes.col.usesMarc")}</th>
              {category === "PHYSICAL" && (
                <>
                  <th className="px-3 py-2">{t("settings.materialTypes.col.maxLoanCount")}</th>
                  <th className="px-3 py-2">{t("settings.materialTypes.col.loanPeriodDays")}</th>
                </>
              )}
              <th className="px-3 py-2">{t("settings.materialTypes.col.action")}</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-neutral-100">
            {rows.map((item) => (
              <tr key={item.id}>
                <td className="whitespace-nowrap px-3 py-2 font-medium">{item.nameKo}</td>
                <td className="whitespace-nowrap px-3 py-2 text-neutral-500">{item.code}</td>
                <td className="whitespace-nowrap px-3 py-2 text-neutral-500">
                  {item.usesMarc ? t("settings.materialTypes.yes") : t("settings.materialTypes.no")}
                </td>
                {category === "PHYSICAL" && (
                  <>
                    <td className="whitespace-nowrap px-3 py-2 text-neutral-500">{item.maxLoanCount ?? "-"}</td>
                    <td className="whitespace-nowrap px-3 py-2 text-neutral-500">{item.loanPeriodDays ?? "-"}</td>
                  </>
                )}
                <td className="whitespace-nowrap px-3 py-2">
                  <div className="flex gap-2">
                    <button
                      type="button"
                      onClick={() => openEditModal(item)}
                      className="cursor-pointer rounded border px-2 py-1 text-xs"
                    >
                      {t("settings.materialTypes.editBtn")}
                    </button>
                    {item.code === "book" && (
                      <button
                        type="button"
                        onClick={openKdcModal}
                        className="cursor-pointer rounded border px-2 py-1 text-xs"
                      >
                        {t("settings.materialTypes.kdcBtn")}
                      </button>
                    )}
                  </div>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    );
  }

  return (
    <div className="flex flex-col gap-6">
      <div>
        <p className="mb-2 text-sm font-semibold">{t("settings.materialTypes.sectionPhysical")}</p>
        {renderTable("PHYSICAL")}
      </div>
      <div>
        <p className="mb-2 text-sm font-semibold">{t("settings.materialTypes.sectionDigital")}</p>
        {renderTable("DIGITAL")}
      </div>

      <div className="flex justify-end">
        <ThemedButton preset="버튼1" onClick={openAddModal}>
          {t("settings.materialTypes.addBtn")}
        </ThemedButton>
      </div>

      {showModal && (
        <div
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"
          onClick={() => setShowModal(false)}
        >
          <div
            className="w-full max-w-sm overflow-hidden rounded-xl bg-white shadow-xl"
            onClick={(e) => e.stopPropagation()}
          >
            <div className="max-h-[80vh] overflow-y-auto p-6">
              <p className="mb-4 text-sm font-semibold">
                {editingId ? t("settings.materialTypes.modal.editTitle") : t("settings.materialTypes.modal.addTitle")}
              </p>

              <div className="space-y-3">
                {!editingId && (
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.field.category")}</span>
                    <select
                      value={form.category}
                      onChange={(e) => setForm({ ...form, category: e.target.value as "PHYSICAL" | "DIGITAL" })}
                      className="w-full cursor-pointer rounded-lg border border-neutral-200 bg-white px-3 py-2 text-sm"
                    >
                      <option value="PHYSICAL">{t("settings.materialTypes.field.categoryPhysical")}</option>
                      <option value="DIGITAL">{t("settings.materialTypes.field.categoryDigital")}</option>
                    </select>
                  </label>
                )}

                {!editingId && (
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.field.code")}</span>
                    <input
                      value={form.code}
                      onChange={(e) => setForm({ ...form, code: e.target.value })}
                      className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                    />
                  </label>
                )}

                <label className="block">
                  <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.field.nameKo")} *</span>
                  <input
                    value={form.nameKo}
                    onChange={(e) => setForm({ ...form, nameKo: e.target.value })}
                    className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                  />
                </label>

                <label className="block">
                  <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.field.nameEn")}</span>
                  <input
                    value={form.nameEn}
                    onChange={(e) => setForm({ ...form, nameEn: e.target.value })}
                    className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                  />
                </label>

                {!editingId && (
                  <label className="flex cursor-pointer items-center gap-2">
                    <input
                      type="checkbox"
                      checked={form.usesMarc}
                      onChange={(e) => setForm({ ...form, usesMarc: e.target.checked })}
                    />
                    <span className="text-sm text-neutral-500">{t("settings.materialTypes.field.usesMarc")}</span>
                  </label>
                )}

                {form.category === "PHYSICAL" && (
                  <>
                    <label className="block">
                      <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.field.maxLoanCount")} *</span>
                      <input
                        type="number"
                        min={1}
                        value={form.maxLoanCount}
                        onChange={(e) => setForm({ ...form, maxLoanCount: e.target.value })}
                        className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                      />
                    </label>
                    <label className="block">
                      <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.field.loanPeriodDays")} *</span>
                      <input
                        type="number"
                        min={1}
                        value={form.loanPeriodDays}
                        onChange={(e) => setForm({ ...form, loanPeriodDays: e.target.value })}
                        className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                      />
                    </label>
                  </>
                )}
              </div>

              <ThemedButton preset="버튼1" onClick={handleSave} className="mt-5 w-full">
                {t("settings.materialTypes.save")}
              </ThemedButton>

              {editingId && (
                <button
                  onClick={handleDelete}
                  className="mt-2 w-full cursor-pointer rounded-lg bg-red-600 py-2.5 text-sm font-semibold text-white hover:bg-red-700"
                >
                  {t("settings.materialTypes.deleteBtn")}
                </button>
              )}
            </div>
          </div>
        </div>
      )}

      {showKdcModal && bookType && (
        <div
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"
          onClick={closeKdcModal}
        >
          <div
            className="w-full max-w-md overflow-hidden rounded-xl bg-white shadow-xl"
            onClick={(e) => e.stopPropagation()}
          >
            <div className="max-h-[80vh] overflow-y-auto p-6">
              <p className="mb-4 text-sm font-semibold">{t("settings.materialTypes.kdc.modalTitle")}</p>

              <div className="flex flex-col gap-2">
                {bookType.kdcRules.length === 0 && (
                  <p className="text-sm text-neutral-400">{t("settings.materialTypes.kdc.empty")}</p>
                )}
                {bookType.kdcRules.map((rule) => (
                  <button
                    key={rule.id}
                    type="button"
                    onClick={() => openEditKdcForm(rule)}
                    className={`flex items-center justify-between rounded-lg border px-3 py-2 text-left text-sm ${
                      kdcEditingId === rule.id ? "border-neutral-800" : "border-neutral-200"
                    }`}
                  >
                    <span>{rule.label} ({rule.kdcPrefix})</span>
                    <span className="text-neutral-500">{rule.maxLoanCount}권</span>
                  </button>
                ))}
              </div>

              <button
                type="button"
                onClick={openAddKdcForm}
                className="mt-3 w-full cursor-pointer rounded-lg border border-dashed border-neutral-300 py-2 text-xs font-medium text-neutral-500 hover:bg-neutral-50"
              >
                {t("settings.materialTypes.kdc.addBtn")}
              </button>

              {showKdcForm && (
                <div className="mt-4 space-y-3 border-t border-neutral-100 pt-4">
                  <p className="text-sm font-semibold">
                    {kdcEditingId ? t("settings.materialTypes.kdc.modal.editTitle") : t("settings.materialTypes.kdc.modal.addTitle")}
                  </p>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.kdc.field.kdcPrefix")}</span>
                    <input
                      value={kdcForm.kdcPrefix}
                      onChange={(e) => setKdcForm({ ...kdcForm, kdcPrefix: e.target.value })}
                      className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                    />
                  </label>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.kdc.field.label")}</span>
                    <input
                      value={kdcForm.label}
                      onChange={(e) => setKdcForm({ ...kdcForm, label: e.target.value })}
                      className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                    />
                  </label>
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("settings.materialTypes.kdc.field.maxLoanCount")}</span>
                    <input
                      type="number"
                      min={1}
                      value={kdcForm.maxLoanCount}
                      onChange={(e) => setKdcForm({ ...kdcForm, maxLoanCount: e.target.value })}
                      className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                    />
                  </label>

                  <ThemedButton preset="버튼1" onClick={handleSaveKdc} className="w-full">
                    {t("settings.materialTypes.kdc.save")}
                  </ThemedButton>

                  {kdcEditingId && (
                    <button
                      onClick={handleDeleteKdc}
                      className="w-full cursor-pointer rounded-lg bg-red-600 py-2.5 text-sm font-semibold text-white hover:bg-red-700"
                    >
                      {t("settings.materialTypes.kdc.deleteBtn")}
                    </button>
                  )}
                </div>
              )}

              <button
                type="button"
                onClick={closeKdcModal}
                className="mt-4 w-full cursor-pointer rounded-lg border border-neutral-200 py-2 text-sm text-neutral-500"
              >
                {t("settings.materialTypes.kdc.close")}
              </button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
```

### 무슨 코드인가요?

- 위쪽 표(실물 자료 / 디지털 자료)는 `renderTable()` 함수로 두 번 그려요. 실물 자료 표에만 '대출 가능 권수'·'대출 기간' 칸이 추가돼요.
- '수정' 버튼을 누르면 첫 번째 모달이 열려요. 코드/구분/MARC 사용 여부는 **새로 추가할 때만** 입력할 수 있고, 수정할 때는 이름과(실물이면) 대출 설정만 바꿀 수 있어요. (서버 쪽 `update()`도 코드·구분은 바꾸지 않도록 만들어져 있어서, 화면도 거기에 맞췄어요.)
- '도서' 항목에만 'KDC 하위 규칙' 버튼이 보여요(`item.code === "book"`으로 확인). 이 버튼을 누르면 두 번째 모달이 열리고, 지금까지 만든 규칙들이 목록으로 나와요.
- 규칙 목록에서 하나를 클릭하면 그 규칙을 수정하는 폼이 아래에 펼쳐지고, '+ 규칙 추가'를 누르면 빈 폼이 펼쳐져요. `showKdcForm`이라는 값으로 이 폼을 보여줄지 말지를 관리해요.
- 상위(도서) 권수보다 크게 설정하거나, 하위 규칙보다 작게 상위 권수를 낮추려고 하면 서버가 거부하고, 그 오류 메시지가 그대로 알림으로 떠요.
- 모달 모서리는 두 곳 다 '바깥은 `overflow-hidden`+둥근 모서리, 안쪽은 `overflow-y-auto`+여백' 패턴을 그대로 따랐어요.

---

## 3단계. 설정 화면에 새 탭 연결하기

**파일**: `frontend/src/app/admin/settings/page.tsx`

### 1) 새 컴포넌트 불러오기

**찾기:**

```tsx
import CopyOptionsSettingsForm from "@/components/copy-options-settings-form";
```

**이렇게 바꿔주세요:**

```tsx
import CopyOptionsSettingsForm from "@/components/copy-options-settings-form";
import MaterialTypesSettingsForm from "@/components/material-types-settings-form";
```

### 2) 탭 버튼 추가하기

**찾기:**

```tsx
          <TabsTrigger value="copyOptions">{t("settings.tabs.copyOptions")}</TabsTrigger>
          <TabsTrigger value="library">{t("settings.tabs.library")}</TabsTrigger>
```

**이렇게 바꿔주세요:**

```tsx
          <TabsTrigger value="copyOptions">{t("settings.tabs.copyOptions")}</TabsTrigger>
          <TabsTrigger value="materialTypes">{t("settings.tabs.materialTypes")}</TabsTrigger>
          <TabsTrigger value="library">{t("settings.tabs.library")}</TabsTrigger>
```

### 3) 탭 내용 연결하기

**찾기:**

```tsx
        <TabsContent value="copyOptions" className="mt-4">
          <CopyOptionsSettingsForm />
        </TabsContent>
```

**이렇게 바꿔주세요:**

```tsx
        <TabsContent value="copyOptions" className="mt-4">
          <CopyOptionsSettingsForm />
        </TabsContent>

        <TabsContent value="materialTypes" className="mt-4">
          <MaterialTypesSettingsForm />
        </TabsContent>
```

---

## ✅ 확인하기

1. 프론트엔드를 재시작해주세요.
2. 관리자 페이지 → 설정 → '자료 종류' 탭을 열어보세요.
3. 실물 자료 8종(도서, DVD, 보드게임, 공구, 장비, 논문(실물), 자료집, 스크랩)과 디지털 자료 7종(논문(디지털), 법령, 영상, 음악, 웹페이지, 사진, 기사)이 표로 잘 보이는지 확인해주세요.
4. '+ 새 자료 종류 추가'를 눌러 실물 자료를 하나 만들어보세요. 대출 가능 권수·대출 기간을 비워두고 저장하면 오류가 뜨는지 확인해주세요.
5. 기존 항목(예: DVD)을 눌러 대출 가능 권수를 바꾸고 저장해보세요.
6. '도서' 항목의 'KDC 하위 규칙' 버튼을 눌러, KDC 번호(예: 657), 이름(예: 만화), 권수(예: 5)를 입력해 규칙을 하나 만들어보세요.
7. 도서 항목을 수정 모달에서 열어 대출 가능 권수를 방금 만든 KDC 규칙(5권)보다 작은 값(예: 4)으로 낮춰보고, 오류 메시지가 뜨며 저장이 안 되는지 확인해주세요.
8. 이미 등록된 자료가 있는 종류(예: 도서)를 삭제해보고, "등록된 자료가 있어 삭제할 수 없다"는 메시지가 뜨는지 확인해주세요.

---

## 📌 GitHub에 저장하기

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "22단계_2: 자료 종류 및 도서 KDC 하위 규칙 관리 화면 추가"
git push
```

---

## 📋 최종 점검표

- [ ] `dictionary.ts`에 한국어/영어 문구를 모두 추가했다
- [ ] `frontend/src/components/material-types-settings-form.tsx`를 새로 만들었다
- [ ] `settings/page.tsx`에 '자료 종류' 탭을 연결했다
- [ ] 실물 8종·디지털 7종이 표로 잘 보인다
- [ ] 새 자료 종류를 추가할 수 있다 (실물이면 대출 설정 필수)
- [ ] 기존 종류를 수정할 수 있다
- [ ] 도서 KDC 하위 규칙을 추가·수정·삭제할 수 있다
- [ ] 상위/하위 대출 권수 제한이 잘 지켜진다
- [ ] 등록된 자료가 있는 종류는 삭제가 막힌다
- [ ] GitHub에 커밋 & 푸시를 완료했다

수고하셨습니다! 이제 관리자가 화면에서 직접 자료 종류를 관리할 수 있어요. 다음 가이드(22단계_3)에서는 자료 등록 화면 자체를 "실물/디지털 선택 → 세부 종류 선택 → MARC 편집기 또는 간단 입력폼"의 2단계 흐름으로 개편할게요.

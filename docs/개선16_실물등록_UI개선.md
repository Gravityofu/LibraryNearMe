# 개선 (16) — 실물 등록 화면 UI 개선 (신규/수정 모드 구분)

> 목표: 왼쪽 "소장 부수 목록" 표와 오른쪽 입력 박스가 "지금 신규 등록
> 모드인지, 어떤 실물을 수정하는 중인지"를 색으로 짝지어 한눈에 보여주게
> 만듭니다. 버튼도 "수정"/"추가" 둘에서 "저장" 하나로 합칩니다.
> 소요 시간: 약 30분

---

## 오늘 만드는 것 (그림으로)

```
신규 등록 모드 (처음 들어왔을 때)
왼쪽 표                        오른쪽 박스
┌─────────────────────┐      ┌──────────────────┐
│ 등록번호 청구기호 소장처 상태 │      │ 새 부수 추가          │  ← 검정 테두리
│■■■■■■■■■■■■■■■■■■■■■■■│      │ 등록번호 [7]         │
│(검정 바탕/흰 글자) 실물 자료 등록│      │ ...                │
│  123    ...   ...   ...  │      │      [저장]         │
└─────────────────────┘      └──────────────────┘

기존 자료를 클릭해서 수정하는 모드
왼쪽 표                        오른쪽 박스
┌─────────────────────┐      ┌──────────────────┐
│ 등록번호 청구기호 소장처 상태 │      │ 실물 등록자료 수정      │  ← 도서관 색 테두리
│ (검정 테두리만) 실물 자료 등록  │      │ 등록번호 [123]       │
│■■■■■■■■■■■■■■■■■■■■■■■│      │ ...                │
│(도서관색 바탕/흰 글자) 123 ...│      │      [저장]         │
└─────────────────────┘      └──────────────────┘
```

---

## `admin/materials/copies/page.tsx` 수정

`frontend/src/app/admin/materials/copies/page.tsx` 를 엽니다.

### 1. 도서관 대표 색상 가져오기 — 약 4분

아래 부분을 찾으세요.

```tsx
  const [material, setMaterial] = useState<MaterialFull | null>(null);
  const [marc, setMarc] = useState<MarcField[]>(DEFAULT_FIELDS);
  const [form, setForm] = useState(EMPTY_FORM);
  const [selectedCopyId, setSelectedCopyId] = useState<number | null>(null);
  const [latestRegNo, setLatestRegNo] = useState<string | null>(null);
```

이렇게 바꿔주세요.

```tsx
  const [material, setMaterial] = useState<MaterialFull | null>(null);
  const [marc, setMarc] = useState<MarcField[]>(DEFAULT_FIELDS);
  const [form, setForm] = useState(EMPTY_FORM);
  const [selectedCopyId, setSelectedCopyId] = useState<number | null>(null);
  const [latestRegNo, setLatestRegNo] = useState<string | null>(null);
  const [primaryColor, setPrimaryColor] = useState("#2563eb");
```

이어서, 아래 부분을 찾으세요.

```tsx
  useEffect(() => {
    loadLatestRegNo();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);
```

바로 아래에 추가하세요. (기존 자료를 선택했을 때 표시할 도서관 색상을
한 번 불러와요.)

```tsx

  useEffect(() => {
    fetch(`${API_URL}/library`)
      .then((res) => res.json())
      .then((data) => setPrimaryColor(data?.primaryColor || "#2563eb"))
      .catch(() => {});
  }, []);
```

---

### 2. dictionary.ts 문구 수정/추가 — 약 4분

`frontend/src/lib/dictionary.ts` 를 엽니다.

아래 줄을 찾으세요.

```ts
    "materials.copies.latestRegNo": "최근 등록된 번호",
```

이렇게 바꿔주세요.

```ts
    "materials.copies.latestRegNo": "최근 저장된 등록번호",
```

아래 줄을 찾으세요.

```ts
    "materials.copies.editHeading": "실물 수정",
```

이렇게 바꿔주세요.

```ts
    "materials.copies.editHeading": "실물 등록자료 수정",
```

`"materials.copies.newEntry": "입력 데이터 비우기",` 줄 바로 아래에
추가하세요.

```ts
    "materials.copies.newRegistrationRow": "실물 자료 등록",
    "materials.copies.save": "저장",
```

**영어 쪽**도 같은 방식으로 찾아서 바꿔주세요.

```ts
    "materials.copies.latestRegNo": "Last saved registration No.",
```

```ts
    "materials.copies.editHeading": "Edit Registered Copy",
```

`"materials.copies.newEntry": "Clear Form",` 줄 바로 아래에 추가하세요.

```ts
    "materials.copies.newRegistrationRow": "Register New Copy",
    "materials.copies.save": "Save",
```

---

### 3. 제목 글자 조금 크게 — 약 3분

같은 `copies/page.tsx` 파일에서, 아래 세 곳을 각각 찾아서 `text-sm`을
`text-base`로 바꿔주세요.

**① MARC 정보**

```tsx
            <p className="mb-2 text-sm font-semibold">{t("materials.copies.marcBoxTitle")}</p>
```

```tsx
            <p className="mb-2 text-base font-semibold">{t("materials.copies.marcBoxTitle")}</p>
```

**② 소장 부수 목록**

```tsx
            <p className="mb-2 text-sm font-semibold">
              {t("materials.copies.copyListHeading")} ({material.copies.length}
              {t("materials.copies.countUnitBooks")})
            </p>
```

```tsx
            <p className="mb-2 text-base font-semibold">
              {t("materials.copies.copyListHeading")} ({material.copies.length}
              {t("materials.copies.countUnitBooks")})
            </p>
```

**③ 새 부수 추가 / 실물 등록자료 수정** — 이 부분은 4번에서 버튼 삭제와
함께 통째로 바꿀 거라, 지금은 건너뛰어도 됩니다.

---

### 4. "입력 데이터 비우기" 버튼 삭제 + 제목 글자 크기 반영 — 약 5분

아래 부분을 찾으세요.

```tsx
          <div className="mb-3 flex items-center justify-between">
            <p className="text-sm font-semibold">
              {selectedCopyId ? t("materials.copies.editHeading") : t("materials.copies.addHeading")}
            </p>
            {selectedCopyId && (
              <button
                type="button"
                onClick={resetForm}
                className="cursor-pointer text-xs text-neutral-400 underline"
              >
                {t("materials.copies.newEntry")}
              </button>
            )}
          </div>
```

이렇게 바꿔주세요.

```tsx
          <div className="mb-3">
            <p className="text-base font-semibold">
              {selectedCopyId ? t("materials.copies.editHeading") : t("materials.copies.addHeading")}
            </p>
          </div>
```

---

### 5. 오른쪽 박스 테두리를 모드에 따라 다르게 — 약 4분

아래 부분을 찾으세요.

```tsx
        {/* 오른쪽: 등록/수정 폼 */}
        <div className="rounded-lg border border-neutral-200 bg-white p-3">
```

이렇게 바꿔주세요. (신규 등록 모드면 검정 테두리, 기존 자료 수정 모드면
도서관 대표 색 테두리가 나옵니다.)

```tsx
        {/* 오른쪽: 등록/수정 폼 */}
        <div
          className="rounded-lg border-2 bg-white p-3"
          style={{ borderColor: selectedCopyId ? primaryColor : "#000000" }}
        >
```

---

### 6. 왼쪽 표에 "실물 자료 등록" 행 추가 + 색상 강조 — 약 10분

아래 부분을 찾으세요. (표 전체를 조건부로 보여주던 부분입니다.)

```tsx
            {material.copies.length > 0 ? (
              <table className="w-full text-left text-sm">
                <thead className="sticky top-0 bg-neutral-50 text-neutral-500">
                  <tr>
                    <th className="px-2 py-1.5">{t("materials.copies.regNo")}</th>
                    <th className="px-2 py-1.5">{t("materials.copies.callNumber")}</th>
                    <th className="px-2 py-1.5">{t("materials.copies.location")}</th>
                    <th className="px-2 py-1.5">{t("materials.copies.status")}</th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-neutral-200">
                  {material.copies.map((c) => {
                    const statusOpt = STATUS_OPTIONS.find((s) => s.value === c.status);
                    return (
                      <tr
                        key={c.id}
                        onClick={() => selectCopy(c)}
                        className={`cursor-pointer ${
                          selectedCopyId === c.id ? "bg-neutral-100" : "hover:bg-neutral-50"
                        }`}
                      >
                        <td className="whitespace-nowrap px-2 py-2">{c.registrationNo}</td>
                        <td className="whitespace-nowrap px-2 py-2">{c.callNumber || "-"}</td>
                        <td className="whitespace-nowrap px-2 py-2">{c.location || "-"}</td>
                        <td className="whitespace-nowrap px-2 py-2">
                          {statusOpt ? t(statusOpt.labelKey) : c.status}
                        </td>
                      </tr>
                    );
                  })}
                </tbody>
              </table>
            ) : (
              <p className="text-sm text-neutral-400">{t("materials.copies.noCopies")}</p>
            )}
```

이렇게 바꿔주세요. (조건 없이 표를 항상 보여주고, 맨 위에 "실물 자료
등록" 행을 추가했어요. 이 행은 신규 등록 모드일 때 검정 바탕/흰 글자로,
그 외에는 검정 테두리만 있는 모습으로 바뀌어요. 기존 자료 행을 선택하면
도서관 대표 색 바탕/흰 글자로 강조돼요.)

```tsx
            <table className="w-full text-left text-sm">
              <thead className="sticky top-0 bg-neutral-50 text-neutral-500">
                <tr>
                  <th className="px-2 py-1.5">{t("materials.copies.regNo")}</th>
                  <th className="px-2 py-1.5">{t("materials.copies.callNumber")}</th>
                  <th className="px-2 py-1.5">{t("materials.copies.location")}</th>
                  <th className="px-2 py-1.5">{t("materials.copies.status")}</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-neutral-200">
                <tr onClick={resetForm} className="cursor-pointer">
                  <td
                    colSpan={4}
                    className={`px-2 py-2 font-semibold ${
                      selectedCopyId === null ? "" : "border border-black text-black"
                    }`}
                    style={selectedCopyId === null ? { backgroundColor: "#000000", color: "#ffffff" } : undefined}
                  >
                    {t("materials.copies.newRegistrationRow")}
                  </td>
                </tr>
                {material.copies.map((c) => {
                  const statusOpt = STATUS_OPTIONS.find((s) => s.value === c.status);
                  return (
                    <tr
                      key={c.id}
                      onClick={() => selectCopy(c)}
                      className={`cursor-pointer ${selectedCopyId === c.id ? "" : "hover:bg-neutral-50"}`}
                      style={selectedCopyId === c.id ? { backgroundColor: primaryColor, color: "#ffffff" } : undefined}
                    >
                      <td className="whitespace-nowrap px-2 py-2">{c.registrationNo}</td>
                      <td className="whitespace-nowrap px-2 py-2">{c.callNumber || "-"}</td>
                      <td className="whitespace-nowrap px-2 py-2">{c.location || "-"}</td>
                      <td className="whitespace-nowrap px-2 py-2">
                        {statusOpt ? t(statusOpt.labelKey) : c.status}
                      </td>
                    </tr>
                  );
                })}
              </tbody>
            </table>
```

- 등록된 실물이 하나도 없어도 "실물 자료 등록" 행은 항상 보여서, 표가
  텅 비어 보이지 않아요. (그래서 예전에 있던 "등록된 실물이 없습니다"
  안내 문구는 이제 필요 없어져서 지웠어요.)

---

### 7. "수정"/"추가" 버튼을 "저장" 버튼 하나로 통합 — 약 4분

아래 부분을 찾으세요.

```tsx
          <div className="mt-4 flex gap-2">
            <button
              type="button"
              onClick={handleUpdateCopy}
              disabled={!selectedCopyId}
              className="flex-1 cursor-pointer rounded-lg border px-4 py-2.5 text-sm font-semibold disabled:cursor-not-allowed disabled:opacity-40"
            >
              {t("materials.copies.update")}
            </button>
            <button
              type="button"
              onClick={handleAddCopy}
              className="flex-1 cursor-pointer rounded-lg bg-[#383838] px-4 py-2.5 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("materials.copies.add")}
            </button>
          </div>
```

이렇게 바꿔주세요.

```tsx
          <div className="mt-4">
            <button
              type="button"
              onClick={selectedCopyId ? handleUpdateCopy : handleAddCopy}
              className="w-full cursor-pointer rounded-lg bg-[#383838] px-4 py-2.5 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("materials.copies.save")}
            </button>
          </div>
```

- 지금 어떤 실물이 선택돼 있으면(수정 모드) 수정 처리를, 아니면(신규
  등록 모드) 추가 처리를 하도록 버튼 하나가 알아서 나눠서 실행해요.

---

## 확인하기 — 약 8분

1. 실물 등록 화면에 처음 들어가면 왼쪽 표 맨 위 "실물 자료 등록" 행이
   검정 바탕/흰 글자로 강조돼 있는지 확인합니다.
2. 오른쪽 박스 테두리가 검정색인지, 제목이 "새 부수 추가"인지 확인합니다.
3. 기존 실물 행을 클릭하면 그 행이 도서관 대표 색 바탕/흰 글자로
   바뀌고, "실물 자료 등록" 행은 검정 바탕이 사라지고 테두리만 검정으로
   남는지 확인합니다.
4. 이때 오른쪽 박스 테두리가 도서관 대표 색으로 바뀌고, 제목이 "실물
   등록자료 수정"으로 바뀌며 그 실물의 정보가 채워지는지 확인합니다.
5. "실물 자료 등록" 행을 다시 클릭하면 신규 등록 모드로 돌아가는지
   (입력칸이 비워지고, 등록번호엔 다음 번호가 채워지는지) 확인합니다.
6. "MARC 정보", "소장 부수 목록", "새 부수 추가"/"실물 등록자료 수정"
   제목 글자가 살짝 커졌는지 확인합니다.
7. "입력 데이터 비우기" 버튼이 더 이상 안 보이는지 확인합니다.
8. 하단 버튼이 "저장" 하나로 합쳐졌는지, 신규 등록/수정 각각 상황에서
   잘 저장되는지 확인합니다.
9. "(최근 저장된 등록번호 : 6)"으로 문구가 바뀌었는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선(16): 실물 등록 화면 신규/수정 모드 색상 구분, 저장 버튼 통합"
git push
```

---

## 최종 점검표

- [ ] `primaryColor` 상태 추가 + 도서관 정보 불러오기
- [ ] `dictionary.ts` — 문구 수정/추가(한글·영어)
- [ ] 제목 세 곳 글자 크기 키움
- [ ] "입력 데이터 비우기" 버튼 삭제
- [ ] 오른쪽 박스 테두리 모드별 색상 적용
- [ ] 왼쪽 표에 "실물 자료 등록" 행 추가, 표 항상 보이도록 변경
- [ ] 기존 행 선택 시 도서관 색상 강조
- [ ] "수정"/"추가" 버튼 → "저장" 버튼 하나로 통합
- [ ] 신규 등록 확인
- [ ] 기존 자료 수정 확인
- [ ] GitHub에 올림

완료되면 알려주세요!

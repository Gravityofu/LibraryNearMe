# 16단계 (1) — 로그인 화면에 회원가입·아이디 찾기 링크 추가

> 목표: 로그인 화면 아래에 "회원가입", "아이디 찾기", "비밀번호 찾기" 세 링크를 추가합니다.
> "아이디 찾기"는 실제로 동작하도록(이름+휴대폰 번호로 조회) 만들고, "비밀번호 찾기"는
> 안내 페이지만 만들어둡니다 — 문자(SMS) 인증 기능이 아직 없어서, 본인 확인 없이
> 비밀번호를 바꾸게 하면 보안 문제가 생기기 때문이에요(예전 7단계 설계 논의 때 정한 방침).
> 나중에 문자 인증 기능을 붙일 때, 그 안내 페이지를 진짜 재설정 기능으로 바꿀 거예요.
> 소요 시간: 약 25분

---

## 오늘 만드는 것 (그림으로)

```
로그인 화면
┌─────────────────────┐
│  아이디  [        ]  │
│  비밀번호[        ]  │
│      [로그인]        │
│                       │
│ 회원가입 | 아이디 찾기 | 비밀번호 찾기
└─────────────────────┘
        │           │              │
        ▼           ▼              ▼
    /signup     /find-id      /find-password
   (이미 있음)  (오늘 새로 만듦)  (오늘 안내페이지만)
```

준비: backend·frontend 두 서버를 모두 켜 두세요.

---

## 1. 백엔드 — 아이디 찾기 API (약 10분)

### 1-1. 서비스에 함수 추가

`backend/src/users/users.service.ts` 를 엽니다. 맨 위, `type SignupData = {...}` 블록 **바로 아래**에 아이디를 가려서 보여주는 도구 함수를 추가하세요.

```typescript
// "abcd1234" → "ab****34" 처럼 가운데를 가려서 보여줍니다.
function maskLoginId(id: string): string {
  if (id.length <= 2) return id[0] + '*'.repeat(id.length - 1);
  if (id.length <= 4) return id.slice(0, 1) + '*'.repeat(id.length - 2) + id.slice(-1);
  return id.slice(0, 2) + '*'.repeat(id.length - 4) + id.slice(-2);
}
```

이어서, `update` 함수 바로 아래(파일 맨 끝, 마지막 닫는 중괄호 `}` 바로 앞)에 아래 함수를 추가하세요.

```typescript
  // 이름 + 휴대폰 번호로 아이디 찾기. 로그인 전(누구나 접근 가능)이라 libraryId를 직접 못 받고,
  // signup()처럼 "현재 첫 번째 도서관"을 기준으로 찾습니다.
  async findLoginId(name: string, phone: string) {
    if (!name?.trim() || !phone?.trim()) {
      throw new BadRequestException('이름과 휴대폰 번호를 입력해주세요.');
    }

    const library = await this.prisma.library.findFirst();
    if (!library) {
      throw new BadRequestException('도서관 설정이 없습니다.');
    }

    const users = await this.prisma.user.findMany({
      where: {
        libraryId: library.id,
        name: name.trim(),
        phone: phone.trim(),
        role: { in: [Role.MEMBER, Role.ADMIN] },
      },
      select: { loginId: true },
    });

    const loginIds = users
      .filter((u) => !!u.loginId)
      .map((u) => maskLoginId(u.loginId as string));

    return { loginIds };
  }
```

- 이름·휴대폰 번호가 **둘 다 정확히 일치**하는 회원만 찾아요. 둘 중 하나만 알아서는 못 찾게 해서, 아무나 남의 아이디를 알아낼 수 없게 막았어요.
- 아이디를 그대로 보여주지 않고 `maskLoginId()`로 가운데를 가려서(`ab****34`) 돌려줘요. 완전한 아이디는 본인만 알아볼 수 있을 정도로만 힌트를 줍니다.
- 같은 이름·휴대폰 번호로 등록된 회원이 여러 명일 수도 있어서(가족이 같은 번호를 쓰는 경우 등), 배열로 여러 개를 돌려줄 수 있게 했어요.

### 1-2. 컨트롤러에 경로 추가

`backend/src/users/users.controller.ts` 를 엽니다. `signup` 메서드 **바로 아래**에 추가하세요.

```typescript
  // POST 요청: 이름+휴대폰 번호로 아이디 찾기 — 누구나 접근 가능(로그인 전)
  @Post('find-id')
  findLoginId(@Body() body: { name: string; phone: string }) {
    return this.usersService.findLoginId(body.name, body.phone);
  }
```

저장하면 backend 서버가 재시작됩니다. `POST /users/find-id` 주소가 새로 생겼어요.

---

## 2. dictionary.ts 수정 — 약 5분

`frontend/src/lib/dictionary.ts` 를 엽니다.

### 2-1. 로그인 화면 링크 문구

`"login.fail"` 줄 바로 아래(한글·영어 각각)에 추가하세요.

**한글 쪽:**

```ts
    "login.signupLink": "회원가입",
    "login.findIdLink": "아이디 찾기",
    "login.findPasswordLink": "비밀번호 찾기",
```

**영어 쪽:**

```ts
    "login.signupLink": "Sign up",
    "login.findIdLink": "Find ID",
    "login.findPasswordLink": "Find Password",
```

### 2-2. 아이디 찾기 화면 문구

이어서 추가하세요.

**한글 쪽:**

```ts
    "findId.title": "아이디 찾기",
    "findId.name": "이름",
    "findId.phone": "휴대폰 번호",
    "findId.submit": "아이디 찾기",
    "findId.requiredFields": "이름과 휴대폰 번호를 입력해주세요.",
    "findId.invalidPhone": "휴대폰 번호 11자리를 모두 입력해주세요.",
    "findId.resultFound": "찾으신 아이디예요:",
    "findId.resultNotFound": "일치하는 회원 정보가 없습니다.",
    "findId.fail": "조회에 실패했습니다.",
    "findId.backToLogin": "로그인으로 돌아가기",
```

**영어 쪽:**

```ts
    "findId.title": "Find ID",
    "findId.name": "Name",
    "findId.phone": "Mobile Phone",
    "findId.submit": "Find ID",
    "findId.requiredFields": "Please enter your name and mobile phone number.",
    "findId.invalidPhone": "Please enter all 11 digits of the mobile phone number.",
    "findId.resultFound": "Here's your ID:",
    "findId.resultNotFound": "No matching member found.",
    "findId.fail": "Search failed.",
    "findId.backToLogin": "Back to login",
```

### 2-3. 비밀번호 찾기 안내 화면 문구

이어서 추가하세요.

**한글 쪽:**

```ts
    "findPassword.title": "비밀번호 찾기",
    "findPassword.message": "비밀번호 찾기는 휴대폰 문자 인증 기능이 준비되면 이용하실 수 있어요. 그 전까지는 도서관에 문의해주세요.",
    "findPassword.backToLogin": "로그인으로 돌아가기",
```

**영어 쪽:**

```ts
    "findPassword.title": "Find Password",
    "findPassword.message": "Password recovery will be available once SMS verification is added. Until then, please contact the library.",
    "findPassword.backToLogin": "Back to login",
```

---

## 3. 로그인 화면에 링크 3개 추가 — 약 3분

`frontend/src/app/(site)/login/page.tsx` 를 엽니다.

**① import 추가** — 맨 위에 추가하세요.

```tsx
import Link from "next/link";
```

**② 링크 추가** — 아래 부분을 찾으세요.

```tsx
            <Button type="submit" className="cursor-pointer">
              {t("login.submit")}
            </Button>
          </form>
        </CardContent>
      </Card>
    </main>
  );
}
```

이렇게 바꿔주세요. (제출 버튼 아래에 링크 3개를 나란히 넣었어요.)

```tsx
            <Button type="submit" className="cursor-pointer">
              {t("login.submit")}
            </Button>
          </form>
          <div className="mt-4 flex items-center justify-center gap-3 text-sm text-neutral-500">
            <Link href="/signup" className="hover:text-neutral-800">
              {t("login.signupLink")}
            </Link>
            <span className="text-neutral-300">|</span>
            <Link href="/find-id" className="hover:text-neutral-800">
              {t("login.findIdLink")}
            </Link>
            <span className="text-neutral-300">|</span>
            <Link href="/find-password" className="hover:text-neutral-800">
              {t("login.findPasswordLink")}
            </Link>
          </div>
        </CardContent>
      </Card>
    </main>
  );
}
```

---

## 4. "아이디 찾기" 화면 만들기 — 약 10분

`frontend/src/app/(site)` 폴더 안에 **`find-id`** 폴더를 새로 만들고, 그 안에 **`page.tsx`** 파일을 만들어 아래를 붙여넣습니다.

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = "http://localhost:3001";

function formatPhone(raw: string) {
  const digits = raw.replace(/\D/g, "").slice(0, 11);
  if (digits.length < 4) return digits;
  if (digits.length < 8) return `${digits.slice(0, 3)}-${digits.slice(3)}`;
  return `${digits.slice(0, 3)}-${digits.slice(3, 7)}-${digits.slice(7)}`;
}

function isValidPhone(phone: string) {
  return phone.replace(/\D/g, "").length === 11;
}

export default function FindIdPage() {
  const { t } = useI18n();
  const { notify } = useNotify();
  const [name, setName] = useState("");
  const [phone, setPhone] = useState("");
  const [result, setResult] = useState<string[] | null>(null);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();

    if (!name.trim() || !phone.trim()) {
      notify("❌ " + t("findId.requiredFields"), "error");
      return;
    }
    if (!isValidPhone(phone)) {
      notify("❌ " + t("findId.invalidPhone"), "error");
      return;
    }

    const res = await fetch(`${API_URL}/users/find-id`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ name, phone }),
    });

    if (res.ok) {
      const data = await res.json();
      setResult(data.loginIds || []);
    } else {
      notify("❌ " + t("findId.fail"), "error");
    }
  }

  return (
    <main className="mx-auto max-w-md p-8">
      <Card>
        <CardHeader>
          <CardTitle>{t("findId.title")}</CardTitle>
        </CardHeader>
        <CardContent>
          {result === null ? (
            <form onSubmit={handleSubmit} className="flex flex-col gap-4">
              <div className="flex flex-col gap-2">
                <Label htmlFor="name">{t("findId.name")}</Label>
                <Input id="name" value={name} onChange={(e) => setName(e.target.value)} />
              </div>
              <div className="flex flex-col gap-2">
                <Label htmlFor="phone">{t("findId.phone")}</Label>
                <Input
                  id="phone"
                  value={phone}
                  onChange={(e) => setPhone(formatPhone(e.target.value))}
                  placeholder="000-0000-0000"
                  inputMode="numeric"
                  maxLength={13}
                />
              </div>
              <Button type="submit" className="cursor-pointer">
                {t("findId.submit")}
              </Button>
            </form>
          ) : (
            <div className="flex flex-col gap-4">
              {result.length > 0 ? (
                <div className="flex flex-col gap-1">
                  <p className="text-sm text-neutral-500">{t("findId.resultFound")}</p>
                  {result.map((id, i) => (
                    <p key={i} className="text-lg font-semibold">
                      {id}
                    </p>
                  ))}
                </div>
              ) : (
                <p className="text-sm text-neutral-500">{t("findId.resultNotFound")}</p>
              )}
              <Link href="/login" className="text-center text-sm text-neutral-500 hover:text-neutral-800">
                {t("findId.backToLogin")}
              </Link>
            </div>
          )}
        </CardContent>
      </Card>
    </main>
  );
}
```

- 조회하면 입력 폼 대신 **결과 화면**으로 바뀌어요(`result` 값이 `null`이면 폼, 배열이면 결과). 아이디가 하나 이상 나오면 가려진 모양(`ab****34`)으로 보여주고, 없으면 "일치하는 회원 정보가 없습니다."라고 안내해요.

---

## 5. "비밀번호 찾기" 안내 화면 만들기 — 약 3분

`frontend/src/app/(site)` 폴더 안에 **`find-password`** 폴더를 새로 만들고, 그 안에 **`page.tsx`** 파일을 만들어 아래를 붙여넣습니다.

```tsx
import Link from "next/link";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function FindPasswordPage() {
  return (
    <main className="mx-auto max-w-md p-8">
      <Card>
        <CardHeader>
          <CardTitle>비밀번호 찾기</CardTitle>
        </CardHeader>
        <CardContent className="flex flex-col gap-4">
          <p className="text-sm text-neutral-500">
            비밀번호 찾기는 휴대폰 문자 인증 기능이 준비되면 이용하실 수 있어요. 그 전까지는 도서관에 문의해주세요.
          </p>
          <Link href="/login" className="text-center text-sm text-neutral-500 hover:text-neutral-800">
            로그인으로 돌아가기
          </Link>
        </CardContent>
      </Card>
    </main>
  );
}
```

> 이 화면은 언어 토글(다국어)까지는 아직 안 붙였어요 — 나중에 진짜 재설정 기능을 만들 때 함께 다듬을 예정이라, 지금은 한글 안내문 그대로 간단하게 뒀습니다.

---

## 6. 확인하기 — 약 5분

1. `/login` 화면 아래에 "회원가입 | 아이디 찾기 | 비밀번호 찾기" 세 링크가 보이는지 확인합니다.
2. "회원가입" 클릭 → `/signup`으로 이동하는지 확인합니다.
3. "아이디 찾기" 클릭 → `/find-id`에서 실제 회원의 이름·휴대폰 번호를 입력하면 가려진 아이디(`ab****34`)가 나오는지 확인합니다.
4. 없는 이름·번호로 조회하면 "일치하는 회원 정보가 없습니다."가 나오는지 확인합니다.
5. "비밀번호 찾기" 클릭 → 안내 문구가 나오는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## 7. GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "16단계(1): 로그인 화면에 회원가입/아이디찾기/비밀번호찾기 링크, 아이디 찾기 기능 추가"
git push
```

---

## 최종 점검표

- [ ] `users.service.ts` — `maskLoginId()`, `findLoginId()` 추가
- [ ] `users.controller.ts` — `POST /users/find-id` 추가
- [ ] `dictionary.ts` — login/findId/findPassword 문구 한글·영어 추가
- [ ] `login/page.tsx` — 회원가입/아이디찾기/비밀번호찾기 링크 3개 추가
- [ ] `(site)/find-id/page.tsx` 생성
- [ ] `(site)/find-password/page.tsx` 생성
- [ ] 아이디 찾기 정상 조회 확인
- [ ] 결과 없음 케이스 확인
- [ ] GitHub에 올림

---

## 다음 단계 예고

문자(SMS) 인증 기능을 만들 때, `/find-password` 안내 페이지를 "휴대폰으로 인증코드 받기 → 새 비밀번호 설정" 진짜 기능으로 바꿀 거예요. 그전까지는 이 상태로 충분합니다. 완료되면 알려주세요!

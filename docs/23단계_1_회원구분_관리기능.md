# 23단계-1: 회원 구분(개인/단체/자료지원) 관리 기능 만들기

## 목표

앞으로 '대출' 관련 설정을 만들려면, 먼저 "이 회원이 개인회원인지, 단체회원인지, 자료지원회원인지"를 구분할 수 있어야 합니다. 이번 가이드에서는 그 준비 작업으로, '설정' → '도서관' 화면에 **회원 구분을 추가/수정/삭제할 수 있는 기능**을 만듭니다. ('대출' 메뉴는 이 작업이 끝난 뒤, 다음 가이드(23단계-2)에서 따로 만듭니다.)

이번에 만드는 내용은 다음과 같습니다.

1. '설정' → '도서관' 화면 아래에 "회원 구분" 목록이 나오고, 값을 추가·수정·삭제할 수 있습니다. 처음 쓰실 때는 "개인회원", "단체회원", "자료지원회원" 3개가 자동으로 만들어집니다.
2. 최소 1개는 항상 남아있어야 하고, 어떤 회원 구분을 이미 쓰고 있는 회원이 있으면 삭제할 수 없습니다. (이때 그 구분을 쓰는 회원 이름과 총 인원수를 안내합니다.)
3. '회원 관리' 화면(설정이 아니라 사이드바의 '회원' 메뉴)에서 회원을 등록하거나 수정할 때, 역할이 "회원"이면 반드시 회원 구분 하나를 선택해야 합니다. (역할이 "관리자"인 경우는 회원 구분이 필요 없어서 선택 항목이 보이지 않습니다.)
4. 홈페이지에서 방문자가 직접 회원가입을 하는 경우에는 회원 구분을 직접 고르는 화면까지는 만들지 않고, 자동으로 첫 번째 회원 구분("개인회원")으로 등록되게 했습니다. 나중에 관리자가 '회원 관리' 화면에서 그 회원의 구분을 원하는 값으로 바꿔줄 수 있습니다.

이미 등록되어 있던 기존 회원들은 이번 작업 직후에는 회원 구분이 비어있는 상태로 남아있습니다. 그 회원을 '회원 관리' 화면에서 다시 열어 수정하려고 하면, 그때 회원 구분을 선택하도록 화면이 안내합니다.

---

## 1. 데이터베이스에 '회원 구분' 테이블 추가하기

`C:\projects\LibraryNearMe\backend\prisma\schema.prisma` 파일을 여세요.

### 1-1. `Library` 모델에 연결고리 추가

`model Library {` 안에서 아래 부분을 찾으세요.

```prisma
  materialTypes MaterialType[] // ← 추가 (자료 종류 목록)
  bookKdcRules  BookKdcRule[]  // ← 추가 (도서 KDC 하위 규칙)
}
```

이 부분을 아래 내용으로 바꿔주세요. (`memberTypes` 한 줄이 추가됩니다.)

```prisma
  materialTypes MaterialType[] // ← 추가 (자료 종류 목록)
  bookKdcRules  BookKdcRule[]  // ← 추가 (도서 KDC 하위 규칙)
  memberTypes   MemberType[]   // ← 추가 (회원 구분 목록)
}
```

### 1-2. `User` 모델에 회원 구분 칸 추가

`model User {` 안에서 아래 부분을 찾으세요.

```prisma
  memberNo      String?                       // 회원증 번호(사람이 읽는). 기존 "U012345" 그대로 or 신규 발급
  cardToken     String     @unique            // QR 보안 코드(기계용). 무작위 고유값

  role          Role       @default(MEMBER)   // 역할
  status        UserStatus @default(ACTIVE)   // 계정 상태
```

이 부분을 아래 내용으로 바꿔주세요. (`memberTypeId`, `memberType` 두 줄이 추가됩니다.)

```prisma
  memberNo      String?                       // 회원증 번호(사람이 읽는). 기존 "U012345" 그대로 or 신규 발급
  cardToken     String     @unique            // QR 보안 코드(기계용). 무작위 고유값

  memberTypeId  Int?                          // 회원 구분 (역할이 "회원"일 때만 사용)
  memberType    MemberType? @relation(fields: [memberTypeId], references: [id])

  role          Role       @default(MEMBER)   // 역할
  status        UserStatus @default(ACTIVE)   // 계정 상태
```

### 1-3. `MemberType` 모델 새로 추가

파일 맨 아래(제일 마지막 줄)로 이동해서, 아래 내용을 새로 추가해주세요.

```prisma

// 회원 구분 (예: 개인회원, 단체회원, 자료지원회원)
model MemberType {
  id        Int      @id @default(autoincrement())

  libraryId Int
  library   Library  @relation(fields: [libraryId], references: [id])

  name      String
  order     Int      @default(0)

  createdAt DateTime @default(now())

  users     User[]

  @@unique([libraryId, name])
}
```

파일을 저장하세요.

---

## 2. 마이그레이션 실행하기

명령 프롬프트(cmd)나 PowerShell을 여세요.

```
cd C:\projects\LibraryNearMe\backend
npx prisma migrate dev --name add_member_type
```

정상적으로 끝나면 "Your database is now in sync with your schema" 메시지가 나옵니다.

> 💡 만약 `EPERM: operation not permitted, rename ...` 메시지가 나오면, 실행 중인 백엔드 서버(`npm run start:dev` 창)를 잠깐 끄고 다시 실행해보세요.

---

## 3. 백엔드 새 파일 만들기: `member-types.service.ts`

`C:\projects\LibraryNearMe\backend\src\settings\` 폴더 안에 `member-types.service.ts`라는 새 파일을 만들고, 아래 내용을 붙여넣으세요.

```ts
import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma.service';

// 처음 이 도서관이 '회원 구분' 기능을 쓸 때 자동으로 채워지는 기본값입니다.
const DEFAULT_MEMBER_TYPES = ['개인회원', '단체회원', '자료지원회원'];

@Injectable()
export class MemberTypesService {
  constructor(private prisma: PrismaService) {}

  // 목록 조회. 이 도서관에 회원 구분이 하나도 없으면, 기본 3개를 자동으로 채워 넣습니다.
  async list(libraryId: number) {
    const count = await this.prisma.memberType.count({ where: { libraryId } });
    if (count === 0) {
      await this.prisma.memberType.createMany({
        data: DEFAULT_MEMBER_TYPES.map((name, i) => ({ libraryId, name, order: i })),
      });
    }
    return this.prisma.memberType.findMany({
      where: { libraryId },
      orderBy: [{ order: 'asc' }, { id: 'asc' }],
    });
  }

  // 값 추가
  async create(libraryId: number, data: any) {
    const name = String(data.name || '').trim();
    if (!name) {
      throw new BadRequestException('이름을 입력하세요.');
    }
    const count = await this.prisma.memberType.count({ where: { libraryId } });
    try {
      return await this.prisma.memberType.create({
        data: { libraryId, name, order: count },
      });
    } catch (e: any) {
      if (e.code === 'P2002') {
        throw new BadRequestException('이미 있는 이름입니다.');
      }
      throw e;
    }
  }

  // 값 수정 - User.memberTypeId가 실제 관계(FK)로 연결되어 있어서,
  // 이름만 바꾸면 이 구분을 쓰는 회원들에게도 자동으로 새 이름이 반영됩니다. (따로 손댈 것이 없습니다.)
  async update(libraryId: number, id: number, data: any) {
    const existing = await this.prisma.memberType.findFirst({ where: { id, libraryId } });
    if (!existing) {
      throw new NotFoundException('회원 구분을 찾을 수 없습니다.');
    }
    const name = String(data.name || '').trim();
    if (!name) {
      throw new BadRequestException('이름을 입력하세요.');
    }
    if (name === existing.name) {
      return existing;
    }
    try {
      return await this.prisma.memberType.update({ where: { id }, data: { name } });
    } catch (e: any) {
      if (e.code === 'P2002') {
        throw new BadRequestException('이미 있는 이름입니다.');
      }
      throw e;
    }
  }

  // 삭제 - 최소 1개는 남아있어야 하고, 이 구분을 쓰는 회원이 있으면 삭제할 수 없습니다.
  async remove(libraryId: number, id: number) {
    const existing = await this.prisma.memberType.findFirst({ where: { id, libraryId } });
    if (!existing) {
      throw new NotFoundException('회원 구분을 찾을 수 없습니다.');
    }

    const siblingCount = await this.prisma.memberType.count({ where: { libraryId } });
    if (siblingCount <= 1) {
      throw new BadRequestException('최소 1개의 회원 구분은 남아있어야 합니다.');
    }

    const usedUsers = await this.prisma.user.findMany({
      where: { libraryId, memberTypeId: id },
      select: { name: true },
      orderBy: { name: 'asc' },
      take: 6, // 5명까지만 보여주고, 6번째가 있으면 "더 있다"는 표시만 합니다.
    });

    if (usedUsers.length > 0) {
      const totalCount = await this.prisma.user.count({ where: { libraryId, memberTypeId: id } });
      const shown = usedUsers.slice(0, 5).map((u) => u.name);
      const shownText = usedUsers.length > 5 ? `${shown.join(', ')}, ...` : shown.join(', ');
      throw new BadRequestException(
        `이 구분을 사용 중인 회원이 있어 삭제할 수 없습니다. (이름: ${shownText}) (총 ${totalCount}명)`,
      );
    }

    await this.prisma.memberType.delete({ where: { id } });
    return { success: true };
  }
}
```

---

## 4. 백엔드 새 파일 만들기: `member-types.controller.ts`

같은 폴더(`C:\projects\LibraryNearMe\backend\src\settings\`) 안에 `member-types.controller.ts`라는 새 파일을 만들고, 아래 내용을 붙여넣으세요.

```ts
import { Controller, Get, Post, Patch, Delete, Body, Param, Req, UseGuards } from '@nestjs/common';
import { MemberTypesService } from './member-types.service';
import { AdminGuard } from '../auth/admin.guard';

@Controller('member-types')
export class MemberTypesController {
  constructor(private memberTypesService: MemberTypesService) {}

  @Get()
  @UseGuards(AdminGuard)
  list(@Req() req: any) {
    return this.memberTypesService.list(req.user.libraryId);
  }

  @Post()
  @UseGuards(AdminGuard)
  create(@Req() req: any, @Body() body: any) {
    return this.memberTypesService.create(req.user.libraryId, body);
  }

  @Patch(':id')
  @UseGuards(AdminGuard)
  update(@Req() req: any, @Param('id') id: string, @Body() body: any) {
    return this.memberTypesService.update(req.user.libraryId, parseInt(id, 10), body);
  }

  @Delete(':id')
  @UseGuards(AdminGuard)
  remove(@Req() req: any, @Param('id') id: string) {
    return this.memberTypesService.remove(req.user.libraryId, parseInt(id, 10));
  }
}
```

---

## 5. `app.module.ts`에 새 기능 등록하기

`C:\projects\LibraryNearMe\backend\src\app.module.ts` 파일을 여세요.

**5-1.** 아래 부분을 찾으세요.

```ts
import { MaterialTypesController } from './settings/material-types.controller';
import { MaterialTypesService } from './settings/material-types.service';
```

이렇게 바꿔주세요.

```ts
import { MaterialTypesController } from './settings/material-types.controller';
import { MaterialTypesService } from './settings/material-types.service';
import { MemberTypesController } from './settings/member-types.controller';
import { MemberTypesService } from './settings/member-types.service';
```

**5-2.** 아래 부분을 찾으세요.

```ts
    CopyOptionsController,
    MaterialTypesController,
  ],
```

이렇게 바꿔주세요.

```ts
    CopyOptionsController,
    MaterialTypesController,
    MemberTypesController,
  ],
```

**5-3.** 아래 부분을 찾으세요.

```ts
    CopyOptionsService,
    MaterialTypesService,
  ],
```

이렇게 바꿔주세요.

```ts
    CopyOptionsService,
    MaterialTypesService,
    MemberTypesService,
  ],
```

파일을 저장하세요.

---

## 6. `users.service.ts` 수정하기 (회원 등록/수정 시 회원 구분 반영)

`C:\projects\LibraryNearMe\backend\src\users\users.service.ts` 파일을 여세요.

### 6-1. import 추가

파일 맨 위쪽, 아래 부분을 찾으세요.

```ts
import { PrismaService } from '../prisma.service';
import * as bcrypt from 'bcryptjs';
```

이렇게 바꿔주세요.

```ts
import { PrismaService } from '../prisma.service';
import { MemberTypesService } from '../settings/member-types.service';
import * as bcrypt from 'bcryptjs';
```

### 6-2. 생성자(constructor)에 회원 구분 서비스 연결

아래 부분을 찾으세요.

```ts
export class UsersService {
  constructor(private prisma: PrismaService) {}
```

이렇게 바꿔주세요.

```ts
export class UsersService {
  constructor(
    private prisma: PrismaService,
    private memberTypesService: MemberTypesService,
  ) {}
```

### 6-3. `signup()` (홈페이지 회원가입) — 자동으로 첫 번째 회원 구분 지정

아래 부분을 찾으세요.

```ts
    const library = await this.prisma.library.findFirst();
    if (!library) {
      throw new BadRequestException('도서관 설정이 없습니다.');
    }

    const existing = await this.prisma.user.findFirst({
      where: { libraryId: library.id, loginId: data.loginId },
    });
```

이렇게 바꿔주세요. (회원 구분 목록을 불러와서, 첫 번째 값을 기본으로 씁니다.)

```ts
    const library = await this.prisma.library.findFirst();
    if (!library) {
      throw new BadRequestException('도서관 설정이 없습니다.');
    }

    // 홈페이지에서 직접 가입하는 회원은 회원 구분을 직접 고르지 않으므로,
    // 첫 번째 회원 구분(기본값 "개인회원")으로 자동 등록합니다. 나중에 관리자가 바꿔줄 수 있습니다.
    const memberTypes = await this.memberTypesService.list(library.id);
    const defaultMemberTypeId = memberTypes[0]?.id;

    const existing = await this.prisma.user.findFirst({
      where: { libraryId: library.id, loginId: data.loginId },
    });
```

바로 아래에 있는, 회원을 실제로 만드는 부분을 찾으세요.

```ts
    try {
      await this.prisma.user.create({
        data: {
          libraryId: library.id,
          loginId: data.loginId,
          passwordHash,
          name: data.name,
          phone: data.phone,
          email: data.email || undefined,
          birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
          address: data.address || undefined,
          memberNo: data.memberNo || undefined,
          cardToken,
        },
      });
    } catch (e: any) {
```

이렇게 바꿔주세요. (`memberTypeId: defaultMemberTypeId,` 한 줄이 추가됩니다.)

```ts
    try {
      await this.prisma.user.create({
        data: {
          libraryId: library.id,
          loginId: data.loginId,
          passwordHash,
          name: data.name,
          phone: data.phone,
          email: data.email || undefined,
          birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
          address: data.address || undefined,
          memberNo: data.memberNo || undefined,
          memberTypeId: defaultMemberTypeId,
          cardToken,
        },
      });
    } catch (e: any) {
```

### 6-4. `adminCreate()` (관리자가 직접 등록) — 회원 구분 필수로 받기

아래 부분을 찾으세요.

```ts
  async adminCreate(
    libraryId: number,
    data: {
      loginId: string;
      password: string;
      name: string;
      phone?: string;
      email?: string;
      memberNo?: string;
      birthDate?: string;
      address?: string;
      role?: string;
    },
  ) {
    if (!data.loginId || !data.password || !data.name) {
      throw new BadRequestException('아이디, 비밀번호, 이름은 필수입니다.');
    }

    // role은 MEMBER 또는 ADMIN만 허용. 그 외 값(SUPER 포함)은 막습니다.
    const role =
      data.role === 'ADMIN' ? Role.ADMIN : Role.MEMBER;

    const existing = await this.prisma.user.findFirst({
      where: { libraryId, loginId: data.loginId },
    });
    if (existing) {
      throw new ConflictException('이미 사용 중인 아이디입니다.');
    }

    const passwordHash = await bcrypt.hash(data.password, 10);
    const cardToken = randomUUID();

    try {
      await this.prisma.user.create({
        data: {
          libraryId,
          loginId: data.loginId,
          passwordHash,
          name: data.name,
          phone: data.phone || undefined,
          email: data.email || undefined,
          memberNo: data.memberNo || undefined,
          birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
          address: data.address || undefined,
          role,
          cardToken,
        },
      });
    } catch (e: any) {
      if (e.code === 'P2002') {
        throw new ConflictException('이미 사용 중인 회원번호입니다.');
      }
      throw e;
    }

    return { message: '등록되었습니다.' };
  }
```

이 부분을 통째로 아래 내용으로 바꿔주세요.

```ts
  async adminCreate(
    libraryId: number,
    data: {
      loginId: string;
      password: string;
      name: string;
      phone?: string;
      email?: string;
      memberNo?: string;
      birthDate?: string;
      address?: string;
      role?: string;
      memberTypeId?: number;
    },
  ) {
    if (!data.loginId || !data.password || !data.name) {
      throw new BadRequestException('아이디, 비밀번호, 이름은 필수입니다.');
    }

    // role은 MEMBER 또는 ADMIN만 허용. 그 외 값(SUPER 포함)은 막습니다.
    const role =
      data.role === 'ADMIN' ? Role.ADMIN : Role.MEMBER;

    // 역할이 "회원"이면 반드시 회원 구분을 하나 선택해야 합니다. "관리자"는 회원 구분이 필요 없습니다.
    let memberTypeId: number | null = null;
    if (role === Role.MEMBER) {
      if (!data.memberTypeId) {
        throw new BadRequestException('회원구분을 선택하세요.');
      }
      const memberType = await this.prisma.memberType.findFirst({
        where: { id: Number(data.memberTypeId), libraryId },
      });
      if (!memberType) {
        throw new BadRequestException('올바르지 않은 회원구분입니다.');
      }
      memberTypeId = memberType.id;
    }

    const existing = await this.prisma.user.findFirst({
      where: { libraryId, loginId: data.loginId },
    });
    if (existing) {
      throw new ConflictException('이미 사용 중인 아이디입니다.');
    }

    const passwordHash = await bcrypt.hash(data.password, 10);
    const cardToken = randomUUID();

    try {
      await this.prisma.user.create({
        data: {
          libraryId,
          loginId: data.loginId,
          passwordHash,
          name: data.name,
          phone: data.phone || undefined,
          email: data.email || undefined,
          memberNo: data.memberNo || undefined,
          birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
          address: data.address || undefined,
          role,
          memberTypeId,
          cardToken,
        },
      });
    } catch (e: any) {
      if (e.code === 'P2002') {
        throw new ConflictException('이미 사용 중인 회원번호입니다.');
      }
      throw e;
    }

    return { message: '등록되었습니다.' };
  }
```

### 6-5. `update()` (회원 정보 수정) — 회원 구분 반영

아래 부분을 찾으세요.

```ts
  async update(
    libraryId: number,
    id: number,
    data: {
      name?: string;
      phone?: string;
      email?: string;
      memberNo?: string;
      birthDate?: string;
      address?: string;
      status?: string;
      role?: string;
      password?: string;
    },
  ) {
    const existing = await this.prisma.user.findFirst({
      where: { id, libraryId, role: { in: [Role.MEMBER, Role.ADMIN] } },
    });
    if (!existing) {
      throw new NotFoundException('회원을 찾을 수 없습니다.');
    }

    const updateData: any = {
      name: data.name,
      phone: data.phone,
      email: data.email || undefined,
      memberNo: data.memberNo || undefined,
      birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
      address: data.address || undefined,
      status: data.status ? (data.status as UserStatus) : undefined,
    };

    // 역할은 MEMBER ↔ ADMIN 사이에서만 바꿀 수 있습니다. (SUPER로는 여기서 못 바꿉니다.)
    if (data.role === 'ADMIN' || data.role === 'MEMBER') {
      updateData.role = data.role as Role;
    }
```

이 부분을 아래 내용으로 바꿔주세요.

```ts
  async update(
    libraryId: number,
    id: number,
    data: {
      name?: string;
      phone?: string;
      email?: string;
      memberNo?: string;
      birthDate?: string;
      address?: string;
      status?: string;
      role?: string;
      password?: string;
      memberTypeId?: number | null;
    },
  ) {
    const existing = await this.prisma.user.findFirst({
      where: { id, libraryId, role: { in: [Role.MEMBER, Role.ADMIN] } },
    });
    if (!existing) {
      throw new NotFoundException('회원을 찾을 수 없습니다.');
    }

    const updateData: any = {
      name: data.name,
      phone: data.phone,
      email: data.email || undefined,
      memberNo: data.memberNo || undefined,
      birthDate: data.birthDate ? new Date(data.birthDate) : undefined,
      address: data.address || undefined,
      status: data.status ? (data.status as UserStatus) : undefined,
    };

    // 역할은 MEMBER ↔ ADMIN 사이에서만 바꿀 수 있습니다. (SUPER로는 여기서 못 바꿉니다.)
    if (data.role === 'ADMIN' || data.role === 'MEMBER') {
      updateData.role = data.role as Role;
    }

    // 최종적으로 "회원"이 되는 경우에는 회원 구분이 반드시 있어야 하고,
    // "관리자"가 되는 경우에는 회원 구분을 비웁니다.
    const finalRole = updateData.role || existing.role;
    if (finalRole === Role.MEMBER) {
      const requestedId =
        data.memberTypeId !== undefined ? Number(data.memberTypeId) : existing.memberTypeId;
      if (!requestedId) {
        throw new BadRequestException('회원구분을 선택하세요.');
      }
      const memberType = await this.prisma.memberType.findFirst({
        where: { id: requestedId, libraryId },
      });
      if (!memberType) {
        throw new BadRequestException('올바르지 않은 회원구분입니다.');
      }
      updateData.memberTypeId = memberType.id;
    } else {
      updateData.memberTypeId = null;
    }
```

### 6-6. `list()` (회원 목록 조회) — 회원 구분도 함께 내려주기

아래 부분을 찾으세요.

```ts
        select: {
          id: true,
          loginId: true,
          name: true,
          phone: true,
          email: true,
          memberNo: true,
          birthDate: true,
          address: true,
          role: true,
          status: true,
          createdAt: true,
          // passwordHash는 절대 내보내지 않습니다.
        },
```

이렇게 바꿔주세요.

```ts
        select: {
          id: true,
          loginId: true,
          name: true,
          phone: true,
          email: true,
          memberNo: true,
          birthDate: true,
          address: true,
          role: true,
          status: true,
          createdAt: true,
          memberTypeId: true,
          memberType: { select: { id: true, name: true } },
          // passwordHash는 절대 내보내지 않습니다.
        },
```

파일을 저장하세요.

---

## 7. 프런트엔드 새 파일 만들기: `member-types-settings-form.tsx`

`C:\projects\LibraryNearMe\frontend\src\components\` 폴더 안에 `member-types-settings-form.tsx`라는 새 파일을 만들고, 아래 내용을 붙여넣으세요.

```tsx
"use client";

import { useEffect, useState } from "react";
import ThemedButton from "@/components/themed-button";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";

type MemberType = { id: number; name: string; order: number };

export default function MemberTypesSettingsForm() {
  const { t } = useI18n();
  const { notify } = useNotify();

  const [items, setItems] = useState<MemberType[]>([]);
  const [showModal, setShowModal] = useState(false);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [nameText, setNameText] = useState("");

  async function loadItems() {
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/member-types`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      setItems(await res.json());
    } else {
      notify("❌ " + t("settings.memberTypes.loadFail"), "error");
    }
  }

  useEffect(() => {
    loadItems();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  function openAddModal() {
    setEditingId(null);
    setNameText("");
    setShowModal(true);
  }

  function openEditModal(item: MemberType) {
    setEditingId(item.id);
    setNameText(item.name);
    setShowModal(true);
  }

  function closeModal() {
    setShowModal(false);
    setEditingId(null);
  }

  async function handleSave() {
    const token = localStorage.getItem("token");
    if (!token) return;
    const name = nameText.trim();
    if (!name) {
      notify("❌ " + t("settings.memberTypes.nameRequired"), "error");
      return;
    }
    const url = editingId ? `${API_URL}/member-types/${editingId}` : `${API_URL}/member-types`;
    const res = await fetch(url, {
      method: editingId ? "PATCH" : "POST",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify({ name }),
    });
    if (res.ok) {
      notify("✅ " + t("settings.memberTypes.saveSuccess"), "success");
      closeModal();
      await loadItems();
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("settings.memberTypes.saveFail")), "error");
    }
  }

  async function handleDelete() {
    if (!editingId) return;
    if (!window.confirm(t("settings.memberTypes.deleteConfirm"))) return;
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/member-types/${editingId}`, {
      method: "DELETE",
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      notify("✅ " + t("settings.memberTypes.deleteSuccess"), "success");
      closeModal();
      await loadItems();
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("settings.memberTypes.deleteFail")), "error");
    }
  }

  return (
    <div className="mt-8">
      <p className="mb-2 text-sm font-semibold">{t("settings.memberTypes.title")}</p>
      <div className="flex flex-wrap gap-2">
        {items.map((item) => (
          <button
            key={item.id}
            type="button"
            onClick={() => openEditModal(item)}
            className="cursor-pointer rounded-full border border-neutral-200 bg-white px-3 py-1.5 text-xs font-medium text-neutral-700 shadow-sm hover:bg-neutral-50"
          >
            {item.name}
          </button>
        ))}
        <button
          type="button"
          onClick={openAddModal}
          className="cursor-pointer rounded-full border border-dashed border-neutral-300 bg-white px-3 py-1.5 text-xs font-medium text-neutral-500 hover:bg-neutral-50"
        >
          {t("settings.memberTypes.addBtn")}
        </button>
      </div>

      {showModal && (
        <div
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"
          onClick={closeModal}
        >
          <div className="w-full max-w-sm overflow-hidden rounded-xl bg-white shadow-xl" onClick={(e) => e.stopPropagation()}>
            <div className="p-6">
              <p className="mb-4 text-sm font-semibold">
                {editingId ? t("settings.memberTypes.modal.editTitle") : t("settings.memberTypes.modal.addTitle")}
              </p>
              <label className="block">
                <span className="mb-1 block text-sm text-neutral-500">{t("settings.memberTypes.field.name")} *</span>
                <input
                  value={nameText}
                  onChange={(e) => setNameText(e.target.value)}
                  className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                />
              </label>

              <ThemedButton preset="버튼1" onClick={handleSave} className="mt-5 w-full">
                {t("settings.memberTypes.save")}
              </ThemedButton>

              {editingId && (
                <button
                  onClick={handleDelete}
                  className="mt-2 w-full cursor-pointer rounded-lg bg-red-600 py-2.5 text-sm font-semibold text-white hover:bg-red-700"
                >
                  {t("settings.memberTypes.deleteBtn")}
                </button>
              )}
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## 8. '설정' → '도서관' 화면에 회원 구분 넣기

`C:\projects\LibraryNearMe\frontend\src\app\admin\settings\page.tsx` 파일을 여세요.

**8-1.** 아래 부분을 찾으세요.

```tsx
import MaterialTypesSettingsForm from "@/components/material-types-settings-form";
```

이렇게 바꿔주세요.

```tsx
import MaterialTypesSettingsForm from "@/components/material-types-settings-form";
import MemberTypesSettingsForm from "@/components/member-types-settings-form";
```

**8-2.** 아래 부분을 찾으세요.

```tsx
        <TabsContent value="library" className="mt-4">
          <LibrarySettingsForm />
        </TabsContent>
```

이렇게 바꿔주세요.

```tsx
        <TabsContent value="library" className="mt-4">
          <LibrarySettingsForm />
          <MemberTypesSettingsForm />
        </TabsContent>
```

파일을 저장하세요.

---

## 9. '회원 관리' 화면에 회원 구분 반영하기

`C:\projects\LibraryNearMe\frontend\src\app\admin\members\page.tsx` 파일을 여세요.

### 9-1. 타입 정의에 회원 구분 추가

아래 부분을 찾으세요.

```tsx
type MemberRow = {
  id: number;
  loginId: string | null;
  name: string;
  phone: string | null;
  email: string | null;
  memberNo: string | null;
  birthDate: string | null;
  address: string | null;
  role: string;
  status: string;
  createdAt: string;
};
```

이렇게 바꿔주세요.

```tsx
type MemberRow = {
  id: number;
  loginId: string | null;
  name: string;
  phone: string | null;
  email: string | null;
  memberNo: string | null;
  birthDate: string | null;
  address: string | null;
  role: string;
  status: string;
  createdAt: string;
  memberTypeId: number | null;
  memberType: { id: number; name: string } | null;
};

type MemberType = { id: number; name: string };
```

### 9-2. 표에 보일 칸 개수 수정

아래 부분을 찾으세요.

```tsx
const COLUMN_COUNT = 10;
```

이렇게 바꿔주세요. (회원구분 칸이 하나 늘어나서 11개가 됩니다.)

```tsx
const COLUMN_COUNT = 11;
```

### 9-3. 등록/수정 폼의 기본값에 회원 구분 추가

아래 부분을 찾으세요.

```tsx
const EMPTY_FORM = {
  loginId: "",
  password: "",
  name: "",
  phone: "",
  email: "",
  memberNo: "",
  birthYear: "",
  birthMonth: "",
  birthDay: "",
  addressMain: "",
  addressDetail: "",
  role: "MEMBER",
  status: "ACTIVE",
};
```

이렇게 바꿔주세요.

```tsx
const EMPTY_FORM = {
  loginId: "",
  password: "",
  name: "",
  phone: "",
  email: "",
  memberNo: "",
  birthYear: "",
  birthMonth: "",
  birthDay: "",
  addressMain: "",
  addressDetail: "",
  role: "MEMBER",
  status: "ACTIVE",
  memberTypeId: "",
};
```

### 9-4. 회원 구분 목록을 불러오는 코드 추가

아래 부분을 찾으세요.

```tsx
  const [showForm, setShowForm] = useState(false);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [form, setForm] = useState(EMPTY_FORM);
```

이렇게 바꿔주세요.

```tsx
  const [showForm, setShowForm] = useState(false);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [form, setForm] = useState(EMPTY_FORM);

  const [memberTypes, setMemberTypes] = useState<MemberType[]>([]);

  async function loadMemberTypes() {
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/member-types`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      setMemberTypes(await res.json());
    }
  }

  useEffect(() => {
    loadMemberTypes();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);
```

> 이 파일 위쪽에 이미 `import { useState } from "react";`만 있고 `useEffect`는 안 쓰이고 있을 수 있습니다. 파일 맨 위의 import 줄을 확인해서, 아래처럼 `useEffect`가 함께 들어있는지 확인해주세요.
>
> ```tsx
> import { useState, useEffect } from "react";
> ```
>
> 만약 `import { useState } from "react";`로만 되어 있다면 위 내용으로 바꿔주세요.

### 9-5. 회원 등록 모달을 열 때 회원 구분 기본값 지정

아래 부분을 찾으세요.

```tsx
  async function openAddModal() {
    setEditingId(null);
    setForm(EMPTY_FORM);
    setShowForm(true);
```

이렇게 바꿔주세요.

```tsx
  async function openAddModal() {
    setEditingId(null);
    setForm({ ...EMPTY_FORM, memberTypeId: memberTypes[0] ? String(memberTypes[0].id) : "" });
    setShowForm(true);
```

### 9-6. 회원 수정 모달을 열 때 그 회원의 회원 구분을 채워넣기

아래 부분을 찾으세요.

```tsx
  function openEditModal(row: MemberRow) {
    setEditingId(row.id);
    const [by, bm, bd] = row.birthDate ? row.birthDate.slice(0, 10).split("-") : ["", "", ""];
    setForm({
      loginId: row.loginId || "",
      password: "",
      name: row.name,
      phone: row.phone || "",
      email: row.email || "",
      memberNo: row.memberNo || "",
      birthYear: by,
      birthMonth: bm,
      birthDay: bd,
      addressMain: row.address || "",
      addressDetail: "",
      role: row.role,
      status: row.status,
    });
    setShowForm(true);
  }
```

이렇게 바꿔주세요.

```tsx
  function openEditModal(row: MemberRow) {
    setEditingId(row.id);
    const [by, bm, bd] = row.birthDate ? row.birthDate.slice(0, 10).split("-") : ["", "", ""];
    setForm({
      loginId: row.loginId || "",
      password: "",
      name: row.name,
      phone: row.phone || "",
      email: row.email || "",
      memberNo: row.memberNo || "",
      birthYear: by,
      birthMonth: bm,
      birthDay: bd,
      addressMain: row.address || "",
      addressDetail: "",
      role: row.role,
      status: row.status,
      memberTypeId: row.memberTypeId
        ? String(row.memberTypeId)
        : memberTypes[0]
        ? String(memberTypes[0].id)
        : "",
    });
    setShowForm(true);
  }
```

### 9-7. 저장할 때 회원 구분 값 검사 및 전송

아래 부분을 찾으세요.

```tsx
    if (!isValidEmail(form.email)) {
      notify("❌ " + t("members.form.invalidEmail"), "error");
      return;
    }
```

이렇게 바꿔주세요.

```tsx
    if (!isValidEmail(form.email)) {
      notify("❌ " + t("members.form.invalidEmail"), "error");
      return;
    }

    if (form.role === "MEMBER" && !form.memberTypeId) {
      notify("❌ " + t("members.form.memberTypeRequired"), "error");
      return;
    }
```

바로 아래에 있는, 저장할 내용을 만드는 부분을 찾으세요.

```tsx
    const body = editingId
      ? {
          name: form.name,
          phone: form.phone,
          email: form.email,
          memberNo: form.memberNo,
          birthDate: birthDateValue,
          address,
          status: form.status,
          role: form.role,
          password: form.password || undefined,
        }
      : {
          loginId: form.loginId,
          password: form.password,
          name: form.name,
          phone: form.phone,
          email: form.email,
          memberNo: form.memberNo,
          birthDate: birthDateValue,
          address,
          role: form.role,
        };
```

이렇게 바꿔주세요. (`memberTypeId` 한 줄씩 추가됩니다.)

```tsx
    const memberTypeIdValue = form.role === "MEMBER" ? parseInt(form.memberTypeId, 10) : null;

    const body = editingId
      ? {
          name: form.name,
          phone: form.phone,
          email: form.email,
          memberNo: form.memberNo,
          birthDate: birthDateValue,
          address,
          status: form.status,
          role: form.role,
          password: form.password || undefined,
          memberTypeId: memberTypeIdValue,
        }
      : {
          loginId: form.loginId,
          password: form.password,
          name: form.name,
          phone: form.phone,
          email: form.email,
          memberNo: form.memberNo,
          birthDate: birthDateValue,
          address,
          role: form.role,
          memberTypeId: memberTypeIdValue,
        };
```

### 9-8. 표에 "회원구분" 칸 추가

아래 부분을 찾으세요.

```tsx
              <th className="px-4 py-2.5">{t("members.list.col.role")}</th>
              <th className="px-4 py-2.5">{t("members.list.col.status")}</th>
```

이렇게 바꿔주세요.

```tsx
              <th className="px-4 py-2.5">{t("members.list.col.role")}</th>
              <th className="px-4 py-2.5">{t("members.list.col.memberType")}</th>
              <th className="px-4 py-2.5">{t("members.list.col.status")}</th>
```

바로 아래, 표 내용(각 행) 부분을 찾으세요.

```tsx
                <td className="whitespace-nowrap px-4 py-2.5">{t(`members.role.${row.role}`)}</td>
                <td className="whitespace-nowrap px-4 py-2.5">
                  {t(`members.status.${row.status}`)}
                </td>
```

이렇게 바꿔주세요.

```tsx
                <td className="whitespace-nowrap px-4 py-2.5">{t(`members.role.${row.role}`)}</td>
                <td className="whitespace-nowrap px-4 py-2.5">{row.memberType?.name || "-"}</td>
                <td className="whitespace-nowrap px-4 py-2.5">
                  {t(`members.status.${row.status}`)}
                </td>
```

### 9-9. "검색 결과가 없습니다" 줄의 칸 개수 수정

아래 부분을 찾으세요.

```tsx
              <tr>
                <td colSpan={10} className="px-4 py-6 text-center text-neutral-400">
                  {t("members.list.noResults")}
                </td>
              </tr>
```

이렇게 바꿔주세요.

```tsx
              <tr>
                <td colSpan={11} className="px-4 py-6 text-center text-neutral-400">
                  {t("members.list.noResults")}
                </td>
              </tr>
```

### 9-10. 등록/수정 모달에 "회원구분" 선택 항목 추가

이 파일 안에는 "역할" 선택 드롭다운이 **똑같은 모양으로 두 군데** 있습니다. (하나는 신규 등록 시, 하나는 수정 시) 아래 내용을 찾으세요. (파일 안에 정확히 같은 내용이 두 번 나옵니다.)

```tsx
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.role")}</span>
                    <select
                      value={form.role}
                      onChange={(e) => setForm({ ...form, role: e.target.value })}
                      className="w-full cursor-pointer rounded-lg border px-3 py-2 text-sm"
                    >
                      <option value="MEMBER">{t("members.role.MEMBER")}</option>
                      <option value="ADMIN">{t("members.role.ADMIN")}</option>
                    </select>
                  </label>
```

이 내용을 **두 군데 모두** 아래 내용으로 바꿔주세요. (에디터에서 "모두 바꾸기(Replace All)" 기능을 쓰면 편합니다.)

```tsx
                  <label className="block">
                    <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.role")}</span>
                    <select
                      value={form.role}
                      onChange={(e) => setForm({ ...form, role: e.target.value })}
                      className="w-full cursor-pointer rounded-lg border px-3 py-2 text-sm"
                    >
                      <option value="MEMBER">{t("members.role.MEMBER")}</option>
                      <option value="ADMIN">{t("members.role.ADMIN")}</option>
                    </select>
                  </label>

                  {form.role === "MEMBER" && (
                    <label className="block">
                      <span className="mb-1 block text-sm text-neutral-500">{t("members.form.field.memberType")} *</span>
                      <select
                        value={form.memberTypeId}
                        onChange={(e) => setForm({ ...form, memberTypeId: e.target.value })}
                        className="w-full cursor-pointer rounded-lg border px-3 py-2 text-sm"
                      >
                        {memberTypes.map((mt) => (
                          <option key={mt.id} value={mt.id}>
                            {mt.name}
                          </option>
                        ))}
                      </select>
                    </label>
                  )}
```

> 역할을 "관리자"로 바꾸면 회원구분 선택 항목이 자동으로 화면에서 사라지고, 저장할 때도 `memberTypeId`가 `null`로 전송됩니다.

파일을 저장하세요.

---

## 10. 화면에 나올 문구 추가하기 (`dictionary.ts`)

`C:\projects\LibraryNearMe\frontend\src\lib\dictionary.ts` 파일을 여세요.

이 파일 안에는 한국어(ko) 문구 목록과 영어(en) 문구 목록, 두 군데가 있습니다. **두 곳 모두** 아래 순서대로 찾아서 수정해주세요.

### 10-1. 회원 구분 설정 화면 문구 (한국어)

아래 부분을 찾으세요.

```ts
    "settings.copyOptions.noFloorOptions": "먼저 '층' 목록에 값을 하나 이상 추가해주세요.",
```

이렇게 바꿔주세요. (아래에 새 줄들이 추가됩니다.)

```ts
    "settings.copyOptions.noFloorOptions": "먼저 '층' 목록에 값을 하나 이상 추가해주세요.",

    "settings.memberTypes.title": "회원 구분",
    "settings.memberTypes.addBtn": "+ 값 추가",
    "settings.memberTypes.modal.addTitle": "회원 구분 추가",
    "settings.memberTypes.modal.editTitle": "회원 구분 수정",
    "settings.memberTypes.field.name": "이름",
    "settings.memberTypes.save": "저장",
    "settings.memberTypes.deleteBtn": "삭제",
    "settings.memberTypes.deleteConfirm": "정말 삭제하시겠어요?",
    "settings.memberTypes.nameRequired": "이름을 입력하세요.",
    "settings.memberTypes.saveSuccess": "저장되었습니다.",
    "settings.memberTypes.saveFail": "저장에 실패했습니다.",
    "settings.memberTypes.deleteSuccess": "삭제되었습니다.",
    "settings.memberTypes.deleteFail": "삭제에 실패했습니다.",
    "settings.memberTypes.loadFail": "목록을 불러오지 못했습니다.",
```

### 10-2. 회원 구분 설정 화면 문구 (영어)

아래 부분을 찾으세요.

```ts
    "settings.copyOptions.noFloorOptions": "Please add at least one 'Floor' value first.",
```

이렇게 바꿔주세요.

```ts
    "settings.copyOptions.noFloorOptions": "Please add at least one 'Floor' value first.",

    "settings.memberTypes.title": "Member Types",
    "settings.memberTypes.addBtn": "+ Add Value",
    "settings.memberTypes.modal.addTitle": "Add Member Type",
    "settings.memberTypes.modal.editTitle": "Edit Member Type",
    "settings.memberTypes.field.name": "Name",
    "settings.memberTypes.save": "Save",
    "settings.memberTypes.deleteBtn": "Delete",
    "settings.memberTypes.deleteConfirm": "Delete this value?",
    "settings.memberTypes.nameRequired": "Please enter a name.",
    "settings.memberTypes.saveSuccess": "Saved.",
    "settings.memberTypes.saveFail": "Save failed.",
    "settings.memberTypes.deleteSuccess": "Deleted.",
    "settings.memberTypes.deleteFail": "Delete failed.",
    "settings.memberTypes.loadFail": "Failed to load list.",
```

### 10-3. 회원 관리 화면 문구 (한국어)

아래 부분을 찾으세요.

```ts
    "members.form.invalidBirthDate": "생년월일을 올바르게 입력해주세요.",
```

이렇게 바꿔주세요.

```ts
    "members.form.invalidBirthDate": "생년월일을 올바르게 입력해주세요.",
    "members.list.col.memberType": "회원구분",
    "members.form.field.memberType": "회원구분",
    "members.form.memberTypeRequired": "회원구분을 선택하세요.",
```

### 10-4. 회원 관리 화면 문구 (영어)

`members.form.invalidBirthDate`의 영어 버전을 찾으세요. (한국어 블록과 똑같이 생겼지만 영어 문장으로 되어 있습니다. 예: `"Please enter a valid birth date."` 같은 값입니다.) 그 줄 바로 아래에 아래 내용을 추가해주세요.

```ts
    "members.list.col.memberType": "Member Type",
    "members.form.field.memberType": "Member Type",
    "members.form.memberTypeRequired": "Please select a member type.",
```

파일을 저장하세요.

---

## 확인하기

1. 백엔드 서버(`npm run start:dev`)와 프런트엔드 서버(`npm run dev`)를 재시작하세요.
2. 관리자로 로그인 후 '설정' → '도서관' 화면으로 이동하세요. 'LibrarySettingsForm' 아래에 "회원 구분" 목록이 나오고, "개인회원", "단체회원", "자료지원회원" 3개가 자동으로 만들어져 있는지 확인하세요.
3. 회원 구분을 하나 추가해보고, 이름을 수정해보고, 마지막으로 사용하지 않는(아무도 안 쓰는) 회원 구분을 삭제해보세요. 정상적으로 되는지 확인하세요.
4. 사이드바의 '회원' 메뉴로 이동해서 '회원 등록' 버튼을 눌러보세요. 역할이 "회원"으로 되어 있을 때 "회원구분" 선택 항목이 보이는지, 역할을 "관리자"로 바꾸면 그 항목이 사라지는지 확인하세요.
5. 회원구분을 선택하지 않고 저장을 시도하면(역할이 "회원"인 상태에서) 저장이 안 되고 안내 문구가 뜨는지 확인하세요. 회원구분을 선택한 뒤 저장하면 정상적으로 등록되고, 목록의 "회원구분" 칸에 값이 나오는지 확인하세요.
6. 기존에 등록되어 있던 회원(이번 작업 전부터 있던 회원)을 하나 열어서 '수정' 버튼을 눌러보세요. 회원구분이 비어있다면 자동으로 첫 번째 값이 선택된 채로 나오는지 확인하고, 저장이 잘 되는지 확인하세요.
7. 회원 여러 명이 쓰고 있는 회원구분을 '설정' 화면에서 삭제해보세요. "이 구분을 사용 중인 회원이 있어 삭제할 수 없습니다" 같은 안내와 함께 삭제가 막히는지 확인하세요.
8. (선택) 홈페이지 회원가입 화면에서 새 계정을 하나 만들어보고, '회원 관리' 화면에서 그 계정을 열어 회원구분이 "개인회원"으로 자동 지정되어 있는지 확인하세요.

---

## GitHub 커밋

모든 확인이 끝났다면 아래 명령어로 변경 내용을 저장하세요.

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "23단계-1: 회원 구분(개인/단체/자료지원) 관리 기능 추가"
git push
```

---

## 최종 점검표

- [ ] `schema.prisma`에 `MemberType` 모델을 추가하고, `Library`/`User`에 관계를 연결했다.
- [ ] `npx prisma migrate dev --name add_member_type`을 성공적으로 실행했다.
- [ ] `backend/src/settings/member-types.service.ts`, `member-types.controller.ts` 파일을 새로 만들었다.
- [ ] `app.module.ts`에 `MemberTypesController`/`MemberTypesService`를 등록했다.
- [ ] `users.service.ts`의 `signup()`/`adminCreate()`/`update()`/`list()`를 모두 수정했다.
- [ ] `frontend/src/components/member-types-settings-form.tsx` 파일을 새로 만들었다.
- [ ] '설정' → '도서관' 화면에 회원 구분 관리 UI가 나온다.
- [ ] '회원 관리' 화면에서 등록/수정 시 역할이 "회원"이면 회원구분을 반드시 선택해야 한다.
- [ ] 회원 목록 표에 "회원구분" 칸이 나온다.
- [ ] 사용 중인 회원구분은 삭제가 안 되고, 사용 중인 회원 이름과 인원수가 안내 메시지로 나온다.
- [ ] 변경 내용을 GitHub에 커밋 및 푸시했다.

---

이 작업이 끝나고 확인까지 마치시면, 다음 가이드(23단계-2)에서 '설정' 안에 '대출' 메뉴를 새로 만들어서 실물/디지털 자료를 구분해 보여주고, 회원구분별 기본 대출 설정(최대 대출 권수, 최대 예약 권수, 최대 대출 제한 일수, 예약자료 보관일)과 자료 종류별 대출 설정(대출 가능 권수, 대출 기간, 예약 가능 권수)을 만들겠습니다. 그리고 그때 '자료 종류' 화면에서는 대출 관련 입력 항목(대출 가능 권수, 대출 일수)을 빼서, 자료의 추가·수정·삭제 기능만 남기도록 정리하겠습니다.

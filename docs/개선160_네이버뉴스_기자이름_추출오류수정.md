# 개선160. 네이버 뉴스에서 기자 이름을 다시 못 가져오던 문제 수정

## 목표

개선159에서 매체·날짜는 고쳤는데, 그 대신 이번엔 기자 이름을 못 가져오게 된 문제를 고칩니다.

## 원인

개선159에서 매체·날짜 찾는 방식을 "정확히 똑같은 이름"에서 "이 이름이 포함되어 있으면 OK"로 넉넉하게 바꿨는데, 기자 이름을 찾는 부분은 그때 건드리지 않고 그대로 뒀습니다. 그런데 다시 보니 이 부분에도 비슷한 함정이 있었습니다.

네이버 뉴스 페이지에는 기자 이름과 비슷한 class 이름을 가진 요소가 **한 군데가 아니라 여러 군데** 있을 수 있습니다. (예: 기사 맨 위 작성자 표시 + 기사 맨 아래 "이 기자의 다른 기사" 추천 영역 등) 지금 코드는 페이지에서 **가장 먼저 나오는** 위치만 찾아서 쓰는데, 만약 그 첫 번째 위치가 실제로는 이름 글자 없이 다른 요소(아이콘이나 다음 태그)로 바로 이어지는 자리라면, 거기서 이름을 못 찾고 그냥 실패해버립니다. 두 번째, 세 번째 자리에 진짜 이름이 있어도 거기까지 찾아보지 않는 게 문제였습니다.

## 수정 방법

`backend/src/scrap/scrap.service.ts` 파일을 여세요.

아래 코드를 찾으세요.

```typescript
    // 기자명: 'media_end_head_journalist_name'이 포함된 태그 바로 뒤에 나오는 글자를 꺼냅니다.
    const reporterMatch = html.match(/media_end_head_journalist_name[^"']*["'][^>]*>\s*([^<]+)</i);
    if (reporterMatch) reporter = this.decodeHtmlEntities(reporterMatch[1]);
```

아래로 바꿔주세요.

```typescript
    // 기자명: 'media_end_head_journalist_name'이 포함된 태그가 페이지에 여러 군데 있을 수 있어서,
    // 첫 번째 자리만 보지 않고 하나씩 확인하면서 실제로 이름 글자가 바로 뒤에 있는 첫 번째 자리를 씁니다.
    // (어떤 자리는 이름 없이 바로 다른 태그로 이어지기도 해서, 그런 자리는 건너뜁니다.)
    const reporter2 = this.findTextAfterClassToken(html, 'media_end_head_journalist_name');
    if (reporter2) reporter = reporter2;
```

이제 바로 위, `findTagByClassToken` 함수 **바로 아래**에 새 함수를 하나 추가해야 합니다. 아래 코드를 찾으세요.

```typescript
  private findTagByClassToken(html: string, classToken: string): string | null {
    const re = new RegExp(`<[a-zA-Z0-9]+[^>]*class=["'][^"']*\\b${classToken}\\b[^"']*["'][^>]*>`, 'i');
    const m = html.match(re);
    return m ? m[0] : null;
  }
```

이 코드 **바로 아래**에 아래 함수를 추가하세요.

```typescript

  // class 속성 안에 이 이름이 포함된 태그를 페이지에서 하나씩 찾아보면서, 태그 바로 뒤에
  // 실제 글자(공백이 아닌 내용)가 있는 첫 번째 자리를 찾아서 그 글자를 돌려줍니다.
  // (같은 class 이름이 여러 군데 있고, 그중 일부는 글자 없이 다른 태그로 바로 이어지는
  // 경우가 있어서, 그런 자리는 건너뛰고 진짜 글자가 있는 자리를 찾습니다.)
  private findTextAfterClassToken(html: string, classToken: string): string | null {
    const re = new RegExp(
      `<[a-zA-Z0-9]+[^>]*class=["'][^"']*\\b${classToken}\\b[^"']*["'][^>]*>\\s*([^<\\s][^<]*)`,
      'gi',
    );
    let m: RegExpExecArray | null;
    while ((m = re.exec(html)) !== null) {
      const text = m[1].trim();
      if (text) return this.decodeHtmlEntities(text);
    }
    return null;
  }
```

---

## 확인하기

1. 백엔드 서버를 다시 켜주세요.
2. 여러 개의 네이버 뉴스 기사 주소로 '가져오기'를 눌러서, 제목·매체·기자·날짜 4개가 모두 채워지는지 확인합니다.
3. 혹시 특정 기사에서 기자 이름이 여전히 비어있다면, 그 기사에는 애초에 기자 이름 표시 자체가 없는 기사(예: 연합뉴스 같은 통신사 단신 기사)일 수 있습니다. 그런 경우는 직접 입력해 주시면 됩니다.
4. 매체·날짜는 이전처럼 계속 잘 가져와지는지 확인합니다.

---

## GitHub 커밋

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선160 가이드: 네이버 뉴스 기자 이름 추출 오류 수정"
git push
```

---

## 최종 점검표

- [ ] 네이버 뉴스 기사에서 제목·매체·기자·날짜가 모두 정상적으로 채워진다.
- [ ] 여러 기사로 테스트해도 안정적으로 작동한다.
- [ ] 변경 사항을 GitHub에 커밋하고 push했다.

# 개선166. 설정 > 알림 메뉴 신설 + 게시판별 알림 사용 여부 + 처리상태별 문구 템플릿

## 목표

이전에 합의했던 '알림' 기능 5단계 중 3번째 단계입니다. 이번 가이드에서는 실제로 이메일을 보내는 기능은 아직 만들지 않고, 그 준비 단계로 아래 2가지를 만듭니다.

1. **설정 > 게시판**: 게시판 목록에서 '자료를 신청합니다', '참고서비스', '1:1 상담' 게시판에 한해 "알림 기능 사용" 여부를 켜고 끌 수 있게 합니다. (꺼두면 다음 단계에서 만들 '알림 보내기' 체크박스 자체가 화면에 나오지 않습니다.)
2. **설정 > 알림** (새 메뉴): '자료를 신청합니다' 게시판의 처리상태 3가지(구입 진행 중 / 구입 완료 / 미구입)마다, 신청자에게 보여줄 안내 문구를 관리자가 직접 수정할 수 있게 합니다. 문구 안에 `{title}`이라고 적으면, 실제로 보여질 때 신청한 자료의 제목으로 자동으로 바뀝니다.

이번 가이드에서는 실제 이메일 발송이나 체크박스는 다루지 않습니다. (다음 개선167, 개선168에서 다룰 예정입니다.) 지금은 어디까지나 "관리자가 문구를 미리 준비해두는 설정 화면"만 만듭니다.

바꾸는 파일은 6개, 새로 만드는 파일은 2개입니다.

- `backend/prisma/schema.prisma` (DB에 칸 추가)
- `backend/src/settings/notification-templates.controller.ts` (새 파일)
- `backend/src/settings/notification-templates.service.ts` (새 파일)
- `backend/src/settings/boards.service.ts` (알림 사용 여부 저장)
- `backend/src/app.module.ts` (새 기능 등록)
- `frontend/src/components/boards-settings-form.tsx` (게시판별 알림 사용 여부 켜고 끄기)
- `frontend/src/components/notification-settings-form.tsx` (새 파일: 문구 템플릿 화면)
- `frontend/src/app/admin/settings/page.tsx` ('알림' 탭 추가)
- `frontend/src/lib/dictionary.ts` (화면에 보이는 문구들)

---

## 1단계. DB에 칸 추가

`backend/prisma/schema.prisma` 파일을 여세요.

### 1-1. 게시판에 '알림 사용 여부' 칸 추가

아래 코드를 찾으세요.

```
  isMaterialRequest Boolean  @default(false)  // "자료를 신청합니다" 게시판인지 (추가 입력 필드 + 처리 상태 기능이 붙습니다)

  createdAt         DateTime @default(now())

  posts             Post[]

  @@unique([libraryId, code])
}
```

아래로 바꿔주세요.

```
  isMaterialRequest Boolean  @default(false)  // "자료를 신청합니다" 게시판인지 (추가 입력 필드 + 처리 상태 기능이 붙습니다)
  notifyEnabled     Boolean  @default(false)  // ← 추가: 이 게시판에서 '이메일로 알림 받기' 기능을 쓸지 여부

  createdAt         DateTime @default(now())

  posts             Post[]

  @@unique([libraryId, code])
}
```

### 1-2. `Library` 모델에 새 표와의 연결 칸 추가

Prisma는 새 표를 만들 때 `Library` 모델에도 "이 도서관에 이런 목록이 딸려 있다"는 연결 칸을 함께 적어줘야 합니다. (다른 설정 표들도 전부 이렇게 되어 있습니다.)

아래 코드를 찾으세요.

```
  materialRequestTypes MaterialRequestType[] // ← 추가 (자료신청 자료 종류 목록)
  boardFonts    BoardFont[]    // ← 추가 (게시판 글꼴 목록)
  postReferences PostReference[] // ← 추가 (게시판 글의 참고자료 목록)
}
```

아래로 바꿔주세요.

```
  materialRequestTypes MaterialRequestType[] // ← 추가 (자료신청 자료 종류 목록)
  boardFonts    BoardFont[]    // ← 추가 (게시판 글꼴 목록)
  postReferences PostReference[] // ← 추가 (게시판 글의 참고자료 목록)
  notificationTemplates NotificationTemplate[] // ← 추가 (알림 문구 템플릿 목록)
}
```

### 1-3. 문구 템플릿을 저장할 새 표 추가

아래 코드를 찾으세요.

```
// "자료를 신청합니다" 게시판 글쓰기에서 고를 수 있는 '자료 종류' 드롭다운 항목들입니다.
// 설정 > 자료 메뉴에서 관리자가 추가·수정·삭제할 수 있습니다.
model MaterialRequestType {
```

아래로 바꿔주세요. (바로 위에 새 표를 하나 추가합니다)

```
// "자료를 신청합니다" 게시판의 처리상태(구입 진행 중/구입 완료/미구입)가 바뀌었을 때,
// 신청자에게 보여줄 안내 문구입니다. 설정 > 알림 메뉴에서 관리자가 수정할 수 있습니다.
// (아직 실제로 이메일을 보내지는 않습니다. 문구를 미리 준비해두는 단계입니다.)
model NotificationTemplate {
  id        Int      @id @default(autoincrement())

  libraryId Int
  library   Library  @relation(fields: [libraryId], references: [id])

  status    String   // PURCHASING(구입 진행 중) | PURCHASED(구입 완료) | NOT_PURCHASED(미구입)
  message   String   @default("") // 안내 문구. "{title}" 부분은 실제로 보여질 때 자료 제목으로 바뀝니다.

  createdAt DateTime @default(now())

  @@unique([libraryId, status])
}

// "자료를 신청합니다" 게시판 글쓰기에서 고를 수 있는 '자료 종류' 드롭다운 항목들입니다.
// 설정 > 자료 메뉴에서 관리자가 추가·수정·삭제할 수 있습니다.
model MaterialRequestType {
```

### 1-4. 마이그레이션 실행

터미널에서 아래 명령을 실행하세요.

```
cd C:\projects\LibraryNearMe\backend
npx prisma migrate dev --name add_notification_template_and_board_notify
```

---

## 2단계. 백엔드 - 문구 템플릿 API 만들기

### 2-1. `backend/src/settings/notification-templates.service.ts` 새 파일 만들기

`backend/src/settings/` 폴더 안에 `notification-templates.service.ts` 파일을 새로 만들고, 아래 내용을 그대로 넣으세요.

```typescript
import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma.service';

// 이 도서관에 문구 템플릿이 하나도 없을 때 자동으로 채워지는 기본 문구입니다.
// "{title}" 부분은 화면에 보여줄 때 실제 자료 제목으로 바뀝니다.
const DEFAULT_TEMPLATES: { status: string; message: string }[] = [
  { status: 'PURCHASING', message: "신청하신 '{title}' 자료가 구입 진행 중입니다." },
  { status: 'PURCHASED', message: "신청하신 '{title}' 자료 구입이 완료되었습니다." },
  { status: 'NOT_PURCHASED', message: "신청하신 '{title}' 자료는 구입하지 않기로 결정되었습니다." },
];

@Injectable()
export class NotificationTemplatesService {
  constructor(private prisma: PrismaService) {}

  // 목록 조회. 3가지 상태 중 아직 만들어지지 않은 것이 있으면 기본 문구로 자동으로 채워 넣습니다.
  async list(libraryId: number) {
    const existing = await this.prisma.notificationTemplate.findMany({ where: { libraryId } });
    const existingStatuses = existing.map((t) => t.status);
    const missing = DEFAULT_TEMPLATES.filter((t) => !existingStatuses.includes(t.status));

    if (missing.length > 0) {
      await this.prisma.notificationTemplate.createMany({
        data: missing.map((t) => ({ libraryId, status: t.status, message: t.message })),
      });
    }

    const all = await this.prisma.notificationTemplate.findMany({ where: { libraryId } });
    // 화면에 항상 같은 순서(구입 진행 중 → 구입 완료 → 미구입)로 보이도록 정렬합니다.
    const order = ['PURCHASING', 'PURCHASED', 'NOT_PURCHASED'];
    return all.sort((a, b) => order.indexOf(a.status) - order.indexOf(b.status));
  }

  async update(libraryId: number, id: number, data: any) {
    const existing = await this.prisma.notificationTemplate.findFirst({ where: { id, libraryId } });
    if (!existing) {
      throw new NotFoundException('문구 템플릿을 찾을 수 없습니다.');
    }
    const message = String(data.message ?? '').trim();
    if (!message) {
      throw new BadRequestException('문구를 입력하세요.');
    }
    return this.prisma.notificationTemplate.update({ where: { id }, data: { message } });
  }
}
```

### 2-2. `backend/src/settings/notification-templates.controller.ts` 새 파일 만들기

같은 폴더 안에 `notification-templates.controller.ts` 파일을 새로 만들고, 아래 내용을 그대로 넣으세요.

```typescript
import { Controller, Get, Patch, Body, Param, Req, UseGuards } from '@nestjs/common';
import { NotificationTemplatesService } from './notification-templates.service';
import { AdminGuard } from '../auth/admin.guard';

@Controller('notification-templates')
export class NotificationTemplatesController {
  constructor(private notificationTemplatesService: NotificationTemplatesService) {}

  @Get()
  @UseGuards(AdminGuard)
  list(@Req() req: any) {
    return this.notificationTemplatesService.list(req.user.libraryId);
  }

  @Patch(':id')
  @UseGuards(AdminGuard)
  update(@Req() req: any, @Param('id') id: string, @Body() body: any) {
    return this.notificationTemplatesService.update(req.user.libraryId, parseInt(id, 10), body);
  }
}
```

---

## 3단계. 백엔드 - 게시판별 '알림 사용 여부' 저장

`backend/src/settings/boards.service.ts` 파일을 여세요.

아래 코드를 찾으세요.

```typescript
    if (data.defaultThumbnailUrl !== undefined) {
      updateData.defaultThumbnailUrl = data.defaultThumbnailUrl || null;
    }

    return this.prisma.board.update({ where: { id }, data: updateData });
```

아래로 바꿔주세요.

```typescript
    if (data.defaultThumbnailUrl !== undefined) {
      updateData.defaultThumbnailUrl = data.defaultThumbnailUrl || null;
    }

    if (data.notifyEnabled !== undefined) {
      updateData.notifyEnabled = !!data.notifyEnabled;
    }

    return this.prisma.board.update({ where: { id }, data: updateData });
```

---

## 4단계. 백엔드 - 새 기능 등록

`backend/src/app.module.ts` 파일을 여세요.

### 4-1. import 추가

아래 코드를 찾으세요.

```typescript
import { MaterialRequestTypesController } from './settings/material-request-types.controller';
import { MaterialRequestTypesService } from './settings/material-request-types.service';
```

아래로 바꿔주세요.

```typescript
import { MaterialRequestTypesController } from './settings/material-request-types.controller';
import { MaterialRequestTypesService } from './settings/material-request-types.service';
import { NotificationTemplatesController } from './settings/notification-templates.controller';
import { NotificationTemplatesService } from './settings/notification-templates.service';
```

### 4-2. controllers 배열에 추가

아래 코드를 찾으세요. (`controllers: [` 배열 안에 있습니다)

```typescript
    MaterialRequestTypesController,
```

아래로 바꿔주세요.

```typescript
    MaterialRequestTypesController,
    NotificationTemplatesController,
```

### 4-3. providers 배열에 추가

아래 코드를 찾으세요. (`providers: [` 배열 안에 있습니다. **주의**: 4-2에서 바꾼 곳과 글자가 똑같이 생겼으니, `controllers`가 아니라 `providers` 배열 안에 있는 것을 찾아주세요.)

```typescript
    MaterialRequestTypesService,
```

아래로 바꿔주세요.

```typescript
    MaterialRequestTypesService,
    NotificationTemplatesService,
```

---

## 5단계. 프론트엔드 - 게시판 설정에 '알림 사용 여부' 추가

`frontend/src/components/boards-settings-form.tsx` 파일을 여세요.

### 5-1. `Board` 타입에 칸 추가

아래 코드를 찾으세요.

```typescript
  defaultThumbnailUrl: string | null;
  isMaterialRequest: boolean;
};
```

아래로 바꿔주세요.

```typescript
  defaultThumbnailUrl: string | null;
  isMaterialRequest: boolean;
  notifyEnabled: boolean;
};
```

### 5-2. state 추가

아래 코드를 찾으세요.

```typescript
  const [defaultThumbnailUrlValue, setDefaultThumbnailUrlValue] = useState("");
```

아래로 바꿔주세요.

```typescript
  const [defaultThumbnailUrlValue, setDefaultThumbnailUrlValue] = useState("");
  const [notifyEnabledValue, setNotifyEnabledValue] = useState(false);
```

### 5-3. 수정 화면을 열 때 값 불러오기

아래 코드를 찾으세요.

```typescript
    setDefaultThumbnailUrlValue(board.defaultThumbnailUrl || "");
  }
```

아래로 바꿔주세요.

```typescript
    setDefaultThumbnailUrlValue(board.defaultThumbnailUrl || "");
    setNotifyEnabledValue(board.notifyEnabled);
  }
```

### 5-4. 저장할 때 값 함께 보내기

아래 코드를 찾으세요.

```typescript
        defaultThumbnailUrl: defaultThumbnailUrlValue,
      }),
    });
```

아래로 바꿔주세요.

```typescript
        defaultThumbnailUrl: defaultThumbnailUrlValue,
        notifyEnabled: notifyEnabledValue,
      }),
    });
```

### 5-5. 화면에 켜고 끄는 버튼 추가

'자료를 신청합니다', '참고서비스', '1:1 상담' 게시판에서만 이 설정이 보이도록 합니다.

아래 코드를 찾으세요.

```typescript
              <div>
                <span className="mb-1 block text-sm text-neutral-500">
                  {t("settings.boards.field.defaultThumbnailUrl")}
                </span>
```

아래로 바꿔주세요. (이 블록 바로 위에 알림 사용 여부 설정을 추가합니다)

```typescript
              {(editingBoard.isMaterialRequest ||
                editingBoard.code === "refService" ||
                editingBoard.code === "counsel") && (
                <div>
                  <span className="mb-1 block text-sm text-neutral-500">
                    {t("settings.boards.field.notifyEnabled")}
                  </span>
                  <ChoiceCardGroup
                    name="notifyEnabled"
                    value={notifyEnabledValue}
                    onChange={setNotifyEnabledValue}
                    yesLabel={t("settings.boards.yes")}
                    noLabel={t("settings.boards.no")}
                  />
                  <p className="mt-1 text-xs text-neutral-400">
                    {t("settings.boards.field.notifyEnabledHint")}
                  </p>
                </div>
              )}

              <div>
                <span className="mb-1 block text-sm text-neutral-500">
                  {t("settings.boards.field.defaultThumbnailUrl")}
                </span>
```

---

## 6단계. 프론트엔드 - 알림 문구 템플릿 화면 새로 만들기

`frontend/src/components/` 폴더 안에 `notification-settings-form.tsx` 파일을 새로 만들고, 아래 내용을 그대로 넣으세요.

```typescript
"use client";

import { useEffect, useState } from "react";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";

type NotificationTemplate = {
  id: number;
  status: string;
  message: string;
};

export default function NotificationSettingsForm() {
  const { t } = useI18n();
  const { notify } = useNotify();

  const [templates, setTemplates] = useState<NotificationTemplate[]>([]);
  const [messages, setMessages] = useState<Record<number, string>>({});
  const [saving, setSaving] = useState(false);

  async function loadTemplates() {
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/notification-templates`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      const data: NotificationTemplate[] = await res.json();
      setTemplates(data);
      const next: Record<number, string> = {};
      data.forEach((tpl) => {
        next[tpl.id] = tpl.message;
      });
      setMessages(next);
    } else {
      notify("❌ " + t("settings.notifications.loadFail"), "error");
    }
  }

  useEffect(() => {
    loadTemplates();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  function statusLabel(status: string) {
    if (status === "PURCHASING") return t("settings.notifications.status.PURCHASING");
    if (status === "PURCHASED") return t("settings.notifications.status.PURCHASED");
    if (status === "NOT_PURCHASED") return t("settings.notifications.status.NOT_PURCHASED");
    return status;
  }

  async function handleSaveAll() {
    const token = localStorage.getItem("token");
    if (!token) return;

    const hasEmpty = templates.some((tpl) => !messages[tpl.id]?.trim());
    if (hasEmpty) {
      notify("❌ " + t("settings.notifications.messageRequired"), "error");
      return;
    }

    setSaving(true);
    try {
      const results = await Promise.all(
        templates.map((tpl) =>
          fetch(`${API_URL}/notification-templates/${tpl.id}`, {
            method: "PATCH",
            headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
            body: JSON.stringify({ message: messages[tpl.id].trim() }),
          }),
        ),
      );
      if (results.every((res) => res.ok)) {
        notify("✅ " + t("settings.notifications.saveSuccess"), "success");
        await loadTemplates();
      } else {
        notify("❌ " + t("settings.notifications.saveFail"), "error");
      }
    } finally {
      setSaving(false);
    }
  }

  return (
    <div className="flex flex-col gap-4">
      <div className="rounded-lg border border-neutral-200 bg-white p-4">
        <p className="mb-1 text-sm font-semibold">{t("settings.notifications.title")}</p>
        <p className="mb-4 text-xs text-neutral-400">{t("settings.notifications.desc")}</p>

        <div className="flex flex-col gap-4">
          {templates.map((tpl) => (
            <label key={tpl.id} className="block">
              <span className="mb-1 block text-sm text-neutral-500">{statusLabel(tpl.status)}</span>
              <input
                value={messages[tpl.id] ?? ""}
                onChange={(e) => setMessages({ ...messages, [tpl.id]: e.target.value })}
                className="w-full rounded-lg border border-neutral-200 bg-white px-3 py-2 text-sm"
              />
            </label>
          ))}
        </div>

        <button
          type="button"
          onClick={handleSaveAll}
          disabled={saving}
          className="mt-5 cursor-pointer rounded-lg bg-[#383838] px-4 py-2.5 text-sm font-semibold text-[#F9F6F0] disabled:cursor-not-allowed disabled:opacity-50"
        >
          {t("settings.notifications.save")}
        </button>
      </div>
    </div>
  );
}
```

---

## 7단계. 프론트엔드 - '알림' 탭 추가

`frontend/src/app/admin/settings/page.tsx` 파일을 여세요.

### 7-1. import 추가

아래 코드를 찾으세요.

```typescript
import BoardsSettingsForm from "@/components/boards-settings-form";
```

아래로 바꿔주세요.

```typescript
import BoardsSettingsForm from "@/components/boards-settings-form";
import NotificationSettingsForm from "@/components/notification-settings-form";
```

### 7-2. 탭 목록에 추가

아래 코드를 찾으세요.

```typescript
const SETTINGS_TABS = ["library", "design", "materialTypes", "copyOptions", "kormarcTags", "loan", "boards"];
```

아래로 바꿔주세요.

```typescript
const SETTINGS_TABS = [
  "library",
  "design",
  "materialTypes",
  "copyOptions",
  "kormarcTags",
  "loan",
  "boards",
  "notifications",
];
```

### 7-3. 탭 버튼 추가

아래 코드를 찾으세요.

```typescript
          <TabsTrigger value="boards">{t("settings.tabs.boards")}</TabsTrigger>
        </TabsList>
```

아래로 바꿔주세요.

```typescript
          <TabsTrigger value="boards">{t("settings.tabs.boards")}</TabsTrigger>
          <TabsTrigger value="notifications">{t("settings.tabs.notifications")}</TabsTrigger>
        </TabsList>
```

### 7-4. 탭 내용 추가

아래 코드를 찾으세요.

```typescript
        <TabsContent value="boards" className="mt-4">
          <BoardsSettingsForm />
        </TabsContent>
      </Tabs>
```

아래로 바꿔주세요.

```typescript
        <TabsContent value="boards" className="mt-4">
          <BoardsSettingsForm />
        </TabsContent>

        <TabsContent value="notifications" className="mt-4">
          <NotificationSettingsForm />
        </TabsContent>
      </Tabs>
```

---

## 8단계. 화면에 보이는 문구 추가

`frontend/src/lib/dictionary.ts` 파일을 여세요. 이 파일은 한글(ko) 구역과 영어(en) 구역, 두 군데에 각각 똑같은 항목을 추가해야 합니다.

### 8-1. 한글(ko) 구역 - 탭 이름 추가

아래 코드를 찾으세요.

```typescript
    "settings.tabs.loan": "대출",
    "settings.tabs.boards": "게시판",
    "settings.copyOptions.category.status": "상태",
```

아래로 바꿔주세요.

```typescript
    "settings.tabs.loan": "대출",
    "settings.tabs.boards": "게시판",
    "settings.tabs.notifications": "알림",
    "settings.copyOptions.category.status": "상태",
```

### 8-2. 한글(ko) 구역 - 게시판 알림 사용 여부 문구 추가

아래 코드를 찾으세요.

```typescript
    "settings.boards.field.allowGuestComment": "비회원 댓글 허용",
    "settings.boards.save": "저장",
```

아래로 바꿔주세요.

```typescript
    "settings.boards.field.allowGuestComment": "비회원 댓글 허용",
    "settings.boards.field.notifyEnabled": "알림 기능 사용",
    "settings.boards.field.notifyEnabledHint": "켜두면 이 게시판에 '이메일로 알림 받기' 체크박스가 나타납니다.",
    "settings.boards.save": "저장",
```

### 8-3. 한글(ko) 구역 - 알림 문구 템플릿 화면 문구 추가

아래 코드를 찾으세요.

```typescript
    "settings.boards.field.defaultThumbnailUrlHint": "이 게시판 글에 사진이 없을 때 대신 보여줄 사진입니다. 등록하지 않으면 설정 > 도서관에 등록한 기본 썸네일을 대신 씁니다.",

    "settings.boards.fonts.title": "게시판 글꼴",
```

아래로 바꿔주세요.

```typescript
    "settings.boards.field.defaultThumbnailUrlHint": "이 게시판 글에 사진이 없을 때 대신 보여줄 사진입니다. 등록하지 않으면 설정 > 도서관에 등록한 기본 썸네일을 대신 씁니다.",

    "settings.notifications.title": "자료를 신청합니다 - 처리상태 안내 문구",
    "settings.notifications.desc": "신청자에게 처리상태별로 보여줄 안내 문구입니다. \"{title}\"이라고 적으면 실제로는 신청한 자료의 제목으로 바뀌어 보입니다.",
    "settings.notifications.status.PURCHASING": "구입 진행 중",
    "settings.notifications.status.PURCHASED": "구입 완료",
    "settings.notifications.status.NOT_PURCHASED": "미구입",
    "settings.notifications.save": "저장",
    "settings.notifications.saveSuccess": "저장되었습니다.",
    "settings.notifications.saveFail": "저장에 실패했습니다.",
    "settings.notifications.loadFail": "문구 템플릿을 불러오지 못했습니다.",
    "settings.notifications.messageRequired": "문구를 모두 입력하세요.",

    "settings.boards.fonts.title": "게시판 글꼴",
```

### 8-4. 영어(en) 구역 - 탭 이름 추가

아래 코드를 찾으세요.

```typescript
    "settings.tabs.loan": "Loan",
    "settings.tabs.boards": "Boards",
    "settings.copyOptions.category.status": "Status",
```

아래로 바꿔주세요.

```typescript
    "settings.tabs.loan": "Loan",
    "settings.tabs.boards": "Boards",
    "settings.tabs.notifications": "Notifications",
    "settings.copyOptions.category.status": "Status",
```

### 8-5. 영어(en) 구역 - 게시판 알림 사용 여부 문구 추가

아래 코드를 찾으세요.

```typescript
    "settings.boards.field.allowGuestComment": "Can non-members write comments?",
    "settings.boards.save": "Save",
```

아래로 바꿔주세요.

```typescript
    "settings.boards.field.allowGuestComment": "Can non-members write comments?",
    "settings.boards.field.notifyEnabled": "Enable Notifications",
    "settings.boards.field.notifyEnabledHint": "When on, an \"Email me updates\" checkbox appears on this board.",
    "settings.boards.save": "Save",
```

### 8-6. 영어(en) 구역 - 알림 문구 템플릿 화면 문구 추가

아래 코드를 찾으세요. (한글 구역과 똑같이 생긴 줄이 영어 구역에도 있습니다. **주의**: 반드시 `defaultThumbnailUrlHint` 값이 영어(`"Shown instead when a post on this board has no photo...`)로 되어 있는 곳을 찾아주세요.)

```typescript
    "settings.boards.field.defaultThumbnailUrlHint": "Shown instead when a post on this board has no photo. If not set, the library-wide default thumbnail (Settings > Library) is used instead.",

    "settings.boards.fonts.title": "Board Fonts",
```

아래로 바꿔주세요.

```typescript
    "settings.boards.field.defaultThumbnailUrlHint": "Shown instead when a post on this board has no photo. If not set, the library-wide default thumbnail (Settings > Library) is used instead.",

    "settings.notifications.title": "Material Requests - Status Notification Messages",
    "settings.notifications.desc": "Messages shown to requesters for each status. Write \"{title}\" to have it replaced with the requested material's title.",
    "settings.notifications.status.PURCHASING": "Purchasing",
    "settings.notifications.status.PURCHASED": "Purchased",
    "settings.notifications.status.NOT_PURCHASED": "Not Purchased",
    "settings.notifications.save": "Save",
    "settings.notifications.saveSuccess": "Saved.",
    "settings.notifications.saveFail": "Save failed.",
    "settings.notifications.loadFail": "Failed to load notification templates.",
    "settings.notifications.messageRequired": "Please fill in all messages.",

    "settings.boards.fonts.title": "Board Fonts",
```

---

## 확인하기

1. 백엔드 서버를 다시 켜세요. (마이그레이션이 잘 적용됐는지 콘솔에 오류가 없는지 확인)
2. 관리자 페이지 > 설정 > 게시판에서 '자료를 신청합니다' 게시판을 눌러보세요. '알림 기능 사용' 켜고 끄기 버튼이 새로 보이는지 확인합니다.
3. '참고서비스', '1:1 상담' 게시판도 같은 설정이 보이는지 확인합니다. 반대로 '공지', '열린 게시판' 등 다른 게시판을 누르면 이 설정이 보이지 않는지 확인합니다.
4. '알림 기능 사용'을 켜고 저장한 뒤, 다시 그 게시판을 눌러서 켜진 상태가 그대로 저장되어 있는지 확인합니다.
5. 관리자 페이지 > 설정 > 알림 탭을 눌러보세요. '구입 진행 중', '구입 완료', '미구입' 3가지 문구 입력창이 나오는지 확인합니다.
6. 문구를 하나 수정하고 저장한 뒤, 새로고침해도 수정한 문구가 그대로 남아있는지 확인합니다.
7. 문구 중 하나를 비워두고 저장을 눌러서 "문구를 모두 입력하세요" 알림이 뜨는지 확인합니다.

---

## GitHub 커밋

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선166 가이드: 설정 > 알림 메뉴 신설 및 게시판별 알림 사용 여부, 처리상태 문구 템플릿"
git push
```

---

## 최종 점검표

- [ ] 설정 > 게시판에서 '자료를 신청합니다'/'참고서비스'/'1:1 상담' 게시판에만 '알림 기능 사용' 설정이 보인다.
- [ ] '알림 기능 사용'을 켜고 끈 것이 저장되고, 다시 열어도 유지된다.
- [ ] 설정 > 알림 탭에서 처리상태 3가지 문구를 각각 수정하고 저장할 수 있다.
- [ ] 문구를 비워두고 저장하면 오류 알림이 뜬다.
- [ ] 변경 사항을 GitHub에 커밋하고 push했다.

# 15단계 (7) — 홈페이지 회원가입 화면에 휴대폰·이메일 검사 적용

> 목표: 관리자 페이지의 "회원" 화면에 이미 적용한 것과 같은 규칙을, 홈페이지
> 회원가입 화면(`/signup`)에도 그대로 적용합니다. 숫자만 입력해도 자동으로
> `000-0000-0000` 모양이 되고, 11자리를 다 채우지 않으면 가입이 안 되며,
> 이메일도 형식이 아니면 가입이 막힙니다.
> 소요 시간: 약 10분

---

## 1. dictionary.ts에 오류 문구 추가 — 약 3분

`frontend/src/lib/dictionary.ts` 를 엽니다. `"signup.fail"` 줄 바로 아래(한글·영어 각각)에 추가하세요.

**한글 쪽:**

```ts
    "signup.invalidPhone": "휴대폰 번호 11자리를 모두 입력해주세요.",
    "signup.invalidEmail": "이메일 형식이 올바르지 않습니다.",
```

**영어 쪽:**

```ts
    "signup.invalidPhone": "Please enter all 11 digits of the mobile phone number.",
    "signup.invalidEmail": "Please enter a valid email address.",
```

---

## 2. `(site)/signup/page.tsx` 수정 — 약 7분

`frontend/src/app/(site)/signup/page.tsx` 를 엽니다.

### 2-1. 휴대폰 번호 서식·검사 함수 추가

`const API_URL = "http://localhost:3001";` 바로 아래에 아래 세 함수를 추가하세요. (회원 관리 화면에서 쓴 것과 똑같은 함수예요 — 화면마다 각자 따로 갖고 있는 방식이라, 여기도 하나 더 넣어줍니다.)

```tsx
// 숫자만 남기고, 3자리-4자리-4자리 모양으로 하이픈을 자동으로 붙여줍니다.
function formatPhone(raw: string) {
  const digits = raw.replace(/\D/g, "").slice(0, 11);
  if (digits.length < 4) return digits;
  if (digits.length < 8) return `${digits.slice(0, 3)}-${digits.slice(3)}`;
  return `${digits.slice(0, 3)}-${digits.slice(3, 7)}-${digits.slice(7)}`;
}

// 하이픈을 뺀 숫자가 정확히 11자리인지 확인합니다.
function isValidPhone(phone: string) {
  return phone.replace(/\D/g, "").length === 11;
}

// 이메일은 입력했을 때만(선택 항목이라) 형식을 확인합니다.
function isValidEmail(email: string) {
  if (!email.trim()) return true;
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim());
}
```

### 2-2. 가입 요청 보내기 전 검사 추가

`handleSignup` 함수 안, 아래 부분을 찾으세요.

```tsx
  async function handleSignup() {
    const res = await fetch(`${API_URL}/users`, {
```

이렇게 바꿔주세요. (검사 두 개를 함수 맨 앞에 추가했어요.)

```tsx
  async function handleSignup() {
    if (!isValidPhone(phone)) {
      notify("❌ " + t("signup.invalidPhone"), "error");
      return;
    }
    if (!isValidEmail(email)) {
      notify("❌ " + t("signup.invalidEmail"), "error");
      return;
    }

    const res = await fetch(`${API_URL}/users`, {
```

### 2-3. 휴대폰 번호 입력칸에 자동 서식 적용

아래 부분을 찾으세요.

```tsx
          <div className="flex flex-col gap-2">
            <Label htmlFor="phone">{t("signup.phone")}</Label>
            <Input id="phone" value={phone} onChange={(e) => setPhone(e.target.value)} placeholder="010-1234-5678" />
          </div>
```

이렇게 바꿔주세요.

```tsx
          <div className="flex flex-col gap-2">
            <Label htmlFor="phone">{t("signup.phone")}</Label>
            <Input
              id="phone"
              value={phone}
              onChange={(e) => setPhone(formatPhone(e.target.value))}
              placeholder="000-0000-0000"
              inputMode="numeric"
              maxLength={13}
            />
          </div>
```

- 회원가입은 원래도 휴대폰 번호가 필수 항목이었어요(백엔드 `signup()`이 이미 필수로 확인하고 있었어요). 이번엔 "11자리를 다 채워야 한다"는 조건이 새로 추가된 거예요.

---

## 3. 확인하기 — 약 3분

1. `/signup` 화면 휴대폰 번호 칸에 숫자만 이어서 입력하면 자동으로 `010-1234-5678` 모양이 되는지 확인합니다.
2. 11자리를 다 안 채우고 "가입하기"를 누르면 "휴대폰 번호 11자리를 모두 입력해주세요." 오류가 뜨는지 확인합니다.
3. 이메일에 이상한 형식(`abc` 등)을 넣고 가입 시도 → "이메일 형식이 올바르지 않습니다." 오류가 뜨는지 확인합니다.
4. 모두 올바르게 입력하고 가입 → 정상적으로 가입되는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## 4. GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "15단계(7): 홈페이지 회원가입 화면에 휴대폰 번호 자동 하이픈+검사, 이메일 형식 검사 적용"
git push
```

---

## 최종 점검표

- [ ] `dictionary.ts` — `signup.invalidPhone`/`invalidEmail` 한글/영어 추가
- [ ] `signup/page.tsx` — `formatPhone`/`isValidPhone`/`isValidEmail` 함수 추가
- [ ] `handleSignup`에 검사 추가
- [ ] 휴대폰 번호 입력칸 자동 서식 적용
- [ ] 자동 하이픈 동작 확인
- [ ] 11자리 미만 시 가입 차단 확인
- [ ] 이메일 형식 오류 시 가입 차단 확인
- [ ] GitHub에 올림

---

## 15단계 전체 완료!

회원 관리(등록/수정/정지/역할/비밀번호변경/유효성검사) + 홈페이지 회원가입 검사까지 모두 끝났어요. 다음은 대출/반납/예약, 슈퍼계정 자료종류 토글 등 중에서 정하시면 됩니다. 완료되면 알려주세요!

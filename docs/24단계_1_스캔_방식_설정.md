# 24단계 1: '설정'에 "스캔 방식" 항목 추가하기

## 목표

앞으로 만들 대출/반납 화면에서는, 자료의 등록번호를 "바코드 스캔" 또는 "RFID 리더기"로 입력받게 됩니다. 도서관마다 사용하는 장비가 다르기 때문에, 도서관별로 어떤 방식을 쓰는지 미리 저장해 둘 자리가 필요합니다.

이번 단계에서는 '설정' → '도서관' 화면에 "스캔 방식"이라는 항목을 추가합니다. 고를 수 있는 값은 두 가지입니다.

- **단일 태그 인식** — 바코드 스캐너, 또는 RFID 리더기 중 "책 한 권씩 인식해서 키보드로 숫자를 입력한 것처럼 동작"하는 방식입니다. 지금 만들 대출/반납 화면은 이 방식으로 동작합니다.
- **다중 태그 인식(SDK 연동, 추후 지원 예정)** — 여러 권을 한 번에 인식하는 RFID 리더기용입니다. 이 방식은 리더기 제조사마다 별도의 연동 프로그램이 필요해서, 지금 당장은 개발하지 않습니다. 우선 도서관이 이 값을 선택해 둘 수만 있게 만들어 두고, 실제 연동 기능은 나중에 별도 단계에서 추가합니다.

지금은 값을 저장하는 자리만 만드는 단계이고, 실제 대출/반납 화면 만들기는 다음 단계(24단계 2)부터 시작합니다.

---

## 1. 데이터베이스에 "스캔 방식" 칸 추가하기: `schema.prisma`

`C:\projects\LibraryNearMe\backend\prisma\schema.prisma` 파일을 여세요.

`Library` 모델 안에서 아래 부분을 찾으세요.

```prisma
  maxSubjectKeywords Int   @default(10)                      // ← 추가: 자료 등록 시 주제어 최대 입력 개수
  createdAt    DateTime @default(now())
```

이렇게 바꿔주세요.

```prisma
  maxSubjectKeywords Int   @default(10)                      // ← 추가: 자료 등록 시 주제어 최대 입력 개수
  scanMode     String   @default("SINGLE")                   // ← 추가: 스캔 방식 "SINGLE"(단일 태그) | "SDK"(다중 태그, 추후 지원)
  createdAt    DateTime @default(now())
```

파일을 저장하세요.

터미널을 열어서 아래 명령어를 순서대로 입력하세요.

```
cd C:\projects\LibraryNearMe\backend
npx prisma migrate dev --name add_scan_mode
```

명령어가 끝나면 정상적으로 실행되었는지 확인하세요.

---

## 2. 백엔드 수정하기: `library.service.ts`

`C:\projects\LibraryNearMe\backend\src\library\library.service.ts` 파일을 여세요. 아래 내용 전체로 파일을 바꿔주세요.

```ts
import { Injectable } from '@nestjs/common';
import { PrismaService } from '../prisma.service';

@Injectable()
export class LibraryService {
  constructor(private prisma: PrismaService) {}

  // 도서관 설정 가져오기
  getLibrary() {
    return this.prisma.library.findFirst();
  }

  // 도서관 설정 바꿔서 저장하기
  async updateLibrary(data: {
    name: string; primaryColor: string; logoUrl?: string;
    footerVersion?: string; footerCopyright?: string;
    footerBgColor?: string; footerTextColor?: string;
    sidebarBgColor?: string; sidebarTextColor?: string;
    buttonStyles?: any;
    fontFamily?: string;
    fontWeight?: string;
    maxSubjectKeywords?: number;
    scanMode?: string;
  }) {
    const library = await this.prisma.library.findFirst();
    if (!library) return null;
    return this.prisma.library.update({
      where: { id: library.id },
      data: {
        name: data.name, primaryColor: data.primaryColor,
        logoUrl: data.logoUrl || undefined,
        footerVersion: data.footerVersion || undefined,
        footerCopyright: data.footerCopyright || undefined,
        footerBgColor: data.footerBgColor || undefined,
        footerTextColor: data.footerTextColor || undefined,
        sidebarBgColor: data.sidebarBgColor || undefined,
        sidebarTextColor: data.sidebarTextColor || undefined,
        buttonStyles: data.buttonStyles ?? undefined,
        fontFamily: data.fontFamily || undefined,
        fontWeight: data.fontWeight || undefined,
        maxSubjectKeywords: data.maxSubjectKeywords ?? undefined,
        scanMode: data.scanMode || undefined,
      },
    });
  }

}
```

파일을 저장하세요. (바뀐 점: `updateLibrary`가 받을 수 있는 값에 `scanMode`가 추가되고, 저장할 때도 함께 반영됩니다.)

---

## 3. 화면에 보일 문구 추가하기: `dictionary.ts`

`C:\projects\LibraryNearMe\frontend\src\lib\dictionary.ts` 파일을 여세요.

### 3-1. 한글(ko) 문구 추가하기

아래 부분을 찾으세요.

```ts
    "admin.settings.footerVersion": "버전",
    "admin.settings.footerCopyright": "저작권 문구",
```

이렇게 바꿔주세요.

```ts
    "admin.settings.footerVersion": "버전",
    "admin.settings.footerCopyright": "저작권 문구",
    "admin.settings.scanMode.label": "스캔 방식",
    "admin.settings.scanMode.single": "단일 태그 인식 (바코드 / RFID 단일 태그)",
    "admin.settings.scanMode.sdk": "다중 태그 인식 (제조사 SDK 연동, 추후 지원 예정)",
    "admin.settings.scanMode.hint": "대출/반납 화면에서 등록번호를 입력받는 방식이에요. '다중 태그 인식'은 지금은 선택만 가능하고, 실제 리더기 연동 기능은 나중에 추가될 예정이에요.",
```

### 3-2. 영어(en) 문구 추가하기

아래 부분을 찾으세요.

```ts
    "admin.settings.logoUrl": "Logo Image URL",
```

이 줄 근처(파일에서 두 번째로 나오는, 영어 블록의 `admin.settings.footerCopyright` 다음 줄)를 찾아야 합니다. 아래처럼 찾아서 바꿔주세요.

```ts
    "admin.settings.footerVersion": "Version",
    "admin.settings.footerCopyright": "Copyright Text",
```

이렇게 바꿔주세요.

```ts
    "admin.settings.footerVersion": "Version",
    "admin.settings.footerCopyright": "Copyright Text",
    "admin.settings.scanMode.label": "Scan Mode",
    "admin.settings.scanMode.single": "Single-tag recognition (barcode / RFID single tag)",
    "admin.settings.scanMode.sdk": "Multi-tag recognition (vendor SDK integration, coming later)",
    "admin.settings.scanMode.hint": "This controls how registration numbers are entered on the loan/return screens. 'Multi-tag recognition' can be selected now, but the actual reader integration will be added later.",
```

파일을 저장하세요.

> 참고: `footerVersion`/`footerCopyright` 문구가 한글/영어 블록에 각각 한 번씩, 총 두 번 나옵니다. 3-1은 한글 블록(파일 앞쪽), 3-2는 영어 블록(파일 뒤쪽)에 적용하는 것이니 헷갈리지 않게 위치를 확인하고 수정하세요.

---

## 4. 화면 수정하기: `library-settings-form.tsx`

`C:\projects\LibraryNearMe\frontend\src\components\library-settings-form.tsx` 파일을 여세요. 아래 내용 전체로 파일을 바꿔주세요.

```tsx
"use client";

import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = "http://localhost:3001";

export default function LibrarySettingsForm() {
  const { t } = useI18n();
  const { notify } = useNotify();
  const [name, setName] = useState("");
  const [primaryColor, setPrimaryColor] = useState("#2563eb");
  const [logoUrl, setLogoUrl] = useState("");
  const [footerVersion, setFooterVersion] = useState("1.0.0");
  const [footerCopyright, setFooterCopyright] = useState("ⓒ 2026 Gravityofu");
  const [scanMode, setScanMode] = useState("SINGLE");

  useEffect(() => {
    fetch(`${API_URL}/library`)
      .then((res) => res.json())
      .then((data) => {
        if (data) {
          setName(data.name);
          setPrimaryColor(data.primaryColor);
          setLogoUrl(data.logoUrl || "");
          setFooterVersion(data.footerVersion || "1.0.0");
          setFooterCopyright(data.footerCopyright || "ⓒ 2026 Gravityofu");
          setScanMode(data.scanMode || "SINGLE");
        }
      });
  }, []);

  async function handleSave() {
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/library`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify({
        name,
        primaryColor,
        logoUrl,
        footerVersion,
        footerCopyright,
        scanMode,
      }),
    });
    if (res.ok) {
      notify(t("admin.settings.saved"), "success", () => window.location.reload());
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("admin.settings.saveFail")), "error");
    }
  }

  return (
    <div className="max-w-md">
      <div className="flex flex-col gap-4">
        <div className="flex flex-col gap-2">
          <Label htmlFor="name">{t("admin.settings.name")}</Label>
          <Input id="name" value={name} onChange={(e) => setName(e.target.value)} />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="color">{t("admin.settings.color")}</Label>
          <Input id="color" type="color" value={primaryColor} onChange={(e) => setPrimaryColor(e.target.value)} className="h-10 w-20 p-1" />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="logoUrl">{t("admin.settings.logoUrl")}</Label>
          <Input id="logoUrl" value={logoUrl} onChange={(e) => setLogoUrl(e.target.value)} placeholder="https://..." />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="footerVersion">{t("admin.settings.footerVersion")}</Label>
          <Input id="footerVersion" value={footerVersion} onChange={(e) => setFooterVersion(e.target.value)} />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="footerCopyright">{t("admin.settings.footerCopyright")}</Label>
          <Input id="footerCopyright" value={footerCopyright} onChange={(e) => setFooterCopyright(e.target.value)} />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="scanMode">{t("admin.settings.scanMode.label")}</Label>
          <select
            id="scanMode"
            value={scanMode}
            onChange={(e) => setScanMode(e.target.value)}
            className="cursor-pointer rounded-lg border border-neutral-200 bg-white px-3 py-2 text-sm"
          >
            <option value="SINGLE">{t("admin.settings.scanMode.single")}</option>
            <option value="SDK">{t("admin.settings.scanMode.sdk")}</option>
          </select>
          <span className="text-xs text-neutral-400">{t("admin.settings.scanMode.hint")}</span>
        </div>
        <Button className="cursor-pointer" onClick={handleSave}>
          {t("admin.settings.save")}
        </Button>
      </div>
    </div>
  );
}
```

파일을 저장하세요. (바뀐 점: `scanMode` 상태값과, 도서관 정보를 불러올 때 함께 불러오는 부분, 저장할 때 함께 보내는 부분, 그리고 드롭다운 선택창이 추가됩니다.)

---

## 확인하기

1. 백엔드 서버(`npm run start:dev`)와 프런트엔드 서버를 재시작하세요.
2. '설정' → '도서관' 화면으로 들어가세요.
3. "스캔 방식"이라는 항목과 드롭다운이 새로 보이는지 확인하세요. 기본값이 "단일 태그 인식 (바코드 / RFID 단일 태그)"으로 되어 있는지 확인하세요.
4. 드롭다운에서 "다중 태그 인식 (제조사 SDK 연동, 추후 지원 예정)"을 선택하고 '저장하기'를 눌러보세요. 저장 후 화면을 새로고침해도 선택한 값이 그대로 유지되는지 확인하세요.
5. 다시 "단일 태그 인식"으로 돌려놓고 저장해서, 원래 값으로 잘 돌아오는지도 확인하세요.

---

## GitHub 커밋

모든 확인이 끝났다면 아래 명령어로 변경 내용을 저장하세요.

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "24단계1: 설정에 스캔 방식(단일 태그/다중 태그 SDK) 항목 추가"
git push
```

---

## 최종 점검표

- [ ] `schema.prisma`의 `Library` 모델에 `scanMode` 칸을 추가하고 마이그레이션을 실행했다.
- [ ] `library.service.ts`의 `updateLibrary`가 `scanMode`를 받아서 저장한다.
- [ ] `dictionary.ts`에 한글/영어 "스캔 방식" 관련 문구를 추가했다.
- [ ] '설정' → '도서관' 화면에 "스캔 방식" 드롭다운이 보인다.
- [ ] 드롭다운에서 값을 바꾸고 저장하면 새로고침 후에도 값이 유지된다.
- [ ] 변경 내용을 GitHub에 커밋 및 푸시했다.

---

## 다음 단계 안내

다음 단계(24단계 2)부터 실제 "대출 처리" 화면을 만들기 시작합니다. 회원번호로 회원을 검색하고, 자료 등록번호를 입력(스캔)하면 순서대로 대출 처리가 되는 화면입니다. 이때 등록번호 입력이 아주 빠르게 여러 번 들어와도(예: 다중 태그 RFID가 나중에 연동되었을 때 대비) 이전 처리가 끝나기 전에 다음 처리가 겹치지 않도록, 입력을 큐(줄서기)에 쌓았다가 순서대로 처리하는 로직도 함께 만들 예정입니다.

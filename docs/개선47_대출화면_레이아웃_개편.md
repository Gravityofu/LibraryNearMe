# 개선 47: '대출/반납' 화면 레이아웃 개편

## 목표

'대출/반납' 화면을 아래와 같이 바꿉니다.

- 탭 이름을 "대출 처리"/"반납 처리"에서 "대출"/"반납"으로 줄입니다.
- 회원 검색은 이름/회원번호로만 되도록 줄입니다. (휴대폰 번호, 아이디로는 검색하지 않습니다.)
- 회원을 검색하면, 결과를 화면에 바로 목록으로 보여주던 것을 모달 팝업으로 바꿉니다.
  - 검색 결과가 여러 명이면 모달에 이름/회원번호/휴대폰번호가 나오고, 하나를 선택하면 모달이 닫힙니다.
  - 검색 결과가 없으면 모달에 "검색 결과가 없습니다" 안내가 나옵니다.
  - **검색 결과가 정확히 1명이면 모달을 띄우지 않고 곧바로 그 회원이 선택됩니다.**
- 처음부터 '회원 검색' 박스와 '회원 정보' 박스가 나란히(수평으로) 있습니다.
  - '회원 검색' 박스: 검색창 + 검색 버튼, 그리고 회원을 선택하면 그 아래에 자료 등록번호 입력창이 나옵니다.
  - '회원 정보' 박스: 제목만 있다가, 회원을 선택하면 이름/회원번호/휴대폰번호/회원구분/상태/생년월일/이메일/주소가 표시됩니다.
- 그 아래에 '처리 내역' 박스를 두 박스를 합한 가로 길이로 둡니다.
- '회원 변경' 버튼은 '초기화'로 이름을 바꿉니다.

---

## 1. 백엔드 수정하기: `loans.service.ts`

`C:\projects\LibraryNearMe\backend\src\loans\loans.service.ts` 파일을 여세요.

아래 부분을 찾으세요.

```ts
  // 대출 화면에서 회원을 찾을 때 씁니다. 이름/휴대폰번호/아이디/회원번호 중 일부만 입력해도 찾을 수 있어요.
  async findMembers(libraryId: number, keyword: string) {
    const kw = (keyword || '').trim();
    if (!kw) return [];
    return this.prisma.user.findMany({
      where: {
        libraryId,
        role: 'MEMBER',
        status: 'ACTIVE',
        OR: [
          { name: { contains: kw } },
          { phone: { contains: kw } },
          { loginId: { contains: kw } },
          { memberNo: { contains: kw } },
        ],
      },
      select: {
        id: true,
        name: true,
        phone: true,
        memberNo: true,
        loginId: true,
        memberType: { select: { id: true, name: true } },
      },
      take: 10,
      orderBy: { name: 'asc' },
    });
  }
```

이렇게 바꿔주세요. (검색 조건을 이름/회원번호로만 줄이고, 회원 정보 박스에 보여줄 항목들을 함께 가져오도록 바꿉니다.)

```ts
  // 대출 화면에서 회원을 찾을 때 씁니다. 이름 또는 회원번호로 찾을 수 있어요.
  async findMembers(libraryId: number, keyword: string) {
    const kw = (keyword || '').trim();
    if (!kw) return [];
    return this.prisma.user.findMany({
      where: {
        libraryId,
        role: 'MEMBER',
        status: 'ACTIVE',
        OR: [
          { name: { contains: kw } },
          { memberNo: { contains: kw } },
        ],
      },
      select: {
        id: true,
        name: true,
        phone: true,
        memberNo: true,
        status: true,
        birthDate: true,
        email: true,
        address: true,
        memberType: { select: { id: true, name: true } },
      },
      take: 10,
      orderBy: { name: 'asc' },
    });
  }
```

파일을 저장하세요.

---

## 2. 화면에 보일 문구 수정하기: `dictionary.ts`

`C:\projects\LibraryNearMe\frontend\src\lib\dictionary.ts` 파일을 여세요.

### 2-1. 한글(ko) 문구 수정하기

아래 부분을 찾으세요.

```ts
    "loans.tabs.checkout": "대출 처리",
    "loans.tabs.return": "반납 처리",
    "loans.return.comingSoon": "반납 처리 기능은 다음 단계에서 추가될 예정이에요.",
    "loans.member.searchLabel": "회원 검색",
    "loans.member.searchPlaceholder": "이름 / 휴대폰 번호 / 아이디 / 회원번호",
    "loans.member.searchBtn": "검색",
    "loans.member.noResults": "검색 결과가 없습니다.",
    "loans.member.selectHint": "회원을 검색해서 선택하세요.",
    "loans.member.changeBtn": "회원 변경",
```

이렇게 바꿔주세요.

```ts
    "loans.tabs.checkout": "대출",
    "loans.tabs.return": "반납",
    "loans.return.comingSoon": "반납 처리 기능은 다음 단계에서 추가될 예정이에요.",
    "loans.member.searchLabel": "회원 검색",
    "loans.member.searchPlaceholder": "이름 / 회원번호",
    "loans.member.searchBtn": "검색",
    "loans.member.noResults": "검색 결과가 없습니다.",
    "loans.member.selectHint": "회원을 검색해서 선택하세요.",
    "loans.member.changeBtn": "초기화",
    "loans.member.info.title": "회원 정보",
    "loans.member.searchModalTitle": "회원 검색 결과",
    "loans.member.closeBtn": "닫기",
```

### 2-2. 영어(en) 문구 수정하기

아래 부분을 찾으세요.

```ts
    "loans.tabs.checkout": "Checkout",
    "loans.tabs.return": "Return",
    "loans.return.comingSoon": "Return processing will be added in the next step.",
    "loans.member.searchLabel": "Search Member",
    "loans.member.searchPlaceholder": "Name / phone / login ID / member no.",
    "loans.member.searchBtn": "Search",
    "loans.member.noResults": "No results found.",
    "loans.member.selectHint": "Search for a member and select one.",
    "loans.member.changeBtn": "Change Member",
```

이렇게 바꿔주세요.

```ts
    "loans.tabs.checkout": "Checkout",
    "loans.tabs.return": "Return",
    "loans.return.comingSoon": "Return processing will be added in the next step.",
    "loans.member.searchLabel": "Search Member",
    "loans.member.searchPlaceholder": "Name / member no.",
    "loans.member.searchBtn": "Search",
    "loans.member.noResults": "No results found.",
    "loans.member.selectHint": "Search for a member and select one.",
    "loans.member.changeBtn": "Reset",
    "loans.member.info.title": "Member Info",
    "loans.member.searchModalTitle": "Search Results",
    "loans.member.closeBtn": "Close",
```

파일을 저장하세요.

---

## 3. 대출 화면 다시 만들기: `page.tsx`

`C:\projects\LibraryNearMe\frontend\src\app\admin\loans\page.tsx` 파일을 여세요. 아래 내용 전체로 파일을 바꿔주세요.

```tsx
"use client";

import { useRef, useState } from "react";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import ThemedButton from "@/components/themed-button";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";

type Member = {
  id: number;
  name: string;
  phone: string | null;
  memberNo: string | null;
  status: string;
  birthDate: string | null;
  email: string | null;
  address: string | null;
  memberType: { id: number; name: string } | null;
};

type HistoryItem = {
  id: number;
  registrationNo: string;
  ok: boolean;
  message: string;
  memberName?: string;
  materialTitle?: string;
  dueDate?: string;
};

// 회원 정보 박스 안에서 "라벨: 값" 한 줄을 보여주는 작은 부품입니다.
function InfoRow({ label, value }: { label: string; value: string }) {
  return (
    <div className="flex items-baseline justify-between gap-3 border-b border-neutral-100 py-1.5 text-sm last:border-b-0">
      <span className="shrink-0 text-neutral-500">{label}</span>
      <span className="text-right font-medium">{value}</span>
    </div>
  );
}

export default function AdminLoansPage() {
  const { t } = useI18n();
  const { notify } = useNotify();

  const [keyword, setKeyword] = useState("");
  const [results, setResults] = useState<Member[]>([]);
  const [showSearchModal, setShowSearchModal] = useState(false);
  const [selectedMember, setSelectedMember] = useState<Member | null>(null);

  const [registrationNo, setRegistrationNo] = useState("");
  const [processing, setProcessing] = useState(false);
  const [history, setHistory] = useState<HistoryItem[]>([]);

  // 등록번호가 아무리 빠르게 여러 번 들어와도, 이전 처리가 끝난 뒤 순서대로 하나씩 처리되도록
  // 여기(큐)에 작업을 이어붙입니다.
  const queueRef = useRef<Promise<void>>(Promise.resolve());

  async function handleSearchMember() {
    const token = localStorage.getItem("token");
    if (!token) return;
    if (!keyword.trim()) return;

    const res = await fetch(`${API_URL}/loans/members?keyword=${encodeURIComponent(keyword.trim())}`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (!res.ok) return;

    const data: Member[] = await res.json();
    if (data.length === 1) {
      // 검색 결과가 정확히 1명이면, 모달 없이 바로 선택합니다.
      selectMember(data[0]);
    } else {
      // 결과가 0명이거나 여러 명이면 모달로 보여줍니다.
      setResults(data);
      setShowSearchModal(true);
    }
  }

  function selectMember(member: Member) {
    setSelectedMember(member);
    setResults([]);
    setKeyword("");
    setShowSearchModal(false);
  }

  function resetAll() {
    setSelectedMember(null);
    setResults([]);
    setKeyword("");
    setRegistrationNo("");
    setShowSearchModal(false);
  }

  // 실제로 서버에 대출 요청을 보내는 부분입니다. 큐에서 하나씩 순서대로 호출됩니다.
  async function processOne(regNo: string) {
    if (!selectedMember) return;
    const token = localStorage.getItem("token");
    if (!token) return;

    setProcessing(true);
    try {
      const res = await fetch(`${API_URL}/loans`, {
        method: "POST",
        headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
        body: JSON.stringify({ userId: selectedMember.id, registrationNo: regNo }),
      });
      const data = await res.json().catch(() => null);
      if (res.ok) {
        setHistory((prev) => [
          {
            id: data.id,
            registrationNo: data.registrationNo,
            ok: true,
            message: t("loans.success"),
            memberName: data.memberName,
            materialTitle: data.materialTitle,
            dueDate: data.dueDate,
          },
          ...prev,
        ]);
      } else {
        setHistory((prev) => [
          {
            id: Date.now(),
            registrationNo: regNo,
            ok: false,
            message: data?.message || t("loans.processing"),
          },
          ...prev,
        ]);
        notify("❌ " + (data?.message || regNo), "error");
      }
    } finally {
      setProcessing(false);
    }
  }

  // 등록번호 입력창에서 Enter를 누르면 호출됩니다. 큐 맨 뒤에 이번 작업을 이어붙입니다.
  function handleRegistrationSubmit() {
    const regNo = registrationNo.trim();
    if (!regNo) return;
    setRegistrationNo("");
    queueRef.current = queueRef.current.then(() => processOne(regNo));
  }

  return (
    <div className="p-6">
      <Tabs defaultValue="checkout">
        <TabsList className="gap-2">
          <TabsTrigger value="checkout">{t("loans.tabs.checkout")}</TabsTrigger>
          <TabsTrigger value="return">{t("loans.tabs.return")}</TabsTrigger>
        </TabsList>

        <TabsContent value="checkout" className="mt-4">
          <div className="grid max-w-3xl grid-cols-1 gap-4 md:grid-cols-2">
            {/* 왼쪽: 회원 검색 + 등록번호 입력 */}
            <div className="rounded-lg border border-neutral-200 bg-white p-4">
              <p className="mb-2 text-sm font-semibold">{t("loans.member.searchLabel")}</p>
              <div className="flex gap-2">
                <input
                  value={keyword}
                  onChange={(e) => setKeyword(e.target.value)}
                  onKeyDown={(e) => e.key === "Enter" && handleSearchMember()}
                  placeholder={t("loans.member.searchPlaceholder")}
                  className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                />
                <ThemedButton preset="버튼1" onClick={handleSearchMember}>
                  {t("loans.member.searchBtn")}
                </ThemedButton>
              </div>

              {selectedMember ? (
                <div className="mt-4">
                  <div className="mb-1 flex items-center justify-between">
                    <span className="text-sm text-neutral-500">{t("loans.registrationNo.label")}</span>
                    <button
                      type="button"
                      onClick={resetAll}
                      className="cursor-pointer rounded border px-2 py-1 text-xs"
                    >
                      {t("loans.member.changeBtn")}
                    </button>
                  </div>
                  <input
                    autoFocus
                    value={registrationNo}
                    onChange={(e) => setRegistrationNo(e.target.value)}
                    onKeyDown={(e) => e.key === "Enter" && handleRegistrationSubmit()}
                    placeholder={t("loans.registrationNo.placeholder")}
                    className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                  />
                  {processing && <p className="mt-2 text-xs text-neutral-400">{t("loans.processing")}</p>}
                </div>
              ) : (
                <p className="mt-3 text-sm text-neutral-400">{t("loans.member.selectHint")}</p>
              )}
            </div>

            {/* 오른쪽: 회원 정보 */}
            <div className="rounded-lg border border-neutral-200 bg-white p-4">
              <p className="mb-2 text-sm font-semibold">{t("loans.member.info.title")}</p>
              {selectedMember && (
                <div className="flex flex-col">
                  <InfoRow label={t("members.form.field.name")} value={selectedMember.name} />
                  <InfoRow label={t("members.form.field.memberNo")} value={selectedMember.memberNo || "-"} />
                  <InfoRow label={t("members.form.field.phone")} value={selectedMember.phone || "-"} />
                  <InfoRow
                    label={t("members.form.field.memberType")}
                    value={selectedMember.memberType?.name || "-"}
                  />
                  <InfoRow label={t("members.form.field.status")} value={t(`members.status.${selectedMember.status}`)} />
                  <InfoRow
                    label={t("members.form.field.birthDate")}
                    value={selectedMember.birthDate ? selectedMember.birthDate.slice(0, 10) : "-"}
                  />
                  <InfoRow label={t("members.form.field.email")} value={selectedMember.email || "-"} />
                  <InfoRow label={t("members.form.field.address")} value={selectedMember.address || "-"} />
                </div>
              )}
            </div>

            {/* 아래: 처리 내역 (두 박스를 합한 가로 길이) */}
            <div className="rounded-lg border border-neutral-200 bg-white p-4 md:col-span-2">
              <p className="mb-2 text-sm font-semibold">{t("loans.history.title")}</p>
              {history.length === 0 && <p className="text-sm text-neutral-400">{t("loans.history.empty")}</p>}
              <div className="flex flex-col gap-2">
                {history.map((h) => (
                  <div
                    key={h.id}
                    className={`rounded-lg border px-3 py-2 text-sm ${
                      h.ok ? "border-green-200 bg-green-50" : "border-red-200 bg-red-50"
                    }`}
                  >
                    <p className="font-medium">
                      {h.registrationNo} — {h.ok ? "✅" : "❌"} {h.message}
                    </p>
                    {h.ok && (
                      <p className="mt-0.5 text-xs text-neutral-500">
                        {h.memberName} · {h.materialTitle} · {t("loans.dueDateLabel")}:{" "}
                        {h.dueDate ? new Date(h.dueDate).toLocaleDateString() : "-"}
                      </p>
                    )}
                  </div>
                ))}
              </div>
            </div>
          </div>
        </TabsContent>

        <TabsContent value="return" className="mt-4">
          <p className="rounded-lg border border-dashed border-neutral-300 bg-white px-4 py-3 text-sm text-neutral-500">
            {t("loans.return.comingSoon")}
          </p>
        </TabsContent>
      </Tabs>

      {/* 회원 검색 결과 모달 */}
      {showSearchModal && (
        <div
          className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"
          onClick={() => setShowSearchModal(false)}
        >
          <div
            className="w-full max-w-sm rounded-xl bg-white p-6 shadow-xl"
            onClick={(e) => e.stopPropagation()}
          >
            <p className="mb-3 text-sm font-semibold">{t("loans.member.searchModalTitle")}</p>

            {results.length === 0 ? (
              <p className="text-sm text-neutral-400">{t("loans.member.noResults")}</p>
            ) : (
              <div className="flex flex-col gap-1">
                {results.map((m) => (
                  <button
                    key={m.id}
                    type="button"
                    onClick={() => selectMember(m)}
                    className="flex cursor-pointer items-center justify-between rounded-lg border border-neutral-200 px-3 py-2 text-left text-sm hover:bg-neutral-50"
                  >
                    <span className="font-medium">{m.name}</span>
                    <span className="text-neutral-500">
                      {m.memberNo ?? "-"} · {m.phone ?? "-"}
                    </span>
                  </button>
                ))}
              </div>
            )}

            <button
              type="button"
              onClick={() => setShowSearchModal(false)}
              className="mt-4 w-full cursor-pointer rounded-lg border border-neutral-200 py-2 text-sm text-neutral-500"
            >
              {t("loans.member.closeBtn")}
            </button>
          </div>
        </div>
      )}
    </div>
  );
}
```

파일을 저장하세요.

**바뀐 점 요약**

- 탭 문구가 "대출"/"반납"으로 줄었습니다.
- 회원 검색은 이름/회원번호로만 됩니다.
- 검색 결과가 여러 명이면 모달 팝업으로 보여주고, 결과가 없어도 모달로 안내합니다. 결과가 정확히 1명이면 모달 없이 바로 선택됩니다.
- '회원 검색' 박스와 '회원 정보' 박스가 처음부터 나란히 있습니다. 왼쪽 박스는 검색창과(선택 후에는) 등록번호 입력창을 담당하고, 오른쪽 박스는 선택된 회원의 상세 정보를 보여줍니다.
- '처리 내역' 박스는 `md:col-span-2`로 위 두 박스를 합한 가로 길이를 차지합니다.
- '회원 변경' 버튼이 '초기화'로 이름이 바뀌었고, 하는 일(선택 해제 + 입력값 비우기)은 그대로입니다.

---

## 확인하기

1. 백엔드 서버(`npm run start:dev`)와 프런트엔드 서버를 재시작하세요.
2. '대출/반납' 화면에서 탭 이름이 "대출"/"반납"으로 보이는지 확인하세요.
3. 처음 화면에서 '회원 검색' 박스와 '회원 정보' 박스가 나란히(수평으로) 있는지, '회원 정보' 박스에는 제목만 있고 값은 비어있는지 확인하세요.
4. 검색창 placeholder가 "이름 / 회원번호"로 보이는지 확인하세요.
5. 이름 전체나 회원번호로 검색했을 때, 검색 결과가 딱 1명이면 모달 없이 바로 '회원 정보' 박스에 값이 채워지고 등록번호 입력창이 나오는지 확인하세요.
6. 여러 명이 검색될 만한 키워드(예: 흔한 성)로 검색해서, 모달이 뜨고 이름/회원번호/휴대폰번호가 보이는지 확인하세요. 하나를 선택하면 모달이 닫히고 '회원 정보' 박스가 채워지는지 확인하세요.
7. 존재하지 않는 이름으로 검색해서, "검색 결과가 없습니다" 모달이 뜨는지 확인하세요. '닫기'를 누르면 모달이 닫히는지 확인하세요.
8. '회원 정보' 박스에 이름/회원번호/휴대폰번호/회원구분/상태/생년월일/이메일/주소가 모두 표시되는지 확인하세요. (값이 없는 항목은 "-"로 보이는지도 확인하세요.)
9. 자료 등록번호를 입력해서 정상적으로 대출 처리가 되는지, '처리 내역' 박스가 위 두 박스를 합한 너비로 넓게 보이는지 확인하세요.
10. '초기화' 버튼을 눌러서 회원 선택과 등록번호 입력창이 모두 사라지고 처음 상태로 돌아오는지 확인하세요.

---

## GitHub 커밋

모든 확인이 끝났다면 아래 명령어로 변경 내용을 저장하세요.

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선47: 대출/반납 화면 레이아웃 개편 (회원검색/회원정보 박스 분리, 검색 모달화)"
git push
```

---

## 최종 점검표

- [ ] `loans.service.ts`의 `findMembers`가 이름/회원번호로만 검색하고, 회원 정보 항목들을 함께 가져온다.
- [ ] `dictionary.ts`의 탭/검색/버튼 문구가 요청하신 대로 바뀌었다.
- [ ] 탭 이름이 "대출"/"반납"으로 보인다.
- [ ] '회원 검색'과 '회원 정보' 박스가 나란히 있다.
- [ ] 검색 결과가 여러 명이면 모달로 뜨고, 결과가 없으면 모달로 안내한다.
- [ ] 검색 결과가 1명이면 모달 없이 바로 선택된다.
- [ ] '회원 정보' 박스에 이름/회원번호/휴대폰번호/회원구분/상태/생년월일/이메일/주소가 모두 나온다.
- [ ] '처리 내역' 박스가 두 박스를 합한 너비로 나온다.
- [ ] '회원 변경' 버튼이 '초기화'로 바뀌었고 정상 동작한다.
- [ ] 변경 내용을 GitHub에 커밋 및 푸시했다.

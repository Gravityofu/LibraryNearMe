# 24단계 2: 대출 처리 화면 만들기

## 목표

'관리자' 메뉴에 있던 "대출/반납 (준비 중)" 자리를 실제로 동작하는 화면으로 채웁니다. 이번 단계에서는 그중 "대출 처리" 기능을 만듭니다. (반납 처리는 다음 단계에서 이어서 만들 예정이라, 이번에는 화면 안에 "반납 처리" 탭 자리만 미리 만들어두고 "다음 단계에서 추가될 예정" 안내만 보여줍니다.)

동작 방식은 이렇습니다.

1. 회원번호/이름/휴대폰번호/아이디로 회원을 검색해서 선택합니다.
2. 회원을 선택하면 자료 등록번호를 입력하는 칸이 나옵니다. 여기에 바코드를 스캔하거나(또는 단일 태그 RFID로 스캔하거나) 직접 숫자를 입력하고 Enter를 누르면 대출 처리가 됩니다.
3. 처리 결과(성공/실패 이유)가 화면 아래에 목록으로 쌓입니다.
4. 등록번호 입력은 이전 처리가 끝나기 전에 다음 입력이 들어와도, 순서대로 하나씩 처리되도록 "큐(줄서기)" 방식으로 처리합니다. (23단계에서 이야기했던, RFID가 여러 권을 연속으로 인식해서 빠르게 여러 번 입력이 들어오는 상황을 대비한 것입니다.)

대출 처리를 할 때는 다음 기준들을 모두 확인해서, 하나라도 어긋나면 대출을 막고 이유를 안내합니다.

- 자료가 지금 "이용가능" 상태인지 (이미 대출중이거나 분실/수선중이면 안내하고 막습니다.)
- 이 회원의 전체 대출 권수가 회원구분의 "최대 대출 권수"를 넘지 않는지
- 이 회원의 해당 자료 종류(예: 도서) 대출 권수가 그 자료 종류의 "대출 가능 권수"를 넘지 않는지
- (도서인 경우) 해당 KDC 하위 규칙에 걸리는 대출 권수가 그 규칙의 권수를 넘지 않는지

모든 조건을 통과하면, 자료의 상태를 "대출중"으로 바꾸고, 반납예정일(대출일 + 자료 종류의 대출 기간)을 계산해서 저장합니다.

---

## 1. 데이터베이스에 "대출 기록" 추가하기: `schema.prisma`

`C:\projects\LibraryNearMe\backend\prisma\schema.prisma` 파일을 여세요.

### 1-1. `Library` 모델에 대출 기록 연결고리 추가하기

아래 부분을 찾으세요.

```prisma
  memberTypes   MemberType[]   // ← 추가 (회원 구분 목록)
  loanSettings  LoanSetting[]  // ← 추가 (회원구분별 기본 대출 설정)
}
```

이렇게 바꿔주세요.

```prisma
  memberTypes   MemberType[]   // ← 추가 (회원 구분 목록)
  loanSettings  LoanSetting[]  // ← 추가 (회원구분별 기본 대출 설정)
  loans         Loan[]         // ← 추가 (대출 기록)
}
```

### 1-2. `User` 모델에 대출 기록 연결고리 추가하기

아래 부분을 찾으세요.

```prisma
  createdAt     DateTime   @default(now())

  @@unique([libraryId, loginId])              // 같은 도서관 안에서 아이디 중복 금지
  @@unique([libraryId, memberNo])             // 같은 도서관 안에서 회원증번호 중복 금지
}
```

이렇게 바꿔주세요.

```prisma
  createdAt     DateTime   @default(now())

  loans         Loan[]     // ← 추가 (이 회원의 대출 기록)

  @@unique([libraryId, loginId])              // 같은 도서관 안에서 아이디 중복 금지
  @@unique([libraryId, memberNo])             // 같은 도서관 안에서 회원증번호 중복 금지
}
```

### 1-3. `Copy` 모델에 대출 기록 연결고리 추가하기

아래 부분을 찾으세요.

```prisma
  createdAt      DateTime   @default(now())
  updatedAt      DateTime   @updatedAt

  @@unique([libraryId, registrationNo])
  @@index([materialId])
}
```

이렇게 바꿔주세요.

```prisma
  createdAt      DateTime   @default(now())
  updatedAt      DateTime   @updatedAt

  loans          Loan[]     // ← 추가 (이 실물의 대출 기록)

  @@unique([libraryId, registrationNo])
  @@index([materialId])
}
```

### 1-4. 새 모델 `Loan` 추가하기

`LoanSetting` 모델 바로 아래(파일 맨 끝)에 아래 내용을 새로 추가하세요.

```prisma

// 대출 기록 (누가, 어떤 실물을, 언제 빌려서 언제까지 반납해야 하는지)
model Loan {
  id          Int       @id @default(autoincrement())

  libraryId   Int
  library     Library   @relation(fields: [libraryId], references: [id])

  copyId      Int
  copy        Copy      @relation(fields: [copyId], references: [id])

  userId      Int
  user        User      @relation(fields: [userId], references: [id])

  loanDate    DateTime  @default(now())  // 대출한 날
  dueDate     DateTime                    // 반납 예정일
  returnedAt  DateTime?                   // 실제로 반납한 날짜/시각 (반납 전이면 비어있음)

  createdAt   DateTime  @default(now())

  @@index([libraryId])
  @@index([copyId])
  @@index([userId])
}
```

파일을 저장하세요.

터미널을 열어서 아래 명령어를 순서대로 입력하세요.

```
cd C:\projects\LibraryNearMe\backend
npx prisma migrate dev --name add_loan
```

명령어가 끝나면 정상적으로 실행되었는지 확인하세요.

---

## 2. 백엔드에 새 파일 만들기: `loans.service.ts`

`C:\projects\LibraryNearMe\backend\src` 안에 `loans`라는 새 폴더를 만들고, 그 안에 `loans.service.ts` 파일을 새로 만드세요. (경로: `C:\projects\LibraryNearMe\backend\src\loans\loans.service.ts`)

아래 내용을 그대로 넣으세요.

```ts
import { Injectable, BadRequestException, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma.service';

const AVAILABLE = '이용가능';
const ON_LOAN = '대출중';

@Injectable()
export class LoansService {
  constructor(private prisma: PrismaService) {}

  // 대출 화면에서 회원을 찾을 때 씁니다. 이름/휴대폰번호/아이디/회원번호 중 일부만 입력해도 찾을 수 있어요.
  async findMembers(libraryId: number, keyword: string) {
    const kw = (keyword || '').trim();
    if (!kw) return [];
    return this.prisma.user.findMany({
      where: {
        libraryId,
        role: 'MEMBER',
        status: 'ACTIVE',
        OR: [
          { name: { contains: kw } },
          { phone: { contains: kw } },
          { loginId: { contains: kw } },
          { memberNo: { contains: kw } },
        ],
      },
      select: {
        id: true,
        name: true,
        phone: true,
        memberNo: true,
        loginId: true,
        memberType: { select: { id: true, name: true } },
      },
      take: 10,
      orderBy: { name: 'asc' },
    });
  }

  // 등록번호로 실물 자료를 찾습니다.
  async findCopyByRegistrationNo(libraryId: number, registrationNo: string) {
    const copy = await this.prisma.copy.findFirst({
      where: { libraryId, registrationNo: (registrationNo || '').trim() },
      include: { material: true },
    });
    if (!copy) {
      throw new NotFoundException(`등록번호 '${registrationNo}' 자료를 찾을 수 없습니다.`);
    }
    return copy;
  }

  // 이 회원이 지금 반납하지 않은(대출중인) 자료가 몇 권인지 셉니다. extraWhere로 자료 종류/KDC 조건을 더할 수 있어요.
  private countActiveLoans(libraryId: number, userId: number, extraWhere: any = {}) {
    return this.prisma.loan.count({
      where: { libraryId, userId, returnedAt: null, ...extraWhere },
    });
  }

  // 대출 처리하기: 회원 1명 + 등록번호 1개를 받아서, 여러 조건을 확인한 뒤 대출을 만듭니다.
  async createLoan(libraryId: number, userId: number, registrationNo: string) {
    // 1. 회원 확인
    const member = await this.prisma.user.findFirst({
      where: { id: userId, libraryId },
      include: { memberType: true },
    });
    if (!member) {
      throw new NotFoundException('회원을 찾을 수 없습니다.');
    }
    if (member.status !== 'ACTIVE') {
      throw new BadRequestException('이용정지 상태이거나 대기중인 회원은 대출할 수 없습니다.');
    }

    // 2. 자료(실물) 확인
    const copy = await this.findCopyByRegistrationNo(libraryId, registrationNo);
    if (copy.status !== AVAILABLE) {
      throw new BadRequestException(
        `현재 대출 가능한 상태가 아닙니다. (현재 상태: ${copy.status})`,
      );
    }

    // 3. 자료 종류(자료 종류별 대출 설정) 확인
    const materialType = await this.prisma.materialType.findFirst({
      where: { libraryId, code: copy.material.type },
      include: { kdcRules: true },
    });
    if (!materialType || materialType.maxLoanCount === null || materialType.loanPeriodDays === null) {
      throw new BadRequestException(
        '이 자료 종류는 아직 대출 설정이 되어 있지 않습니다. 설정 → 대출 화면에서 먼저 설정해주세요.',
      );
    }

    // 4. 회원구분별 전체 대출 한도 확인
    const loanSetting = member.memberTypeId
      ? await this.prisma.loanSetting.findFirst({
          where: { libraryId, memberTypeId: member.memberTypeId },
        })
      : null;
    const overallMax = loanSetting?.maxLoanCount ?? 5;
    const overallCount = await this.countActiveLoans(libraryId, userId);
    if (overallCount >= overallMax) {
      throw new BadRequestException(`전체 대출 가능 권수(${overallMax}권)를 이미 채웠습니다.`);
    }

    // 5. 자료 종류별 대출 한도 확인
    const typeCount = await this.countActiveLoans(libraryId, userId, {
      copy: { material: { type: copy.material.type } },
    });
    if (typeCount >= materialType.maxLoanCount) {
      throw new BadRequestException(
        `'${materialType.nameKo}' 대출 가능 권수(${materialType.maxLoanCount}권)를 이미 채웠습니다.`,
      );
    }

    // 6. KDC 하위 규칙 확인 (해당하는 규칙이 있을 때만)
    if (materialType.kdcRules.length > 0 && copy.material.classNumber) {
      const matchedRule = materialType.kdcRules.find((r) =>
        copy.material.classNumber!.startsWith(r.kdcPrefix),
      );
      if (matchedRule) {
        const kdcCount = await this.countActiveLoans(libraryId, userId, {
          copy: {
            material: {
              type: copy.material.type,
              classNumber: { startsWith: matchedRule.kdcPrefix },
            },
          },
        });
        if (kdcCount >= matchedRule.maxLoanCount) {
          throw new BadRequestException(
            `'${matchedRule.label}(${matchedRule.kdcPrefix})' 대출 가능 권수(${matchedRule.maxLoanCount}권)를 이미 채웠습니다.`,
          );
        }
      }
    }

    // 7. 모든 조건 통과 → 대출 만들고, 자료 상태를 "대출중"으로 바꾸기
    const dueDate = new Date();
    dueDate.setDate(dueDate.getDate() + materialType.loanPeriodDays);

    const [loan] = await this.prisma.$transaction([
      this.prisma.loan.create({
        data: { libraryId, copyId: copy.id, userId, dueDate },
      }),
      this.prisma.copy.update({
        where: { id: copy.id },
        data: { status: ON_LOAN },
      }),
    ]);

    return {
      id: loan.id,
      memberName: member.name,
      materialTitle: copy.material.title,
      registrationNo: copy.registrationNo,
      loanDate: loan.loanDate,
      dueDate: loan.dueDate,
    };
  }
}
```

---

## 3. 백엔드에 새 파일 만들기: `loans.controller.ts`

같은 `C:\projects\LibraryNearMe\backend\src\loans` 폴더 안에 `loans.controller.ts` 파일을 새로 만드세요.

아래 내용을 그대로 넣으세요.

```ts
import { Controller, Get, Post, Body, Query, Req, UseGuards } from '@nestjs/common';
import { LoansService } from './loans.service';
import { AdminGuard } from '../auth/admin.guard';

@Controller('loans')
export class LoansController {
  constructor(private loansService: LoansService) {}

  // GET 요청: 회원 검색 — 관리자만
  @Get('members')
  @UseGuards(AdminGuard)
  findMembers(@Req() req: any, @Query('keyword') keyword: string) {
    return this.loansService.findMembers(req.user.libraryId, keyword);
  }

  // POST 요청: 대출 처리 — 관리자만
  @Post()
  @UseGuards(AdminGuard)
  create(@Req() req: any, @Body() body: { userId: number; registrationNo: string }) {
    return this.loansService.createLoan(req.user.libraryId, body.userId, body.registrationNo);
  }
}
```

---

## 4. 새로 만든 것을 앱에 등록하기: `app.module.ts`

`C:\projects\LibraryNearMe\backend\src\app.module.ts` 파일을 여세요.

### 4-1. import 추가하기

아래 부분을 찾으세요.

```ts
import { LoanSettingsController } from './settings/loan-settings.controller';
import { LoanSettingsService } from './settings/loan-settings.service';
```

이렇게 바꿔주세요.

```ts
import { LoanSettingsController } from './settings/loan-settings.controller';
import { LoanSettingsService } from './settings/loan-settings.service';
import { LoansController } from './loans/loans.controller';
import { LoansService } from './loans/loans.service';
```

### 4-2. `controllers` 목록에 추가하기

아래 부분을 찾으세요.

```ts
    MemberTypesController,
    LoanSettingsController,
  ],
```

이렇게 바꿔주세요.

```ts
    MemberTypesController,
    LoanSettingsController,
    LoansController,
  ],
```

### 4-3. `providers` 목록에 추가하기

아래 부분을 찾으세요.

```ts
    MemberTypesService,
    LoanSettingsService,
  ],
})
```

이렇게 바꿔주세요.

```ts
    MemberTypesService,
    LoanSettingsService,
    LoansService,
  ],
})
```

파일을 저장하세요.

---

## 5. 화면에 보일 문구 추가하기: `dictionary.ts`

`C:\projects\LibraryNearMe\frontend\src\lib\dictionary.ts` 파일을 여세요.

### 5-1. 한글(ko) 문구 추가하기

아래 부분을 찾으세요. (파일 앞쪽, 한글 블록입니다.)

```ts
    "settings.loan.member.modalTitle": "기본 대출 설정 수정",
    "settings.loan.material.modalTitle": "자료별 대출 설정 수정",
```

이렇게 바꿔주세요.

```ts
    "settings.loan.member.modalTitle": "기본 대출 설정 수정",
    "settings.loan.material.modalTitle": "자료별 대출 설정 수정",

    "loans.tabs.checkout": "대출 처리",
    "loans.tabs.return": "반납 처리",
    "loans.return.comingSoon": "반납 처리 기능은 다음 단계에서 추가될 예정이에요.",
    "loans.member.searchLabel": "회원 검색",
    "loans.member.searchPlaceholder": "이름 / 휴대폰 번호 / 아이디 / 회원번호",
    "loans.member.searchBtn": "검색",
    "loans.member.noResults": "검색 결과가 없습니다.",
    "loans.member.selectHint": "회원을 검색해서 선택하세요.",
    "loans.member.changeBtn": "회원 변경",
    "loans.registrationNo.label": "자료 등록번호",
    "loans.registrationNo.placeholder": "등록번호를 입력하거나 스캔한 뒤 Enter",
    "loans.history.title": "처리 내역",
    "loans.history.empty": "아직 처리한 내역이 없습니다.",
    "loans.success": "대출 처리되었습니다.",
    "loans.dueDateLabel": "반납예정일",
    "loans.processing": "처리 중...",
```

### 5-2. 영어(en) 문구 추가하기

아래 부분을 찾으세요. (파일 뒤쪽, 영어 블록입니다.)

```ts
    "settings.loan.member.modalTitle": "Edit Default Loan Setting",
    "settings.loan.material.modalTitle": "Edit Material Loan Setting",
```

이렇게 바꿔주세요.

```ts
    "settings.loan.member.modalTitle": "Edit Default Loan Setting",
    "settings.loan.material.modalTitle": "Edit Material Loan Setting",

    "loans.tabs.checkout": "Checkout",
    "loans.tabs.return": "Return",
    "loans.return.comingSoon": "Return processing will be added in the next step.",
    "loans.member.searchLabel": "Search Member",
    "loans.member.searchPlaceholder": "Name / phone / login ID / member no.",
    "loans.member.searchBtn": "Search",
    "loans.member.noResults": "No results found.",
    "loans.member.selectHint": "Search for a member and select one.",
    "loans.member.changeBtn": "Change Member",
    "loans.registrationNo.label": "Item Registration No.",
    "loans.registrationNo.placeholder": "Type or scan the registration number, then Enter",
    "loans.history.title": "History",
    "loans.history.empty": "No history yet.",
    "loans.success": "Checked out successfully.",
    "loans.dueDateLabel": "Due Date",
    "loans.processing": "Processing...",
```

### 5-3. '대출/반납' 메뉴 문구 바꾸기

아래 부분을 찾으세요.

```ts
    "admin.menu.loans": "대출/반납 (준비 중)",
```

이렇게 바꿔주세요.

```ts
    "admin.menu.loans": "대출/반납",
```

아래 부분도 찾으세요. (영어 블록)

```ts
    "admin.menu.loans": "Loans/Returns (soon)",
```

이렇게 바꿔주세요.

```ts
    "admin.menu.loans": "Loans/Returns",
```

파일을 저장하세요.

---

## 6. 관리자 메뉴에 '대출/반납' 링크 연결하기: `layout.tsx`

`C:\projects\LibraryNearMe\frontend\src\app\admin\layout.tsx` 파일을 여세요.

### 6-1. 브레드크럼(경로 안내) 추가하기

아래 부분을 찾으세요.

```tsx
const BREADCRUMB_MAP: Record<string, string[]> = {
  "/admin": ["admin.menu.dashboard"],
  "/admin/settings": ["admin.menu.systemSettings"],
  "/admin/materials/list": ["admin.menu.materialsList"],
  "/admin/materials/new": ["admin.menu.materialsList", "admin.menu.materialsNew"],
  "/admin/materials/copies": ["admin.menu.materialsList", "admin.menu.materialsCopy"],
  "/admin/members": ["admin.menu.members"],
};
```

이렇게 바꿔주세요.

```tsx
const BREADCRUMB_MAP: Record<string, string[]> = {
  "/admin": ["admin.menu.dashboard"],
  "/admin/settings": ["admin.menu.systemSettings"],
  "/admin/materials/list": ["admin.menu.materialsList"],
  "/admin/materials/new": ["admin.menu.materialsList", "admin.menu.materialsNew"],
  "/admin/materials/copies": ["admin.menu.materialsList", "admin.menu.materialsCopy"],
  "/admin/members": ["admin.menu.members"],
  "/admin/loans": ["admin.menu.loans"],
};
```

### 6-2. 사이드바에서 '대출/반납'을 클릭 가능한 링크로 바꾸기

아래 부분을 찾으세요.

```tsx
          <span className="px-3 py-2.5" style={{ color: hexToRgba(sidebarTextColor, 0.6) }}>
            {t("admin.menu.loans")}
          </span>
```

이렇게 바꿔주세요.

```tsx
          <Link href="/admin/loans" className={navClass("/admin/loans")}>
            {t("admin.menu.loans")}
          </Link>
```

파일을 저장하세요.

---

## 7. 대출 처리 화면 만들기: `page.tsx`

`C:\projects\LibraryNearMe\frontend\src\app\admin\loans` 폴더를 새로 만들고, 그 안에 `page.tsx` 파일을 새로 만드세요. (경로: `C:\projects\LibraryNearMe\frontend\src\app\admin\loans\page.tsx`)

아래 내용을 그대로 넣으세요.

```tsx
"use client";

import { useRef, useState } from "react";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import ThemedButton from "@/components/themed-button";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";

type Member = {
  id: number;
  name: string;
  phone: string | null;
  memberNo: string | null;
  loginId: string | null;
  memberType: { id: number; name: string } | null;
};

type HistoryItem = {
  id: number;
  registrationNo: string;
  ok: boolean;
  message: string;
  memberName?: string;
  materialTitle?: string;
  dueDate?: string;
};

export default function AdminLoansPage() {
  const { t } = useI18n();
  const { notify } = useNotify();

  const [keyword, setKeyword] = useState("");
  const [results, setResults] = useState<Member[]>([]);
  const [selectedMember, setSelectedMember] = useState<Member | null>(null);

  const [registrationNo, setRegistrationNo] = useState("");
  const [processing, setProcessing] = useState(false);
  const [history, setHistory] = useState<HistoryItem[]>([]);

  // 등록번호가 아무리 빠르게 여러 번 들어와도, 이전 처리가 끝난 뒤 순서대로 하나씩 처리되도록
  // 여기(큐)에 작업을 이어붙입니다.
  const queueRef = useRef<Promise<void>>(Promise.resolve());

  async function handleSearchMember() {
    const token = localStorage.getItem("token");
    if (!token) return;
    if (!keyword.trim()) {
      setResults([]);
      return;
    }
    const res = await fetch(`${API_URL}/loans/members?keyword=${encodeURIComponent(keyword.trim())}`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      setResults(await res.json());
    }
  }

  function selectMember(member: Member) {
    setSelectedMember(member);
    setResults([]);
    setKeyword("");
  }

  function changeMember() {
    setSelectedMember(null);
    setResults([]);
    setKeyword("");
    setRegistrationNo("");
  }

  // 실제로 서버에 대출 요청을 보내는 부분입니다. 큐에서 하나씩 순서대로 호출됩니다.
  async function processOne(regNo: string) {
    if (!selectedMember) return;
    const token = localStorage.getItem("token");
    if (!token) return;

    setProcessing(true);
    try {
      const res = await fetch(`${API_URL}/loans`, {
        method: "POST",
        headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
        body: JSON.stringify({ userId: selectedMember.id, registrationNo: regNo }),
      });
      const data = await res.json().catch(() => null);
      if (res.ok) {
        setHistory((prev) => [
          {
            id: data.id,
            registrationNo: data.registrationNo,
            ok: true,
            message: t("loans.success"),
            memberName: data.memberName,
            materialTitle: data.materialTitle,
            dueDate: data.dueDate,
          },
          ...prev,
        ]);
      } else {
        setHistory((prev) => [
          {
            id: Date.now(),
            registrationNo: regNo,
            ok: false,
            message: data?.message || t("loans.processing"),
          },
          ...prev,
        ]);
        notify("❌ " + (data?.message || regNo), "error");
      }
    } finally {
      setProcessing(false);
    }
  }

  // 등록번호 입력창에서 Enter를 누르면 호출됩니다. 큐 맨 뒤에 이번 작업을 이어붙입니다.
  function handleRegistrationSubmit() {
    const regNo = registrationNo.trim();
    if (!regNo) return;
    setRegistrationNo("");
    queueRef.current = queueRef.current.then(() => processOne(regNo));
  }

  return (
    <div className="p-6">
      <Tabs defaultValue="checkout">
        <TabsList className="gap-2">
          <TabsTrigger value="checkout">{t("loans.tabs.checkout")}</TabsTrigger>
          <TabsTrigger value="return">{t("loans.tabs.return")}</TabsTrigger>
        </TabsList>

        <TabsContent value="checkout" className="mt-4">
          <div className="flex max-w-xl flex-col gap-6">
            {!selectedMember ? (
              <div className="rounded-lg border border-neutral-200 bg-white p-4">
                <p className="mb-2 text-sm font-semibold">{t("loans.member.searchLabel")}</p>
                <div className="flex gap-2">
                  <input
                    value={keyword}
                    onChange={(e) => setKeyword(e.target.value)}
                    onKeyDown={(e) => e.key === "Enter" && handleSearchMember()}
                    placeholder={t("loans.member.searchPlaceholder")}
                    className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                  />
                  <ThemedButton preset="버튼1" onClick={handleSearchMember}>
                    {t("loans.member.searchBtn")}
                  </ThemedButton>
                </div>

                <div className="mt-3 flex flex-col gap-1">
                  {results.length === 0 && keyword.trim() !== "" && (
                    <p className="text-sm text-neutral-400">{t("loans.member.noResults")}</p>
                  )}
                  {results.length === 0 && keyword.trim() === "" && (
                    <p className="text-sm text-neutral-400">{t("loans.member.selectHint")}</p>
                  )}
                  {results.map((m) => (
                    <button
                      key={m.id}
                      type="button"
                      onClick={() => selectMember(m)}
                      className="flex cursor-pointer items-center justify-between rounded-lg border border-neutral-200 px-3 py-2 text-left text-sm hover:bg-neutral-50"
                    >
                      <span className="font-medium">{m.name}</span>
                      <span className="text-neutral-500">
                        {m.memberType?.name ?? "-"} · {m.phone ?? "-"} · {m.memberNo ?? "-"}
                      </span>
                    </button>
                  ))}
                </div>
              </div>
            ) : (
              <div className="rounded-lg border border-neutral-200 bg-white p-4">
                <div className="mb-3 flex items-center justify-between">
                  <div>
                    <p className="text-sm font-semibold">{selectedMember.name}</p>
                    <p className="text-xs text-neutral-500">
                      {selectedMember.memberType?.name ?? "-"} · {selectedMember.phone ?? "-"} ·{" "}
                      {selectedMember.memberNo ?? "-"}
                    </p>
                  </div>
                  <button
                    type="button"
                    onClick={changeMember}
                    className="cursor-pointer rounded border px-2 py-1 text-xs"
                  >
                    {t("loans.member.changeBtn")}
                  </button>
                </div>

                <label className="block">
                  <span className="mb-1 block text-sm text-neutral-500">{t("loans.registrationNo.label")}</span>
                  <input
                    autoFocus
                    value={registrationNo}
                    onChange={(e) => setRegistrationNo(e.target.value)}
                    onKeyDown={(e) => e.key === "Enter" && handleRegistrationSubmit()}
                    placeholder={t("loans.registrationNo.placeholder")}
                    className="w-full rounded-lg border border-neutral-200 px-3 py-2 text-sm"
                  />
                </label>
                {processing && <p className="mt-2 text-xs text-neutral-400">{t("loans.processing")}</p>}
              </div>
            )}

            <div className="rounded-lg border border-neutral-200 bg-white p-4">
              <p className="mb-2 text-sm font-semibold">{t("loans.history.title")}</p>
              {history.length === 0 && <p className="text-sm text-neutral-400">{t("loans.history.empty")}</p>}
              <div className="flex flex-col gap-2">
                {history.map((h) => (
                  <div
                    key={h.id}
                    className={`rounded-lg border px-3 py-2 text-sm ${
                      h.ok ? "border-green-200 bg-green-50" : "border-red-200 bg-red-50"
                    }`}
                  >
                    <p className="font-medium">
                      {h.registrationNo} — {h.ok ? "✅" : "❌"} {h.message}
                    </p>
                    {h.ok && (
                      <p className="mt-0.5 text-xs text-neutral-500">
                        {h.memberName} · {h.materialTitle} · {t("loans.dueDateLabel")}:{" "}
                        {h.dueDate ? new Date(h.dueDate).toLocaleDateString() : "-"}
                      </p>
                    )}
                  </div>
                ))}
              </div>
            </div>
          </div>
        </TabsContent>

        <TabsContent value="return" className="mt-4">
          <p className="rounded-lg border border-dashed border-neutral-300 bg-white px-4 py-3 text-sm text-neutral-500">
            {t("loans.return.comingSoon")}
          </p>
        </TabsContent>
      </Tabs>
    </div>
  );
}
```

파일을 저장하세요.

**바뀐 점 요약**

- '관리자' 사이드바의 "대출/반납" 메뉴가 이제 클릭 가능한 링크가 되어 `/admin/loans` 화면으로 이동합니다.
- 대출 처리 화면은 "회원 검색 → 선택 → 등록번호 반복 입력" 순서로 동작합니다.
- 등록번호 입력창에서 Enter를 누를 때마다 `queueRef`에 처리 작업이 하나씩 이어붙습니다. 이전 작업이 끝나야 다음 작업이 시작되므로, 아주 빠르게 여러 번 입력이 들어와도(예: 향후 다중 태그 RFID) 서버에 동시에 여러 요청이 겹쳐 들어가지 않고 순서대로 안전하게 처리됩니다.
- 처리 결과는 성공/실패와 함께 "처리 내역" 목록에 쌓입니다.
- '반납 처리' 탭은 자리만 만들어두고, 다음 단계에서 채울 예정입니다.

---

## 확인하기

1. 백엔드 서버(`npm run start:dev`)와 프런트엔드 서버를 재시작하세요.
2. '관리자' 화면의 사이드바에서 '대출/반납' 메뉴를 클릭해서 새 화면으로 이동하는지 확인하세요.
3. 회원 검색창에 회원 이름이나 회원번호 일부를 입력하고 검색해서, 결과가 나오는지 확인하세요.
4. 회원을 선택하면 등록번호 입력창이 나오는지 확인하세요.
5. 이용가능 상태인 실물 자료의 등록번호를 입력하고 Enter를 누르면, "처리 내역"에 성공으로 기록되고 반납예정일이 표시되는지 확인하세요.
6. '자료' 화면(또는 자료 관리 화면)에서 방금 대출 처리한 자료의 상태가 "대출중"으로 바뀌었는지 확인하세요.
7. 방금 대출한 등록번호를 다시 한번 입력해보세요. 이미 "대출중" 상태라서 대출이 막히고 실패 메시지가 뜨는지 확인하세요.
8. 회원의 최대 대출 권수(예: 5권)를 넘도록 서로 다른 등록번호를 계속 입력해보세요. 한도를 넘으면 "전체 대출 가능 권수(5권)를 이미 채웠습니다" 같은 메시지와 함께 막히는지 확인하세요.
9. '회원 변경' 버튼을 눌러서 다른 회원으로 바꿀 수 있는지 확인하세요.
10. '반납 처리' 탭을 눌러서 "다음 단계에서 추가될 예정" 안내가 보이는지 확인하세요.

---

## GitHub 커밋

모든 확인이 끝났다면 아래 명령어로 변경 내용을 저장하세요.

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "24단계2: 대출 처리 화면 및 대출 한도 검증 로직 추가"
git push
```

---

## 최종 점검표

- [ ] `schema.prisma`에 `Loan` 모델을 추가하고 마이그레이션을 실행했다.
- [ ] `loans.service.ts`, `loans.controller.ts` 파일을 새로 만들었다.
- [ ] `app.module.ts`에 `LoansController`/`LoansService`를 등록했다.
- [ ] `dictionary.ts`에 대출 화면 관련 한글/영어 문구를 추가했다.
- [ ] 관리자 사이드바의 '대출/반납' 메뉴가 클릭 가능한 링크로 바뀌었다.
- [ ] `/admin/loans` 화면에서 회원 검색 → 선택 → 등록번호 입력 순서로 대출 처리가 된다.
- [ ] 이미 대출중인 자료는 다시 대출할 수 없다.
- [ ] 회원구분별 최대 대출 권수를 넘으면 대출이 막힌다.
- [ ] 자료 종류별 대출 가능 권수를 넘으면 대출이 막힌다.
- [ ] (해당하는 경우) KDC 하위 규칙의 대출 가능 권수를 넘으면 대출이 막힌다.
- [ ] 대출이 성공하면 자료 상태가 "대출중"으로 바뀌고 반납예정일이 계산된다.
- [ ] 변경 내용을 GitHub에 커밋 및 푸시했다.

---

## 다음 단계 안내

다음 단계(24단계 3)에서는 '반납 처리' 탭을 채워서, 등록번호를 입력하면 해당 대출 기록을 찾아 반납 처리하고, 연체된 경우 연체 일수를 계산해서 "최대 대출 제한 일수" 설정에 따라 대출 정지 기간을 적용하는 기능을 만들 예정입니다.

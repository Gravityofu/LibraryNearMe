# 개선100: '예약' 페이지 디자인 만들기 (화면만, 기능은 다음 단계)

## 목표

'대출/반납 → 예약' 탭에 지금까지는 "예약 기능은 다음 단계에서 추가될 예정이에요"라는 안내 문구만 있었습니다. 이번 가이드는 말씀하신 디자인대로 화면의 틀을 만듭니다.

- 상단: '대출' 탭처럼 테두리 있는 흰 박스 하나, 그 안에 메뉴 버튼 5개 — **예약 중인 자료 / 연체 중인 예약자료 / 보관 중인 예약자료 / 보관일이 지난 예약자료 / 예약 이력**
- 중간: '목록' 화면과 똑같은 디자인의 표
- 하단 오른쪽: '자료 등록'과 같은 스타일의 버튼 — **예약하기**

**이번 가이드는 화면 디자인(레이아웃과 모양)까지만 만듭니다.** 상단 5개 메뉴 버튼을 누르면 선택된 버튼의 모양만 바뀌고, 표는 아직 실제 예약 데이터와 연결되어 있지 않아서 항상 "표시할 예약 자료가 없습니다"로 나옵니다. '예약하기' 버튼을 눌러도 아직은 "다음 단계에서 추가될 예정" 안내만 뜹니다. 화면을 보시고 마음에 드시면, 실제 예약 데이터(예약 신청 만들기, 상태 계산 등)를 연결하는 다음 단계로 진행하면 됩니다.

**이 파일에 대한 안내:** `frontend/src/app/admin/loans/page.tsx`는 1,500줄이 넘는 아주 큰 파일이라, 지난번처럼 이번에도 필요한 부분만 찾아서 바꾸는 방식으로 안내드립니다. 세 군데를 순서대로 고치시면 됩니다.

---

## 1단계: 화면에 나올 문구 추가하기 (`dictionary.ts`)

`frontend/src/lib/dictionary.ts` 파일을 여세요.

### 1-1. 한국어 문구 추가하기

아래 내용을 찾으세요.

```typescript
    "loans.reservation.comingSoon": "예약 기능은 다음 단계에서 추가될 예정이에요.",
```

이 내용을 아래 내용으로 바꿔주세요.

```typescript
    "loans.reservation.comingSoon": "예약 기능은 다음 단계에서 추가될 예정이에요.",
    "loans.reservation.filter.reserved": "예약 중인 자료",
    "loans.reservation.filter.overdue": "연체 중인 예약자료",
    "loans.reservation.filter.holding": "보관 중인 예약자료",
    "loans.reservation.filter.holdExpired": "보관일이 지난 예약자료",
    "loans.reservation.filter.history": "예약 이력",
    "loans.reservation.addBtn": "예약하기",
    "loans.reservation.noResults": "표시할 예약 자료가 없습니다.",
    "loans.reservation.col.no": "번호",
    "loans.reservation.col.status": "예약상태",
    "loans.reservation.col.memberNo": "회원번호",
    "loans.reservation.col.memberName": "회원이름",
    "loans.reservation.col.registrationNo": "등록번호",
    "loans.reservation.col.title": "자료명",
    "loans.reservation.col.creator": "저자",
    "loans.reservation.col.reservedDate": "예약일",
    "loans.reservation.col.holdDueDate": "보관예정일",
    "loans.reservation.col.location": "소장처",
```

### 1-2. 영어 문구 추가하기

아래 내용을 찾으세요.

```typescript
    "loans.reservation.comingSoon": "Reservation feature will be added in the next step.",
```

이 내용을 아래 내용으로 바꿔주세요.

```typescript
    "loans.reservation.comingSoon": "Reservation feature will be added in the next step.",
    "loans.reservation.filter.reserved": "Reserved",
    "loans.reservation.filter.overdue": "Overdue Reservations",
    "loans.reservation.filter.holding": "On Hold",
    "loans.reservation.filter.holdExpired": "Hold Period Expired",
    "loans.reservation.filter.history": "Reservation History",
    "loans.reservation.addBtn": "New Reservation",
    "loans.reservation.noResults": "No reservations to show.",
    "loans.reservation.col.no": "No.",
    "loans.reservation.col.status": "Status",
    "loans.reservation.col.memberNo": "Member No.",
    "loans.reservation.col.memberName": "Member Name",
    "loans.reservation.col.registrationNo": "Reg. No.",
    "loans.reservation.col.title": "Title",
    "loans.reservation.col.creator": "Creator",
    "loans.reservation.col.reservedDate": "Reserved Date",
    "loans.reservation.col.holdDueDate": "Hold Due Date",
    "loans.reservation.col.location": "Location",
```

---

## 2단계: 상단 5개 메뉴 버튼 정보와 표 칸 개수를 미리 정의하기

**무엇을, 왜 바꾸나요:** `loans/page.tsx` 파일 위쪽, 다른 상수(`HISTORY_COLUMN_COUNT` 등)들이 모여 있는 자리에 두 가지를 추가합니다. 상단 5개 메뉴 버튼의 이름표(키)와 문구를 배열로 정의한 `RESERVATION_VIEWS`, 그리고 표의 칸 개수를 나타내는 `RESERVATION_COLUMN_COUNT`입니다.

`frontend/src/app/admin/loans/page.tsx` 파일을 여세요.

아래 내용을 찾으세요.

```typescript
const HISTORY_PAGE_SIZE_OPTIONS = [10, 20, 30, 40, 50];
const HISTORY_COLUMN_COUNT = 11;
```

이 내용을 아래 내용으로 바꿔주세요.

```typescript
const HISTORY_PAGE_SIZE_OPTIONS = [10, 20, 30, 40, 50];
const HISTORY_COLUMN_COUNT = 11;

// '예약' 탭 상단의 5개 메뉴 버튼입니다. key는 지금 화면에서 어떤 버튼이 선택됐는지 구분하는 용도로만 쓰이고,
// 아직 실제 예약 데이터와 연결되어 있지는 않습니다. (다음 단계에서 이 key별로 실제 데이터를 불러오게 됩니다.)
const RESERVATION_VIEWS: { key: string; labelKey: string }[] = [
  { key: "RESERVED", labelKey: "loans.reservation.filter.reserved" },
  { key: "OVERDUE", labelKey: "loans.reservation.filter.overdue" },
  { key: "HOLDING", labelKey: "loans.reservation.filter.holding" },
  { key: "HOLD_EXPIRED", labelKey: "loans.reservation.filter.holdExpired" },
  { key: "HISTORY", labelKey: "loans.reservation.filter.history" },
];
const RESERVATION_COLUMN_COUNT = 10;
```

이어서 조금 아래로 내려가면 `activeTab`(지금 열려있는 탭)을 관리하는 부분이 나옵니다. 아래 내용을 찾으세요.

```typescript
  // 지금 어느 탭('checkout' 대출 / 'return' 반납)이 열려 있는지 직접 관리합니다.
  // 반납 탭으로 넘어왔을 때 등록번호 입력폼으로 커서를 자동으로 옮기기 위해 필요합니다.
  const [activeTab, setActiveTab] = useState("checkout");
```

이 내용을 아래 내용으로 바꿔주세요.

```typescript
  // 지금 어느 탭('checkout' 대출 / 'return' 반납)이 열려 있는지 직접 관리합니다.
  // 반납 탭으로 넘어왔을 때 등록번호 입력폼으로 커서를 자동으로 옮기기 위해 필요합니다.
  const [activeTab, setActiveTab] = useState("checkout");

  // '예약' 탭 상단 5개 메뉴 버튼 중 지금 선택된 것이 무엇인지 기억합니다. (기본값: '예약 중인 자료')
  const [reservationView, setReservationView] = useState("RESERVED");
```

---

## 3단계: '예약' 탭 화면 만들기

**무엇을, 왜 바꾸나요:** 지금까지 '예약' 탭에는 안내 문구 한 줄만 있었습니다. 이 부분을 말씀하신 디자인대로 바꿉니다.

- 맨 위: '대출' 탭 상단과 같은 스타일(테두리 있는 흰 박스)의 박스 안에 5개 메뉴 버튼. 선택된 버튼은 진회색으로 채워지고, 선택 안 된 버튼은 흰 바탕에 테두리만 있습니다.
- 가운데: '목록' 화면과 같은 디자인(스크롤되는 흰 박스, 회색 머리글, 줄무늬 없는 표)의 표. 아직 실제 데이터가 없어서 항상 "표시할 예약 자료가 없습니다"로 나옵니다.
- 맨 아래 오른쪽: '자료 등록' 버튼과 같은 스타일의 '예약하기' 버튼. 지금 누르면 "다음 단계에서 추가될 예정" 안내가 뜹니다.

`frontend/src/app/admin/loans/page.tsx` 파일에서 아래 내용을 찾으세요.

```typescript
        <TabsContent value="reservation" className="mt-4">
          <p className="rounded-lg border border-dashed border-neutral-300 bg-white px-4 py-3 text-sm text-neutral-500">
            {t("loans.reservation.comingSoon")}
          </p>
        </TabsContent>
```

이 내용을 아래 내용으로 바꿔주세요.

```typescript
        <TabsContent value="reservation" className="mt-4">
          <div className="flex flex-col gap-4">
            {/* 상단 메뉴 박스: '대출' 탭 상단 박스와 같은 스타일입니다.
                지금은 화면 디자인만 만든 단계라, 버튼을 누르면 선택된 모양만 바뀌고
                표에 나오는 내용은 아직 바뀌지 않습니다. */}
            <div className="flex flex-wrap items-center gap-2 rounded-lg border border-neutral-200 bg-white p-4">
              {RESERVATION_VIEWS.map((view) => (
                <button
                  key={view.key}
                  type="button"
                  onClick={() => setReservationView(view.key)}
                  className={`cursor-pointer rounded-full border px-4 py-2 text-sm font-medium transition-colors ${
                    reservationView === view.key
                      ? "border-[#383838] bg-[#383838] text-[#F9F6F0]"
                      : "border-neutral-200 bg-white text-neutral-700 hover:bg-neutral-50"
                  }`}
                >
                  {t(view.labelKey)}
                </button>
              ))}
            </div>

            {/* 중간 표 영역: '목록' 화면과 같은 디자인입니다. 아직 예약 데이터를 연결하지 않아 항상 빈 상태입니다. */}
            <div className="max-h-[65vh] overflow-auto rounded-lg border border-neutral-200 bg-white">
              <table className="w-full min-w-[900px] text-left text-sm">
                <thead className="sticky top-0 bg-neutral-100 text-neutral-500">
                  <tr>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.no")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.status")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.memberNo")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.memberName")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.registrationNo")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.title")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.creator")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.reservedDate")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.holdDueDate")}</th>
                    <th className="px-4 py-2.5">{t("loans.reservation.col.location")}</th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-neutral-100">
                  <tr>
                    <td colSpan={RESERVATION_COLUMN_COUNT} className="px-4 py-6 text-center text-neutral-400">
                      {t("loans.reservation.noResults")}
                    </td>
                  </tr>
                </tbody>
              </table>
            </div>

            {/* 하단 버튼 영역: '목록' 화면의 '자료 등록' 버튼과 같은 위치·스타일입니다. */}
            <div className="flex justify-end pt-2">
              <button
                type="button"
                onClick={() => notify(t("loans.reservation.comingSoon"), "info")}
                className="cursor-pointer rounded-lg bg-[#383838] px-5 py-2.5 text-sm font-semibold text-[#F9F6F0]"
              >
                {t("loans.reservation.addBtn")}
              </button>
            </div>
          </div>
        </TabsContent>
```

---

## 확인하기

1. `frontend` 서버를 재시작해 주세요.
2. '대출/반납 → 예약' 탭을 열어보세요.
3. 상단에 5개 메뉴 버튼(예약 중인 자료 / 연체 중인 예약자료 / 보관 중인 예약자료 / 보관일이 지난 예약자료 / 예약 이력)이 흰 박스 안에 나오는지 확인해 주세요.
4. 버튼을 눌러보면서 선택된 버튼만 진회색으로 바뀌는지 확인해 주세요.
5. 그 아래 '목록' 화면과 같은 디자인의 표가 나오고, "표시할 예약 자료가 없습니다"라고 나오는지 확인해 주세요.
6. 표 아래 오른쪽에 '예약하기' 버튼이 '자료 등록' 버튼과 같은 스타일로 나오는지 확인해 주세요.
7. '예약하기' 버튼을 누르면 "예약 기능은 다음 단계에서 추가될 예정이에요" 안내가 뜨는지 확인해 주세요.

---

## GitHub 커밋

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "개선100: 예약 탭 화면 디자인 추가 (상단 5개 메뉴 + 목록형 표 + 예약하기 버튼)"
git push
```

---

## 최종 점검표

- [ ] '예약' 탭 상단에 5개 메뉴 버튼이 흰 박스 안에 나온다.
- [ ] 버튼을 누르면 선택된 버튼만 진회색으로 표시된다.
- [ ] '목록' 화면과 같은 디자인의 표가 나온다.
- [ ] 표에 "표시할 예약 자료가 없습니다" 안내가 나온다.
- [ ] '예약하기' 버튼이 '자료 등록' 버튼과 같은 스타일로, 오른쪽 아래에 나온다.
- [ ] '예약하기' 버튼을 누르면 안내 메시지가 뜬다.
- [ ] GitHub에 커밋 및 푸시를 완료했다.

---

화면을 보시고 마음에 드시면, 다음 단계로 실제 예약 데이터(예약 신청 만들기, 상태 계산, 5개 메뉴별로 실제 자료 목록 보여주기 등)를 연결하는 가이드를 이어서 만들어 드리겠습니다.

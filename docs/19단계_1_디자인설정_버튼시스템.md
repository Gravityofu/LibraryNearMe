# 19단계 (1) — "디자인" 탭 신설 + Footer/사이드바 색상 분리 + 재사용 가능한 버튼 색상 시스템

> 목표: 관리자 "설정" 화면에 "디자인" 탭을 새로 만들고, 지금 "도서관" 탭에
> 있는 Footer/사이드바 배경색·글자색을 이 탭으로 옮기면서 **Footer 색상과
> 사이드바 색상을 서로 다르게** 설정할 수 있도록 나눕니다. 그리고 "버튼1"
> 같은 이름으로 배경색·글자색을 저장해두고, 코드에서 그 이름만 지정하면
> 자동으로 그 색이 적용되는 **재사용 버튼 시스템**을 만듭니다. 나중에
> "버튼2"를 관리자 설정에서 추가하고 코드에서 `preset="버튼2"`라고만
> 적으면, 그 버튼도 바로 관리자 설정 색상을 따라가게 됩니다.
> 소요 시간: 약 50분 (오늘은 분량이 많아요. 순서대로 천천히 진행해주세요.)

---

## 오늘 만드는 것 (그림으로)

```
관리자 → 설정 화면
┌────────────────────────────────┐
│ [KORMARC 태그] [도서관] [디자인]    │  ← "디자인" 탭 신설
└────────────────────────────────┘

"디자인" 탭 안
┌────────────────────────────────┐
│ Footer 색상                       │
│  배경색 [■]   글자색 [■]           │
│                                  │
│ 사이드바 색상                      │
│  배경색 [■]   글자색 [■]           │
│                                  │
│ 버튼 스타일                       │
│  ┌ 버튼1 ─────────────┐          │
│  │ 배경색 [■]  글자색 [■]│          │
│  └──────────────────┘          │
│  [+ 새 버튼 추가]                  │
│                                  │
│  [저장하기]                       │
└────────────────────────────────┘

코드에서 버튼을 만들 때:
<ThemedButton preset="버튼1">저장</ThemedButton>
   → 관리자 설정의 "버튼1" 색상을 자동으로 읽어와서 적용됨
   → 나중에 관리자가 "버튼1" 색을 바꾸면, 이 버튼도 자동으로 바뀜
```

---

## 1. 설계도(schema.prisma) 수정 — 약 8분

`backend/prisma/schema.prisma` 를 엽니다. `model Library { ... }` 안에서
아래 두 줄을 찾으세요.

```prisma
  chromeBgColor   String   @default("#383838")             // 오늘 추가: Footer/사이드바 배경색
  chromeTextColor String   @default("#F9F6F0")              // 오늘 추가: Footer/사이드바 글자색
```

이 두 줄을 지우고 아래 다섯 줄로 바꿔주세요. (배경색·글자색을 Footer용과
사이드바용으로 각각 나누고, 버튼 스타일 목록을 담을 칸을 새로 추가했어요.)

```prisma
  footerBgColor    String @default("#383838")                                        // Footer 배경색
  footerTextColor  String @default("#F9F6F0")                                         // Footer 글자색
  sidebarBgColor   String @default("#383838")                                         // 사이드바 배경색
  sidebarTextColor String @default("#F9F6F0")                                         // 사이드바 글자색
  buttonStyles     Json   @default("[{\"name\": \"버튼1\", \"bgColor\": \"#383838\", \"textColor\": \"#F9F6F0\"}]")  // 버튼 색상 목록
```

- `Json` 타입은 "표 하나로는 표현하기 애매한, 자유로운 모양의 데이터"를
  저장할 때 써요. 버튼은 몇 개가 될지 미리 정해져 있지 않아서(나중에
  "버튼2", "버튼3"이 계속 늘어날 수 있으니) 목록(배열) 형태로 통째로
  저장하는 게 편해요.
- 처음에는 "버튼1" 하나가 기본으로 들어있어요.

터미널에서 마이그레이션을 실행하세요.

```
cd C:\projects\LibraryNearMe\backend
npx prisma migrate dev --name add_design_settings
```

> ⚠️ `chromeBgColor`/`chromeTextColor` 칸을 지웠기 때문에, 터미널에
> "이 칸을 지우면 그 안의 데이터가 사라집니다"라는 경고가 뜰 수 있어요.
> 이 칸은 오늘 새 칸(`footerBgColor` 등)으로 대체되는 것이니 그대로
> 진행(y 입력 등)하시면 됩니다.

✅ **확인하기**: 에러 없이 끝나면 성공입니다.

---

## 2. 백엔드 — 저장 API 수정 — 약 5분

`backend/src/library/library.service.ts` 를 엽니다. `updateLibrary` 함수
전체를 아래로 바꿔주세요.

```typescript
  // 도서관 설정 바꿔서 저장하기
  async updateLibrary(data: {
    name: string;
    primaryColor: string;
    logoUrl?: string;
    footerVersion?: string;
    footerCopyright?: string;
    footerBgColor?: string;
    footerTextColor?: string;
    sidebarBgColor?: string;
    sidebarTextColor?: string;
    buttonStyles?: any;
  }) {
    const library = await this.prisma.library.findFirst();
    if (!library) return null;

    return this.prisma.library.update({
      where: { id: library.id },
      data: {
        name: data.name,
        primaryColor: data.primaryColor,
        logoUrl: data.logoUrl || undefined,
        footerVersion: data.footerVersion || undefined,
        footerCopyright: data.footerCopyright || undefined,
        footerBgColor: data.footerBgColor || undefined,
        footerTextColor: data.footerTextColor || undefined,
        sidebarBgColor: data.sidebarBgColor || undefined,
        sidebarTextColor: data.sidebarTextColor || undefined,
        buttonStyles: data.buttonStyles ?? undefined,
      },
    });
  }
```

`backend/src/library/library.controller.ts` 는 이미 `body: any`로 받고
있어서 따로 고칠 게 없어요.

저장하면 backend가 재시작됩니다.

---

## 3. dictionary.ts 문구 추가 — 약 3분

`frontend/src/lib/dictionary.ts` 를 엽니다. `"settings.tabs.library"` 줄
바로 아래(한글·영어 각각)에 추가하세요.

**한글 쪽:**

```ts
    "settings.tabs.design": "디자인",
    "design.footerSection": "Footer 색상",
    "design.footerBgColor": "Footer 배경색",
    "design.footerTextColor": "Footer 글자색",
    "design.sidebarSection": "사이드바 색상",
    "design.sidebarBgColor": "사이드바 배경색",
    "design.sidebarTextColor": "사이드바 글자색",
    "design.buttonSection": "버튼 스타일",
    "design.buttonBgColor": "배경색",
    "design.buttonTextColor": "글자색",
    "design.addButtonStyle": "+ 새 버튼 추가",
```

**영어 쪽:**

```ts
    "settings.tabs.design": "Design",
    "design.footerSection": "Footer Colors",
    "design.footerBgColor": "Footer Background",
    "design.footerTextColor": "Footer Text",
    "design.sidebarSection": "Sidebar Colors",
    "design.sidebarBgColor": "Sidebar Background",
    "design.sidebarTextColor": "Sidebar Text",
    "design.buttonSection": "Button Styles",
    "design.buttonBgColor": "Background",
    "design.buttonTextColor": "Text",
    "design.addButtonStyle": "+ Add Button Style",
```

---

## 4. "도서관" 탭에서 Footer/사이드바 색상 칸 삭제 — 약 5분

`frontend/src/components/library-settings-form.tsx` 를 엽니다.

아래 부분을 찾으세요.

```tsx
  const [logoUrl, setLogoUrl] = useState("");
  const [chromeBgColor, setChromeBgColor] = useState("#383838");
  const [chromeTextColor, setChromeTextColor] = useState("#F9F6F0");
  const [footerVersion, setFooterVersion] = useState("1.0.0");
```

이렇게 바꿔주세요. (색상 두 개를 관리하던 상태를 지웠어요. 이제 "디자인"
탭이 그 역할을 해요.)

```tsx
  const [logoUrl, setLogoUrl] = useState("");
  const [footerVersion, setFooterVersion] = useState("1.0.0");
```

아래 부분을 찾으세요.

```tsx
          setLogoUrl(data.logoUrl || "");
          setChromeBgColor(data.chromeBgColor || "#383838");
          setChromeTextColor(data.chromeTextColor || "#F9F6F0");
          setFooterVersion(data.footerVersion || "1.0.0");
```

이렇게 바꿔주세요.

```tsx
          setLogoUrl(data.logoUrl || "");
          setFooterVersion(data.footerVersion || "1.0.0");
```

아래 부분을 찾으세요.

```tsx
      body: JSON.stringify({
        name,
        primaryColor,
        logoUrl,
        chromeBgColor,
        chromeTextColor,
        footerVersion,
        footerCopyright,
      }),
```

이렇게 바꿔주세요.

```tsx
      body: JSON.stringify({
        name,
        primaryColor,
        logoUrl,
        footerVersion,
        footerCopyright,
      }),
```

아래 부분을 찾으세요.

```tsx
        <div className="flex flex-col gap-2">
          <Label htmlFor="chromeBg">{t("admin.settings.chromeBgColor")}</Label>
          <Input
            id="chromeBg"
            type="color"
            value={chromeBgColor}
            onChange={(e) => setChromeBgColor(e.target.value)}
            className="h-10 w-20 p-1"
          />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="chromeText">{t("admin.settings.chromeTextColor")}</Label>
          <Input
            id="chromeText"
            type="color"
            value={chromeTextColor}
            onChange={(e) => setChromeTextColor(e.target.value)}
            className="h-10 w-20 p-1"
          />
        </div>
        <div className="flex flex-col gap-2">
          <Label htmlFor="footerVersion">{t("admin.settings.footerVersion")}</Label>
```

이 블록에서 `chromeBg`/`chromeText` 두 개의 `<div>` 통째로 지워주세요.
이렇게 남으면 됩니다.

```tsx
        <div className="flex flex-col gap-2">
          <Label htmlFor="footerVersion">{t("admin.settings.footerVersion")}</Label>
```

---

## 5. 색상 변환 도구 함수 만들기 — 약 3분

`frontend/src/lib` 폴더 안에 **`color.ts`** 파일을 새로 만들고 아래
내용을 붙여넣습니다.

```ts
// "#383838" 같은 색상 코드를, 투명도가 있는 "rgba(56, 56, 56, 0.6)" 형태로 바꿔줍니다.
// 사이드바의 흐린 글자(부제목 등)를 관리자가 고른 색에 맞춰 자동으로 흐리게 만들 때 씁니다.
export function hexToRgba(hex: string, alpha: number): string {
  const clean = hex.replace("#", "");
  const bigint = parseInt(clean, 16);
  const r = (bigint >> 16) & 255;
  const g = (bigint >> 8) & 255;
  const b = bigint & 255;
  return `rgba(${r}, ${g}, ${b}, ${alpha})`;
}
```

---

## 6. "테마(색상) 정보"를 앱 전체에 공급하는 부품 만들기 — 약 8분

지금까지 언어(`LanguageProvider`), 알림(`NotifyProvider`), 로그인상태
(`AuthProvider`)를 앱 전체에 공급했던 것과 똑같은 방식으로, 색상 정보도
공급하는 부품을 만들어요.

`frontend/src/components` 폴더 안에 **`theme-provider.tsx`** 파일을 새로
만들고 아래 내용을 붙여넣습니다.

```tsx
"use client";

import { createContext, useContext, useEffect, useState } from "react";

export type ButtonStyle = {
  name: string;
  bgColor: string;
  textColor: string;
};

type ThemeState = {
  footerBgColor: string;
  footerTextColor: string;
  sidebarBgColor: string;
  sidebarTextColor: string;
  buttonStyles: ButtonStyle[];
};

const DEFAULT_THEME: ThemeState = {
  footerBgColor: "#383838",
  footerTextColor: "#F9F6F0",
  sidebarBgColor: "#383838",
  sidebarTextColor: "#F9F6F0",
  buttonStyles: [{ name: "버튼1", bgColor: "#383838", textColor: "#F9F6F0" }],
};

const API_URL = "http://localhost:3001";

const ThemeContext = createContext<ThemeState>(DEFAULT_THEME);

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<ThemeState>(DEFAULT_THEME);

  useEffect(() => {
    fetch(`${API_URL}/library`)
      .then((res) => res.json())
      .then((data) => {
        if (!data) return;
        setTheme({
          footerBgColor: data.footerBgColor || DEFAULT_THEME.footerBgColor,
          footerTextColor: data.footerTextColor || DEFAULT_THEME.footerTextColor,
          sidebarBgColor: data.sidebarBgColor || DEFAULT_THEME.sidebarBgColor,
          sidebarTextColor: data.sidebarTextColor || DEFAULT_THEME.sidebarTextColor,
          buttonStyles:
            Array.isArray(data.buttonStyles) && data.buttonStyles.length > 0
              ? data.buttonStyles
              : DEFAULT_THEME.buttonStyles,
        });
      })
      .catch(() => {});
  }, []);

  return <ThemeContext.Provider value={theme}>{children}</ThemeContext.Provider>;
}

// 다른 화면에서는 이 훅(useTheme) 하나만 불러서 색상 정보를 쓸 수 있어요.
export function useTheme() {
  return useContext(ThemeContext);
}
```

`frontend/src/app/layout.tsx` 를 엽니다.

**① import 추가** — 아래 부분을 찾으세요.

```tsx
import { LanguageProvider } from "@/components/language-provider";
import { NotifyProvider } from "@/components/notify-provider";
import { AuthProvider } from "@/components/auth-provider";
```

이렇게 바꿔주세요.

```tsx
import { LanguageProvider } from "@/components/language-provider";
import { NotifyProvider } from "@/components/notify-provider";
import { AuthProvider } from "@/components/auth-provider";
import { ThemeProvider } from "@/components/theme-provider";
```

**② Provider 감싸기** — 아래 부분을 찾으세요.

```tsx
        <LanguageProvider>
          <NotifyProvider>
            <AuthProvider>{children}</AuthProvider>
          </NotifyProvider>
        </LanguageProvider>
```

이렇게 바꿔주세요.

```tsx
        <LanguageProvider>
          <NotifyProvider>
            <AuthProvider>
              <ThemeProvider>{children}</ThemeProvider>
            </AuthProvider>
          </NotifyProvider>
        </LanguageProvider>
```

---

## 7. 재사용 버튼 부품(ThemedButton) 만들기 — 약 5분

`frontend/src/components` 폴더 안에 **`themed-button.tsx`** 파일을 새로
만들고 아래 내용을 붙여넣습니다.

```tsx
"use client";

import { useTheme } from "@/components/theme-provider";

type Props = {
  preset?: string; // 관리자 설정 "디자인" 탭에서 만든 버튼 이름 (기본값: "버튼1")
  className?: string;
} & React.ButtonHTMLAttributes<HTMLButtonElement>;

// 관리자 설정에서 정한 색상을 자동으로 읽어와 적용하는 버튼입니다.
// 예: <ThemedButton preset="버튼1">저장</ThemedButton>
export default function ThemedButton({ preset = "버튼1", className = "", style, children, ...rest }: Props) {
  const { buttonStyles } = useTheme();
  const found = buttonStyles.find((b) => b.name === preset);
  const bgColor = found?.bgColor || "#383838";
  const textColor = found?.textColor || "#F9F6F0";

  return (
    <button
      type="button"
      className={`cursor-pointer rounded-lg px-4 py-2.5 text-sm font-semibold ${className}`}
      style={{ backgroundColor: bgColor, color: textColor, ...style }}
      {...rest}
    >
      {children}
    </button>
  );
}
```

- `preset` 값(예: `"버튼1"`)에 맞는 색상을 관리자 설정 목록에서 찾아서
  적용해요. 못 찾으면 기본 색(짙은 회색/아이보리)으로 보여줘요.
- 앞으로 새 버튼을 만들 때, `<button className="bg-[#383838] ...">` 대신
  `<ThemedButton preset="버튼1">` 을 쓰면, 관리자가 "디자인" 탭에서
  "버튼1" 색을 바꿀 때마다 자동으로 반영돼요.

---

## 8. "디자인" 탭 화면 만들기 — 약 8분

`frontend/src/components` 폴더 안에 **`design-settings-form.tsx`** 파일을
새로 만들고 아래 내용을 붙여넣습니다.

```tsx
"use client";

import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useI18n } from "@/components/language-provider";
import { useNotify } from "@/components/notify-provider";

const API_URL = "http://localhost:3001";

type ButtonStyle = { name: string; bgColor: string; textColor: string };

export default function DesignSettingsForm() {
  const { t } = useI18n();
  const { notify } = useNotify();
  const [footerBgColor, setFooterBgColor] = useState("#383838");
  const [footerTextColor, setFooterTextColor] = useState("#F9F6F0");
  const [sidebarBgColor, setSidebarBgColor] = useState("#383838");
  const [sidebarTextColor, setSidebarTextColor] = useState("#F9F6F0");
  const [buttonStyles, setButtonStyles] = useState<ButtonStyle[]>([
    { name: "버튼1", bgColor: "#383838", textColor: "#F9F6F0" },
  ]);

  useEffect(() => {
    fetch(`${API_URL}/library`)
      .then((res) => res.json())
      .then((data) => {
        if (!data) return;
        setFooterBgColor(data.footerBgColor || "#383838");
        setFooterTextColor(data.footerTextColor || "#F9F6F0");
        setSidebarBgColor(data.sidebarBgColor || "#383838");
        setSidebarTextColor(data.sidebarTextColor || "#F9F6F0");
        setButtonStyles(
          Array.isArray(data.buttonStyles) && data.buttonStyles.length > 0
            ? data.buttonStyles
            : [{ name: "버튼1", bgColor: "#383838", textColor: "#F9F6F0" }],
        );
      });
  }, []);

  function updateButtonStyle(index: number, field: "bgColor" | "textColor", value: string) {
    setButtonStyles((prev) => prev.map((b, i) => (i === index ? { ...b, [field]: value } : b)));
  }

  function addButtonStyle() {
    const nextNum = buttonStyles.length + 1;
    setButtonStyles((prev) => [...prev, { name: `버튼${nextNum}`, bgColor: "#383838", textColor: "#F9F6F0" }]);
  }

  async function handleSave() {
    const token = localStorage.getItem("token");
    if (!token) return;
    const res = await fetch(`${API_URL}/library`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
      body: JSON.stringify({ footerBgColor, footerTextColor, sidebarBgColor, sidebarTextColor, buttonStyles }),
    });
    if (res.ok) {
      notify(t("admin.settings.saved"), "success");
    } else {
      const data = await res.json().catch(() => null);
      notify("❌ " + (data?.message || t("admin.settings.saveFail")), "error");
    }
  }

  return (
    <div className="max-w-md">
      <div className="flex flex-col gap-6">
        <div>
          <p className="mb-2 text-sm font-semibold">{t("design.footerSection")}</p>
          <div className="flex flex-col gap-3">
            <div className="flex flex-col gap-2">
              <Label>{t("design.footerBgColor")}</Label>
              <Input type="color" value={footerBgColor} onChange={(e) => setFooterBgColor(e.target.value)} className="h-10 w-20 p-1" />
            </div>
            <div className="flex flex-col gap-2">
              <Label>{t("design.footerTextColor")}</Label>
              <Input type="color" value={footerTextColor} onChange={(e) => setFooterTextColor(e.target.value)} className="h-10 w-20 p-1" />
            </div>
          </div>
        </div>

        <div>
          <p className="mb-2 text-sm font-semibold">{t("design.sidebarSection")}</p>
          <div className="flex flex-col gap-3">
            <div className="flex flex-col gap-2">
              <Label>{t("design.sidebarBgColor")}</Label>
              <Input type="color" value={sidebarBgColor} onChange={(e) => setSidebarBgColor(e.target.value)} className="h-10 w-20 p-1" />
            </div>
            <div className="flex flex-col gap-2">
              <Label>{t("design.sidebarTextColor")}</Label>
              <Input type="color" value={sidebarTextColor} onChange={(e) => setSidebarTextColor(e.target.value)} className="h-10 w-20 p-1" />
            </div>
          </div>
        </div>

        <div>
          <p className="mb-2 text-sm font-semibold">{t("design.buttonSection")}</p>
          <div className="flex flex-col gap-3">
            {buttonStyles.map((b, i) => (
              <div key={b.name} className="rounded-lg border border-neutral-200 p-3">
                <p className="mb-2 text-sm font-medium">{b.name}</p>
                <div className="flex gap-4">
                  <div className="flex flex-col gap-2">
                    <Label>{t("design.buttonBgColor")}</Label>
                    <Input
                      type="color"
                      value={b.bgColor}
                      onChange={(e) => updateButtonStyle(i, "bgColor", e.target.value)}
                      className="h-10 w-20 p-1"
                    />
                  </div>
                  <div className="flex flex-col gap-2">
                    <Label>{t("design.buttonTextColor")}</Label>
                    <Input
                      type="color"
                      value={b.textColor}
                      onChange={(e) => updateButtonStyle(i, "textColor", e.target.value)}
                      className="h-10 w-20 p-1"
                    />
                  </div>
                </div>
              </div>
            ))}
            <button
              type="button"
              onClick={addButtonStyle}
              className="cursor-pointer self-start rounded-full border border-neutral-200 bg-white px-3 py-1.5 text-xs font-medium text-neutral-700 shadow-sm hover:bg-neutral-50"
            >
              {t("design.addButtonStyle")}
            </button>
          </div>
        </div>

        <Button className="cursor-pointer" onClick={handleSave}>
          {t("admin.settings.save")}
        </Button>
      </div>
    </div>
  );
}
```

- "+ 새 버튼 추가"를 누르면 "버튼2", "버튼3"... 이름으로 새 항목이
  생겨요. 이름은 자동으로 붙고, 배경색·글자색만 골라서 저장하면 됩니다.

---

## 9. "설정" 화면에 "디자인" 탭 연결 — 약 3분

`frontend/src/app/admin/settings/page.tsx` 를 엽니다.

**① import 추가** — 아래 부분을 찾으세요.

```tsx
import LibrarySettingsForm from "@/components/library-settings-form";
```

이렇게 바꿔주세요.

```tsx
import LibrarySettingsForm from "@/components/library-settings-form";
import DesignSettingsForm from "@/components/design-settings-form";
```

**② 탭 추가** — 아래 부분을 찾으세요.

```tsx
      <Tabs defaultValue="kormarcTags">
        <TabsList className="gap-2">
          <TabsTrigger value="kormarcTags">{t("settings.tabs.kormarcTags")}</TabsTrigger>
          <TabsTrigger value="library">{t("settings.tabs.library")}</TabsTrigger>
        </TabsList>
```

이렇게 바꿔주세요.

```tsx
      <Tabs defaultValue="kormarcTags">
        <TabsList className="gap-2">
          <TabsTrigger value="kormarcTags">{t("settings.tabs.kormarcTags")}</TabsTrigger>
          <TabsTrigger value="library">{t("settings.tabs.library")}</TabsTrigger>
          <TabsTrigger value="design">{t("settings.tabs.design")}</TabsTrigger>
        </TabsList>
```

**③ 탭 내용 추가** — 아래 부분을 찾으세요.

```tsx
        <TabsContent value="library" className="mt-4">
          <LibrarySettingsForm />
        </TabsContent>
      </Tabs>
```

이렇게 바꿔주세요.

```tsx
        <TabsContent value="library" className="mt-4">
          <LibrarySettingsForm />
        </TabsContent>

        <TabsContent value="design" className="mt-4">
          <DesignSettingsForm />
        </TabsContent>
      </Tabs>
```

---

## 10. 홈페이지·로그인 화면의 Footer가 새 칸을 쓰도록 수정 — 약 4분

`frontend/src/app/(site)/layout.tsx` 를 엽니다. 아래 부분을 찾으세요.

```tsx
        bgColor={library?.chromeBgColor || "#383838"}
        textColor={library?.chromeTextColor || "#F9F6F0"}
```

이렇게 바꿔주세요.

```tsx
        bgColor={library?.footerBgColor || "#383838"}
        textColor={library?.footerTextColor || "#F9F6F0"}
```

`frontend/src/app/(auth)/layout.tsx` 를 엽니다. 똑같은 부분을 찾아서
똑같이 바꿔주세요.

```tsx
        bgColor={library?.footerBgColor || "#383838"}
        textColor={library?.footerTextColor || "#F9F6F0"}
```

---

## 11. 관리자 사이드바가 새 색상을 쓰도록 수정 — 약 8분

`frontend/src/app/admin/layout.tsx` 를 엽니다.

**① import 추가** — 아래 부분을 찾으세요.

```tsx
import { useI18n } from "@/components/language-provider";
import { useAuth } from "@/components/auth-provider";
import { useRequireAuth } from "@/hooks/useRequireAuth";
```

이렇게 바꿔주세요.

```tsx
import { useI18n } from "@/components/language-provider";
import { useAuth } from "@/components/auth-provider";
import { useRequireAuth } from "@/hooks/useRequireAuth";
import { useTheme } from "@/components/theme-provider";
import { hexToRgba } from "@/lib/color";
```

**② 색상 값 가져오기 + navClass 단순화** — 아래 부분을 찾으세요.

```tsx
  const { t, lang, setLang } = useI18n();
  const { logout } = useAuth();
  const router = useRouter();
  useRequireAuth();
  const pathname = usePathname();
  const crumbs = BREADCRUMB_MAP[pathname];

  function navClass(href: string, exact = false) {
    const active = exact ? pathname === href : pathname.startsWith(href);
    return `rounded-lg px-3 py-2.5 ${
      active
        ? "bg-white/10 font-bold text-[#F9F6F0]"
        : "text-[#F9F6F0] hover:bg-white/10"
    }`;
  }
```

이렇게 바꿔주세요. (글자색은 이제 사이드바 전체(`aside`)에 한 번만
지정해서 안쪽 글자들이 물려받게(상속) 할 거라, `navClass`에서는 배경만
신경 쓰면 돼요.)

```tsx
  const { t, lang, setLang } = useI18n();
  const { logout } = useAuth();
  const router = useRouter();
  useRequireAuth();
  const pathname = usePathname();
  const crumbs = BREADCRUMB_MAP[pathname];
  const { sidebarBgColor, sidebarTextColor } = useTheme();

  function navClass(href: string, exact = false) {
    const active = exact ? pathname === href : pathname.startsWith(href);
    return `rounded-lg px-3 py-2.5 font-medium ${active ? "bg-white/10 font-bold" : "hover:bg-white/10"}`;
  }
```

**③ `<aside>`에 색상 적용** — 아래 부분을 찾으세요.

```tsx
      <aside className="flex w-full flex-col bg-[#383838] p-6 text-[#F9F6F0] md:w-52">
```

이렇게 바꿔주세요.

```tsx
      <aside
        className="flex w-full flex-col p-6 md:w-52"
        style={{ backgroundColor: sidebarBgColor, color: sidebarTextColor }}
      >
```

**④ 언어 토글 버튼의 흐린 글자색** — 아래 부분을 찾으세요.

```tsx
          <button
            onClick={() => setLang(lang === "ko" ? "en" : "ko")}
            className="cursor-pointer rounded-md border border-white/25 px-2 py-1 text-xs font-semibold text-[rgba(249,246,240,0.8)] hover:bg-white/10"
          >
```

이렇게 바꿔주세요.

```tsx
          <button
            onClick={() => setLang(lang === "ko" ? "en" : "ko")}
            className="cursor-pointer rounded-md border border-white/25 px-2 py-1 text-xs font-semibold hover:bg-white/10"
            style={{ color: hexToRgba(sidebarTextColor, 0.8) }}
          >
```

**⑤ "대출/반납 (준비 중)" 흐린 글자색** — 아래 부분을 찾으세요.

```tsx
          <span className="px-3 py-2.5 text-[rgba(249,246,240,0.6)]">{t("admin.menu.loans")}</span>
```

이렇게 바꿔주세요.

```tsx
          <span className="px-3 py-2.5" style={{ color: hexToRgba(sidebarTextColor, 0.6) }}>
            {t("admin.menu.loans")}
          </span>
```

**⑥ "홈으로" 링크 흐린 글자색** — 아래 부분을 찾으세요.

```tsx
          <Link href="/" className="text-[0.8125rem] text-[rgba(249,246,240,0.6)]">
```

이렇게 바꿔주세요.

```tsx
          <Link href="/" className="text-[0.8125rem]" style={{ color: hexToRgba(sidebarTextColor, 0.6) }}>
```

**⑦ 버전·저작권 흐린 글자색** — 아래 부분을 찾으세요.

```tsx
          <p className="text-xs text-[rgba(249,246,240,0.55)]">버전 1.0.0</p>
          <p className="mt-1 text-xs text-[rgba(249,246,240,0.55)]">ⓒ 2026 Gravityofu</p>
          <button
            onClick={handleLogout}
            className="mt-3 w-full cursor-pointer rounded-lg border border-white/25 py-2 text-xs font-semibold text-[#F9F6F0] hover:bg-white/10"
          >
```

이렇게 바꿔주세요.

```tsx
          <p className="text-xs" style={{ color: hexToRgba(sidebarTextColor, 0.55) }}>
            버전 1.0.0
          </p>
          <p className="mt-1 text-xs" style={{ color: hexToRgba(sidebarTextColor, 0.55) }}>
            ⓒ 2026 Gravityofu
          </p>
          <button
            onClick={handleLogout}
            className="mt-3 w-full cursor-pointer rounded-lg border border-white/25 py-2 text-xs font-semibold hover:bg-white/10"
          >
```

> 사이드바의 "버전 1.0.0"/"ⓒ 2026 Gravityofu" 문구 자체(관리자 설정
> "도서관" 탭의 버전·저작권 문구와 연결하는 것)는 오늘 범위에는 없어요.
> 오늘은 색상만 관리자 설정과 연결했어요. 문구까지 연결하고 싶으시면
> 다음에 말씀해주세요.

---

## 12. (예시) 실물 등록 화면의 "저장" 버튼을 새 시스템으로 교체 — 약 4분

지난번 여쭤보셨던 그 버튼이에요. 오늘 만든 시스템이 실제로 잘 작동하는지
확인할 겸, 이 버튼 하나를 `ThemedButton`으로 바꿔볼게요.

`frontend/src/app/admin/materials/copies/page.tsx` 를 엽니다.

**① import 추가** — 맨 위에 추가하세요.

```tsx
import ThemedButton from "@/components/themed-button";
```

**② 버튼 교체** — 아래 부분을 찾으세요.

```tsx
          <div className="mt-4">
            <button
              type="button"
              onClick={selectedCopyId ? handleUpdateCopy : handleAddCopy}
              className="w-full cursor-pointer rounded-lg bg-[#383838] px-4 py-2.5 text-sm font-semibold text-[#F9F6F0]"
            >
              {t("materials.copies.save")}
            </button>
          </div>
```

이렇게 바꿔주세요.

```tsx
          <div className="mt-4">
            <ThemedButton
              preset="버튼1"
              onClick={selectedCopyId ? handleUpdateCopy : handleAddCopy}
              className="w-full"
            >
              {t("materials.copies.save")}
            </ThemedButton>
          </div>
```

---

## 확인하기 — 약 8분

1. 관리자 → 설정 화면에 "KORMARC 태그" / "도서관" / "디자인" 세 탭이
   보이는지 확인합니다.
2. "도서관" 탭에서 Footer/사이드바 색상 칸이 사라졌는지 확인합니다.
3. "디자인" 탭에서 Footer 배경색·글자색, 사이드바 배경색·글자색, "버튼1"
   배경색·글자색이 각각 따로 보이는지 확인합니다.
4. "+ 새 버튼 추가"를 누르면 "버튼2"가 생기는지 확인합니다.
5. Footer 배경색만 바꾸고 저장한 뒤 홈페이지를 새로고침하면 **Footer만**
   바뀌고 사이드바는 그대로인지 확인합니다. (반대로도 확인)
6. 사이드바 색상을 바꾸고 저장한 뒤 관리자 페이지를 새로고침하면 사이드바
   배경·글자색이 바뀌는지 확인합니다. (버전·저작권 같은 흐린 글자도 톤이
   같이 바뀌는지 확인)
7. "버튼1" 색상을 바꾸고 저장한 뒤, 실물 등록 화면의 "저장" 버튼 색이
   바뀌는지 확인합니다.

✅ 모두 확인되면 성공입니다! 🎉

---

## GitHub에 저장하기 — 약 2분

```
cd C:\projects\LibraryNearMe
git add .
git commit -m "19단계(1): 설정에 디자인 탭 추가, Footer/사이드바 색상 분리, 재사용 버튼 색상 시스템 추가"
git push
```

---

## 최종 점검표

- [ ] `schema.prisma` — `chromeBgColor`/`chromeTextColor` 제거, 새 5개 칸 추가, 마이그레이션 완료
- [ ] `library.service.ts` — `updateLibrary` 수정
- [ ] `dictionary.ts` — 문구 추가
- [ ] `library-settings-form.tsx` — 색상 칸 제거
- [ ] `lib/color.ts` 새로 생성
- [ ] `components/theme-provider.tsx` 새로 생성
- [ ] `app/layout.tsx` — `ThemeProvider` 연결
- [ ] `components/themed-button.tsx` 새로 생성
- [ ] `components/design-settings-form.tsx` 새로 생성
- [ ] `admin/settings/page.tsx` — "디자인" 탭 연결
- [ ] `(site)/layout.tsx`, `(auth)/layout.tsx` — Footer 색상 칸 이름 변경
- [ ] `admin/layout.tsx` — 사이드바 색상 연결
- [ ] `materials/copies/page.tsx` — 저장 버튼 `ThemedButton`으로 교체
- [ ] Footer/사이드바 색상 따로 동작 확인
- [ ] 버튼1 색상 변경이 실물 등록 저장 버튼에 반영되는지 확인
- [ ] GitHub에 올림

완료되면 알려주세요!
